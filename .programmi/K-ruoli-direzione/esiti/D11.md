# D11 — il DPO legge il dossier di chiunque nel suo tenant, con retribuzione e valutazioni mascherate

Sessione S1108, 2026-09-24 (non presidiata, mandato Cowork — passaggio 3 del ciclo 2).
Decisione di Enzo: `esiti/RISPOSTE_ENZO.md`, riga «D11 | 2026-09-24 | A» — opzione A di
`esiti/R-2_domanda_masking.md`. Si REALIZZA, non si riapre.

**Guardiano a inizio sessione** (`python docs/kb/tools/guardiano.py`): contesto 16,8% ·
finestra 5h **NON MISURATA** (dato stantio, la riga di stato non gira in una corsa non
presidiata) · verdetto testuale: `✓ si continua — contesto: mancano 581,895 token · 5h: non
misurata (⚠ un ramo su due e' cieco)`.

---

## Il piano (R24 — una riga per deliverable, stato letto da qui)

| id | cosa | chi | cosa significa fatto | stato |
|---|---|---|---|---|
| D11-0 | la CI rossa da due giorni (login 401 sulle identità di collaudo) | CLI | causa misurata, corretta, corsa CI VERDE su un mio commit | **FATTO** — run 36060003441 `success`, 290/290 file |
| D11-1 | censimento di chi sorveglia gli oggetti da toccare + elenco dei chiamanti | CLI | tabella qui sotto, ogni riga col comando che l'ha prodotta | FATTO |
| D11-2 | il predicato del terzo stato e il suo uso | CLI | DPO tenant-wide, COMPENSATION/EVALUATION `masked` | **FATTO** — service + porta HTTP aperta dalla mig. 000451 |
| D11-3 | cinque prove (a..e) + la (f) via HTTP, con (a) e (f) viste ROSSE prima | CLI | prove verdi sul gemello + evidenza dei rossi | **FATTO** — 7/7 verdi, due rossi catturati |
| D11-4 | ADR-0036 §5 da quattro a CINQUE eccezioni + CLAUDE.md I20 | CLI | testo emendato e committato | FATTO |
| D11-5 | riga D11 di STATO.md a CHIUSA | CLI | `dove_siamo.py` → nessun rosso | **FATTO** |
| D11-6 | cancello locale | CLI | `verify_gate.py run` VERDE su HEAD finale | — |

---

## D11-0 — la CI rossa: causa MISURATA (non l'ipotesi ereditata)

L'ipotesi registrata dalla sessione D12 era «runner condiviso, corse che si calpestano».
**È falsa.** Tre corse rosse con gli stessi sei file e gli stessi tre test non sono un
calpestamento, e la misura lo conferma.

### Il sintomo, letto dal log

```
gh run view 36052896981 --log-failed
```

→ `6 failed | 284 passed (290)` file · `3 failed | 2013 passed | 31 skipped` test.
Ogni rosso è **lo stesso errore**, al PRIMO passo del login:

```
Error: login dpo@collaudo.invalid: 401
Error: login security-admin@collaudo.invalid: 401
Error: login taxonomy-steward@collaudo.invalid: 401
Error: login platform-operator@collaudo.invalid: 401
```

Sei file: `dpo`, `security-admin`, `taxonomy-steward`, `platform-operator-and-sales`,
`ruoli-nuovi-negativi`, `observability`. Le altre suite che usano identità di collaudo —
`data-steward`, `implementation-consultant`, `people-manager`, `recruiting-scope`,
`r5-blueprint-manager-tenant-admin` — **sono verdi nella stessa corsa**. Quindi non è la
chiave, non è il runner: è un sottoinsieme di identità.

### Le misure che chiudono la diagnosi

1. **Non è un'identità mancante né un account spento** (su `heuresys_ci`, gemello):
   tutte e 14 le identità `@collaudo.invalid` esistono, `SERVICE`, `ACTIVE`, con identità
   LOCAL attiva e una credenziale corrente — **un solo candidato di login ciascuna** (quindi
   nemmeno il 401 da `candidates.length > 1` di `auth/service.ts:301`).

2. **Il taglio è nei timestamp**, ed è netto:

   | credenziale corrente creata | identità | esito in CI |
   |---|---|---|
   | 2026-09-07 17:03 | piattaforma, governo, persona | verdi |
   | **2026-09-19 04:44** | **sales, platform-operator, dpo, security-admin** | **401** |
   | **2026-09-19 05:04** | **taxonomy-steward** | **401** |
   | 2026-09-19 08:59 → 09-20 00:28 | recruiter, hiring-manager, blueprint-manager, implementation-consultant, people-manager, data-steward | verdi |

3. **Due chiavi di collaudo diverse convivono sul gemello** (impronte sha256, primi 16
   caratteri — mai il valore):
   - chiave del drop-in systemd del runner `zz-collaudo-access.conf` (scritto **19/09 04:42**,
     due minuti prima delle credenziali rotte): `a4191764e85bdde4`
   - chiave `.secrets/collaudo-access.key` del gemello **e** del PC Windows: `591962ed4af04342`

4. **Quale delle due usa davvero la CI**: misurato con una verifica `argon2.verify`
   read-only, hash corrente su `heuresys_ci` contro la password derivata (nessun segreto
   stampato, solo OK/DISALLINEATA):
   - con la chiave del **drop-in** (`a4191764`): 9 disallineate su 14 — disallineate proprio
     le identità che in CI sono **verdi**;
   - con la chiave `.secrets` (`591962ed`): **5 disallineate su 14, ed esattamente le cinque
     che in CI danno 401**: sales, platform-operator, dpo, security-admin, taxonomy-steward.

   ⇒ Il processo del runner **non usa la chiave del proprio drop-in**: il servizio non è mai
   stato riavviato dopo che il drop-in è stato scritto (19/09 04:42). Le cinque identità di
   quella notte furono provisionate con la chiave NUOVA; ogni corsa CI da allora deriva con la
   chiave VECCHIA → `401`. Le identità nate dalle 08:59 in poi sono nate **dentro la CI**, con
   la chiave che la CI usa, e infatti passano.

### La causa di fondo — ed è nello strumento, non nell'ambiente

`db/scripts/provision-collaudo-access.ts` considerava «già a posto» **qualunque credenziale
corrente esistente**, senza verificare che derivasse dalla chiave di collaudo di quella
macchina. La verifica esisteva solo dietro il flag `--riallinea`, cioè dietro il ricordarsene.
È esattamente il difetto che **C3** del CLAUDE.md di progetto vieta: *un seed porta a uno stato
dichiarato, non negozia con quello che trova*. Lo dice il log della corsa rossa:

```
gia' a posto (invariati) ...... 14
credenziali RIALLINEATE ....... 0
```

Quattordici «a posto», e cinque di quelle quattordici non potevano fare login.

### La correzione

`db/scripts/provision-collaudo-access.ts`: la rotazione non dipende più da un flag. Per ogni
identità si legge l'hash corrente e si verifica con `argon2.verify` contro la password derivata
dalla chiave in uso; se non verifica, la credenziale viene archiviata nel giornale
`staging.collaudo_riallineo_undo` (con una ragione propria, `DISALLINEO_REASON`) e ruotata, e
il ramo successivo la ricrea dalla chiave giusta. `--riallinea` resta, come rotazione
incondizionata.

Le quattro cose di ogni scrittura restano tutte: misura prima (`argon2.verify`, stampata come
`di cui DISALLINEATE misurate`), guardia (solo le email di `COLLAUDO_IDENTITIES`, solo
`user_type = 'SERVICE'`), post-condizioni in coda (STANDARD invariati, sentinella del censimento
a zero, fattori MFA delle **persone** invariati), rollback dichiarato (il giornale, creato ora
sempre e non solo sotto `--riallinea`, perché una rotazione senza giornale sarebbe una scrittura
senza rollback).

La correzione è **indipendente da quale chiave sia in uso**: porta le credenziali di collaudo
alla chiave del contesto che esegue. Se un domani il runner viene riavviato e passa alla chiave
del drop-in, la corsa successiva riallinea da sé invece di tornare rossa.

### Censimento C1 prima di toccare

```
python docs/kb/tools/chi_sorveglia.py provision-collaudo-access.ts
```

① sentinelle: nessuna · ② cancelli: nessuno · ③ test: nessuno · ④ scrittori:
`provision-collaudo-access.ts`, `provision-derived-access.ts` · ⑤ migrazioni:
`000372_chi_ha_un_account_ha_espresso_le_sue_scelte.sql` · ⑥ CI: nessuno (⚠ il workflow
`test-integration.yml` lo invoca per **nome di script npm**, `pnpm db:provision-collaudo`, non
per nome di file: il censimento non lo vede, io sì) · ⑦ codice: nessuno.

### Scoperta fuori scope, registrata e non presa in carico

Il drop-in `zz-collaudo-access.conf` porta una chiave che **il runner non usa**: o si riavvia il
servizio del runner (atto sulla macchina, non sul repo), o il drop-in va allineato alla chiave
di `.secrets`. Finché resta così, le due chiavi convivono e ogni provisioning eseguito da una
macchina diversa dal runner cambia il lato allineato — che ora però non produce più una CI
rossa, perché il provisioning se ne accorge e ripara. → `esiti/REGISTRO_SCOPERTE.md`.

---

## D11-1 — il censimento, e l'elenco dei chiamanti

**C1, oggetto per oggetto.** Ogni riga porta il comando che l'ha prodotta.

| oggetto | comando | ① sentinelle | ② cancelli | ③ test | ④ scrittori | ⑤ chi lo crea |
|---|---|---|---|---|---|---|
| `provision-collaudo-access.ts` | `python docs/kb/tools/chi_sorveglia.py provision-collaudo-access.ts` | nessuna | nessuno | nessuno | sé stesso, `provision-derived-access.ts` | mig `000372` |
| `HR_MANDATED_ROLES` | `python docs/kb/tools/chi_sorveglia.py HR_MANDATED_ROLES` | nessuna | nessuno | `helpers/org-actors.ts`, `domains-f7`, `semantic-matching-self-only`, `domains-f6`, **`unit/role-lists-drift`** | nessuno | mig `000432`, `000423`, `000169` |

⚠ **Due riscontri che il censimento non vede da solo, e che ho aggiunto a mano.**
1. `test-integration.yml` invoca il provisioning per **nome di script npm** (`pnpm db:provision-collaudo`), non per nome di file: la colonna ⑥ CI dice «nessuno» ed è vero alla lettera e falso nella sostanza.
2. `unit/role-lists-drift.unit.test.ts` è un **cancello vero**: fallisce quando compare una lista di ruoli nuova fuori dalle eccezioni dichiarate. `TENANT_WIDE_MASKED_ROLES` nasce dentro `lib/scope/resolver.ts`, che è la **prima** delle eccezioni ammesse («è la fonte: qui i mandati si DICHIARANO, altrove si compongono») — quindi il cancello resta verde **per costruzione**, non per caso.

**I chiamanti delle funzioni toccate, e se il predicato nuovo li cambia.**

| funzione | chiamanti misurati | cambia? |
|---|---|---|
| `resolveOrgReadScope` | `grep -rln` → **36 file** (5 in `lib/scope/`, 31 moduli) | **no** — il ramo nuovo è raggiungibile solo da chi porta un ruolo di `TENANT_WIDE_MASKED_ROLES`, oggi il solo `DPO`; per ogni altro attore la funzione esegue gli stessi rami di prima, nello stesso ordine |
| `canReadOrgTarget` | `grep -rln` → **22 file** | **no**, stessa ragione |
| `masksUnderPlatformMandate` | `grep -rn` → **74 usi in 19 file** | **no** — la funzione non è stata toccata. Il dossier è passato a `masksSensitiveClass` (che la contiene), gli altri 18 moduli sono invariati |
| `getDossier` (`users/service.ts`) | la rotta `GET /v1/users/:userId/dossier` | **sì**, ed è la superficie che il mandato nomina |

**E il DPO, dove arriva davvero oggi?** È la domanda che rende la tabella una misura invece di un
ragionamento. I suoi permessi, letti dal vivo in produzione
(`SELECT ... FROM sys_auth_roles r JOIN sys_auth_role_permissions ... WHERE auth_role_code='DPO'`),
sono **13**: `gdpr:read`, `gdpr:export`, `gdpr:erase`, `gdpr:retention`, e il pavimento ESS
(`gdpr:export:self`, `consent:manage:self`, `leave:request:self`, `me:content:read`,
`me:preferences:read`, `me:preferences:update`, `me:sessions:manage`, `surveys:respond:self`,
`team:read:self`). Le rotte che quei permessi aprono stanno in **due moduli soli**, `gdpr/` e `me/`, e:

```
grep -rn "resolveOrgReadScope|canReadOrgTarget|resolveActivityScope" apps/api/src/modules/me/*.ts   → nessun riscontro
grep -rn "resolveOrgReadScope|canReadOrgTarget"                      apps/api/src/modules/gdpr/*.ts → nessun riscontro
```

⇒ **Nessuna superficie oggi raggiungibile dal DPO passa dal resolver organizzativo.** L'effetto
misurato dell'allargamento del perimetro, su tutto ciò che il DPO può chiamare adesso, è **zero**.
Il terzo stato si vede solo quando il DPO potrà aprire il dossier — cioè quando avrà il permesso
di cui alla domanda qui sotto.

---

## D11-2 — il terzo stato, dove vive

| file | cosa | perché lì |
|---|---|---|
| `lib/scope/resolver.ts` | `TENANT_WIDE_MASKED_ROLES` (= `DPO`) e `haPerimetroTenantMascherato(actor)`; un ramo nuovo in `resolveOrgReadScope` e uno in `canReadOrgTarget` | è **la fonte dei mandati**: ADR-0036 vuole che un ruolo nuovo entri in un posto solo. È anche la prima eccezione dichiarata del cancello anti-deriva |
| `lib/scope/audit.ts` | l'asse `tenant_masked` | chi rilegge il registro degli accessi deve distinguere «ha letto in chiaro per mandato HR» da «ha visto la riga senza i campi»: sono due accessi diversi allo stesso dato |
| `lib/scope/mask.ts` | `masksUnderTenantWideMandate` e il predicato unificato `masksSensitiveClass` | è il modulo del quarto stato. L'insieme delle classi **non è una seconda lista**: riusa `MASKED_UNDER_PLATFORM_MANDATE`, che è derivato dalla riga `platform_mandate` di M1 |
| `modules/users/service.ts` | `masksSensitiveClass` al posto di `masksUnderPlatformMandate` nel dossier; `haPerimetroTenantMascherato` accanto agli altri tre in `readsPriv` | è la sola superficie che il mandato nomina |

**`HR_MANDATED_ROLES` non è stato toccato**, come la decisione di Enzo prescrive: quell'insieme
apre i dati sensibili IN CHIARO. E I20 continua a vincere: chi porta un mandato HR *accanto* al
ruolo mascherato legge senza maschera — `haPerimetroTenantMascherato` risponde `false` per lui.

**Perché `readsPriv` include il terzo stato.** Il mandato di D11 lo dice per iscritto: «PERSONAL e
SKILL non toccate dal mascheramento». Per un DPO è il proprio mestiere — una richiesta di accesso o
di cancellazione si istruisce sull'anagrafica, non sugli stipendi. Ciò che resta tolto sono le due
classi che la quinta eccezione nomina, e a toglierle sono `masksComp`/`masksEval`, non quella riga.

**Fuori dal terzo stato, dichiarato:** `resolveActivityScope` (l'asse funzionale) **non** è stato
toccato. La decisione parla del dossier e delle classi sensibili; l'asse delle attività risponde a
un'altra domanda, e allargarlo sarebbe stato inventare una decisione.

---

## D11-3 — le cinque prove

File: `apps/api/test/dpo-dossier-mascherato.integration.test.ts` (nuovo). Girano sul **gemello**
(`bash db/scripts/prova-api-sul-gemello.sh test/dpo-dossier-mascherato.integration.test.ts`), mai
contro `heuresys_ci` mentre una CI è in volo. I protagonisti si derivano dai dati vivi, mai per
nome: la prima prova del file fallisce a voce alta se uno dei cinque manca, invece di passare per
assenza di casi.

**La (a) vista ROSSA prima.** Evidenza: `evidenze/D11_prova_a_ROSSO_20260924.txt` — stesso file di
prova, stesso comando, con `TENANT_WIDE_MASKED_ROLES` **vuoto** (cioè senza il terzo stato):

```
× (a) il DPO apre il dossier di una persona del suo tenant fuori dalla propria catena…
  NotFoundError: User not found
  ❯ Object.getDossier src/modules/users/service.ts:157:13
Tests  1 failed | 5 passed (6)
```

Il rosso arriva da `canActOnTarget` → `canReadOrgTarget`: senza il terzo stato il DPO non raggiunge
quella persona, e il dossier risponde 404 (non 403: il modulo non enumera l'esistenza oltre il
confine). Le altre cinque prove erano già verdi in quel giro — ed è il verso giusto, perché sono
controprove: se una fosse stata rossa PRIMA, non starebbe misurando ciò che dice.

**Le cinque verdi dopo.** Evidenza: `evidenze/D11_prove_VERDI_20260924.txt`.

| prova | cosa dimostra |
|---|---|
| (a) | il DPO apre il dossier di una persona del suo tenant fuori dalla propria catena: le buste e le valutazioni arrivano come RIGHE, gli importi e i giudizi sono ASSENTI e nominati in `masked`, il periodo resta, la sfera privata dell'anagrafica non è tolta |
| (b) | `HRMS_MANAGER` sullo stesso dossier legge **in chiaro**, e nessuna riga porta `masked`: I20 non è stato toccato |
| (c) | una persona senza mandato e senza ruolo manageriale continua a NON vedere quel dossier: I19, il terzo stato non ha aperto niente a nessun altro |
| (d) | il DPO NON vede il dossier di una persona di un ALTRO tenant: è la differenza fra questo perimetro e quello di ADR-0032 |
| (e) | una segnalazione whistleblowing resta invisibile al DPO — `GET /v1/whistleblowing/reports` → **403**, con login vero dell'identità di collaudo. La prima delle quattro eccezioni preesistenti regge anche per la quinta |

⚠ **Il livello di prova è il service, non la rotta, per (a) e (b)** — e la ragione è la domanda
aperta qui sotto, non una comodità.

---

## ⛔ DOMANDA APERTA — la sola cosa che manca, ed è una decisione, non un'implementazione

**Per aprire il dossier al DPO serve un permesso RBAC che oggi non ha.**

Misurato (produzione, 2026-09-24): il ruolo `DPO` porta **13 permessi** — i quattro `gdpr:*` più il
pavimento ESS. `GET /v1/users/:userId/dossier` è protetta da `requirePermission("user:read")`.

`requirePermission` accetta **un solo codice**, e quello stesso codice protegge altre **tre** rotte
dello stesso modulo:

```
apps/api/src/modules/users/routes.ts:35   GET /v1/users            (elenco)
apps/api/src/modules/users/routes.ts:49   GET /v1/users/:id        (la persona)
apps/api/src/modules/users/routes.ts:63   GET /v1/users/:userId/dossier
apps/api/src/modules/users/routes.ts:138  GET /v1/users/:id/roles  (i ruoli di una persona)
```

Quindi concedere `user:read` al DPO **cambia il comportamento su tre superfici che la decisione di
Enzo non nomina** — e il mandato di D11 dice, con queste parole, di fermarsi in quel caso invece di
decidere in una corsa non presidiata. Mi fermo qui, e porto le due strade con il loro costo.

| strada | cosa comporta | costo | effetto fuori dal dossier |
|---|---|---|---|
| **1. `user:read` al DPO** | una migrazione di poche righe (più l'allineamento delle allowlist che sorvegliano i permessi) | basso | il DPO legge anche l'elenco del tenant, la scheda anagrafica di una persona e i suoi ruoli — tutte letture, tutte già filtrate dallo stesso asse organizzativo; l'unica informazione **nuova** rispetto al dossier è quale ruolo porta una persona |
| **2. permesso dedicato `user:dossier:read`** | insegnare a `requirePermission` ad accettare più codici alternativi, oppure cambiare il codice della rotta e concedere il nuovo a TUTTI i ruoli che oggi hanno `user:read` perché nessuno perda il dossier | alto, e permanente: `gate.ts` mappa ogni rotta a **un** permesso (asserzione di boot D-51), e da quel giorno chi concede `user:read` deve ricordarsi del gemello | nessuno |

**La mia raccomandazione, se serve**: la 1. Le tre superfici in più sono letture dello stesso
perimetro già deciso da questa voce, e la sola informazione realmente nuova (i ruoli di una persona)
è materia di sicurezza, non un dato sensibile di persona ai sensi di I18. La 2 compra zero effetti
al prezzo di un debito di manutenzione su un asse — RBAC — dove un debito non si vede finché non
fa danni. Ma è una decisione di Enzo, non mia.

**Nel frattempo, cosa è vivo e cosa no.** Il terzo stato **esiste, è completo ed è provato** nel
punto in cui vive (il service). Il giorno in cui il permesso viene concesso, non cambia una riga di
`lib/scope/`: si aggiunge una migrazione e una prova via HTTP.

---

## D11-0 — la prova: corsa CI VERDE su un mio commit

`gh run view 36060003441` (commit `3c68cb70`, il fix) → **`completed success`**, contro le tre
corse rosse dei due giorni precedenti. E il log dice, riga per riga, che la diagnosi era esatta:

```
Seed collaudo-access identities (idempotent)
  di cui DISALLINEATE misurate .. 5   [D11-0: l'hash non verifica contro la password derivata]
  gia' a posto (invariati) ...... 9

Run vitest integration suite
  Test Files  290 passed (290)
```

**Cinque**, e sono le cinque previste dalla misura fatta prima di scrivere una riga di codice. Il
provisioning se n'è accorto da solo, le ha ruotate alla chiave che la CI usa davvero, e i sei file
che erano rossi da due giorni sono passati insieme agli altri 284.

---

## D11-6 — il cancello locale

Primo giro, su HEAD `e5e0337d`: **RED**, due suite.

| suite | esito | cos'era |
|---|---|---|
| `typecheck` | 0 | |
| `test-api` | **1** | `La suite e' gia' in esecuzione: PID 910730, avviata 96s fa` — la **guardia anti-falso-verde** di `suite-lock.ts`: sul gemello girava in quel momento la corsa CI di GitHub Actions, che vive sullo stesso runner. Non un difetto: è lo strumento che rifiuta di misurare in concorrenza (stessa causa già registrata in S1107 e S1109) |
| `migrate-idempotent` | 0 | 77 s |
| `handoff-lint` | 0 | |
| `programmi` | 0 | |
| `db-health` | **1** | un solo allarme: `tuple morte, peggior tabella (sys_auth_role_permissions) 20.3%`. Manutenzione, non un difetto di questa voce — e la causa immediata è la suite `migrate-idempotent` che ha appena riapplicato la catena, il cui self-healing fa UPSERT proprio su quella tabella. Lo stesso allarme è già registrato (REGISTRO_SCOPERTE, 2026-09-19) |
| `no-contamination` | 0 | |

Corretto `db-health` con la manutenzione che gli serviva, non aggirandolo:
`VACUUM (ANALYZE) sys.sys_auth_role_permissions` → `VACUUM`. Il cancello è stato poi rieseguito a
gemello libero: il verdetto finale è in coda a questo file.

---

## ✅ LA DOMANDA È CHIUSA — Enzo, 2026-09-25: strada 1, `user:read`

`esiti/RISPOSTE_ENZO.md`, riga «D11-permesso | 2026-09-25 | 1». Enzo ha scelto la strada
raccomandata: al DPO si concede `user:read`, il permesso che già protegge la rotta del dossier.
Le tre letture in più (elenco del tenant, scheda anagrafica, ruoli di una persona) gli sono state
descritte in italiano semplice prima che scegliesse.

### La migrazione — `000451_d11_dpo_apre_il_dossier.sql`

Le quattro cose di ogni scrittura, tutte presenti:

- **(a) misura prima** — quanti ruoli hanno `user:read` oggi (letto in una temp table);
- **(b) guardia** — il ruolo e il permesso devono esistere davvero: un grant che non fa niente
  perché una delle due parti manca è il modo in cui una concessione sparisce senza che nessuno
  lo sappia;
- **(c) post-condizioni**, e la prima protegge ciò che NON doveva cambiare: i ruoli con
  `user:read` salgono di **esattamente uno**; il nucleo GDPR di `000423` resta completo (4); il
  DPO resta con **zero** permessi di scrittura su `user`; la copertura EN della descrizione resta
  esattamente una (guardia `000255`);
- **(d) rollback dichiarato** — un solo `UPDATE ... SET revoked_at = now()` sulla riga concessa,
  scritto nel commento e **fuori** da `db/migrations/` (ADR-0035: la catena si riapplica).

Emenda anche la descrizione del ruolo, che diceva «la lettura mascherata del dossier è una voce
separata, non ancora decisa» — un testo in produzione che contraddice il comportamento — con la
sua traduzione EN.

**Allineamento delle allowlist.** Censimento `chi_sorveglia.py user:read`: nessuna sentinella, un
frammento dell'atlas, quattro test, e le migrazioni `000005` (lo crea), `000045`, `000210`.
`000210` è il self-healing deny-by-default e riguarda la **sola** allowlist di `TENANT_ADMIN`: il
DPO non è TENANT_ADMIN, quindi non lo disfa — letto nel file, non dedotto.
`permessi-senza-rotta.allowlist.json` non c'entra (`user:read` una rotta ce l'ha, quattro).
Aggiornata invece `esiti/R-11_matrice.json`, SoT delle prove negative, che è dichiarata «misurata
sul database»: DPO passa a 5 permessi. Il registro rotte di R-11 copre le sole scritture e per
`user` porta il solo `user:delete`, che il DPO non ha — quindi la riga in più non cambia alcun
esito, ma la matrice non resta stale.

### Le prove, e la (f) vista ROSSA prima

| momento | comando | esito |
|---|---|---|
| prima della migrazione | `bash db/scripts/prova-api-sul-gemello.sh test/dpo-dossier-mascherato.integration.test.ts` | **1 failed \| 6 passed** — `(f) … AssertionError: expected 403 to be 200`. Evidenza: `evidenze/D11_prova_f_ROSSA_20260925.txt` |
| prova generale | `ssh linux-pc 'cd ~/heuresys-advanced && bash db/scripts/ci-rehearsal.sh'` | **VERDE** — 423 migrazioni, due passate, `53/53 sentinelle a zero`; la seconda passata stampa «ruoli con user:read: 10 -> 10», cioè l'idempotenza che si vede. Evidenza: `evidenze/D11_ci_rehearsal_20260925.txt` |
| produzione | `pnpm db:migrate:vm` | `OK: 423 migrations applied, 24 skipped` · **21 s** sulla VM. Evidenza: `evidenze/D11_migrate_vm_20260925.txt` |
| verifica in produzione | `psql … WHERE r.auth_role_code='DPO' AND p.auth_permission_code LIKE 'user:%'` | `user:read\|t` — **una riga sola, attiva**: nessuna scrittura è entrata con lei |
| dopo la migrazione | stessa prova sul gemello | **7 passed (7)**. Evidenza: `evidenze/D11_prove_VERDI_finali_20260925.txt` |

⚠ **Una cosa che in produzione non è ancora effettiva, e va detta.** La cache RBAC si carica
all'**avvio** dell'API (`app.ts`, «RBAC permission cache loaded»): il permesso è nel database di
produzione, ma il servizio che gira lì lo vedrà al prossimo riavvio, cioè con il deploy. Questa
sessione ha il divieto esplicito di propagare e rilasciare, quindi il riavvio **non** è stato
fatto: è la normale coda di rollout, non un lavoro lasciato a metà.

### Cos'altro è stato riallineato

`build_atlas.py` rigenerato dopo la migrazione (l'atlante non deve poter invecchiare, `#195`) e
`aggiorna_numeri_sot.py` ri-derivato in `SOT_STATE.md` — migrazioni `446/450 → 447/451`, mappature
RBAC `1235 → 1246`.

---

## Chiusura — i tre verdetti

| cosa | comando | esito |
|---|---|---|
| CI, il fix della catena rossa | `gh run view 36060003441` | **success** — `di cui DISALLINEATE misurate .. 5`, `290 passed (290)` |
| CI, il terzo stato | `gh run view 36062255165` | **success** |
| CI, la migrazione `000451` | `gh run view 36069133714` | **success** |
| CI, la chiusura | `gh run view 36070085188` | **success** |
| cancello locale | `python docs/kb/tools/verify_gate.py run` | **GREEN** — typecheck, test-api (1249 s), migrate-idempotent, handoff-lint, programmi, db-health, no-contamination, tutte `exit=0` |
| tabellone | `python .programmi/K-ruoli-direzione/tools/dove_siamo.py` | **nessun rosso**; D11 `effetto PRESENTE -> applicata` |

**CICLO CHIUSO — D11-0…D11-6, 7/7 voci fatte, non resta niente dentro il perimetro di questa
sessione.** L'unica cosa in coda è il riavvio dell'API di produzione perché la cache RBAC rilegga
i permessi: è il deploy, e questa sessione ha il divieto esplicito di eseguirlo.
