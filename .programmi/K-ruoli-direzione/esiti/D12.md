# D12 — provenienza di `sys.sys_attendance` dichiarata per SCRITTORE (opzione A di Enzo)

Sessione S1108, 2026-09-24. Decisione di Enzo (`RISPOSTE_ENZO.md`, 2026-09-24 21:58): opzione A —
dichiarare per SCRITTORE, non riga per riga. Ogni riga sotto porta il comando che l'ha prodotta.

## D12-1 — rimisura degli scrittori vivi (2026-09-24, ri-verifica di X-6 del 2026-09-19)

```
rg --no-ignore --hidden -l "INSERT INTO sys.sys_attendance" apps/api/src db/
```
→ identici ai 4 file di X-6, nessun nuovo scrittore comparso in 5 giorni:

| file | come scrive | invocato da | registro provenienza? |
|---|---|---|---|
| `db/seeds/storia36/01_attendance_timeoff.sql` (riga 279, valore riga 300) | `INSERT INTO sys.sys_attendance (...) SELECT ... 'IMPORT'` — SQL puro, valore fisso | `storia36.sh costruzione` (costruzione iniziale one-shot della storia 36 mesi) | NO — 0 riscontri `sys_source_lineage_records` |
| `db/seeds/storia36/13_avanzamento.sql` (riga 458, valore riga 478) | `INSERT INTO sys.sys_attendance (...) SELECT ... 'IMPORT'` — SQL puro, valore fisso | `storia36.sh avanzamento`, timer notturno 03:45 su VM/linux-pc dove `.env` dichiara `STORIA36_AVANZAMENTO=1` (CLAUDE.md) | NO — 0 riscontri |
| `db/seeds/rtl-rebuild/07_attendance_topup.sql` (UPDATE riga 41 relabel SYSTEM→IMPORT; INSERT difensivo riga 52, valore riga 71) | UPDATE + INSERT difensivo diretti, SQL puro, valore fisso `'IMPORT'` | eseguito manualmente durante il rebuild/collasso del tenant RTL (one-shot storico, non schedulato) | NO — 0 riscontri |
| `db/migrations/000392_la_sentinella_straordinari_guarda_la_data_del_lavoro.sql` | riga di prova per la propria sentinella `v_straordinari_non_autorizzati` | migrazione, non un job | escluso da X-6 e qui: non è dato reale |

Comando di conferma "nessuno scrive il registro":
```
grep -c "sys_source_lineage_records" db/seeds/storia36/13_avanzamento.sql db/seeds/storia36/01_attendance_timeoff.sql db/seeds/rtl-rebuild/07_attendance_topup.sql
```
→ `0, 0, 0` (identico a X-6).

`chi_sorveglia.py sys_attendance`: nessun modulo applicativo (`apps/api/src/modules/*`) scrive la
tabella — i 4 repository elencati (`analytics`, `me`, `insights`, `org-health`) non compaiono nel
grep INSERT: sono lettori, non scrittori. Confermato: **zero rotta API di scrittura** su
`sys_attendance` (I23 — tabella `importato`, non `nativo`/`ibrido`).

**Conclusione D12-1**: tre scrittori vivi, tutti seed/script SQL (mai codice applicativo), tutti e
tre scrivono `attendance_source='IMPORT'` fisso e mai il registro di provenienza riga-per-riga.
Coerente al 100% con X-6 (119.226 righe spiegate dall'ipotesi 2) e con X-2 (`attendance_source`
un solo valore, `'IMPORT'`, su tutte le righe).

## D12-2 — la dichiarazione (fatta e verificata)

Migrazione `db/migrations/000450_d12_origine_dato_sys_attendance.sql`: colonna `origine_dato
varchar(32) NOT NULL DEFAULT 'MATERIALIZZAZIONE'` + CHECK + commento. Commit `b0974d63`.

**ROSSO prima** (colonna non esiste):
```
psql ... -c "select origine_dato from sys.sys_attendance limit 1;"
→ ERROR:  column "origine_dato" does not exist
```

Prova generale (`verify_gate.py run` → `migrate-idempotent` → `ci-rehearsal.sh` sul gemello,
dopo due tentativi rossi per corsa CI GitHub in volo — vedi REGISTRO_SCOPERTE): **VERDE**,
54.9s, HEAD `70fc813e`.

Applicata in produzione: `pnpm db:migrate:vm` — 19s, 422 applied/24 skipped.

**VERDE dopo** (verificato sul vivo, produzione):
```sql
select origine_dato, count(*) from sys.sys_attendance group by 1;
→ MATERIALIZZAZIONE | 122739   (unico valore, 0 eccezioni)

select conname from pg_constraint where conrelid='sys.sys_attendance'::regclass
  and conname='sys_attendance_origine_dato_check';
→ presente

select * from sys.v_direzione_del_dato_violata where tabella='sys_attendance';
→ 0 righe (nessuna violazione: X-1 classifica 'importato', origine_dato='MATERIALIZZAZIONE'
  != 'NATIVO', la sentinella non scatta — coerente con le 4 tabelle D6)
```

Effetti collaterali corretti nello stesso giro (commit `cc7fc43b`, `5d19c8ed`): SOT_STATE.md §0
headline migrazioni 445→446 (handoff-lint D3), atlas rigenerato (CI "Atlas freshness").

**Prova in produzione, richiesta dal governo (Cowork), con lo strumento canonico**
(`.programmi/K-ruoli-direzione/tools/q.py`, sola lettura, transazione read-only sul database
di produzione via tunnel):

```
python .programmi/K-ruoli-direzione/tools/q.py "select origine_dato, count(*) from sys.sys_attendance group by 1"
→ origine_dato,count
  MATERIALIZZAZIONE,122739

python .programmi/K-ruoli-direzione/tools/q.py "select migration_id, file_name, applied_at from sys.sys_schema_migrations order by migration_id desc limit 1"
→ migration_id,file_name,applied_at
  110242,000450_d12_origine_dato_sys_attendance.sql,2026-09-24 20:42:00.870325+00:00
```

La 000450 e' applicata in produzione e il suo effetto (colonna uniforme, 122.739 righe) c'e'.

## D12-3bis — prenotazione della migrazione, correzione (segnalata dal governo)

`dove_siamo.py` usciva ROSSO: la migrazione 000450 era prenotata due volte (colonna
`migrazione_prenotata` sia sulla riga X-2 sia sulla riga D12) e non aveva query di EFFETTO
nel dizionario `EFFETTI` dello strumento. Corretto: aggiunta `EFFETTI["X-2/000450"]` (stessa
forma delle 12 gemelle, `X-2/000433..000444`) e tolto il numero dalla colonna strutturata
della riga D12 (resta nel testo della nota; la migrazione e' prenotata una sola volta, sotto
X-2, che e' la voce con le 13 tabelle). `dove_siamo.py` → "ESITO: nessun rosso." dopo il fix.
