# F0.4 — Controprova del meccanismo di ripresa (simulazione 3.5)

Voce finta `K-PROVA`, numeri prenotati 000415 / 000416 / 000417 (ultimo presente misurato: 000414). Ogni riga qui sotto e' un caso della sezione 3.4 visto riconoscere da `tools/dove_siamo.py`, con l'output testuale.

## Sessione 1 — S1102, 2026-09-14

| caso | quando | output di `dove_siamo.py` | esito |
|---|---|---|---|
| (iii) file assente, numero prenotato | dopo la prenotazione in STATO.md, prima di scrivere il file | `K-PROVA 000415: FILE ASSENTE -> caso (iii): il numero e' prenotato, il file si scrive` (idem 000416, 000417) | riconosciuto |
| (i) file completo, effetto assente | dopo aver scritto `000415_prova_di_ripresa_k.sql` con `-- FINE 000415`, prima di applicarla | `K-PROVA 000415: 000415_prova_di_ripresa_k.sql  completo · NON registrata · effetto ASSENTE -> caso (i): file completo, effetto assente: si (ri)applica` | riconosciuto |
| (ii) file completo, effetto presente | dopo `pnpm db:migrate:vm` (17 s, 388 applicate + 24 saltate) | `K-PROVA 000415: 000415_prova_di_ripresa_k.sql  completo · registrata 2026-09-14T21:26+00:00 · effetto PRESENTE -> caso (ii): applicata, passa alla post-condizione` — e `db_health.py`: «tutto nei limiti» con la vista nuova | riconosciuto a CALDO (stessa sessione); la prova a FREDDO e' della sessione 2 |

Poi la sessione S1102 si chiude SENZA chiudere il passo 10 (F0.4 e K-PROVA in `SOSPESA`, `prossimo_passo` = «ripresa»): e' la parte della prova che nessun agente puo' fare.

## Sessione 2 — S1103, 2026-09-15 (evidenza grezza: `evidenze/F0.4_ripresa_s2_202609150135.txt`)

Procedura di ripresa 3.3 eseguita alla lettera: R1 `git pull --ff-only` (up to date) · R2 `dove_siamo.py` (nessun rosso) · R3(c) le righe `SOSPESA` di S1102 sono orfane → prese · R4 commit della presa `d4e4ad93` PRIMA di lavorare.

| caso | quando | output di `dove_siamo.py` | esito |
|---|---|---|---|
| session-id | alla riapertura | `cat .handoff/session-id` → **S1103** (era S1102 alla chiusura) | **DIVERSO**: il caso R3(a) non si presenta mai; la ripresa passa SEMPRE da R3(c) su una riga `SOSPESA`/`IN CORSO` orfana. Non e' un errore, e' un fatto del meccanismo |
| (ii) a freddo | prima di qualunque scrittura di S1103 | `K-PROVA 000415: … completo · registrata 2026-09-14T21:26+00:00 · effetto PRESENTE -> caso (ii): applicata, passa alla post-condizione` | riconosciuto a FREDDO |
| effetto RITIRATO | dopo `000416_rollback_di_000415.sql` (ALTER VIEW … RENAME TO `v_prova_ripresa_k_ritirata`, non DROP; no-op alla seconda passata) applicata con `pnpm db:migrate:vm` (389 applicate) | `K-PROVA 000415: … effetto ASSENTE`; sul vivo `q.py` elenca la sola `v_prova_ripresa_k_ritirata` | riconosciuto: la query consapevole del ritiro dice ASSENTE |
| (iv) file incompleto | `000417_incompleto_prova.sql` scritto SENZA `-- FINE` | `K-PROVA 000417: … FILE INCOMPLETO (manca -- FINE 000417) -> caso (iv): si riscrive per intero  ROSSO` · `ESITO: ROSSO` · **exit 1** | riconosciuto ROSSO |
| (iv) completata | riscritta per intero come `BEGIN; select 1; COMMIT; -- FINE 000417`, applicata (390 applicate, registro 390 = 390 file) | `K-PROVA 000417: … completo · registrata 2026-09-14T23:43+00:00` · `ESITO: nessun rosso` · exit 0 | riconosciuto; nessun buco di numero |

Per ogni migrazione applicata: prova generale sul gemello VERDE (`prova-idempotenza.sh`: 000416 22 s, 000417 28 s, sentinelle 49/49 a zero), `pg_dump_pre_k.sh` sulla VM (138 MB ciascuno), commit e push PRIMA di `pnpm db:migrate:vm`. `db_health.py` dopo: tutte le viste `sys.v_*` a zero, compresa `v_prova_ripresa_k_ritirata`.

**Un difetto trovato e corretto nello strumento** (non nel mandato): `dove_siamo.py` leggeva il file di rollback come «caso (i): si (ri)applica». Ora un file `_rollback_di_` completo con effetto assente e' «ROLLBACK APPLICATO, NON si riapplica», e la voce creatrice e' «RITIRATA da NNN». Dettaglio e limite residuo in `REGISTRO_SCOPERTE.md`.

**Verdetto F0.4: il meccanismo di ripresa REGGE sui quattro casi di 3.4 piu' l'effetto ritirato.** Le voci vere possono partire. K-PROVA → `RITIRATA` («prova del meccanismo, passata il 2026-09-15»); i tre file 000415/000416/000417 restano nel repo come prova (V5).
