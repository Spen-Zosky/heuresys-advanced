# Rapporto S1112 — 2026-09-19

Sessione non presidiata, mandato K (Cowork). Aggiornato a ogni voce chiusa.

## Voce 1 — Spedire — CHIUSA

- Verificati i 4 commit non spediti da S1111 (`9df1a1b5`, `761fdf73`, `5331c085`, `cc48542c`):
  grep sui pattern di segreto sul diff `origin/main..HEAD` → 6 riscontri, tutti parole
  dentro commenti/documentazione (nessun valore reale). Confermato quanto scritto nel
  mandato.
- `git push origin main`: fatto, `ba8a2d75..9df1a1b5`.
- `.gitignore` depositato da Cowork (V7): letto il diff, coerente col mandato — aggiunge
  `.commit_msg_*`, `.scratch_*`, `_zp_verify_tmp/`, `.programmi/_zp_verify_tmp/`.
  Misurato: i file non tracciati sono scesi da ~90 a 45 con lo stesso comando
  `git status --porcelain`. Committato (`e362beec`) e spedito.

## Voce 2 — I due stash — CHIUSA

Nessuno dei due porta lavoro da integrare (dettaglio in `esiti/INVENTARIO_stash.md`,
committato `dadb098a`):
- **stash@{1}** (26/7, GDPR reference_kind): gia' fatto meglio nel main — mig `000216` +
  `000304` + test "Z-259" coprono esattamente il rilievo citato (export ACTOR).
- **stash@{0}** (18/8, `concepts-corpus.jsonl`): snapshot di un artefatto rigenerabile,
  superato da un mese di rigenerazioni successive.

Entrambi lasciati nello stash (non applicati, non droppati) — la decisione di droppare
spetta a Enzo.

## Voce 3 — R-11 — CHIUSA

Il materiale (test + route registry + matrice) era gia' scritto da S1110 ma non era mai
stato eseguito. Trovati e corretti due difetti reali:
- la persona di collaudo `people-manager@collaudo.invalid` non era mai stata provisionata
  (R-6/S1110 aveva registrato l'identita' ma non aveva lanciato lo script) — risolto con
  `pnpm db:provision-collaudo` (idempotente, ha creato solo la riga mancante);
- l'`it()` principale non aveva un timeout esplicito: 360 chiamate HTTP sequenziali via
  tunnel SSH (10 ruoli × 36 rotte) superano i 20s di default di vitest — aggiunto
  `{ timeout: 180_000 }`.

Dopo le correzioni: 2/2 test verdi (matrice completa + controprova), typecheck:test pulito.
Committato e spedito (`292da313`).

## Voce 4 — F5 (X-2..X-6)

**X-2 CHIUSA** (12/13 tabelle; `sys_attendance` resta `ATTESA_ENZO`). Il mandato parlava di
"11 tabelle" senza elencarle — ricostruito l'elenco dallo strumento canonico del passo 61
(`misura_k.py`, 13 tabelle), migrate le 12 diverse da `sys_attendance` in un solo giro
(decisione e dettaglio in `esiti/X-2_tabelle.md`). Due difetti reali trovati e corretti
prima di toccare la produzione:
- la prova generale (`ci-rehearsal.sh` sul gemello) ha bocciato `varchar(16)` — troppo corto
  per `'MATERIALIZZAZIONE'` (18 caratteri) — corretto a `varchar(32)`;
- `dove_siamo.py` segnalava rosso per due difetti meccanici (formato della prenotazione,
  marcatore di fine-file mancante) — corretti nello strumento stesso.

Applicate in produzione (`pnpm db:migrate:vm`, 16s), verificate sul vivo (12/12 colonne coi
default attesi), `db_health.py` verde. I cinque scrittori nativi aggiornati per dichiarare
esplicitamente `'NATIVO'`; test verdi. `sys_attendance`: proposta scritta in
`esiti/X-2_attendance.md`, in attesa della conferma di Enzo.

**X-3 CHIUSA**: registro di provenienza riconciliato per vista (`v_source_lineage_normalizzata`
+ sentinella `v_registro_provenienza_prefisso_nuovo`). Un bug reale trovato e corretto: il
JOIN di `evidence/repository.ts` falliva silenziosamente per le 7 tabelle con prefisso storico.

**X-4 CHIUSA**: la regola degli ibridi (D5=C, decisione di Enzo del 2026-09-15). Tabella
`sys_conflitti_ibridi` + funzione che un futuro connettore di importazione dovrà chiamare
prima di sovrascrivere un valore su una riga con gesto nativo aperto. Due difetti trovati
dalla prova generale, stesso genere in entrambi i casi (ADR-0035): la nuova tabella andava
registrata in DUE file di numero minore che controllano l'esaustività della classificazione
(`000062`, registro di riconciliazione; `000429`, classificazione I23) — emendati entrambi,
non aggiunte migrazioni a valle.

**X-5 CHIUSA**: la sentinella della direzione (`v_direzione_del_dato_violata`). Prevista e
verificata correttamente PRIMA di applicare: nasce con 20 righe note (conseguenza di una
decisione già presa in X-2, non un difetto nuovo), dichiarata `INFORMATIVE`. Unica voce di
questa sessione senza bisogno di correzioni alla prova generale.

**X-6 CHIUSA**: l'indagine sulle 119.226 presenze senza provenienza. Eseguita io stesso, in
linea (non con il meccanismo a più agenti che il mandato disegnava, perché le regole del mio
harness vietano di usarlo senza un'autorizzazione esplicita di Enzo che qui non c'era —
deviazione dichiarata). **Trovata la spiegazione completa**: nessuno dei tre script che
scrivono oggi le presenze (l'avanzamento notturno della storia, il seed iniziale, la
ricostruzione) ha mai scritto il registro di provenienza — spiega il 100% del divario. Non
ho corretto nulla (il mandato lo vieta esplicitamente: non si inventa una provenienza) — ho
scritto tre opzioni per Enzo in `esiti/X-6.md`, aperta come voce **D12** in attesa della sua
scelta.

**F5 è chiusa per intero** (X-1..X-6, cancello d'uscita del mandato verificato: tutte le
evidenze rosse/verdi ci sono, `db_health.py` verde). Riconciliata anche una misura che Cowork
aveva depositato nel frattempo sull'inbox (confermava indipendentemente la mia su
`sys_attendance`, sul gemello anziché in produzione — stesso risultato).

**G-1 (F6) non aperta**: sbloccata (R-6 e X-2 chiuse), ma il mandato stesso la dichiara "non
sta in una sessione" (2 sessioni, ~160k token) — non l'ho iniziata per non rischiare di
consegnarla a metà con il contesto residuo di questa sessione, già a quasi il 60%.

## Il cancello di verifica finale — ROSSO, non risolto a fine sessione

Prima di chiudere ho lanciato la prova generale completa (`verify_gate.py run`) e ha trovato
**tre difetti reali**, tutti corretti in sessione:

1. `docs/kb/SOT_STATE.md` dichiarava la migrazione più recente come 000432, il disco arrivava
   a 000447 — corretto.
2. **Il difetto più serio**: `findApprovers` (modulo tenant-blueprints) escludeva
   `PLATFORM_ADMIN` dagli approvatori di un fascicolo, perché filtrava per il tenant "di casa"
   della persona invece di riconoscere che un ruolo di piattaforma opera su ogni azienda.
   Introdotto da un fix di S1111 (spedito da questa sessione in Voce 1, non scritto da me) e
   mai stato provato fino ad ora. Corretto e verificato: 5/5 + 12/12 test tornano verdi.
3. L'atlante di conoscenza del progetto era invecchiato — rigenerato.

Ho anche risolto un allarme di manutenzione (spazio da recuperare su una tabella di
permessi, conseguenza delle molte prove fatte in questa sessione).

**Il problema**: dopo aver corretto tutto, la verifica completa finale non ha fatto in tempo a
confermarlo — un rilancio era in corso quando il guardiano ha imposto lo stop (contesto al
77,4%, sopra la soglia dell'75% di Enzo). Le correzioni sono verificate una per una, isolate,
e sembrano risolvere il problema — ma **non ho la conferma di un giro completo pulito**.

## Chiusura

**CICLO NON CHIUSO — cancello di verifica ROSSO, da confermare.**

Fatte e verificate sul vivo: Voce 1 (spedire), Voce 2 (i due stash — inventariati, nessuno
integrato), Voce 3 (R-11), Voce 4 (F5 per intero: X-2, X-3, X-4, X-5, X-6), più un compito di
pulizia aggiuntivo di Cowork (fuori mandato). Manca la Voce 5 (G-1, F6): sbloccata ma il
mandato stesso la dichiara un lavoro da due sessioni, non aperta per non consegnarla a metà.

**La prossima sessione deve, come primo atto, lanciare `python docs/kb/tools/verify_gate.py
run` e leggerne il verdetto fresco prima di qualunque altra cosa.** È probabile che sia già
verde (i tre difetti sono verificati corretti singolarmente), ma non è confermato.

**Tre cose aspettano una risposta di Enzo**: `sys_attendance` (X-2, ora confermata da due
misure indipendenti), **D12** (come completare il registro delle presenze, da X-6), e la
conferma implicita che i due difetti trovati stanotte in tenant-blueprints (R-5/S1111) non
abbiano altre ramificazioni non ancora scoperte.

Nessuna cancellazione eseguita in questa sessione. Tutto committato e spedito, verificato
riga per riga contro origin/main. Chiusura per il guardiano, non per una decisione di
merito: nessuna voce del mandato è stata abbandonata a metà, solo la conferma finale del
cancello resta da fare.
