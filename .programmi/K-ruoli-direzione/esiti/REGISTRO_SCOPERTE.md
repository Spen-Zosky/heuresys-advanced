# Registro delle scoperte — fuori da questo ciclo (R24 §5)

| 2026-09-17 (guardiano A RIDOSSO) | S1105, chiusura richiesta da Enzo: deploy/allineamento | `close-propagate.sh` fallisce SEMPRE sul canale `align-claude-ecosystem` quando lanciato al suo interno (2/2 tentativi, FATAL sul canale 2), ma lo STESSO comando (`align-claude-ecosystem.sh all --resilient --skip-smoke`) riesce SEMPRE se lanciato isolato (2/2 tentativi, exit 0, solo drift-warning non bloccanti). Sospetta race con un'altra sessione attiva sullo stesso working tree (Cowork-S1106, che nello stesso intervallo ha scritto e committato `~/.claude/CLAUDE.md`, `guardiano.py`, `STATO.md`) — non confermato con certezza, il tempo non bastava per indagare oltre. | **Effetto pratico**: allineamento REPO+payload+memorie riuscito su vm e linuxpc (verificato 2 volte); clone-DB armato su linuxpc via systemd (verificato 2 volte, `heuresys-advanced-clonedb.service`); ecosistema Claude allineato (verificato isolatamente, 2 volte). **Il DEPLOY (push su `refs/heads/prod`) NON è armato**: `close-propagate.sh` si rifiuta per policy (mai armare sopra un canale "fallito"), e non ho avuto margine per indagare la race o forzare l'armamento a mano prima che il guardiano arrivasse a "A RIDOSSO". La prossima sessione: verificare prima se un'altra sessione è ancora attiva sul working tree, poi rilanciare `bash scripts/close-propagate.sh` (dovrebbe bastare, se il canale 2 non ha davvero nulla di rotto — solo verificarlo un'altra volta con margine pieno).

Cose trovate lavorando che NON sono voci del mandato: si presentano una volta sola come «fuori da questo ciclo: lo vuoi nel prossimo?». Non entrano in «cosa resta», non bloccano nessuna chiusura.

| data | trovata in | scoperta | proposta |
|---|---|---|---|
| 2026-09-14 | F0.1 | Gli orfani del registro di provenienza (registro > tabella) sono **11 tabelle**, non le 2 del dossier (che guardava solo le amministrative): sys_skills 19.764/14.031, sys_learning_modules 4.959/92, sys_learning_paths 3.294/66, sys_goal_updates, sys_goal_comments, sys_job_roles, sys_skill_categories, sys_job_families, sys_okrs, sys_compensation_bands, sys_leave_balance_transactions. | La eredita I-D (passo 17) e S-3: nessuna azione nuova, ma la vista S-3 nascera' con 11 righe, non 2. |
| 2026-09-14 | F0.2 | Negli script W0/W1/W2 la spia si considera trovata solo se il verificatore riporta la STRINGA del comando identica; i verificatori la riscrivono (tolgono `python q.py`, aggiungono un prefisso) e il confronto fallisce anche quando il numero prova che l'hanno trovata (2 casi su 6 in W0). | Nel prossimo giro: confrontare il NUMERO (atteso = valore alterato) oltre alla stringa. Non emendato di iniziativa: il mandato vuole gli script «esattamente come sono scritti». |
| 2026-09-14 | F0.2 | I file `<oggetto>_lettore.json` / `_verifica.json` scritti a mano dagli agenti non coincidono sempre con l'esito strutturato del run (14 vs 16 scrittori; un file non e' JSON valido). | La fonte e' `_risultato.json` (esito strutturato, validato dallo schema); i file a mano sono materiale grezzo. Applicato gia' in `tools/esito_w0.py`. |
| 2026-09-14 | F0.2 | Tre regex «equivalenti» danno tre numeri (53/56/55) per «migrazioni che scrivono sys_auth_role_permissions». | Nessuna: e' il motivo per cui ogni numero porta il comando esatto. |
| 2026-09-15 | F0.4 | `dove_siamo.py` leggeva il file di ROLLBACK (`_rollback_di_`) come «caso (i): effetto assente, si (ri)applica»: dopo un ritiro voluto, una sessione futura avrebbe letto un invito a riapplicare. CORRETTO in F0.4 (S1103): un rollback completo con effetto assente e' «ROLLBACK APPLICATO, NON si riapplica» e la voce creatrice e' «RITIRATA da NNN». Resta un limite: la query di EFFETTI e' per VOCE, non per file, quindi una migrazione vuota della stessa voce (000417) eredita la lettura della vista. | Per F4: quando una voce ha piu' file con effetti diversi, dichiarare la query per file con la chiave `voce/NNN` (gia' prevista da `EFFETTI.get(f"{voce}/{nnn}")`). |
| 2026-09-15 | F0.4 | `db_health.py`: sonda «tuple morte, peggior tabella» ROSSA al 51,2% su `sys_position_skill_requirements`. Non e' una sentinella (le 49 viste sono a zero) ed e' manutenzione (autovacuum), non mandato K. | Fuori da questo ciclo: `VACUUM (ANALYZE) sys.sys_position_skill_requirements` sulla VM, o verifica delle soglie di autovacuum su quella tabella. Lo vuoi nel prossimo? |
| 2026-09-15 | F0.4 | File scritto per errore fuori dallo scratchpad: `C:\Git\tmp_dove.txt` (2,9 KB, output di dove_siamo.py). V1: non si cancella in regime non presidiato. | Enzo lo puo' cancellare quando vuole; non serve a niente. |
| 2026-09-15 | prima di W1 | **DEROGA CONSAPEVOLE a «gli script si usano esattamente come sono scritti», su istruzione di Enzo (messaggio in sessione S1103).** In `W1_indagini.js`, `W2_classificazione.js`, `W5_presenze.js` il controllo `spiaTrovata` confrontava la sola STRINGA del comando; i verificatori la riscrivono, e in W0 2 casi su 6 (sys_auth_permissions, role-codes.ts) erano stati loggati SCARTATO nonostante la discrepanza numerica provasse la spia trovata (8→1, 10→3). Aggiunto `\|\| d.atteso === spia.comandi[0].numero` (il numero alterato, +7): la stringa resta come prima condizione, niente tolto. Provato con `tools/prova_spia.mjs` sui due casi reali estratti da `_risultato.json` di W0: vecchio SCARTATO / nuovo ACCETTATO, e un verificatore che riporta il numero VERO resta SCARTATO (`evidenze/spia_prova_202609150150.txt`). W0 non e' stato toccato: e' gia' verificato e non si rilancia. | Nessuna: e' la correzione. Se Cowork riscrive il mandato, la riga va portata nel testo degli script (sezioni 6 e 10). |
| 2026-09-15 | W1 | Due agenti di W1 hanno scritto FUORI dalla loro cartella (V10): `tools/misura_i_b.py` (02:37, lettore I-B: uno script di misura suo) e `D:temp_source_lineage.txt` alla radice del repo (02:41, lettore I-D-codice: output di rg; il nome contiene un due punti Unicode). Non usati; non cancellati (V1). | Enzo puo' cancellarli. Nel prossimo giro: aggiungere a REGOLE degli script «non creare file in tools/ ne' alla radice». |
| 2026-09-15 | W2 | Primo lancio di W2 (wf_fdd26207-9df, 03:31-04:06) fallito per RETE: 6 agenti su 7 morti con ENOTFOUND / ConnectionRefused verso l'API di Anthropic; nello stesso quarto d'ora il cancello test-api e' uscito 255 (ssh). Non e' un difetto dello script: e' la connessione di casa caduta di notte. Cartella lasciata senza _VERIFICATO (W5: si rilancia da capo in _b). | Nessuna. |
| 2026-09-16 | R-1 passo 34, sync gemello | Il `git pull --ff-only` sul gemello (linux-pc) rifiutava per 8 file «modificati localmente» + 5 non tracciati che sarebbero stati sovrascritti; `git status` mostrava **206 file** segnati `M`. Verificato file per file (`git diff origin/main -- <file>`): il contenuto coincide byte per byte con `origin/main` per tutti i campioni controllati (differenza di sola riga di comando/CRLF); solo `controlli-per-nome.baseline.json` aveva un contenuto realmente diverso (versione precedente, ora superata dal mio aggiornamento). Il gemello ha già un caso identico e già gestito da una sessione precedente: `stash@{1}: S1092: residui S1091 identici a origin`. | Messo da parte con `git stash push -u -m "K-mandato: residuo pre-pull S1104..."` (NON cancellato, V1) e poi `git pull --ff-only` andato a buon fine. Lo stash resta in lista (`stash@{0}`) accanto al precedente: sono due residui noti dello stesso tipo (probabile differenza di `core.autocrlf`/permessi fra le due macchine che fa apparire ogni file come modificato). Enzo può decidere se vale la pena indagare la causa (config git del gemello) o lasciare che si accumulino stash innocui prima di ogni pull. |
| 2026-09-16 | R-1 passo 34, prima corsa manuale della batteria | Nella PRIMA corsa manuale di `pnpm --filter @heuresys/api test` sul gemello (prima di `verify_gate.py run`), `test/semantic-matching.integration.test.ts` ha dato 2 test rossi su 33 (una skill attesa non compariva nella top-20 di similarità). Il file NON tocca nessuno dei 9 file di R-1 e non usa `mandati.ts` (ha una sua `isPlatform` locale, mai toccata). Rilanciato **isolato** da Windows via tunnel: **33/33 verdi**. Rilanciata l'intera batteria tramite `verify_gate.py run` (via `prova-api-sul-gemello.sh`, il canale canonico): **GREEN, test-api incluso**. Conclusione: flake dipendente dall'ordine/stato del clone del gemello (verosimilmente la ricerca per similarità su un corpus di skill che cresce ad ogni corsa di test), non una regressione di R-1. | Nessuna azione di R-1. Se il flake si ripresenta in altre sessioni, vale la pena indagare se la ricerca vettoriale ha uno stato non isolato per-file (il resto della suite lo è, `tx-isolation.ts`) — fuori da questo ciclo. |
| 2026-09-15 | cancello test-api | **131 file di test su 276 ROSSI sul gemello** con un solo errore: «Nessun segreto TOTP per enzo.spenuso@heuresys.com». I test entrano come `enzo.spenuso` (unico PLATFORM_ADMIN di prova, in `FIXTURE_PERSONA_EMAILS`) e leggono il segreto dal fattore con etichetta `derived-access`; ma `provision-derived-access.ts` **non tocca le persone reali** (decisione S1032) e `seed-test-admin.ts` rifiuta un DB che non si chiama `*_ci`/`*_test` (guardia S1093): sul gemello il DB dei test e' `heuresys_advanced`, clone notturno della produzione, dove Enzo ha un TOTP suo, senza etichetta. Ieri alle 19:11 test-api era verde: il fattore di prova esisteva ancora sul gemello ed e' sparito col clone delle 01:33. Non e' mandato K e tocca le credenziali di una persona reale. | **RISOLTA in sessione (21:57, cancello GREEN, test-api verde in 1875 s)**: la premessa era sbagliata — `enzo.spenuso` NON e' in `REAL_PERSON_EMAILS` (deroga dichiarata da Enzo, #139 2026-08-08: «i test entrano come lui»), quindi `pnpm db:provision-access` lo copre. Dry-run: 1 fattore da creare e nessun'altra scrittura; eseguito in produzione e sul gemello (1 fattore `derived-access` ciascuno; il TOTP personale di Enzo intatto). Resta il difetto di fondo: il clone notturno del gemello cancella il fattore, quindi domani il gemello lo perde di nuovo finche' il clone non ri-esegue `provision-access` dopo il ripristino (via (c)). Le tre vie erano: (a) un secondo fattore TOTP `derived-access` sul SUO utente in produzione (contraddice S1032); (b) i test usano una persona di piattaforma NON reale (es. un utente SERVICE PLATFORM_ADMIN) — la via pulita; (c) il clone notturno del gemello ri-semina il fattore dopo il ripristino. Finche' non si decide, `test-api` e' rosso e il cancello locale con lui. |
| 2026-09-17 | S1105, R-9, sync gemello | Lo stesso fenomeno di S1104 (riga sopra), ma su scala molto piu' ampia: `git pull --ff-only` sul gemello rifiutava per **228 file** `M` + 6 non tracciati (l'intero modulo `platform-tenant-assignments`, il test `perimetro-clienti`, le due migrazioni 000421/000422). `git diff --stat origin/main` ha mostrato che **solo 43 file** avevano un contenuto realmente diverso da `origin/main` — e quei 43 erano tutti file NUOVI arrivati con R-0/R-9 che il gemello semplicemente non aveva mai ricevuto (HEAD locale fermo a `522defe7`), non contenuto divergente. I restanti ~190 erano lo stesso rumore CRLF/permessi gia' documentato. | Messo da parte con `git stash push -u` (V1, non cancellato) e poi `git pull --ff-only` andato a buon fine (64 file, 11.646 inserimenti). Terzo residuo dello stesso tipo in tre sessioni consecutive (S1091→S1092, S1104, S1105): la causa (probabile `core.autocrlf` diverso fra Windows e il gemello Linux) non e' stata ancora indagata, e la crescita (8→228 file) suggerisce che valga la pena farlo — proposta per Enzo, fuori da questo ciclo. |
| 2026-09-17 | S1105, A3 | Misurate le tre copie di `guardiano.py` (mandato S1105, voce A3): `~/.claude/tools/guardiano.py` e `D:/heuresys-advanced/docs/kb/tools/guardiano.py` sono **identiche** (46.195 byte ciascuna, `diff` vuoto). `D:/heuresys-datastore/scripts/guardiano.py` e' **diversa** (41.758 byte, 30/08/2026, -4.437 byte rispetto alla fonte): e' un altro progetto, con un mandato suo. | **NON toccata** (istruzione esplicita del mandato S1105). Registrata qui e basta. La propagazione della fonte emendata (dopo A1) andra' solo sulla copia di `heuresys-advanced`. |
| 2026-09-19 | S1106, R-2 | `gdpr:retention` e' concesso via RBAC a TENANT_ADMIN, HRMS_MANAGER (e ora DPO, mig. 000423), ma `gdpr/service.ts:158` (`runRetention`) risponde comunque **403 PLATFORM_ONLY** a tutti e tre: la porta chiede `haMandatoGdpr(actor)`, e `GDPR_MANDATE_ROLES` contiene solo `PLATFORM_ADMIN` (`mandati.ts`). Misurato sul codice vivo in S1105 (esiti/R-2_domanda_masking.md) per i primi due; DPO eredita lo stesso comportamento per decisione C di Enzo (non entra in GDPR_MANDATE_ROLES). Non e' una regressione introdotta da R-2: e' un disallineamento preesistente fra il permesso RBAC concesso e il mandato di servizio controllato. | Fuori da questo ciclo: decidere se allineare la porta al permesso (aprire `GDPR_MANDATE_ROLES` a chi ha gia' `gdpr:retention`) o restringere il permesso a chi ha davvero il mandato. Lo vuoi nel prossimo? |
| 2026-09-19 | S1106, R-2, prima di applicare 000423 | `linux-pc` (gemello, 192.168.1.11) irraggiungibile: 4 tentativi SSH in totale, tutti `Connection timed out` su porta 22, in orari diversi della stessa sessione. E' anche il runner CI self-hosted: nessun run automatico possibile finche' non torna su. RISOLTO diversamente da come registrato qui la prima volta: la VM (`oracle-vm-default`) e' un gemello alternativo legittimo (`IDEMPOTENZA_HOST=<altro-host>` e' un escape hatch gia' documentato negli script), e la prova generale vi e' stata eseguita con successo — vedi le due righe sotto per i due difetti reali trovati e corretti nel farlo. | Nessuna: la prova generale e' stata eseguita sulla VM, verde, prima di applicare 000423. |
| 2026-09-19 | S1106, R-2, prova generale sulla VM | **Difetto 1**: `createdb --template` (usato da `ci-rehearsal.sh` per la copia usa-e-getta) non copia `pg_database.datacl` — la copia riparte con l'ACL di default, non con quella del modello. La migrazione 000340 (PUBLIC fuori dal database, ruoli nominati dentro per nome — `codex_auditor`, `gov_worker`) trova quei ruoli senza CONNECT sulla copia, perche' quel GRANT non viaggia col template. **Mai visto prima** perche' la prova gira sempre su `linux-pc`, dove quei ruoli cluster-level non esistono affatto (la post-condizione della 000340 li salta in silenzio, `FROM pg_roles WHERE rolname IN (...)` non trova righe). Sulla VM esistono davvero — e' li' che il difetto si vede. | **CORRETTO in sessione**: `ci-rehearsal.sh` ora replica l'ACL del modello riga per riga (aclexplode) dopo il `createdb -T`. |
| 2026-09-19 | S1106, R-7, prova generale della 000425 | `db/migrations/000314_deleghe.sql` contava le concessioni di `delegation:read`/`delegation:manage` GLOBALMENTE («devono essere esattamente 6») invece che sui tre ruoli che quella migrazione stessa concede (PLATFORM_ADMIN, TENANT_ADMIN, HRMS_MANAGER). Aggiungere `SECURITY_ADMIN` come quarto titolare legittimo fa salire il conteggio globale a 8, e la seconda passata della catena andava rossa su un fatto che non era un difetto — è esattamente il "rottura alla seconda passata" che il messaggio di `ci-rehearsal.sh` descrive: un file crea un oggetto che un file di numero minore deve conoscere. | **CORRETTO in sessione** (ADR-0035, il file che crea si emenda): il conteggio ora si scopa ai tre ruoli originari. Verificato: prova generale VERDE dopo il fix. |
| 2026-09-19 | S1106, R-2, dopo aver applicato 000423 sulla VM | **Il bundle di produzione (`apps/api/dist/server.js` sulla VM) e' fermo al 14 settembre** (mtime `Sep 14 15:25`), **prima di R-9** (17 settembre). Riavviando il servizio per far ricaricare la cache RBAC dopo la 000423, il log mostra `unknownRolesSkipped: ["PLATFORM_OPERATOR","SALES","DPO"]`: il bundle compilato non conosce nessuno dei tre ruoli, quindi in produzione **oggi hanno zero permessi effettivi**, nonostante le righe/i grant siano corretti nel database (verificato: `DPO` esiste, `HRMS_MANAGER` ha perso `gdpr:erase`). Causa: `heuresys-advanced-deploy-watch.service` rifiuta di rilasciare — log: *«armato d9a84004 ma origin/main e' a 7f2d4671 — la punta non e' piu' quella autorizzata, non deployo»* — perche' lo sha armato dall'ultima `close-propagate.sh` (sessione precedente, che aveva gia' dichiarato "deploy NON armato" in `ab71ad46`) e' superato, e il rearmo richiede CI verde, che richiede `linux-pc` (fermo, vedi righe sopra). **Non e' un difetto introdotto stanotte**: PLATFORM_OPERATOR/SALES erano gia' nella stessa condizione prima che toccassi qualunque cosa (il bundle e' del 14, R-9 e' del 17) — l'ho solo reso visibile riavviando il servizio, cosa necessaria perche' il ritiro di `gdpr:erase` a HRMS_MANAGER avesse effetto (la cache si carica una volta all'avvio). Il riavvio stesso e' innocuo: stessa immagine di prima, nessuna regressione nuova. `test-api` (che gira da sorgente via `buildTestApp()`, non contro il bundle) non e' toccato da questo — vede il codice giusto. | **Non risolta stanotte, fuori scope R-2**: serve `linux-pc` per il deploy normale (CI verde -> rearmo -> rollout). Se Enzo vuole un deploy manuale prima che `linux-pc` torni, e' una decisione sua (skill `full-alignment-deploy`), non mia da prendere qui. **Segnalazione per Enzo**: R-9 (PLATFORM_OPERATOR, SALES), chiusa il 17 settembre, non e' MAI stata funzionalmente viva in produzione — la sua chiusura era corretta sul codice/DB, ma il deploy non e' mai arrivato. |
| 2026-09-19 | S1106, R-2, prova generale sulla VM | **Difetto 2, piu' serio**: `heuresys_ci` **sulla VM** ha l'ACL **vuota** (misurato: `select datacl from pg_database where datname='heuresys_ci'` -> vuoto) nonostante il suo registro `sys_schema_migrations` dichiari 000340 gia' applicata (max migrazione 000393). Il fix del difetto 1 da solo non bastava: senza un'ACL vera sul modello da cui copiare, replicarla non aggiunge niente. E' un database **stantio/inaffidabile** su quel fronte specifico — non sappiamo come e' nato ne' se e' mantenuto da qualcosa. La prova di stanotte ha usato `heuresys_advanced` (la produzione reale, locale sulla VM) come modello via `CI_REHEARSAL_TEMPLATE=heuresys_advanced` (nuovo passthrough aggiunto a `prova-idempotenza.sh`, che prima non passava la variabile al comando ssh remoto): esito verde, 420 file, due passate, 53/53 sentinelle. **Nota collaterale, non verificata a fondo**: la VM non ha un database di test/CI separato — `.env` li' punta direttamente a `heuresys_advanced` (produzione reale). `prova-api-sul-gemello.sh HOST=oracle-vm-default` (usata per la voce di test-api di questa stessa sessione) quindi esercita i test API **contro la produzione reale**, protetta solo dall'isolamento transazionale per-file (D-52, gia' in uso per OGNI test lanciato via tunnel da Windows in questa e altre sessioni — non e' un rischio nuovo, ma vale la pena saperlo esplicitamente). | Fuori da questo ciclo: (a) capire perche' `heuresys_ci` sulla VM ha l'ACL vuota e se vale la pena ripararlo o e' meglio ritirarlo come nome fuorviante; (b) decidere se la VM merita un proprio database di test separato da produzione, come lo ha `linux-pc`. Lo vuoi nel prossimo? |
