# Esito #258 — la persona di collaudo di piattaforma

**Stato: FATTO.** Register: `docs/kb/SOT_BACKLOG.md` #258 → `DONE` (blocco completo spostato in `docs/archive/SOT_BACKLOG_CHIUSI.md`).

## Cosa è cambiato

I 113 (poi risultati 141, contando anche i sorgenti/CI oltre ai test) file che impersonavano
`enzo.spenuso@heuresys.com` per superare la sfida MFA come PLATFORM_ADMIN ora usano una persona
di collaudo dedicata: `platform-test-admin@collaudo.invalid` — utenza `SERVICE` di Heuresys
System, ruolo `PLATFORM_ADMIN` con un vero grant di piattaforma (tenant_id NULL), fattore TOTP
`derived-access` reale (non esente da MFA, come le cinque persone RTL). Enzo non viene più
impersonato in nessun test.

## Commit (in ordine)

- `9dec9321` — F1: crea la persona in produzione (`provision-collaudo-access.ts` esteso con un
  ramo `mfaExempt:false`), verificata dal vivo con un login MFA a due passi riuscito.
- `43d3705c` — F2: sostituisce l'email in 141 file, protegge di nuovo l'email di Enzo
  (`REAL_PERSON_EMAILS`), esclude gli utenti MFA-esenti dal pool impersonabile generico,
  aggiorna i seed e i workflow CI.
- `82dfb4be` — fix: un test (`analytics.integration.test.ts`) assumeva che PLATFORM_ADMIN vedesse
  tutto quanto TENANT_ADMIN sui dati di compensation. Era vero solo perché Enzo era anche al
  vertice reale dell'organigramma (I19) — la nuova persona, senza posizione, è soggetta alla
  soglia di catena sui vertici (ADR-0036 §5) come qualunque mandato senza posizione. Il test ora
  deriva l'atteso invece di presupporre l'uguaglianza.

## Prova di chiusura

`bash db/scripts/prova-api-sul-gemello.sh` (= suite `test-api` di `verify_gate.py`) su HEAD
`82dfb4be`, sul gemello con il clone rinfrescato DOPO il provisioning di F1
(`heuresys-advanced-clonedb.service`, Result=success 2026-09-26 06:30:35, innescato dal
governo dopo la mia domanda) → **verdetto GREEN**: typecheck 24.9s, test-api 1640.7s, entrambi
exit=0. `.zp/verify-verdict.json` porta l'evidenza.

Grep di chiusura: `grep -rn "enzo.spenuso@heuresys.com" apps/api/test apps/web/tests` → 0 righe.

## Cosa NON è di questa voce (residuo dichiarato, non bloccante)

Uno `git stash` ("CRLF residue pre-258") è rimasto sul working tree del clone del linux-pc:
l'ho creato io per sbloccare un `git pull` che il gemello rifiutava per differenze di
terminazione riga (CRLF/LF) preesistenti su ~127-307 file, non legate a #258 — la loro
sostanza (poche righe per file) è cosmetica. Si vede con
`ssh linux-pc 'cd ~/heuresys-advanced && git stash list'` e si recupera con `git stash pop`
se mai servisse; altrimenti sparisce da sé al prossimo `align-clones.sh linuxpc`
(`reset --hard` lo scarta). Non l'ho toccato oltre: non è nel perimetro della voce.

## Incidenti di sessione, per onestà (Test-before-claim)

Ho commesso due errori operativi durante la chiusura, entrambi corretti sul momento:
1. Ho scambiato un processo di test legittimo (v2, avviato dal mio stesso script) per un
   residuo e l'ho terminato — il governo lo ha rilevato e mi ha corretto; ho rilanciato la
   prova pulita subito dopo.
2. Ho eseguito una corsa completa dell'intera suite `test-api` in locale (via tunnel,
   contro produzione) per diagnosticare un fallimento, impiegando ~55 minuti per una misura
   che poteva costare pochi minuti isolando i file coinvolti — il governo mi ha fermato e
   corretto il metodo.
Entrambi hanno consumato tempo e contesto oltre il necessario; li registro qui perché la
sessione lo richiede (accountability, CLAUDE.md).
