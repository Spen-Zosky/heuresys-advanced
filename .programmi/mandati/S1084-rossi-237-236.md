# S1084 — i tre rossi del boot, poi `#237` e `#236`

> **mandato di Enzo (2026-08-29)**: *«risolvi i rossi segnalati dal boot e poi procedi con 237 e 236»*
> **capienza all'apertura** (misurata, non stimata): `guardiano --budget 300000` → `✓ si continua —
> contesto: mancano 640,590 token · 5h: mancano 42.0 punti`.

## Confine di sessione, dichiarato adesso (R24 §4)

**Dentro**: i tre rossi · `#237` **F1 sola** · `#236` **F2 sola**.
**Fuori, e non ci si avvicina**: `#237` F2/F3 (dipendono dal risultato di F1 — decidere la cura
prima di conoscere la ripartizione sarebbe il «per tentativi» che Enzo ha vietato in apertura di
`#237`) · `#236` F3/F4 · le voci DREAM e `docs/vision/**` (sessioni dedicate).

## Il piano

| id | cosa | chi | fatto = | stato |
|---|---|---|---|---|
| **R1** | derivati 2/3 superati | io | `build_derivati.py` rigenera e il registro riparte da `HEAD` | ✅ **FATTO** |
| **R2** | `marciume:fallito` nell'ultima chiusura | io | la misura di oggi è verde **e** il boot sa distinguere un guasto riparato da uno aperto | ✅ **FATTO** — `check_marciume` exit **0** senza pipe; `rendiconto_chiusure --selftest` **5/5**, quattro casi pretendono che il rosso NON si spenga; boot: `OK … tutti sereni · 1 riparati dopo` |
| **R3** | working tree sporco | io | committato il solo file mio; le 4 voci di Codex restano intatte | ✅ **FATTO** — `95e74c40`; le 4 voci di Codex restano untracked |
| **A1** | `#237` **F1** — dove va il costo | io | tabella «categoria → token → % della chiusura» su ≥3 sessioni, e la risposta a *quanto costa la chiusura pura* | ✅ **FATTO** — `4f068a32`; **14** chiusure; **28.352 token = 2,8%**, non il 25%; lo stato è il **48,2%** e leggerlo costa più che scriverlo; `--selftest` **15/15** |
| **A2** | `#236` **F2** — l'armamento del clone | io | la chiusura **arma** e ritorna; un timer sul gemello esegue; prova = chiusura **durante** un clone, e il clone finisce | ✅ **FATTO** — `7050b1fa`; il timer **c'era già**, mancava l'innesco; ucciso ogni ssh a metà corsa → `Result=success`, clone integro; batteria **234 ok / 0** |

## Simulazione (R24 §3) — cinque domande per voce, risposte **prima** di eseguire

### R2 — il rosso del marciume

- **Precondizioni** — ✅ misurate: `check_marciume.py` eseguito **senza pipe** esce **0** e stampa
  «niente è marcito» (la pipe maschera l'exit code — memoria `pipe_masks_exit_code`). Il diario
  `.handoff/close-log.ndjson` riga 488 porta `marciume:fallito` alle 23:22 del 28, e la riga 491
  alle 23:29 dice già *«check_completezza_self riparato, marciume torna verde»*.
- **Meccanismo** — `rendiconto_chiusure.py:righe_boot()` prende la corsa dell'ultima `propaga` e
  marca `BAD` ogni passo il cui esito non è `eseguito|saltato`. **Non guarda oltre quella corsa**,
  quindi una riparazione avvenuta dopo è invisibile per costruzione. Letto il file, non supposto.
- **Propagazione** — lo strumento è versionato in `docs/kb/tools/`: arriva ai cloni col `git pull`
  della propagazione. Il diario è per-macchina e gitignored: la riga di riparazione vale qui, ed è
  giusto così (è cronaca di questa macchina).
- **Chi** — io.
- **Guardia** — la correzione **non deve poter mascherare un guasto aperto**: vale solo se esiste,
  nel diario, una riga **più recente** con lo **stesso** `step` ed esito sereno. Caso limite da
  provare: guasto senza riparazione → deve restare rosso.

### R3 — l'albero sporco

- **Precondizioni** — ✅ `git diff` misurato: un solo file mio, `.claude/settings.local.json`, una
  riga (`outputStyle: default → Concise`). Le altre 4 voci sono di Codex, **attese per contratto**.
- **Meccanismo** — `git add` con **path esplicito**, mai `-A` (le voci di Codex non sono mie).
- **Propagazione** — è un file di configurazione locale tracciato: il commit lo porta ovunque.
- **Chi** — io. Commit locale su `main` pre-autorizzato; il push no.
- **Guardia** — `git status` prima e dopo: le 4 voci di Codex devono restare **untracked**.

### A1 — `#237` F1

- **Precondizioni** — ✅ misurate, e sono migliori di quanto il piano sperasse: i transcript stanno
  in `~/.claude/projects/D--heuresys-advanced/` (**95** file), e **71** di essi portano il campo
  `attributionSkill` sui record `assistant`. Fra questi, decine con `handoff`: la chiusura è
  **marcata alla sorgente**, non va indovinata da euristiche sui prompt.
- **Meccanismo** — uno strumento nuovo, `docs/kb/tools/costo_chiusura.py`, che per ogni turno legge
  `message.usage` (le tre voci che il guardiano già somma) e ne ricava la **crescita** di contesto
  rispetto al turno precedente; attribuisce alla chiusura i soli turni con
  `attributionSkill == "handoff"`, e li ripartisce per categoria in base ai tool realmente usati.
- **Propagazione** — in `docs/kb/tools/`, versionato: vale su tutte le macchine.
- **Chi** — io. Sola lettura: non tocca né il repo né il database.
- **Guardia** — non è distruttivo, quindi la guardia che serve è quella **sulla verità della
  misura**: ⚠ una ripartizione che «torna per costruzione non dimostra niente» (il programma lo
  dice). Perciò due controlli indipendenti — (a) la quota **non attribuita** dev'essere dichiarata
  e piccola, non nascosta; (b) una seconda misura per **byte di contenuto** deve dare una
  ripartizione compatibile con quella per token. Se le due divergono, la misura è sbagliata.

### A2 — `#236` F2

- **Precondizioni** — da misurare **prima di scrivere**: che sul gemello esista già l'unità
  `heuresys-unit-failure@.service` usata da `OnFailure` (il modello di `#165`), e come il timer del
  deploy legge l'armato. Non si copia a memoria: si legge l'unità vera.
- **Meccanismo** — armamento = un file-sentinella con sha e motivo, scritto in modo atomico;
  esecuzione = `heuresys-clone-watch.timer` sul gemello, `Persistent=true`, `OnFailure` esistente.
  `close-propagate.sh` passa da «lancia e aspetta» a «arma e ritorna».
- **Propagazione** — le unità systemd **non arrivano col `git pull`**: vanno installate sul
  gemello, e l'installazione è parte della fase (memoria `vm_deploy_self_modify_buffer`: un'unità
  nuova non è installata al primo deploy).
- **Chi** — io, `linux-pc` raggiungibile via ssh.
- **Guardia** — la prova **è** l'interruzione: si chiude la sessione **mentre** il clone gira e il
  clone deve finire lo stesso. Il caso felice da solo sarebbe verde anche oggi. E finché il timer
  non è installato e visto funzionare, `close-propagate.sh` **non** si converte: armare qualcosa
  che nessuno esegue è peggio di eseguirlo appeso alla sessione.

## ✅ CICLO CHIUSO — 5/5 voci fatte, non resta niente

Lo stato si legge dalla tabella qui sopra. Le voci fuori scope dichiarate in apertura
(`#237` F2/F3 · `#236` F3/F4) restano **fuori**, e non sono pendenze di questo ciclo.

## Registro delle scoperte (R24 §5 — fuori da questo ciclo, presentate una volta sola)

1. **Un apostrofo dentro un `${VAR:-default}` disattiva il codice che segue.** Bash apre lì una
   stringa anche fra doppi apici; `bash -n` resta verde e uno `exit 1` smette di essere un
   comando. Trovato in `arma-clone.sh`, curato, e ora presidiato da un caso della batteria. La
   trappola era **già documentata** in `arma-deploy.sh`: un commento in un altro file non è un
   presidio. → *vale la pena un controllo su tutti gli script?* Una ricerca sul repo dice che
   oggi non ci sono altre occorrenze non bilanciate: la voce si apre solo se ne ricompaiono.
2. **Il rendiconto delle chiusure non sapeva spegnersi.** Curato dentro R2, ma la forma è
   generale: *un allarme che resta acceso su un guasto già riparato insegna a non guardarlo*
   (è `#194` per l'atlante). → *ci sono altri indicatori del boot con questa forma?* Non
   misurato.
3. **`heuresys_clone_probe` esiste sul gemello.** Visto contando i database durante la prova di
   `#236` F2. Non è un residuo dello swap (`_stage`/`_old` sono a zero), sembra una fixture dei
   test. → *è vivo o è residuo?* Non misurato, fuori da questo ciclo.
