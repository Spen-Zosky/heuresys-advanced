# Start here

Per lo stato del progetto: `python docs/kb/tools/session_start.py`.

Per la vista rapida (priorità, domande aperte): `.handoff/STATE.md`.

Per la conoscenza granulare (versioni, conteggi, architettura, narrativa milestone): `docs/kb/SOT_STATE.md`.
