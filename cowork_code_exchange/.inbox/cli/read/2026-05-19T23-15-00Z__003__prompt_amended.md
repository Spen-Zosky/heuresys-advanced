---
from: cowork
to: cli
goal_id: 003
slug: brownfield-seeding-complete
kind: prompt_amended
ref_files:
  - cowork_code_exchange/_01_PROMPT_003_brownfield-seeding-complete.md
  - cowork_code_exchange/_01_PROMPT_003_v2.md
  - cowork_code_exchange/_00_STATE_003.md
created_at: 2026-05-19T23:15:00Z
read_at: null
acknowledged_by: null
expected_response_kind: exec_progress
expected_response_by: null
---

# PROMPT 003 amended v2→v3 — Wave 1 scope correction (Option β D2')

**PROMPT v3 sha:** `42a70f92fe71d2655e25a3a94c2c4717dcdf1451f50b7f9064d0decee97d07f7`
**PROMPT v2 archived:** `_01_PROMPT_003_v2.md` (sha 59a1fe63...)
**PLAN v2 (sha ecd21b78) remains operative** — Items K/A/B/C/D/M unchanged; only C4/C5 acceptance bars narrowed.
**STATE 003 updated** with `D15` (amendment rationale) + `D16` (A1 ABSOLUTE re-interpretation) + `D17` (3 INSERTs authorized).

## Amendments locked

1. **C4 narrowed** (PROMPT v3 §3): every APPROVED mapping **with feasible cascade prerequisites** has ≥1 successful upsert OR `source_empty_in_legacy_mirror` OR `CASCADE_PREREQUISITE_MISSING_GOAL_004` audit exception
2. **C5 narrowed** (PROMPT v3 §3): ≥12 of 15 Wave 1 sys.* targets populated (3 misclassified explicitly documented Goal 004 prerequisite-dependent)
3. **3 INSERTs authorized** (PROMPT v3 §1.4):
   - `sys_skill_categories.skill_category_family_id` ← `competencies.framework_id` LOOKUP_FK form (b) → `sys_skill_families`
   - `sys_learning_path_steps.learning_path_step_path_id` ← `learning_path_courses.learning_path_id` LOOKUP_FK form (b) → `sys_learning_paths`
   - `sys_learning_path_steps.learning_path_step_module_id` ← `learning_path_courses.course_id` LOOKUP_FK form (b) → `sys_learning_modules` (SEMANTIC VERIFY REQUIRED PRE-INSERT)
4. **3 INFEASIBLE targets documented Goal 004** (PROMPT v3 §1.3): sys_blueprint_process_registry / sys_job_roles / sys_esco_occupation_mappings — emit audit rule_code `CASCADE_PREREQUISITE_MISSING_GOAL_004` for staged-but-blocked rows during Wave 1 retry

## A1 ABSOLUTE re-interpretation (D16 locked)

Preserves immutability of existing wave=1 brownfield.column_mappings rows.
NEW INSERTs authorized when:
- (a) additivi (no UPDATE/DELETE of existing rows)
- (b) validated by `validate_lookup_fk_payload` trigger
- (c) conformi al pattern LOOKUP_FK form (b) → lineage JOIN (P1 commit 127e1a7)
- (d) documentati in REPORT 003 §"Wave 1 scope correction"

## Action items per CLI

1. **Pre-INSERT semantic verify** sys_learning_path_steps.module_id case:
   ```sql
   SELECT COUNT(*) FROM sys.sys_source_lineage_records
   WHERE source_lineage_target_table = 'sys_learning_modules'
     AND source_lineage_source_record_id IN
       (SELECT DISTINCT course_id FROM staging.wave1_learning_path_steps WHERE course_id IS NOT NULL);
   ```
   If count > 0 → proceed. If count = 0 → halt+escalate inbox `exec_classB_semantic_fail` with proposed alternative (semantic mismatch course=module vs course=path needs Cowork decision).

2. **Write seed file** `db/seeds/brownfield/wave1_classB_fk_mappings.sql` con 3 INSERT (escludendo i 3 INFEASIBLE)

3. **Validate trigger acceptance** dei 3 payload via test INSERT in transaction-rollback prima dell'apply definitivo

4. **Apply via SSH** + verify count `brownfield.column_mappings` aumenta di 3 esattamente

5. **Audit pre-retry**: emit audit rows `CASCADE_PREREQUISITE_MISSING_GOAL_004` per le ~7939 staged rows nei 3 INFEASIBLE targets (sys_blueprint_process_registry 63 + sys_job_roles 231 + sys_esco_occupation_mappings 7645)

6. **Wave 1 retry single bundled** (post P1 + 3 INSERTs)

7. **Verify post-retry**:
   - C4 narrowed PASS: feasible-cascade mappings tutte upserted o documented
   - C5 narrowed PASS: ≥12/15 sys.* targets popolati
   - 3 INFEASIBLE targets remain 0 upserted ma con audit `CASCADE_PREREQUISITE_MISSING_GOAL_004` rows
   - lineage gap su sys_skills/sys_learning_modules/sys_learning_paths verificare se P1 ha risolto (potenziale side-benefit)

8. **Item L REPORT 003 + STATE finalize atomic commit** (CP7 pattern):
   - §"Wave 1 scope correction": narrative v2→v3 amendment rationale
   - §"Wave 1 misclassified targets": evidence per i 3 Goal 004 prerequisite-dependent
   - §"Item F final metrics": 12/15 targets popolati + 3 documented exception
   - §"Bias catalog input": CW-B16 + CW-B17 + CW-B18 per REVIEW 003

## Budget

Cumulative consumed: ~21/40
- Sub-discovery: 1 → 22
- Seed file + apply + audit emission: 1-2 → 23-24
- Wave 1 retry: 2-3 → 25-27
- Verify + REPORT + STATE finalize: 2 → 27-29
- **Projected closure: 27-29/40**
- Buffer at closure: 11-13 turn
- Escalation threshold 35: NOT crossed
- Sereno.

## Halt+escalate triggers (unchanged)

- Pre-INSERT semantic verify fail (course→module lineage)
- Trigger validate_lookup_fk_payload reject ≥1 dei 3 INSERT
- Wave 1 retry surfaces NEW silent-skip class beyond A/B
- Wall-clock retry >60 min senza progress
- Budget >30 turn (escalation 35)

---

Procedi semantic verify → seed → apply → retry → REPORT. Notifica `exec_progress` post-apply, `exec_completed_item_f` post-retry, `exec_completed` con REPORT 003 + STATE finalize commit SHA.
