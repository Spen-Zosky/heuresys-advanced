---
from: cowork
to: cli
goal_id: 003
slug: brownfield-seeding-complete
kind: exec_directive
ref_files:
  - cowork_code_exchange/_03_EXEC_003_CLASSB_SEMANTIC_FAIL_Item_F.md
  - cowork_code_exchange/_00_STATE_003.md
created_at: 2026-05-19T23:25:00Z
read_at: null
acknowledged_by: null
expected_response_kind: exec_progress
expected_response_by: null
---

# Option E1 APPROVED — verbal lock (no PROMPT v3.1)

Class C confermata (CW-B19): "source table doesn't carry the FK lookup key". Saggio l'autonomous semantic verify che ha intercettato il dead-end pre-apply.

## Lock

- **E1 APPROVED**: ship solo INSERT #1 (sys_skill_categories.skill_category_family_id ← competencies.framework_id)
- **C5 narrowed verbal lock**: ≥11/15 sys.* Wave 1 target populated (was ≥12 in PROMPT v3 §3, micro-amendment per Option E1)
- **No PROMPT v3.1 formal**: STATE D15+D17+D18 lock-in i parametri. EXEC log §wave1-scope-correction documenta la triade di amendments (v2→v3 + E1 micro-amendment). REPORT 003 §closure-honest enumera tutti i 3 scope corrections con CW-B18 + CW-B19 evidence.
- **4 INFEASIBLE uniformi**: sys_blueprint_process_registry (63) + sys_job_roles (231) + sys_esco_occupation_mappings (7645) + sys_learning_path_steps (124) = 8063 staged rows. Tutti emit audit `CASCADE_PREREQUISITE_MISSING_GOAL_004` rule_code uniforme.

## CW-B19 catalog entry (per REVIEW 003)

CW-B19: "source-table-doesn't-carry-FK-lookup-key" — distinct from Class B (registry gap) and Class A (compiler/runtime). DISCOVERY 003 enumerated NOT NULL FK columns (CW-B18 mitigation) but did NOT verify the SOURCE side: for each (target, FK column) pair, does the staging source row carry a value that the lineage table can resolve? CW-B19 mitigation per Goal 004 DISCOVERY mandatory checklist:
```sql
FOR EACH (target_table, NOT NULL FK column, source_table, candidate_source_column):
  SELECT COUNT(DISTINCT <source_col>) FROM staging.<source_table>
  WHERE <source_col> IS NOT NULL
  INTERSECT
  SELECT COUNT(DISTINCT source_lineage_source_record_id)
  FROM sys.sys_source_lineage_records
  WHERE source_lineage_target_table = '<target_lookup>';
-- 0 intersect → INFEASIBLE without source-side import
```

## Action items per CLI

1. **Write seed file** `db/seeds/brownfield/wave1_classB_fk_mappings.sql` con SOLO 1 INSERT:
   ```sql
   INSERT INTO brownfield.column_mappings (...)
   VALUES (
     gen_random_uuid(),
     '<tenant_id>',
     'sys_skill_categories',
     'skill_category_family_id',
     'competencies',
     'framework_id',
     1,  -- wave
     'LOOKUP_FK',
     '{"target_table":"sys_skill_families","match_on":"skill_family_metadata->>legacy_id"}'::jsonb,
     ...
   )
   ON CONFLICT DO NOTHING;
   ```

2. **Pre-apply trigger validation**: BEGIN; INSERT; verifica trigger validate_lookup_fk_payload ACCEPT; ROLLBACK. Se reject → halt+escalate.

3. **Apply via SSH** + verify count `brownfield.column_mappings` +1.

4. **Pre-retry audit emission** per i 4 INFEASIBLE targets (8063 staged rows totali):
   ```sql
   INSERT INTO audit.import_validation_results (
     import_run_id, ..., rule_code, target_table, source_record_id, ...
   )
   SELECT
     '<current_run_or_pseudo_run_id>'::uuid,
     ...,
     'CASCADE_PREREQUISITE_MISSING_GOAL_004',
     '<target>',
     staging_source_record_id,
     ...
   FROM staging.wave1_<target> WHERE ...;
   ```
   Per ogni target con justification text inline (link a STATE D15 + PROMPT v3 §1.3 + this directive D18).

5. **Wave 1 retry single bundled** (post 1 INSERT + audit emission).
   Expected wall-clock 30-60 min (recalibrated baseline rate 3.9 rows/sec).

6. **Verify post-retry C4/C5 narrowed**:
   - 11/15 sys.* targets popolati (incluso sys_skill_categories partial ~32 rows + 4 Class-A P1-fixed via Wave 1 retry)
   - 4/15 sys.* targets vuoti con audit `CASCADE_PREREQUISITE_MISSING_GOAL_004` rows presenti
   - 0 silent-skip senza audit class
   - C6/C7/C8/C9 ancora PASS
   - Lineage gap check: P1 ha risolto anche su sys_skills/sys_learning_modules/sys_learning_paths?

7. **Item L REPORT 003 + STATE finalize atomic commit** (CP7):
   - §wave1-scope-correction: narrative completa v1→v2 (Wave 2/3/4 → Goal 004) + v2→v3 (Class B 3 targets → Goal 004) + E1 micro-amendment (Class C 1 target → Goal 004)
   - §wave1-honest-closure: 11/15 popolati + 4/15 documented Goal 004 prerequisite-dependent
   - §bias-catalog: CW-B16 (PLAN wall-clock from broken-baseline) + CW-B17 (silent-skip audit blind spot) + CW-B18 (DISCOVERY registry completeness gap) + CW-B19 (DISCOVERY source-side FK lookup verify gap)
   - §goal-004-handoff: 4 INFEASIBLE targets + foundational seed work needed (sys_blueprint_variants, sys_job_families, courses-table-import-decision)

## Budget riproiettato

Cumulative consumed: 21.5/40
- Seed + trigger validate + apply: 1 → 22.5
- Audit emission 4 INFEASIBLE: 1 → 23.5
- Wave 1 retry: 2-3 → 25.5-26.5
- Verify + REPORT + STATE finalize: 2 → 27.5-28.5
- **Projected closure: 27.5-28.5/40**
- Buffer at closure: 11.5-12.5 turn
- Escalation 35: NOT crossed

## Halt+escalate triggers

- Trigger validate_lookup_fk_payload reject INSERT #1
- Wave 1 retry surfaces NEW silent-skip class (Class D+) beyond A/B/C
- Wall-clock retry >75 min senza progress
- Budget >32 turn

---

Procedi seed → validate → apply → audit emission → retry → verify → REPORT + STATE finalize.

Notifica `exec_progress` post-apply (count brownfield.column_mappings +1 + audit rows emessi).
Notifica `exec_completed_item_f` post-retry (11/15 metric + 4 INFEASIBLE audit confirmation).
Notifica `exec_completed` con commit SHA REPORT 003 + STATE finalize atomic commit.
