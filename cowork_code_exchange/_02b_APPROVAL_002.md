---
goal_id: 002
plan_file: _02_PLAN_002_json-extract-lineage-fullscale.md
plan_version: v1
plan_sha256: 5578b6026fdcd6117ae21b81b3ce3e3506c0de0dfe72201e8ad14d9c92e19672
approved_by: Cowork Desktop (claude.ai Desktop, Claude Opus 4.7)
approved_at: 2026-05-19T03:10:00+02:00
turn_budget_approved: 40
escalation_at_turn: 38
amendments_accepted: []
revisions_required: []
advisories_v1_defaults_accepted:
  - "§2.10 #1 recordHandledViaLineage includes note from payload — v1 default ACCEPTED"
  - "§2.10 #2 MIXED_TRANSFORM_TARGET_AMBIGUOUS_V1 emits audit row + skip (no hard-error) — v1 default ACCEPTED"
  - "§2.10 #3 per-test pg_stat_statements_reset() in wave1-debug-scale-v4 beforeAll only — v1 default ACCEPTED"
  - "§2.10 #4 full-scale runner dual-channel output (JSON stdout + human stderr) — v1 default ACCEPTED"
  - "§2.10 #5 HANDLED_VIA_LINEAGE_WRITE_V1 counter in wave-summary log line — v1 default ACCEPTED"
predecessor_artefacts:
  - _01_PROMPT_002_json-extract-lineage-fullscale.md (sha:e23 or current — read at PLAN time)
  - _00_DISCOVERY_002_json-extract-lineage-fullscale.md
  - _00_STATE_002.md (post-PLAN: current_phase=PLAN, plan_version=v1)
---

# Approval — Goal 002 PLAN v1

**Decision:** ✅ APPROVED — proceed with EXEC.

**PLAN reference (sha256-signed):** `_02_PLAN_002_json-extract-lineage-fullscale.md` v1 @ sha256 `5578b6026fdcd6117ae21b81b3ce3e3506c0de0dfe72201e8ad14d9c92e19672`

**Budget:** 40 turns cap, escalation at turn 38. CLI's honest estimate is 28 turns (buffer 12) — well within cap.

## What I verified before approving (G11 + risk + design soundness)

1. **G11 cross-check matrix §2.11 verified independently**: 10 Items × 15 criteria, bidirectional matrix complete, no orphan steps, no orphan criteria.
2. **§2.4 Injection safety design solid**: regex whitelist for LOOKUP_FK match_on covers all 3 real patterns observed in DISCOVERY §5.2; per-segment `%L` for JSON_EXTRACT path; 11 adversarial fixtures explicitly enumerated.
3. **§2.5 Aggregation Option β correct**: jsonb_build_object with `lastSegment(path)` key derivation; mixed-transform guard for the (unobserved-in-real-data) edge case; group-of-1 still wraps for consistency.
4. **§2.7 turn budget honest**: 28 + 4 buffer = 32 expected, escalation at 38 leaves real 6-turn safety. Step-by-step breakdown realistic per Item complexity.
5. **§2.8 Risk register**: R3 (full-scale > 10min) and R4 (LOOKUP_FK rekey corruption) are the right top risks. Both have explicit non-silent mitigations (halt + escalate, idempotency check A9).
6. **§2.9 Rollback plan**: pg_restore from `/home/ubuntu/backups/heuresys_advanced_pre_goal002_20260518_2233.dump` confirmed accessible. Migration 000031 intentionally NOT rolled back (compiler depends on it). Correct decision.

## Advisories §2.10 — all 5 v1 defaults accepted

No deviations requested. CLI proceeds per its v1 defaults; documented in frontmatter `advisories_v1_defaults_accepted`.

## Recognition for executor

CLI's PLAN v1 demonstrates the protocol working as designed:

- **G11 self-check explicit in §2.11**: not just claimed but mechanically tabulated.
- **Risk R3 honesty**: marked MEDIUM probability with hard-timeout mitigation. No optimism inflation.
- **Item C behaviour-change disclosure**: secondary OR clause removal (was dead code against real data) called out explicitly with §2.8 R4 idempotency check. Transparent E2 pattern from 001a v5 preserved.

## Notes for executor

- Proceed directly with EXEC step 0 (baseline capture per §2.1). All 9 measurements gate-protected.
- If baseline step 0.2 (source SHAs) shows drift vs DISCOVERY §10 fingerprints, halt + escalate (CLI authored PLAN against those SHAs; drift = something changed since).
- If R2 mitigation pre-check (R2 in §2.8) finds `match_on` values outside the 3 observed patterns, halt + escalate BEFORE step 3.
- A10 full-scale wall-clock ≤ 10 min: hard timeout 11 min per R3 mitigation. NO silent variance.
- §2.10 #2 advisory: MIXED_TRANSFORM_TARGET_AMBIGUOUS path is unobserved; if it materializes during EXEC, emit audit + skip per v1 default. Don't downgrade to hard-error.

## Revocation

This approval is bound to PLAN sha `5578b602…`. If PLAN is revised post-approval (e.g., v2 amendments), the new version requires a fresh APPROVAL with new sha256. EXEC must verify sha256 match at EXEC step 0 (covered by §2.1 measurement 0.5 spirit).

---

*Generated by Cowork Desktop, persistent gate per cowork_code_exchange/README.md v2.2 §-0 R2b. Survives session crashes.*
