# DEBT_REGISTER — Debiti / incoerenze rilevati (CLI-owned)

> Tutti i debiti, drift e incoerenze emersi nella ricognizione forense S939 (4-root). Per ognuno: severità, evidenza, remediation, stato. **Tutti in scope** (R3 cross-project: il codebase va lasciato migliore). Quelli operativi sono linkati al `SOT_BACKLOG.md`.
> **Aggiornato**: 2026-07-02 (S1014 — **D-50 esteso**: F3 anche sui 4 moduli sibling fuori-mappa trovati in code-review → 15 moduli org-gated). Tabella aggiornata fino a D-50.

| ID | Sev | Debito | Evidenza | Remediation | Stato |
|---|---|---|---|---|---|
| **D-01** | 🔴 alta | **Doc drift `CLAUDE.md` + `README.md`**: descrivono stato MVP-1 ("11/22 moduli + auth", "web ancora vuoto", "MVP-1 = API build-out", "69/69 test @64c2a27"). Realtà: ~58 moduli + auth, web MVP-2a/2b shipped, MVP-3 done, MVP-4 in corso, 334 test case. | `CLAUDE.md` §"What this is"; `app.ts:206-265` (60 register); 64 `page.tsx` | Aggiornare le sezioni di stato di `CLAUDE.md` + `README.md` puntando a `docs/kb/SOT_STATE.md` come SoT viva. Le parti invarianti/design-system/security restano valide. | **RISOLTO** 2026-05-28: `CLAUDE.md` §"What this is" + architecture diagram + migrations count allineati a MVP-4 (Fastify 5, ~58 moduli, web shipped, 43 mig, +showcase); puntatore a `docs/kb/SOT_STATE.md`/`SOT_BACKLOG.md`/`DEBT_REGISTER.md`. `README.md` già a MVP-3 → header refresh a state S940 + counts (43 mig). NB: il README NON era fermo a MVP-1 (lo era solo CLAUDE.md). |
| **D-02** | 🟡 media | **Duplicati cross-location**: `GOAL_B_REPORT_2026-05-18.md` e `MIGRATION_STATUS_2026-05-18.md` esistono sia in repo root sia in `Claude Desktop\outputs\`; `HANDOVER_CLI` esiste in **3 copie** (repo `cowork_code_exchange/`, `outputs/`, `Claude Desktop\heuresys-advanced\sessioni\...`). SHA identici. | recon agent + `find` | Canonicalizzare: la copia repo è SoT; le copie Claude Desktop diventano archivio read-only (marker, no delete — vedi `COWORK_ARCHIVE_NOTE.md`). Niente duplicazione in ingestion (Plan 2 legge l'indice una sola volta per path). | **gestito** in P1-9 + Plan 2 |
| **D-03** | 🟡 media | **Snapshot CLAUDE.md obsoleto** `outputs\depot\staging-P0-2026-04-22\CLAUDE.md` = regole R1-R17 (vs SoT viva R1-R23). Rischio: una sessione lo scambia per la SoT. | recon agent | Marcare esplicitamente come snapshot storico nell'indice (status `external-archive`) + nota in `COWORK_ARCHIVE_NOTE.md`. SoT regole = `C:\Users\enzospenuso\.claude\CLAUDE.md`. | **gestito** (indice + archive note) |
| **D-04** | 🟢 bassa | **Root repo cluttered** da doc di milestone chiuse: `GOAL_B_REPORT`, `MIGRATION_STATUS`, `BRAND_SESSION_CHARTER`, `HANDOFF_BRAND`, `NEXT_SESSION_MVP_2A`, `NEXT_SESSION_MVP_CLOSURE`. | `git ls-files` root | Candidati a `docs/archive/` (NON ora — alcuni ancora referenziati, es. NEXT_SESSION_MVP_2A è dottrina E2E live). Spostamento deferito post-doc-drift fix, con grep delle reference prima. | **RISOLTO** 2026-05-28 (post D-01): 5 doc di milestone chiuse → `docs/archive/` via `git mv` (GOAL_B_REPORT, MIGRATION_STATUS, BRAND_SESSION_CHARTER, HANDOFF_BRAND, NEXT_SESSION_MVP_CLOSURE); `INDEX_PATHS.md` aggiornato (`*live*`→`*archive*`). **NEXT_SESSION_MVP_2A.md resta in root** (dottrina E2E live, 32 ref). Menzioni in `cowork_code_exchange/`+`sessioni/` = prosa storica (path d'origine), non link rotti. |
| **D-05** | 🟢 bassa | **`.handoff/STATE.md` lag 1 commit**: mostra HEAD `c2f95ad`, reale `9cd906e` (il commit di handoff stesso). | `git rev-parse HEAD` vs STATE.md | Atteso (il commit handoff aggiorna STATE poi viene committato). Allineare al prossimo handoff. | **non-issue**, monitor |
| **D-06** | ⚪ info | **Falsa discrepancy bias 26 vs 62**: grep `CW-B[0-9]+` su `bias_registry.md` ritorna 26 unici, ma il tally reale è 62. Causa: la registry usa numeri nudi in tabella (`\| 38 \|`) non `CW-B38`. | grep vs §5 tally | Nessuna azione — chiarito. Il tally 62 (B17→B63, B57 withdrawn) è corretto. | **risolto** (chiarimento) |
| **D-07** | ⚪ info | **Gap migration `000035`**: 43 file ma numerati a 000044. | `ls db/migrations` | Cosmetico, documentato (handover §5 "NO 000035 gap cosmetic"). Nessuna azione. | **non-issue** |
| **D-08** | 🟡 media | **Dual-ownership SoT** (Cowork `cowork_reserved/` + Claude Desktop vs CLI): handoff/state/bias vivevano in spazio Cowork → rischio drift ora che il CLI opera in autonomia. | obiettivo sessione | Spostare la SoT viva in `docs/kb/` (CLI-owned, questa sessione); congelare Cowork come archivio read-only. | **RISOLTO** (verificato 2026-05-27: `docs/kb/` SoT completa + `COWORK_ARCHIVE_NOTE.md` presenti) |
| **D-09** | 🟢 bassa | **Dependabot churn**: 7 PR `defer-major` auto-rebasano → CI re-trigger ad ogni move di main (runner singolo). | HANDOFF.md tail; recon | Condition `skip defer-major` o paths-filter nei 6 workflow. | **RISOLTO** 2026-05-28: `if: ${{ !contains(github.event.pull_request.labels.*.name, 'defer-major') }}` aggiunto al job di tutti i 6 workflow con trigger `pull_request` (build-web, i18n-parity, lint, playwright-smoke, test-integration, typecheck). Push a `main` non impattato (evento push → nessuna PR label → job gira). YAML validato. `showcase.yml` escluso (nessun trigger `pull_request`). |
| **D-10** | 🟡 media | **Policy numerazione bias post-Cowork**: il protocollo claim CW-B<N> era anti-race Cowork↔CLI. Ora che il CLI è solo, serve regola chiara per non rompere la continuità storica. | bias_registry §1 | Il CLI continua la numerazione da **CW-B64** scrivendo direttamente in una sezione "post-Cowork" di `docs/kb/` (o estendendo bias_registry come archivio append-only). Documentato in `COWORK_ARCHIVE_NOTE.md`. | **gestito** (archive note) |
| **D-11** | 🟢 bassa | **Overlap script Claude Desktop ↔ repo**: `Claude Desktop\scripts` (bootstrap/OCI/SSH) parzialmente sovrapposto a `repo/scripts/` e `db/scripts/`; converter generici/nose/SMB non-dominio mescolati. | recon agent | Già separati nell'indice (solo 6 file heuresys inclusi; esclusi elencati in `INDEX_PATHS.md` appendice). Nessuna azione fisica. | **gestito** (indice) |
| **D-13** | 🔴 alta | **Ingestion legacy user-centric invece che employee-centric** (ADR-0024). Le metodiche di popolamento bare-metal trattavano legacy `users` come la persona (`users → sys.sys_users`) e `employees` come mera fonte di `tenant_id`. Realtà strutturale (FK census live): **207 FK → `employees`** vs **45 → `users`** (4.6:1); `users.employee_id → employees.id` (users è subordinato). La keying `LEGACY:<users.id>` droppa gli employee senza account (2/162 nel subset S950). | `qa_artifacts/_FK_centric.txt` (207), `_FK_users.txt` (45), `_emp_user_gap.txt` (2 dropped); `BROWNFIELD_IMPORT_PLAN.md:304` (pre-fix), `rtl-rebuild/{04,06,07,08}.sql` JOIN su `LEGACY:\|\|users.id` | **3 fasi (S954)**: (1) doc — ADR-0024 + `EMPLOYEE_CENTRIC_MAPPING_DOCTRINE.md` + correzione forense plan/map/SOT ✅; (2) riscrittura 5 seed `rtl-rebuild` a key `LEGACY_EMP::<employees.id>`; (3) re-key live bare-metal (gated, protocollo S950). | **RISOLTO** 2026-06-01 (S954): tutte e 3 le fasi chiuse — Fase 1 doc (`2d87b77`), Fase 2 re-key 5 seed (`10d9923`), Fase 3 migration `000046` live (`10c0cd1`: 160 `LEGACY:`→`LEGACY_EMP::`). Permanent CI guard `employee-centric-doctrine.integration.test.ts` (WS-1, S957) asserisce 0 righe `LEGACY:%`. Vedi SOT_STATE §4. |
| **D-12** | 🟡 media | **`migrate.sh` non ri-eseguibile end-to-end** (invariante "twice-run idempotent" rotto su 3 punti, pre-esistenti): **(a) 000007** ri-asseriva incondizionatamente il CHECK stretto `scheme_check` (4 valori) clobberando il relax di 000032 → fallimento "violated by some row" sui dati ATECO/NACE; **(b) 000033** ownership di `brownfield.tenant_id_mappings` + funzioni `validate_lookup_fk_payload*` = `postgres` (create manualmente da superuser), `heuresys` non può ALTER → "must be owner"; **(c) 000044** referenziava colonna inesistente `table_mapping_kind` (reale: `table_mapping_classification`) → la migration non era MAI stata applicata (i 3 target application-level restavano IMPORT). | `bash db/scripts/migrate.sh` (fail sequenziale a 000007→000033→000044); `pg_get_userbyid(relowner)`; `grep table_mapping_kind` | (a) guard `ADD CONSTRAINT` solo-se-assente; (b) `ALTER OWNER ... TO heuresys` (live, peer-auth VM; su fresh DB heuresys crea e possiede → nessun fix-file); (c) colonna → `table_mapping_classification` (esegue l'intento ADR-0020: 12 righe IMPORT→REFERENCE_ONLY). | **RISOLTO** 2026-05-27 (commit `9e67d42`); verificato `migrate.sh` OK end-to-end ×2 (43 migration, 0 errori, idempotente) |
| **D-14** | 🟡 media | **`sys_activity_classification_mappings` mapping-vs-schema FK conflict** (ADR-0025 §5.3): il Wave-1 IMPORT card sorgente `industry_ccnl_mapping` risolve `target_id` LOOKUP_FK→`sys_compensation_bands` ma la FK shipped (000007) + il contratto API richiedono `sys_activity_classifications` (0-overlap) → FK-violation permanente → 0 righe. | diagnosi S958 (query live 0-overlap: 7 ccnl in comp_bands, 0 in activity_classifications); `activity-classification-mappings.integration.test.ts` (assume target∈activity_classifications) | Reclassify il card mis-authored IMPORT→REFERENCE_ONLY (mig `000056`, pattern ADR-0020/000044) — il fatto industry→CCNL è già modellato in `sys_compensation_bands`. Evitate le 2 opzioni redesign (alter-FK / re-point) = HIGH risk vs API contract. Reversibile. | **RISOLTO** 2026-06-02 (S958, commit `70a869d`, ADR-0025 §5.4): card=REFERENCE_ONLY, tabella empty-by-design, 000056 idempotente ×2, test verdi (10/1-skip). Popolamento reale gated a una futura sorgente ATECO↔NACE crosswalk. **Flagged per review Enzo** (reclassify vs FK redesign). |
| **D-15** | 🟢 bassa | **`@types/react` override mismatch**: `pnpm.overrides @types/react` pinnato a `19.2.14`, ma le dep ne vogliono `19.2.16` → ~64 peer-warning compile-time (innocui, typecheck verde). Forzare l'override a 19.2.16 ROMPE il typecheck di `apps/showcase` (doppia istanza `@types/react` via la `@heuresys/ui` pubblicata — "two different types"). NB: react/react-dom **runtime** override già riconciliato 19.2.5→19.2.7 (S965). | `pnpm install` (64 unmet-peer @types/react); `tsc -p apps/showcase` fail a 19.2.16 (S965) | Fix vera **upstream in `ux-design-shared`**: allineare `@types/react` dentro `@heuresys/ui` così l'override locale può salire a 19.2.16 senza doppia istanza. Workaround attuale: override resta 19.2.14 (typecheck verde, warning tollerati). | **RISOLTO** 2026-06-05 (S966): diagnosi originale ERRATA (non era "doppia istanza via lib pubblicata"). Root-cause reale = pnpm **stale virtual-store** dopo il flip override S965 *senza clean install* (orphan `@types/react@19.2.14` 0-ref nel lockfile + stale hoist-root `.pnpm/node_modules/@types/react→19.2.14`); tsc compilava il source `.tsx` di `@heuresys/ui` (`./brand/candidates` esporta `src/`, non `dist/`) risolvendo via hoist-root stale → "two unrelated types". Fix **locale**: override→19.2.16 (= ciò che web+showcase già dichiarano) + clean install (commit `5c59295`) → single physical instance, 0 unmet-peer warning, typecheck 4/4 ws + build web/showcase verdi. **Hardening upstream eseguito + PUBBLICATO**: `@heuresys/ui@0.1.3` (optional `@types/react`/`-dom` peerDep, commit upstream `e63f3fe`, npm publish) → consumer bumpato `^0.1.3` (commit `a4abb44`); clean install verificato (ui subtree porta `@types/react@19.2.16`, single instance, typecheck 4/4 + build verdi). Previene strutturalmente il ripetersi su install non-clean / CI / altre macchine. |
| **D-17** | 🟡 media | **`vm-deploy.sh` non rebuildava `@heuresys/shared` → deploy fail su dist stale**: il deploy S975 è fallito al `next build` (`@heuresys/shared has no exported member 'AttendanceAnalyticsResponse'`). Root-cause: `pnpm install --frozen-lockfile` non ri-emette affidabilmente la lib workspace, e l'incrementale tsc usava uno **stale `packages/shared/tsconfig.tsbuildinfo`** che credeva `dist/schemas/analytics.d.ts` già emesso (il file era stato untrackato da git in D-16 → mancante su disco). CI passava (checkout fresco). | primo deploy S975 fallito; `find packages/shared -name '*.tsbuildinfo'` presente; clean rebuild ha riemesso `analytics.d.ts` (count 2) | `vm-deploy.sh` ora fa `rm -rf packages/shared/{dist,tsconfig.tsbuildinfo}` + clean `pnpm --filter @heuresys/shared build` **prima** dei build api/web. | **RISOLTO** 2026-06-07 (S975, commit `ef13856`): clean shared build in-deploy (recovery) + reso durevole nello script. `bash -n` verde. Deploy S975 completato + smoke PROD 200. |
| **D-16** | 🟢 bassa | **`packages/shared/dist/schemas/analytics.d.ts` è tracked benché `dist/` sia gitignored** (`.gitignore:52`): è l'**unico** file dist tracked (stray force-add storico). A5 (S972) ne ha committato la modifica, perpetuando lo stray. Innocuo (typecheck/CI verdi, `shared` ricostruito dal build), ma incoerente con la policy `dist/` ignored. | `git rm --cached packages/shared/dist/schemas/analytics.d.ts` (resta su disco, sparisce dal tracking; consumer/CI ricostruiscono via `pnpm --filter @heuresys/shared run build`). Micro-fix 1-riga. | **RISOLTO** 2026-06-07 (S972, commit `26dfbe9`): `git rm --cached` del file generato (dist resta su disco, ricostruito dal build). Aveva appena bloccato il pull --ff-only sul Mac durante l'allineamento S972; dopo l'untrack i 3 ambienti (Windows/Mac/VM) si allineano puliti. |
| **D-18** | 🟢 bassa | **Insights recompute = append-with-latest-wins → le score-table crescono ad ogni recompute/test**: `sys_flight_risk_scores` + `sys_succession_readiness_scores` + `sys_skill_gap_scores` non fanno UPDATE ma INSERT di una nuova cohort `computed_at`; il read usa `DISTINCT ON (... computed_at DESC)` → **funzionalmente corretto** (sempre l'attivo più recente), ma le righe storiche si accumulano (ogni run dei test `insights*.integration` + ogni `POST .../recompute` aggiunge ~156/468 righe). Innocuo per correttezza/perf attuale (index DESC), ma la tabella cresce monotona. | `SELECT count(*) FROM sys.sys_succession_readiness_scores` cresce ad ogni recompute; design `append-latest-wins` (mig 000082/000092, repo `upsert*Scores`). | Opzioni (P3, non urgente): (a) housekeeping job che cancella le cohort non-attive (`computed_at` < latest per subject); (b) switch a UPSERT su `(user[,position])` con `ON CONFLICT` (perde lo storico ma tabella stabile); (c) retention-window. Decidere se/quando in base alla crescita reale. | **RISOLTO** 2026-06-08 (S977): scelta **opzione (b/a-hybrid) delete-then-insert atomico** (CTE `WITH del AS (DELETE … WHERE user_id = ANY($1)) INSERT …`) nelle 3 upsert (`apps/api/src/modules/insights/repository.ts`) → una sola riga attiva per soggetto (slice B: delete di tutte le righe del soggetto → nessuna coppia (user,position) stale al cambio del top-N), scope-safe (tocca solo gli userId ri-scorati), atomico (singola statement). + **mig `000094`** one-time collapse del backlog (flight-risk 10428→158 · succession 11088→462 · skill-gap 1848→154; idempotente, 2° run DELETE 0). Test idempotenza D-18 in `insights{,-p2}.integration` (rows == distinct-key, recompute ×2 = no growth), suite insights 19/19. |

| **D-20** | 🟢 bassa | **API boot non-resiliente al jitter di connessione DB**: `loadRolePermissionCache` a `server.ts:35` (boot) NON ha retry — un timeout transitorio di establishment del pool (`connectionTimeoutMillis: 5_000` in `db/client.ts`) attraverso il tunnel SSH→OCI **uccide l'API allo start** ("Connection terminated due to connection timeout"). Osservato S980: 4 boot consecutivi falliti durante una finestra di jitter OCI (free-tier), ok al 5° (psql single-shot funzionava nel frattempo → è l'establishment del pool a fallire, non il forward). | Opzioni (P3): retry 2-3× con backoff attorno a `loadRolePermissionCache` al boot; o bump `connectionTimeoutMillis` per la fase boot. Non urgente (recovery via re-run; prod systemd ri-avvia). Cugino di D-19. | **RISOLTO** 2026-06-10 (S981, `913b383`): `loadRolePermissionCacheWithRetry` — 5 tentativi, backoff 1/2/4/8s, fail-fast sul difetto empty-cache (non-jitter); DI seam + 3 unit test no-DB. |
| **D-21** | 🟢 bassa | **Commento stale in `mfa-routes.ts`**: l'header dice "Login-time gating intentionally NOT wired" ma il gate MFA al login È cablato (§3b `auth/service.ts`, MVP-3 Tappa E: utente con fattore verificato → `mfa_required`). Fuorviante per chi legge. | Aggiornare il commento quando si tocca `mfa-routes.ts` in #4 (mandatory-MFA). Micro-fix. | **RISOLTO** 2026-06-10 (S981, in `0c7d7dc`): header + i 2 blocchi interni (verify-login, WEBAUTHN) riallineati al gating reale; anche webauthn-service.ts e webauthn.spec.ts. |
| **D-19** | 🟢 bassa | **Script di allineamento/deploy senza test automatici**: `align-clones.sh`, `env-key-merge.sh`, `sync-memory-tree.sh`, `db/scripts/migrate-if-pending.sh`, `vm-deploy.sh` (+ hook marker in `session-boot.ps1`) sono verificati solo via `bash -n` + E2E manuale. Nessun test ripetibile cattura regressioni — es. il bug **CRLF del marker** (rompeva `git diff <sha>`) è stato preso a mano nell'E2E S979. | `bash -n` verde + run live S979 (PC=Mac=VM @ `e6dc503`); nessun `*.test` sugli script shell | Opzioni (P3): `shellcheck`/`bats` in CI per gli script bash; smoke-target che esercita delta-compute + auto-deploy-gate su un fixture. Non urgente (idempotenti, low-blast-radius, full-clone v1 = recovery). | **RISOLTO** 2026-06-10 (S981, `1613535`): `scripts/test/run-shell-tests.sh` (39 check: bash -n su tutti gli script, shellcheck severity=error, fixture env-key-merge additive/no-overwrite/CRLF/idempotente, regex DEPLOY_PATHS_RE as-shipped, marker CRLF) + workflow CI `shell-tests.yml` su paths scripts/**; shellcheck 0.9.0 installato sul runner; `env-key-merge.sh` rifattorizzato (funzione unica via declare -f, CRLF-safe). |

| **D-24** | 🟡 media | **Full-suite Playwright locale (dev-mode) supera la finestra di validità delle sessioni storageState (~45min)**: il progetto `mobile-a11y` (S983) ha portato la run completa a ~90min in dev-mode → da `me-pages` in poi TUTTE le spec hanno trovato la sessione morta (redirect a /login) = 97 fail a cascata, 0 difetti reali (S984). Le sessioni del setup non vengono mai rinfrescate (`/v1/auth/refresh` MAI chiamato nell'intera run — il client non auto-refresha e il cookie refresh path `/v1/auth` non passa il proxy). Nota: una spec a11y su sessione morta audita la LOGIN page e "passa" silenziosamente → run lunghe possono dare census vacui. | Run E2E in **build prod** (S984: rerun 100 test in 7min, verde — già doctrine `verify in PROD build not dev`) e/o auth.setup re-login per-project/per-chunk; valutare assert anti-vacuità nella spec a11y (es. testid pagina attesa ≠ login). | ✅ **RISOLTO** 2026-06-12 (S985). Diagnosi corretta: la finestra reale è **15 min** (TTL `hrx_access`), non 45 — il "45" era mascheramento da audit a11y vacui; il refresh non può MAI riuscire client-side (→ nuovo **D-26**). Fix: (1) **`playwright.prod.config.ts`** = config full-suite canonico (webServer `next start` fail-fast `reuseExistingServer:false`; catena `setup → mobile-a11y + a11y-desktop → setup-refresh → chromium` — il re-login riscrive `tests/.auth/*.json` dopo il blocco audit; showcase condizionale a `NEXT_PUBLIC_ENABLE_SHOWCASE=1`; catena nel SOLO config prod per non trascinare 70 audit nelle iterazioni single-spec dev) + script **`pnpm test:e2e:prod`** (build+run) + riga CLAUDE.md comandi canonici; (2) **assert anti-vacuità** in `runAxeOnRoute` (URL-based, vale anche su Pixel 7): sessione morta → fail loud "dead session", mai più census vacui. Verifica: **full suite 200 passed / 1 flaky-retry-green in 9.6min** (sotto ogni soffitto). Il guard ha subito smascherato 2 violazioni serious REALI (`scrollable-region-focusable`) nascoste dai census storici vacui → fixate in-session: `<main>` shell keyboard-focusable (**`@heuresys/ui@0.1.5`** upstream `a0aacd2` + bump ×3) + matrice `/admin/roles` `tabIndex/role/aria-label`. |
| **D-25** | 🟢 bassa | **`i18n-en.spec.ts` restore non crash-resilient** (classe D-23): la spec setta il locale server-side (`sys_user_preferences.locale=en` per platformAdmin) e lo ripristina in afterAll — se la run collassa a metà (come S984 per D-24), il leftover `locale=en` persiste nel DB e ogni spec successiva che asserisce testo IT fallisce (visto su `/tenants`: "2 total" vs `/totali/`). Ripristinato a mano S984 (`UPDATE ... locale='it'`). Diagnosi S985: oltre al crash-gap, lo snapshot era **auto-avvelenante** (dopo un crash il run successivo snapshotta 'en' e il restore lo perpetua) + bug latente `{locale:null}`→400 Zod (PATCH schema `.optional()` non `.nullable()`) che silenziava il restore su GET fallito. Sibling-exposure: `me-preferences.spec.ts` flippa employee a EN senza finally (stessa classe). | Fix a 3 strati (S985): (1) snapshot **sanitizzato** in `i18n-en.spec.ts` (un readback 'en' è per dottrina sempre un leftover → normalizzato a 'it') + firma `setServerLocale(locale: string)` (chiude il bug null) + afterAll try/catch **loud** con SQL di recovery nel messaggio; (2) **guard pre-run in `auth.setup.ts`**: PATCH `locale='it'` per OGNI persona riusando i cookie appena ottenuti (0 login extra, partial-update COALESCE — theme/palette intatti) → il project `setup` precede ogni altra spec, quindi un crash di QUALSIASI run viene sanato al run successivo (copre anche la sibling-exposure employee); (3) verifica con simulazione crash: poison `locale=en` via psql → setup → readback `it` (provato ×2, anche da run-invalida reale in-session) + spec 7/7 verde + readback finale `it`. | ✅ **RISOLTO** 2026-06-12 (S985) |
| **D-22** | 🟡 media | **`sys_auth_mfa_factors` mai registrata nel reconciliation registry** (gap latente dal 000058): la tabella (mig 000005) risolveva POPULATED via has_rows sui residui di test → il gap era invisibile; il cleanup rigoroso S982 l'ha svuotata e l'assert 0-UNCLASSIFIED di `000062` è scattato sul re-run della catena (e sarebbe SEMPRE scattato su un rebuild fresco — la tabella esiste vuota al punto-62). | full-chain re-run S982 (`migrate.sh` fail a 000062); query vista UNCLASSIFIED=1 | Registrare la riga D/EXCLUDE **dentro `000062`** (deve precedere l'assert per i rebuild freschi — un fix in migration successiva NON basta); bucket test 99→100/D38. | **RISOLTO** 2026-06-10 (S982, in `65e4172`): amendment `000062` + test aggiornato; migrate ×2 + validate verdi. |
| **D-26** | 🔴 alta | **Silent-refresh strutturalmente rotto dietro i proxy `/api` → hard-logout a 15 min per gli utenti reali in PROD**: `hrx_refresh` è path-scoped a `/v1/auth` (`apps/api/src/modules/auth/tokens.ts:20,63`) ma il client lo invoca su `/api/v1/auth/refresh` (`apps/web/src/lib/api/fetch.ts:48,72`, baseUrl `/api`) → il browser non allega MAI il cookie attraverso il proxy (nginx PROD `/api`→:8013; next.config dev ha la stessa topologia) → il refresh non può riuscire client-side. In più `apps/web/src/proxy.ts:13-23` redirige a `/login` su cookie `hrx_access` assente PRIMA di ogni chiamata client → il silent-refresh non viene nemmeno tentato sulle navigazioni. Effetto: sessione utente reale = 15 min secchi (TTL access), poi re-login — con mandatory-MFA live (S984) ogni volta serve anche il TOTP. Scoperto in S985 (discovery D-24): `/v1/auth/refresh` MAI invocato in nessuna run E2E; spiega anche metà della meccanica D-24. | evidenza file:line sopra; nessuna chiamata refresh osservata in nessuna run E2E (S984 network capture) | Decisione di design tra: (a) `REFRESH_COOKIE_PATH='/'` (semplice; il refresh viaggia su ogni request — più esposizione), (b) secondo Set-Cookie path `/api/v1/auth` (topology-aware), (c) rewrite proxy che preserva il path `/v1/auth` end-to-end, (d) route handler Next dedicato che inoltra il refresh. Hot-path auth (replay-rotation + CSRF, ~80 file test sensibili) → **sessione dedicata ~2-4h**, fix + integration + E2E che provi una sessione >15 min. | ✅ **RISOLTO** 2026-06-13 (S987): WIP opzione (a) completata e committata — `REFRESH_COOKIE_PATH='/'` (proxy-traversable) + clear di ENTRAMBI i path (nuovo `/` + legacy `/v1/auth`) al logout (`auth/tokens.ts`) · silent-refresh **single-flight** (latch module-level) **+ Web Lock cross-tab** `hrx_refresh_rotation` anti-replay sulla rotazione single-use (`web/lib/api/fetch.ts`) · `proxy.ts` passa se c'è `hrx_access` **O** `hrx_refresh` (sessione resumable, no hard-redirect). Gate: typecheck 4/4 ws + vitest auth hot-path **53/53** (auth-refresh-cookie/auth/me-sessions/mfa×3) + **E2E `session-refresh.spec.ts` verde** (la sessione sopravvive alla scadenza access via 1 solo refresh single-flight, no hard-logout; girato isolato `--no-deps` perché la catena `mobile-a11y`→`chromium` era bloccata da D-27). Drift spurio `next-env.d.ts` scartato. Compensating controls per lo scope `/` più ampio: HttpOnly+Secure+SameSite=Lax, TLS, pino redaction, single-use rotation+replay, CSRF double-submit. **NB**: la full prod suite ha 2 fail a11y mobile NON-D-26 → vedi **D-27**. |
| **D-27** | 🟡 media | **a11y mobile `scrollable-region-focusable` su `/me/certifications` + `/kpis`** (project Playwright `mobile-a11y`, Pixel 7, severità **serious**, 1 nodo ciascuna): la `<table>` (DataTable) fa overflow su viewport stretto (Pixel 7) senza keyboard-access (manca `tabIndex=0`/`role=region`/`aria-label` sul wrapper scrollabile). **NON è una regressione di D-26** (scollegato dal silent-refresh). Co-fattore **data-drift**: `/me/certifications` ha **8 righe `E2E Test Cert` residue** (fixture E2E mai pulite, timestamp progressivi) che gonfiano la tabella. Blocca la full prod suite: `mobile-a11y` precede `chromium` nella catena (`playwright.prod.config.ts:73`) → senza `--no-deps` `session-refresh`/`chromium` non girano. | `error-context.md` (snapshot tabella, regola axe `scrollable-region-focusable`); SOT S985 dichiarava census mobile ZERO → drift dati o route non coperte dal fix shell S985 | (1) **verificare se data-drift** (pulire le cert E2E residue + far emettere lo snapshot a tabella piccola — test-hygiene classe D-23/D-18) **o strutturale** (overflow-X 5 colonne su Pixel 7 anche con poche righe); (2) se strutturale → `tabIndex=0`+`role=region`+`aria-label` sul wrapper scrollabile, stesso pattern del fix shell `<main>`/matrice roles di S985 (possibile fix upstream `@heuresys/ui` DataTable). | ✅ **RISOLTO** 2026-06-13 (S988): fix **strutturale upstream** — `@heuresys/ui` `DataTableWithCrossHair` scroll-region ora `tabIndex=0`+`role=region`+`aria-label`(=caption) → `@heuresys/ui@0.1.6` pubblicata (ux `c7832eb`) + consumata `^0.1.6` ×3 (`9e097cb`). Verifica: **mobile-a11y prod gate 42/42** (incl. /kpis + /me/certifications, critical+serious=0). 8 righe E2E cert residue pulite (one-time); il co-fattore data-drift era irrilevante (verdetto: strutturale, overflow 5-col @412px anche con 1 riga). Spec teardown documentato (no DELETE endpoint, a11y-neutro post-0.1.6) → micro-debt residuo (vedi D-29). |
| **D-28** | 🔴 alta | **`TRUST_PROXY=false` (default) dietro il proxy nginx TLS collassa il rate-limiting per-IP in un unico bucket** (S-100X-A2 / `FINDINGS/WS-H.md` F-WS-H-1): con `trustProxy:false` Fastify ignora `X-Forwarded-For` → `req.ip` = socket upstream del proxy (costante) → il guard brute-force login (10/5min) e il limiter per-email perdono efficacia (credential-stuffing distribuito throttlato come 1 IP, o lockout globale DoS). | `apps/api/src/config/env.ts:54` default(false); `.env.example:15`; `app.ts:169 trustProxy: env.TRUST_PROXY`; `deploy/nginx/www.heuresys.com.conf:43` XFF. **`.env` PROD non verificabile da qui** (secret hygiene) → potrebbe già essere override. | (1) **verificare** se il `.env` VM ha `TRUST_PROXY=true` (grep della KEY via SSH, non del valore); se no → settarlo. (2) cambiare il default a `true` per deploy reverse-proxy + nota; evolutivo: `trustProxy` come hop-count/CIDR esplicito anti-spoof XFF. Compone con F-WS-H-2 (keyGen dead). | ✅ **RISOLTO** 2026-06-14 (S988, opzione (b) Enzo). **Verifica**: il `.env` VM aveva `TRUST_PROXY=false` MA `env.ts:54` usava `z.coerce.boolean()` → `Boolean("false")===true` (footgun, come da commento COOKIE_SECURE riga 74): il valore EFFETTIVO era **`true`** (trust-all → **spoofabile** via XFF forgiato, peggio del bucket-collassato ipotizzato dall'audit). **Fix code**: nuovo `apps/api/src/config/trust-proxy.ts` `parseTrustProxy` (false/""→false · true→true · "\<n\>"→hop-count number · ip/cidr→trust-list string), `env.ts` usa `z.string().default("false").transform(parseTrustProxy)` (chiude il footgun); unit test `test/trust-proxy.test.ts` 4/4; `.env.example` documentato. **Config+deploy**: VM `.env` `TRUST_PROXY=1` (1 hop nginx → req.ip = client reale, XFF forgiato ignorato) + redeploy; linuxpc resta `false` (bind diretto, no proxy) ora parsato correttamente. F-WS-H-2 (keyGen dead) resta separato (QW-H3). |
| **D-29** | 🟢 bassa | **Cert E2E spec senza teardown** (`ess-certifications-upload.spec.ts`): crea `E2E Test Cert <ts>` per run senza cancellarla (drift ~1/run). La superficie ESS cert è create+list only (no DELETE endpoint) e Playwright non ha accesso DB qui. | spec line ~28; D-27 discovery | a11y-NEUTRO post-`@heuresys/ui@0.1.6` (la tabella è keyboard-focusable a prescindere dal row-count). Fix-proper = aggiungere un DELETE endpoint o globalTeardown psql (scope creep — deferito). Cleanup manuale ops quando serve (`DELETE … LIKE 'E2E Test Cert%'`). | ✅ **RISOLTO** 2026-06-14 (S989): Playwright `globalTeardown` (`apps/web/tests/e2e/global-teardown.ts`) cancella via psql le righe `E2E Test Cert%` a fine suite (best-effort, host/port/db/user dal `.env`, password da `~/.pgpass` — nessun secret letto/loggato); wired nel base config, ereditato dal prod via `...baseConfig`. Query verificata live (0 residue, già pulite S988). |
| **D-23** | 🟡 media | **Cleanup test "blanket" che cancella config reale**: i 2 integration (`mfa-policy`, `auth-mfa-mandatory`) e la spec E2E `login-mfa-enrollment` chiudevano con DELETE/disable incondizionato delle policy MFA → la prima attivazione reale S982 è stata CANCELLATA dalla run E2E post-attivazione. Classe di bug: i test che toccano config persistente devono fare snapshot-restore, mai blanket-reset. | attivazione S982 wiped (DB readback enabled=f dopo la run E2E); PROD gate non scattava | snapshot dello stato pre-suite in beforeAll + restore esatto in afterAll/finally nei 3 siti. | **RISOLTO** 2026-06-10 (S982, `65e4172`+`3d361b0`): 3 siti convertiti; provato che l'attivazione reale sopravvive alla suite (readback 4 ruoli intatti) e gate ri-verificato LIVE in PROD. |
| **D-30** | 🟢 bassa | **`MFA_ENCRYPTION_KEY` validato ma mai consumato** (S989 discovery, assessment MFA): `env.ts:92` valida la key (+ warn al boot in prod se assente) e `.env.example` claim *"mfa-service throws MFA_ENCRYPTION_KEY_MISSING at boot"* + *"AES-256-GCM at rest"*, ma **nessun codice in `apps/api/src` legge la var** (grep: 1 solo hit, `env.ts` stesso); i TOTP secrets sono **base32-plaintext** (`mfa-service.ts`, `seed-test-admin.ts`). La var è di fatto un no-op e la doc è fuorviante. | grep `MFA_ENCRYPTION_KEY` su `apps/api/src` → solo `env.ts`; secret store plaintext | (a) **doc-fix**: correggere il commento `env.ts` + `.env.example` (key opzionale/inerte oggi); oppure (b) **implementare** davvero l'encryption-at-rest (AES-256-GCM di `factor_secret_encrypted`) consumando la key — decisione di sicurezza (autorità Enzo). Non urgente (no PII reale, ADR-0023; TOTP secret comunque dietro auth+RBAC). | ✅ **RISOLTO (a)** 2026-06-15 (S990, `b1574f0`): commenti `env.ts` + `.env.example` corretti (key INERT/reserved, no AES-GCM-active, no "throws at boot"). (b) encryption-at-rest reale **✅ IMPLEMENTATA S994** (QW-SEC6, `00a6d7b`): `secret-crypto.ts` AES-256-GCM key-presence-gated al boundary `mfa-repository` (retro-compat plaintext + lazy re-encrypt + skip-guard e2e-fixture); `MFA_ENCRYPTION_KEY` ora consumata. NB: i 6 secret correnti sono tutti e2e-fixture (plaintext by-design); l'encryption vale per future enrollment reali. Bulk script `db:encrypt-totp` ready-but-unrun (N/A sui fixture). |
| **D-31** | 🟡 media | **`sys.sys_enterprise_typing_profiles` di fatto vuoto** (= DT-A, analisi ESCO/tenant-onboarding 2026-06-15): la FASE 0 del flusso tenant-onboarding (legame tenant→NACE/ATECO+size+operating-model) NON è istanziata. L'unico profilo (HEURESYS) ha `enterprise_typing_industry_class_id`/`size_band_id`/`operating_model_id`/`employee_count` tutti NULL; RTL_BANK non ha profilo. Sul tenant c'è solo `tenant_industry_code` (stringa) + `tenant_size_band`. | query live `:5433` (1 profilo, tutti FK NULL); mig `000007:128`; report `docs/integrations/tenant_onboarding_esco_01_coherence_report_2026-06-15.md` §3 | T1.3 — valorizzare i 2 tenant TEST (industry_class_id→`sys_activity_classifications` + size_band + operating_model), idempotente `ON CONFLICT (tenant) DO UPDATE`; decisione semantica industry_code→classification = Enzo. Spec: `..._04_tenant_onboarding_spec_2026-06-15.md` §1. | ✅ **RISOLTO** 2026-06-15 (S990, T1.3 mig `000119`, `4b5007c`): 2 tenant TEST wired (ATECO_2025 RTL→64.19/RETAIL/M/HIGH/158, HEU→62.10/B2B_SERVICES/S/MEDIUM), idempotente, live-verified. NB: il pipeline test enterprise-typing cancellava il profilo reference RTL (classe D-23) → snapshot-restore `5e3e56f`. |
| **D-32** | 🟡 media | **Gerarchia ESCO skill = scaffold morto** (= DT-B): i campi `skill_metadata->>'skill_group_uri'` e `broader_uri` (+ narrower/related/reuse/isco_groups/primary_category/cognitive_level) sono popolati su **0/21939** skill, benché previsti nel jsonb. Blocca clustering skill (T2.6) e grafico "Skills Group Share". `skill_type` ESCO (skill/knowledge) invece è popolato (14036). | query live (skill_group_uri 0, broader_uri 0 su 21939; skill_type 14036); report §1.3/§5 | T1.1 — backfill via `reference-sync/esco-connector.ts` server-side (`/resource/skill` `_links.broaderHierarchyConcept`), UPSERT su `skill_esco_uri`, watermark hash-skip. Spec: `..._03_esco_population_spec_2026-06-15.md` §1. | ✅ **RISOLTO** 2026-06-15 (S990, T1.1 mig `000124` + backfill live): connector ESCO skill-hierarchy + EscoSkillFetcher seam + jsonb-merge. **Backfill LIVE eseguito → skill_group_uri 0→12892** (broader_uri 5007; 13183 risolti / 828 URI stale-404 saltati su 14011). Fix robustezza connettore: salta-e-continua sugli URI stale invece di abortire l'intero run (difetto del code-slice, corretto). Sblocca T2.6/T3.8 lato dati. |
| **D-33** | 🟡 media | **occupation→skill mai importate** (= DT-C): il dump legacy `wave1_eskap_esco_occupation_skills.sql` (**126051 righe**: 67600 essential + 58451 optional) è classificato `REFERENCE_ONLY` in `brownfield.table_mappings` e mai caricato in `sys.*` → nessuna derivazione skill-portfolio-da-occupation (FASE 3-ESCO) né dati per Skills Group Share. Le 844 righe `sys_position_skill_requirements` sono derivate da peer-group-prevalence, non da ESCO. | `db/seeds/brownfield/wave1/legacy_data/wave1_eskap_esco_occupation_skills.sql`; `brownfield.table_mappings` (sys_position_skill_requirements=REFERENCE_ONLY); report §1.3; parz. in `SOT_BACKLOG` B-50(b) | T1.2 — nuova tabella `sys.sys_occupation_skill_requirements` (relation varchar+CHECK ESSENTIAL/OPTIONAL, RD-08), import idempotente `ON CONFLICT`, registrare nel reconciliation registry. Decisione semantica occupation-level vs position-level = Enzo. Spec `..._03..._md` §2. | ✅ **RISOLTO** 2026-06-15 (S990, T1.2 mig `000123` + seed 52, `b4e3af8`): `sys_occupation_skill_requirements` **126051 righe** (ESSENTIAL 67600/OPTIONAL 58451), risoluzione 100% via `skill_code='OLDDB::esco_skills::'||id`, registry A/IMPORT, 12 test. |
| **D-34** | 🟢 bassa | **Skill quasi tutte non categorizzate** (= DT-D): solo **31/21939** skill hanno `skill_category_id`; nessuno schema hard/soft/live/conoscenze sopra il `skill_type` ESCO già presente (skill 10797 / knowledge 3230). Le 7 categorie attuali sono comportamentali (Cognitive/Interpersonal/Leadership/…), non la tassonomia hard/soft del flusso. | query live (skill_category_id 31/21939; skill_type 14036); `db/seeds/reconciliation/44_skill_category_mapping.sql:4` ("ONLY the 31 skills referenced"); report §3 | T2.4 — mappare le 21939 skill via `skill_type` ESCO + categorie; eventuale dimensione `skill_kind` varchar+CHECK (no ENUM). Decisione mapping hard/soft↔ESCO = Enzo. Spec `..._03..._md` §3. | ✅ **RISOLTO (slice)** 2026-06-15 (S990, T2.4 mig `000120`, `f94e6f3`): colonna `skill_kind` varchar+CHECK da ESCO skill_type → **14036/21939** (SKILL/KNOWLEDGE/COMPETENCE/BEHAVIOR). Layer hard/soft DEFERRED (ESCO non ha il flag; autorità Enzo); 7903 pollution-rows flaggate non classificate. |
| **D-35** | 🟢 bassa | **Closure-table OU vuota** (= DT-E): `sys.sys_organization_hierarchies` (ancestor/descendant/depth, mig `000009:98`) = **0 righe**; la gerarchia OU vive solo sul `organization_unit_parent_id` (adjacency). Funzionalmente OK via recursive CTE, ma la closure-table prevista è inutilizzata (possibile by-design o gap di popolamento). | query live (org_hierarchies 0 righe); mig `000009:98-104`; report §3 (F1) | Decidere: (a) popolare la closure-table (trigger/seed dal parent_id), oppure (b) documentarla come intenzionalmente non-usata (adjacency+CTE è il pattern del repo). Non urgente. | ✅ **RISOLTO (b)** 2026-06-15 (S990): verdetto evidence-based = **closure-table intenzionalmente non-usata** — la gerarchia OU vive su `organization_unit_parent_id` (adjacency) + recursive CTE (pattern stabilito del repo); nessuna query richiede la closure-table. Documentato qui, no popolamento. |
| **D-36** | 🟢 bassa | **E2E Playwright locale rotto su Node 24**: il Node di sistema Windows (`/c/nodejs/node` = v24.3.0) rompe il loader/transform TS di **Playwright 1.61.0** → ogni spec (incluso `auth.setup.ts`) muore a import-time con `TypeError: context.conditions?.includes is not a function`. La CI gira sul runner OCI VM (Node 22) → non affetta; il difetto è solo locale e non era mai emerso (E2E sempre in CI). | run locale S991 (5 setup + spec falliti a import-time); workaround verificato = Node 22 portable isolato in `%TEMP%\node22` + `playwright install` (browser build 1228) + `next start` prod su porta dedicata. Memoria `reference_local_e2e_node22_playwright`. | (a) **bump Playwright** a una versione che supporta Node 24; oppure (b) **pin Node 22** per lo step E2E locale (nvm-windows / portable). Non urgente (workaround funziona, CI non affetta). | ✅ **RISOLTO** 2026-06-16 — opzione (a) NON disponibile (verificato live: `1.61.0` è la **latest stable** di Playwright; le sole versioni successive sono alpha-prerelease di 1.61 stesso; engines `>=18` ma rompe su Node 24.3.0). Fix-proper = **(b) riproducibile**: wrapper `apps/web/scripts/e2e-node22.mjs` (Node-agnostic) + script `test:e2e:node22` / `test:e2e:prod:node22`. Su Node ≤22 (CI runner, Mac/VM via nvm) = passthrough trasparente, **zero download, CI invariata**; su Node ≥23 individua/provisiona un Node 22 LTS portable (`$E2E_NODE22_BIN` → cache `.cache/node22` → auto-download `nodejs.org` idempotente, Expand-Archive su Windows / tar su *nix) e gira Playwright + webServer sotto di esso. **Verificato LIVE** (Node host 24.3.0): controprova `playwright --list` su Node 24 → crash `context.conditions?.includes`; via wrapper → provisiona `v22.22.3` + suite ESS reale (auth.setup 5 personas + 11 spec `me-pages` incl. `/me/matching`) **16/16 verde** contro DB OCI. `.cache/` gitignored. |
| **D-37** | 🟢 bassa | **`reference-sync.integration.test.ts` hook-timeout sotto carico full-suite**: il test esegue fetch HTTP reali all'API ESCO **esterna** dentro un hook; sotto carico (suite completa, o due suite concorrenti) il `beforeAll`/`afterAll` può superare i 30s default → 1 file fail a cascata. (La "synthetic ESCO fetch failure" interna è un fixture iniettato apposta, NON la causa.) Passa **14/14 in isolamento** (15.8s). | gate finale S994: full-suite concorrente → 1 hook-timeout su reference-sync; re-run isolato 14/14 verde. Anche le 4-file-fail viste durante due vitest concorrenti erano `Connection terminated` (stessa classe D-20). | (P3) opzioni: mockare il fetch ESCO nell'hook (il DI seam `EscoSkillFetcher` esiste già), o alzare il timeout dell'hook network-dependent, o serializzare i test che fanno I/O esterno. Non urgente (esterno, isolato verde, CI VM più stabile). | ✅ **RISOLTO** 2026-06-18 (S996, `bd1d5eb`): diagnosi corretta — la premessa "fetch HTTP reali in un hook" è ERRATA, il file inietta GIÀ i fixture (`FixtureEscoFetcher`/`FixtureAtecoFetcher`); il vero costo è `buildTestApp` (boot Fastify + RBAC cache) + 3 login reali + 2 query DB sul tunnel SSH, che sotto contention full-suite esauriscono il 30s `hookTimeout` (latenza DB/login, NON HTTP). Fix test-only: (a) **per-hook 60s** sui 3 `beforeAll` pesanti (main/ateco/fail); (b) stub difensivo `escoSkillFetcher` + `escoSkillUriSource()=>[]` nel main beforeAll → nessun HTTP ESCO reale può mai partire dal file (il file triggera solo ESCO/ATECO_2025, mai ESCO_SKILL_HIERARCHY). Verificato: 14/14 isolato + full API suite **1064 pass** S996. |
| **D-39** | 🟢 bassa | **`vm-deploy.sh` hard-coded per la VM (ubuntu / 80.225.82.207)**: i systemd unit templates usavano `User=ubuntu` e le NEXT_PUBLIC_* inlinavano `$PUBLIC_HOST=80.225.82.207` a build-time senza che questi fossero env-overridabili. Il deploy sul linux-pc PROD twin (user `enzo`, IP 192.168.1.11) avrebbe creato unit avviabili ma con `User=ubuntu` inesistente e URL pubblici sbagliati → fail silenzioso. Analogo al self-modify-buffer (D-17): il parametro esiste in `exec env` ma non passava le var custom. Progetto `scripts/align-clones.sh` non passava `DEPLOY_ENV` in nessun modo. | `vm-deploy.sh:17-28` (SERVICE_USER/GROUP hardcoded ubuntu); `align-clones.sh` pre-fix (nessun `DEPLOY_ENV` per linux-pc); design §12.4 | Reso env-overridabile: `SERVICE_USER`/`SERVICE_GROUP` default `ubuntu`/`$SERVICE_USER`; `exec env` re-exec guard li propaga; sed nei unit-template `s#^User=ubuntu#User=$SERVICE_USER#g` + `s#@@PUBLIC_HOST@@#$PUBLIC_HOST#g` ecc.; `align-clones.sh` linuxpc-cfg propaga `DEPLOY_ENV="PUBLIC_HOST=192.168.1.11 SERVICE_USER=enzo API_PORT=8013 WEB_PORT=3013"`. | ✅ **RISOLTO** 2026-06-20 (questa sessione, commit `0ec57e4`): `vm-deploy.sh` env-parametrico + re-exec guard esteso + sed unit; `align-clones.sh` `linuxpc_cfg` con `DEPLOY_ENV`; design doc §12.4. Ora il deploy su linux-pc è identico alla VM, salvo topology. |
| **D-38** | 🟢 bassa | **`000078` surveys-perm post-condition non re-run-safe** (deploy blocker, classe D-12/D-22): contava TUTTI i permessi `resource='surveys'` e asseriva esattamente 4, ma `000135` (S995) ne aggiunse un 5° (`surveys:respond:self`) → la catena migrate si rompe al replay. **Latente da S995** (sessione mai deployata) → emerso al **1° deploy post-S995** (S996, durante l'align/deploy del batch #4-#14). | deploy S996 abortito a `000078`: "expected 4 surveys permissions, found 5"; `git log` S995 mostra 000135 senza aggiornare l'assert di 000078. | Scope dell'assert ai 4 codici che 000078 **possiede** (`surveys:{read,create,update,delete}`), non al resource-wide count. | ✅ **RISOLTO** 2026-06-18 (S996, `ac4b723`): assert scope-by-code; full `migrate.sh` chain ri-verde "138 migrations applied" EXIT 0; deploy PROD completato + verificato (`/readyz` OK). R3/R17 (break non-mio, lasciato verde). |

| **D-40** | 🟢 bassa | **`align-claude-ecosystem` reinstall riabilita i plugin → `enabledPlugins` di Windows non preservato sui remoti** (S1001 close-flow discovery): il blocco `reinstall plugins` esegue `claude plugin install <p>` che setta `enabled=true` nel `settings.json` remoto, sovrascrivendo lo stato pushato dal payload Windows. Es. `claude-mem@thedotmack`=`false` su Windows (SoT) ma torna `true` sui remoti ad ogni align → il verify sez.2 lo segnala come settings DRIFT (bloccante) ad ogni run. | `drift-{vm,linuxpc}-*.md` sez.2 (claude-mem false→true); `align-claude-ecosystem.sh` reinstall block | Dopo il reinstall, riapplicare gli `enabledPlugins` dal `settings.json` di Windows (jq merge che preserva `extraKnownMarketplaces` gestito dal CLI remoto), oppure escludere dal reinstall i plugin che Windows ha disabilitati. Coerente con Opzione C §13.2 (enabled-state = gestione per-macchina). | ✅ **RISOLTO** 2026-06-21 (S1001): nuova `reapply_enabled_plugins()` in `align-claude-ecosystem.sh` — DOPO il reinstall ri-applica gli `enabledPlugins` del payload Windows sul remoto (valori Windows vincono via jq `(.enabledPlugins + $win)`), lasciando intatte le `extraKnownMarketplaces` gestite dal CLI; quindi `claude-mem=false` (e ogni plugin disabilitato su Windows) sopravvive a OGNI align (durevole, non più workaround manuale). Applicato live su vm+linux-pc; verify sez.2 settings → CLEAN. `bash -n` OK. |
| **D-41** | 🟢 bassa | **`project_memory` verify dà falso `remote=0` sul Mac** (S1001): il check §13 in `align-claude-ecosystem` `verify_host` confronta il memory-tree win vs remoto via `find ~/.claude/projects -name '*heuresys-advanced*' + cd memory + ls`; su vm+linux-pc (bash) ritorna `42=42 OK`, sul Mac (zsh login shell via ssh) ritorna `remote=0` benché il canale 1 abbia sincronizzato 42 file (`sent 42`). Falso negativo del comando ssh/zsh, NON perdita dati. | `drift-mac-*.md` sez.5 (`win=42 remote=0 DRIFT`); canale 1 `[memory] full: sent 42 -> mac` | Verificare alla prossima accensione del Mac cosa ritornano `find`/`ls` via ssh non-interattivo (probabile dir multipla o quoting su zsh); rendere il comando zsh-safe come `sync-memory-tree.sh` (stesso find, funziona). | ✅ **RISOLTO** 2026-06-21 (S1001): Mac tornato raggiungibile in-session → diagnosi: 1 sola dir match, `ls memory`=42, `claude-mem`=false (D-40 ok anche sul mac). Il `remote=0` era un **falso negativo transitorio** (ssh vuoto sotto carico durante una run lunga, contato come 0 — NON un bug del find zsh). Fix-proper: il check §13 sez.5 ora usa un **sentinel `MEMCOUNT=`** (il remoto stampa SEMPRE il count, anche 0 genuino) + **SKIP** se l'ssh non restituisce il sentinel (host lento/timeout) invece di un falso `remote=0` DRIFT. Test live sul mac (zsh): `win=42 remote=42 OK`. `bash -n` OK. |
| **D-42** | 🟡 media | **Rate-limit per-IP di `/v1/leads` (POST pubblico GTM) potenzialmente mis-keyed dietro i 2 hop nginx→Next→API** (final-review C1, S1002): l'endpoint lead pubblico passa nginx→Next:3013→(rewrite `/api`)→API:8013 = 2 hop; con `TRUST_PROXY=1` il `keyGenerator: req.ip` potrebbe risolvere all'IP del hop più vicino (Next loopback) invece del client → il cap 5/min collasserebbe in un bucket unico (self-DoS o throttle inefficace). È la difesa anti-abuse primaria del solo endpoint pubblico-write. | final review opus C1; `deploy/nginx/www.heuresys.com.conf:41-43` (location / → :3013, NESSUN /api block); `next.config.js` rewrite `/api`→API; `app.ts` `keyGenerator: req.ip`; D-28/`trust-proxy.ts` | Verificare LIVE in PROD: POST ripetuti `/api/v1/leads` da IP diversi devono contare per-IP (non collassare). **NB**: stesso path+keyGen del login (verificato funzionante PROD S984/D-28) → probabilmente già OK. Se collassa → NON è GTM-specifico (app-wide, login incluso) → D-28-followup: tarare `TRUST_PROXY` all'hop-count reale (2) o keyare sul client XFF. Compensanti: honeypot + Zod bounds + RBAC sul GET. | ✅ **RISOLTO** 2026-06-21 (S1002, verifica live post-deploy): 7 POST rapidi a `https://www.heuresys.com/api/v1/leads` → #1-3 `200`, **#4-7 `429`** → il rate-limit **conta per-IP client** (la mia raffica è stata throttlata) dietro i 2 hop. Il `req.ip` risolve correttamente al client (come il login, come previsto). C1 era un falso-positivo del reviewer (premessa "leads unico 2-hop" errata: tutto `/api` è 2-hop, login incluso). Difesa anti-abuse FUNZIONANTE. |
| **D-43** | 🟢 bassa | **Test RACI demo stale dopo il refactor produzione S1002**: `organization-unit-processes-raci-demo.integration.test.ts` (S994 #11) asseriva 12 righe demo (tag `S994-#11`); S1002 (`9ce9a92`) ha sostituito quel seed con 105 RACI di produzione (tag `S1002-#5/#11`) ma non aggiornò il test → `beforeAll` throw "demo seed missing 0/12", file rosso nella full API suite (già rosso pre-S1003, scoperto al gate finale di #4). | full API suite S1003 (1 file failed / 0 test case); rerun isolato 6 skipped; `git log` 9ce9a92 senza update del test demo | Ri-puntato al seed di produzione (tag → `S1002-#5/#11`, soglia → 105) + 4 assert role ri-verificati LIVE contro il DB (DIV-RISK/02 OWNER→INFORMED · DIV-LEGAL/11 CONSULTED→OWNER · DIR-RISKM/10 CONTRIBUTOR→OWNER · DIV-OPS/12 INFORMED inv.). | ✅ **RISOLTO** 2026-06-22 (S1003, `102d49c`): 6/6 + typecheck:test verde + CI test-api verde. R3/R17(c): break non-mio, fixato nello stesso push, dichiarato. |

| **D-44** | 🟢 bassa | **Doc descrittivi citano `user_is_synthetic`/`SYNTHETIC_REFERENCE` dopo il ritiro (S1004, mig 000154)**: ~8 doc di design/plan (`TARGET_SCHEMA_DESIGN`, `BOOTSTRAP_EXECUTION_PLAN §I14`, brownfield {IMPORT_PLAN, ADAPTATION_MAP, TABLE_CLASSIFICATION}, `MIGRATION_IMPLEMENTATION_PLAN`, `AUTH_SECURITY_PLAN`, `MVP_4_ROADMAP`) descrivono ancora la colonna/flag ritirati come parte dello schema. | grep `user_is_synthetic\|SYNTHETIC_REFERENCE` su docs/ (S1004) | Allineare le descrizioni schema a ADR-0026/000154 (user_type `GENERATED_INCUMBENT`, niente `is_synthetic`). Backlog #18. | ✅ **RISOLTO** 2026-06-23 (S1005, commit `c3ac0cc`): 8 doc design/plan allineati a ADR-0026/000154 (`user_is_synthetic` rimosso, `SYNTHETIC_REFERENCE`→`STANDARD`/`GENERATED_INCUMBENT`, vista tolta dalle liste live). Zero residui. Marginali fuori-scope segnalati (wave_runners ×2, due-diligence/WS-T5). |
| **D-45** | 🟡 media | **Drop-column su DB condiviso applicato PRIMA del deploy-codice → finestra di incoerenza schema↔codice in PROD (S1004)**: il refactor A ha droppato `user_is_synthetic` dal DB OCI VM (condiviso col PROD :8013) via `migrate` PRIMA di deployare il codice nuovo sulla VM → nella finestra migrate↔vm-deploy l'API PROD (codice vecchio, `SELECT user_is_synthetic`) avrebbe dato **500** su `GET /v1/users`. | osservato S1004 (drop-first); chiuso col `vm-deploy` immediato (`/users` 401 non-500 post-deploy) | **RISOLTO** 2026-06-22 (S1004): vm-deploy eseguito subito dopo → coerenza ripristinata + verificata live. **Lezione**: per migration distruttive su `sys_users`/tabelle hot del DB condiviso, deployare il codice compatibile PRIMA del drop (o 2-fasi deprecate→drop). |
| **D-46** | 🟢 bassa | **CHECK `sys_ui_interfaces_perspective` accetta l'unione PET∪5-sezioni** (8 valori) invece dei soli 5 valori-sezione: le mig interfaces 000050+ re-inseriscono i valori PET (PROCESS/ENTERPRISE/TALENT) su ogni re-run idempotente PRIMA che 000163 li rimappi, e la riga è validata dal CHECK prima di ON CONFLICT → un CHECK 5-only rompe l'invariante twice-run (deploy VM S1009 fallito a 000050). Le righe persistite sono SEMPRE 5-sezioni; PET è solo transitorio upstream. | deploy VM S1009 fallito a 000050 pre-fix; `000163` ADD CONSTRAINT union | Restringere il CHECK ai soli 5 valori-sezione quando le mig 000050+ saranno normalizzate (o consolidate in una baseline migration). Non urgente: il CHECK è permissivo ma lo stato persistito è sempre valido. | ✅ **RISOLTO** 2026-06-29 (S1011, `d6e6f46`): le 22 migration interface upstream (+CHECK inline 000050) riscritte per seedare DIRETTAMENTE il valore-sezione finale (script content-based, 47 token swap), 000163 stringe la CHECK ai **5 valori-sezione**. **Verificata su DB scratch fresco** sulla VM (interface-chain ×2 = rerun OK, 47 righe/5 sezioni con distribuzione attesa, PET `ENTERPRISE` rifiutato). Inerte sui DB deployati (ON CONFLICT DO NOTHING). Bijection route→sezione confermata (0 orfani). Live in prod (CHECK = solo 5 valori). |
| **D-47** | 🟢 bassa | **`me-preferences.spec` obsoleto dopo S1009 (language toggle)**: il test cercava i 2 bottoni `language-it`/`language-en` con `aria-pressed` (vecchio `LanguageSwitcher`); S1009 li ha sostituiti col **toggle header singolo** (`DashboardHeader.onToggleLanguage`, button "Cambia lingua…": IT/EN). Falliva in dev+prod-build locale; la CI **smoke** (`smoke-5-personas.spec` only, `playwright-smoke.yml:174`) non lo copre → break passato inosservato tra S1009 e S1010. | run E2E S1010 (1 fail consistente) + snapshot header + workflow grep | Aggiornare al toggle (`getByRole` bilingue + `toHaveText` IT/EN). | ✅ **RISOLTO** 2026-06-29 (S1010, `b538aae`): aggiornato, E2E `me-preferences` 7/7 verde. **Nota di classe**: i test full-suite non coperti dalla CI smoke restano non-monitorati cross-sessione — candidato gate periodico full-E2E in CI. |
| **D-48** | 🟢 bassa | **`vm-deploy.sh` verify race (falso negativo)**: lo step `verify` fa `curl /readyz` **subito** dopo il restart systemd, ma l'API impiega ~40s al boot (RBAC cache 681 map + connessione DB attraverso il jitter OCI free-tier) → `Failed to connect :8013` benché l'API parta correttamente subito dopo (`readyz=200` ~40s più tardi). | deploy S1010 F1 (verify FAILED, readyz=200 ~40s dopo, smoke www OK; F2/F3a verify OK perché il build più lungo dà tempo all'API) | Aggiungere retry/backoff al curl di verify (es. `--retry 30 --retry-delay 1 --retry-connrefused`) per coprire il boot ~40s. Cugino di D-20 (boot-retry interno già aggiunto S981). | ✅ **RISOLTO** 2026-06-29 (S1011, `5e52f0a`): `curl /readyz --retry 45 --retry-delay 1 --retry-connrefused --retry-all-errors` copre il boot ~40s (connrefused mentre non ascolta + all-errors per il 503 di RBAC-cache). Verificato al deploy S1011 (verify verde, readyz OK). |
| **D-49** | 🟡 media | **`vm-deploy.sh` interrompibile dal timeout del client SSH → deploy parziale**: lanciato come `ssh host 'bash vm-deploy.sh'` foreground, se il client SSH va in timeout (es. 10min lato chiamante) il SIGTERM interrompe il deploy a metà → `next build` incompleto (`.next` senza `prerender-manifest.json` → il web crasha al `next start` con ENOENT) + restart finale non eseguito (l'API resta sul dist vecchio). Compounding: un rebuild web manuale **senza** `NEXT_PUBLIC_API_PROXY_BASE_URL` inlina il default `:3001` (next.config.js:11) → il proxy `/api` del web va su :3001 (dev, assente) → `ECONNREFUSED` → 500 su tutto `/api`. | incidente deploy S1011 (build 16:40 ma API attiva da 16:01; web ENOENT prerender-manifest; `/api`→:3001 ECONNREFUSED) | Lanciare `vm-deploy.sh` in **background** (`nohup`/detached) decouplato dal client SSH, oppure un wrapper che lo esegue con `setsid` + poll dello stato. Vale anche per `close-propagate --auto-deploy`. Recovery manuale documentato: restart API (carica dist nuovo) + rebuild web con `NEXT_PUBLIC_API_PROXY_BASE_URL=http://localhost:$API_PORT` (come `vm-deploy.sh:133`) + restart web. | **RISOLTO** 2026-06-30 (S1012): nuovo `scripts/vm-deploy-remote.sh` lancia il deploy **detached** (setsid+nohup; output→log remoto, exit-code→sentinel) + poll con ssh brevi; `align-clones.sh:152` instrada VM **e** linux-pc attraverso di esso (close-propagate eredita). Provato LIVE su oracle-vm-default (payload sintetico: success→exit 0, fail→exit 7). Commit `33cdfb5` + hardening `0be1f64` (REMOTE_REPO obbligatorio, no default assoluto) + resilienza shell-tests sez. G. |
| **D-50** | 🔴 alta | **Leak dati sensibili cross-utente (regola cardine ADR-0027 non enforced)**: un ruolo operativo con `compensation_intelligence:read` / `skill:read` / `assessment:read` (es. `MANAGER`) legge i dati sensibili (retribuzione, competenze, valutazioni, dati personali) di **tutti** gli utenti del tenant, non solo dei propri riporti. Il filtro reports-to (`getManagerTeamUserIds`, one-hop) è applicato **solo** a `/v1/users/*`; i moduli sensibili (compensation/skills/assessments/goals) filtrano solo per ruolo+tenant. Nessuna peer-isolation. | map S1012 (subsystem *data-sensitivity*); `apps/api/src/modules/users/repository.ts:162-183` (filtro applicato solo a users) vs `compensation/service.ts` · `skills/service.ts` · `assessments/service.ts` · `goals/service.ts` (solo role+tenant) | Modello bi-assiale **ADR-0027** + spec `docs/superpowers/specs/2026-06-30-two-axis-authorization-model-design.md`. Chiusura = **fase F3** (cardinal-rule enforcement: dati sensibili accessibili solo via org sub-tree transitivo), prerequisiti F0-F2. E2E: `paolo→tommaso` ✅, `paolo→antonio` ❌. | ✅ **RISOLTO** 2026-07-01 (S1013): F3 enforcement shipped su **11 moduli sensibili** (assessments, capability-composition, successor-readiness, compensation, learning-gaps, goals, insights, semantic-matching, mentorship, okrs, predictions) — list filtrata da `resolveOrgReadScope.userIdAllowList`, per-target da `canReadOrgTarget`; 11 nuovi integration test (`paolo→tommaso` 200 / `paolo→antonio` 404) + **F5** peer-isolation (I19 bidirezionale, paolo⊥claudia sub-tree disgiunti) + **F6** scope-access audit (axis autorizzante). 14 commit atomici. **F4** (asse funzionale/attività) deferito a sessione dedicata. **Esteso S1014 (2026-07-02)**: la code-review post-handoff ha scoperto **4 moduli sibling fuori-mappa** con lo stesso leak — enforcement esteso red→green (personas reali) a **assessment-results** (soggetto = subject dell'assessment padre), **successor-candidates** e **user-career-plans** (soggetto diretto), **succession-pools** (soggetto = incumbent ACTIVE della posizione; vacante → tenant-visible) → **15 moduli org-gated**, +4 suite `*-scope` (30 test). Riconciliazione mappa F3: i 13 heading = **11 moduli unici** (2 duplicati: gap_analysis≡learning-gaps, insights ×2); i 4 sibling non erano mappati. |

| **D-51** | 🟢 bassa | **Tassonomia data-class F2 (ADR-0027) descrittiva, non prescrittiva** (audit F-003): l'enforcement org-axis runtime NON consulta `data-classes.ts` — ogni modulo cabla il gate a mano, quindi un nuovo modulo sensibile può ometterlo silenziosamente. | `apps/api/src/lib/scope/data-classes.ts` (nessun consumer runtime; 18 file usano resolveOrgReadScope/canReadOrgTarget senza importarla) | Parz. mitigato: drift-test `scope-data-classes.integration.test.ts` (tassonomia↔RBAC reale) esiste già. Chiusura piena = **auto-gate dichiarativo** guidato dalla tassonomia (preHandler/gate condiviso su tutti i moduli sensibili) — refactor a sessione dedicata (rischio su 15 moduli). fp-check: FALSE_POSITIVE-come-vuln (Info, non sfruttabile). | ✅ **RISOLTO** 2026-07-05 (S1015): tassonomia resa **prescrittiva** via `lib/scope/gate.ts` — collector `onRoute` + assertion `onReady`: il server **rifiuta il boot** (`ORG_GATE_MISSING`) se una route read (`read`/`view`/`list`, non-`:self`) su risorsa sensibile non dichiara `config.orgGate` ∈ {`service`,`catalog`,`aggregate`}; `requirePermission` espone il permission code sull'handler per il mapping route→risorsa. **76 route annotate** con marker verificati per-handler (moduli resolver-gated → `service`; cataloghi/definizioni/template → `catalog`; distribuzioni/maturity → `aggregate`). Il gate ha scovato un residuo reale: `mentorship listSessions/getSession` erano tenant-only mentre il pairing padre era org-gated → **org-gate esteso alle sessions** (stesso predicato mentor/mentee). Test `org-gate.integration.test.ts` 4/4 (superficie reale 0 violazioni + boot-refusal negativo + esenzioni self/write/unclassified); suite API completa verde. |
| **D-52** | 🟡 media | **Suite integration senza isolamento transazionale** (audit F-006): i 185 file girano contro il DB VM condiviso live con cleanup per-test manuale (DELETE) invece di tx-rollback → non-ermetica, order-coupled, drift-prone (classe D-23/D-29). | `apps/api/vitest.config.ts` (singleThread, pool condiviso); ogni `*-scope`/`me-*` fa DELETE in afterAll | Refactor harness verso tx-per-test rollback — **incompatibile col commit dell'app via pool proprio** → richiede design dedicato (savepoint/nested-tx o DB per-run). Alto rischio destabilizzazione. Non azionabile in-batch. | ✅ **RISOLTO** 2026-07-05 (S1015): **isolamento per-FILE** via `test/helpers/tx-isolation.ts` (wired in `setup.ts`, escape hatch `TEST_TX_ISOLATION=0`): ogni file gira dentro UNA transazione reale rollbackata a fine file — zero residui sul DB condiviso, niente coupling inter-file; le fixture `beforeAll` e i flow sequenziali intra-file restano intatti (per-FILE, non per-test — i CRUD-flow non si rompono). Il `withTransaction` dell'app funziona via facade `pool.connect` che mappa BEGIN/COMMIT/ROLLBACK → SAVEPOINT/RELEASE/ROLLBACK-TO; `pool.end` intercettato (110 file chiamano `closePool` in afterAll). Iterazione design: (v2) savepoint su OGNI statement = race sotto `Promise.all` (`RELEASE` distrugge i savepoint successivi) + 3× round-trip sul tunnel (2078s) → (v3, finale) savepoint **solo sulle scritture** (INSERT/UPDATE/DELETE/MERGE + CTE scriventi), serializzate da mutex — gli errori SQL intenzionali dei test (FK/unique → 404/409) mantengono la semantica autocommit senza avvelenare la tx di file (`25P02`). **Evidenza live**: suite completa **186 file passed / 2 skip pre-esistenti, 1285 test, 0 FAIL** (1442s, +20% vs baseline); rollback provato 2× su `sys_auth_login_events` (0 righe committate durante run con centinaia di login). Delta noti documentati nell'header dell'helper (`now()` congelato per file; DELETE legacy ridondanti-ma-innocui; SELECT intenzionalmente fallente = buco residuo dichiarato, oggi inesistente). |
| **D-53** | 🔴 alta | **F-001 rotazione credenziale live PENDING** (audit F-001 Critical): il refactor codice (password env-driven, no default committato) è **fatto+committato** (`0b24b136`), ma il DB prod condiviso detiene ancora la password pubblica → l'exposure resta finché non si ruota + non si pusha. | commit `0b24b136`; DB VM `sys.sys_auth_credentials` (personas ancora su password pubblica); git-history pubblica | Fase B: genera P forte → `.secrets/` + `.env` locale + `.env` VM → `TEST_ADMIN_RESET_PASSWORD=1 pnpm db:seed-test-admin` → verify old→401/new→200 → push (CI VM usa `.env` aggiornato) → `vm-deploy.sh`. **Azione outward-facing negata dall'auto-mode classifier** → attende autorizzazione esplicita Enzo (o Bash permission rule). | ✅ **RISOLTO** 2026-07-04 (S1014, Enzo Opzione A = permission rule `Bash(MSYS_NO_PATHCONV=1 ssh oracle-vm-default:*)`): rotazione live eseguita per **tutte le 11 login-personas** (6 test-admin + 5 R2), **verificata su prod** (vecchia `Admin#PassW0rd!` → 401 · nuova → 200 PLATFORM_ADMIN); nuova password in `.secrets/test_admin_password.txt` (gitignored, chmod 600). Deploy prod verde (readyz+login 200). Secret CI provisionato in `/etc/heuresys-runner.env` + runner restart. **2 regressioni CI colte e fixate**: (a) R2 personas non ruotate (`alberto.rossetti` in `auth-mfa-{mandatory,enforcement-switch}` → 401) → `seed-r2` reset; (b) `playwright.config.ts` `import.meta` incompatibile col config-loader CommonJS di apps/web → parse `.env` via `fs` (`efb71d5b`). **CI 6/6 verde** su `efb71d5b`. |
| **D-54** | 🟢 bassa | **Notifiche inbox polimorfiche senza cascade → orfane su delete risorsa**: `sys_inbox_notifications` referenzia le risorse via (`notification_resource_type` + `notification_resource_id`) **senza FK hard**, quindi eliminare una risorsa (POSITION/LEARNING_MODULE/ASSESSMENT/CAREER_TARGET/KPI/SKILL/APPROVAL_STEP) lascia notifiche dangling. Scoperto S1017 (4 notifiche ASSESSMENT orfane di `paolo.caputo`, create 2026-07-05, verso assessment inesistenti). | `sys.v_inbox_resource_consistency` (validation view che le cattura — tornata a 0 dopo il cleanup S1017); `qa_artifacts/inbox-orphan-cleanup-20260707.md` | **Dati già puliti** S1017 (4 orfane rimosse con snapshot+rollback-guard → 6/6 viste strutturali a 0). Resta la **prevenzione strutturale**: cleanup a delete-time nelle service di delete risorsa, o sweep periodico, o trigger. Basso: la view lo cattura e il cleanup è banale. | ✅ **RISOLTO S1018** — prevenzione a delete-time: `lib/notifications/cleanup.ts` chiamato in-transaction nei 2 hard-delete site live (learning-modules, kpi-definitions — censimento S1018: gli altri 5 tipi non hanno hard-delete); purge one-time strutturale mig `000170` (0 residui); regression net `inbox-consistency.integration.test.ts` (3/3 live: create→notify→DELETE→view=0). Nuovi hard-delete path DEVONO adottare l'helper. |

## Sintesi scope

> ⚠️ **Footer storico (S939)** sotto — superato. **Stato live S1010: 2 debiti aperti** (D-46 cosmetico P3 · D-48 vm-deploy-verify-race P3; D-47 RISOLTO S1010), D-01…D-45 tutti terminali (RISOLTO/non-issue/gestito/monitor). Verifica live: `python docs/kb/tools/status_dashboard.py` (sezione DEBITI). Unico sotto-pezzo deferito = D-34 taxonomy hard/soft skill (decisione Enzo, non un debito azionabile).

- **Trattati nella sessione storica (S939)**: D-02, D-03, D-08, D-10, D-11 (via indice + SoT + archive note); D-06, D-07 (chiariti).
- **Risolti (storico)**: D-12 (migrate.sh idempotency chain repair, 2026-05-27); **D-01** (doc-drift CLAUDE.md+README → MVP-4, 2026-05-28).
- **In backlog operativo**: — (S1007: **0 debiti aperti**, D-01…D-45 tutti chiusi).
- **Deferiti con registrazione**: D-05 (monitor, si allinea al prossimo handoff).
- **Risolti 2026-05-28**: D-01 (doc-drift), D-04 (root cleanup → docs/archive/).

> Nessun debito scartato. Ogni riga ha owner (CLI) e remediation. R3: il codebase va lasciato in stato migliore.
