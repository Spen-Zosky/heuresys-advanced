# SOT_STATE — Snapshot granulare del sistema (handoff-governed, SoT v2)

> **Ruolo (SoT v2, S965)**: vista **granulare** dello stato del SISTEMA (git/release · stack · DB/API/web/CI counts · security · narrativa milestone), **ri-derivata e riscritta dalla skill `handoff` a ogni sessione** (counts via psql/ls/git — decisione "robusta"). Lo **stato di lavoro corrente** (priorità, open questions, cosa-faccio-ora) vive in `.handoff/STATE.md` (vista rapida). **Domini disgiunti**: numeri/architettura SOLO qui; priorità/open-Q SOLO in `.handoff/STATE.md` → nessuna divergenza possibile (un solo updater, la skill `handoff`). Backlog → `docs/kb/SOT_BACKLOG.md` · debiti → `docs/kb/DEBT_REGISTER.md` · indice → `docs/kb/INDEX_PATHS.md` · storici → `docs/archive/`. Razionale: `docs/superpowers/specs/2026-06-05-sot-unification-design.md` §11.

## 0. Snapshot in una riga

Monorepo pnpm HRMS/BPM **a baseline GA v1.0.0** (S957): API Fastify 5 con **80 moduli business + auth** (+`leads` S1002 +`public-stats` S1003), web **Next.js 16** (B-23 bump S969) con **MVP-2a (admin) + MVP-2b (ESS) shipped + RBAC/UIX/Perspectives epic COMPLETE** + **BI analytics** + **AI matching** + **MFA disponibile e in uso, enforcement OFF (S1029: verificato sul reale — 25 fattori registrati, 19 TOTP + 6 WebAuthn, 13 verificati; ma `MFA_ENFORCEMENT_ENABLED=false` nel .env di PROD per decisione di Enzo. La vecchia dicitura «mandatory LIVE» era falsa e contraddiceva la riga «MFA resta OFF» dello stesso documento)** + **Gap#1 COMPLETO** (MLCE capability-composition + Maturity L0-L5 + Porte Process/Org UI + WI-C demo agente, S999). DB PostgreSQL 16 nativo su OCI VM (**163 utenti, 161 posizioni attive; 43 OU attive (45 con le 2 sciolte); 26 team; 2 tenant ACTIVE; RBAC 14 ruoli/211 perm/957 map; 208 tabelle `sys.*`; 280 file migration `000001..000282`**, gap 000035+000139; +S1010 7 satelliti persona (anagrafica+contratti) + S1011 `sys_user_pay_slips` (42 cedolini) + backfill `goal_subject_user_id` (632) + `goal:read:self`/`approval:read:self` + 61 voci sidebar `sys_ui_interfaces` di cui 52 attive (S1026: +me-time-off, +whistleblowing-console; S1041: +org-director-vrio, +org-director-health, +org-director-advisor, +leads); modulo `me` ~46 route `/v1/me/*` (Personal area completa /me/{career,profile-cedolini,analytics,org-chart,approvals}); skill catalogo 14039 dopo clean S1006 + dedup S1024, ESCO 100% IT). **S1004**: **ADR-0026** (ambiente-unico-prod-grade, 2 tenant di produzione correnti, invariante **I15**) + `is_synthetic` **ritirato** (user_type `GENERATED_INCUMBENT` al posto di `SYNTHETIC_REFERENCE`; viste sys 12, era 13). **🎉 v1.0.0 GA RELEASED** (S957); tag `v1.0.0`. **Infra S1000** (2026-06-20): handoff-rigor design (`close-propagate.sh`, `handoff_lint.py`, HOLD register, status vocabulary) + D-39 RISOLTO (`vm-deploy.sh` env-parametrico linux-pc twin) + 3 bugfix ecosystem (zsh word-split, cleanup trap exit-0, MSYS global) + FASE 4 ecosystem alignment COMPLETO (Mac/VM/linux-pc). **Infra S1001** (2026-06-21): design handoff-rigor **IMPLEMENTED end-to-end** (§3-§5 + §11 P1-P9 + §12-§13) — `handoff_lint` 10-check bloccante, `build_menu.py`, session-journal, CI `state-lint`, boot reality-check, `vm-deploy` entrypoint unico, `project_memory` verify; close-flow live exit 0 (VM+linux-pc PROD verde); `claude-mem` enabled=false (decisione Enzo). **#4 go-to-market PRIMO DELIVERABLE SHIPPED (S1002)**: front-door landing pubblica `www.heuresys.com/` + lead capture GDPR (`/v1/leads`, `sys_leads`). Gap#1 follow-up 100% (a/b/c/d) + #5/#11 RACI produzione (105) + #13 B-50 bridges = chiusi S1002. **#4 go-to-market DELIVERABLE 2+3 SHIPPED (S1003)**: investor one-pager `/investors` (metriche live + print-PDF) + demo guidata `/demo` (tour 10 screenshot reali RTL_BANK) + fondazione (`lead_source` enum + `GET /v1/public/platform-stats`). **S1005** (2026-06-23): #18 doc-cleanup `is_synthetic` (8 doc design/plan → ADR-0026/000154) + #19 users-page verificata LIVE (login reale → `GET /v1/users` 200, `type:STANDARD`, no `is_synthetic`) + #17 Wave-3 **L1 confermato già-fatto** (S987/S988, mig 000110+000111) → residuo L2/L3 multi-industry HOLD; **D-44 RISOLTO**. **S1006** (2026-06-24): **audit forense QA E2E 74 pagine** (98 finding confermati via workflow visivo multi-agent) → fase fix end-to-end + deploy, **~20 fix live su PROD** (B-01..B-07 + B-03 RBAC ESS-lockout mig 000155 + G-03 + B-02 + CSP/Permissions-Policy nginx); **G-01 i18n-dati IT-canonical** (KPI/processi/non-ESCO + **13.032 skill ESCO** via API ESCO, mig 000156-159); **clean** 7846 junk-skills (000160, catalogo 21939→14093 archiviati) + skill_code `OLDDB::`→`ESCO::`/`COMP::` (000161); **G-02** org/gaps join-nomi; DATA dashboard/insights = non-bug (valori reali del dataset, verificati). MFA resta OFF (Enzo). **S1008**: **#21 residuo tail a11y+perf CHIUSO live** — `@heuresys/ui@0.1.9` (a11y shell landmark/tap-target/scroll-region + perf subpath split echarts/mermaid), `/dashboard` axe **0 violazioni** su www con login reale. **S1009**: **sidebar IA redesign** — 3 prospettive PET → **5 sezioni** collassabili (Panoramica/Governance/Forza lavoro/Intelligence/Area personale, mig 000163), Dashboard prima, lingua IT/EN nell'header (persistita come il tema), selettore prospettiva ritirato, 6 voci-merge con TabNav (9 pagine `is_active=false`, route vive); CI 7/7 + live www. **S1010**: programma `/me` navtab F1+F2+F3a (7 satelliti persona + tab profilo/My HR). **S1011**: batch menu — **D-48**+**D-46** (CHECK perspective→5 sezioni, verificata su DB scratch) + **F3b** (`/me/career` 3 sub-tab, backfill goals 632 + `goal:read:self`) + **F4** (Cedolini `/me/profile`, `sys_user_pay_slips` 42) + **GTM #4** verify+riconciliazione (moat 21939→14093) — tutto LIVE www. **S1014 (2026-07-03/04)** — audit forense read-only (53 finding verificati, scorecard, gate fp-check 4 TP) + **remediation security live F-001…F-013**: **F-001 Critical (credenziali admin pubbliche committate) CHIUSO** — password env-driven + rotazione live di tutte le 11 login-personas, verificata su **prod E linux-pc** (vecchia→401/nuova→200); F-002…F-013 deployate live; **CI 6/6 verde** (al commit `bf7327e0`). **S1015 (2026-07-05)** — **D-51 + D-52 CHIUSI** (ultimi debiti audit S1014 → **registro debiti a 0 aperti**): org-gate **boot assertion** prescrittiva sulla tassonomia data-class (`lib/scope/gate.ts`, 76 route annotate, fix leak mentorship sessions) + **isolamento transazionale per-FILE** della suite integration (`test/helpers/tx-isolation.ts`, rollback provato live). **S1016 (2026-07-05/06)** — **infrastruttura di conoscenza + portafoglio sviluppo + primo build**: atlas cross-layer SoT (`docs/kb/atlas/`, generatore `build_atlas.py`) + ATLAS_CURATED (193 rilievi sweep 19-agenti) + grafo graphify full-codebase 13.777 nodi (top-up pendente #41) + 7 dossier `docs/product/DEVELOPMENT_LINES_{A..G}` → selezione A+B = register #25-#40 + **#25 SHIPPED live** (ponte posizione→learning, 2 endpoint + pagina 3-sezioni, CI 7/7, www verificato; API test file **189**, E2E spec **60**, i18n parity **1745**). **S1017 (2026-07-08)**: forensica + ottimizzazione **session-start** (>20min → root cause = round-doctrine × decode xhigh, non gli MCP): nuovo `docs/kb/tools/session_start.py` (menu+salute in 1 processo/1 round, `--no-net` al boot), CLAUDE.md "Session start" riscritta (niente lettura raw dei file stato grossi al boot) + slim **270→236 righe** (Design-System→`docs/kb/DESIGN_SYSTEM_UI.md`, R23-project→`docs/kb/AUTONOMY_R23_PROJECT.md`, pointer MVP stale corretto), config globali fuori repo **plugin 40→17** + claude-mem obs 50→12 (effetto next CLI), referto `docs/kb/SESSION_START_FORENSICS.md`; **fix integrità DB** — 4 notifiche inbox ASSESSMENT orfane (paolo.caputo) eliminate → **6/6 viste strutturali a 0**. **Prossimo**: **#66** residuo Dependabot (low-risk + 4 major presidiati) · **#49 D5** employee timeline · serie B (#36/#37/#38). (Proposte Cowork asse professione/i18n: IMPLEMENTATE e riconciliate S1027 — asse professione 2121 nodi bilingue live, gate coverage 0 gap.) **(#68 P2 · #24 F4 · #34 B3 · #46 D1 · #51 E1 · #55 F1 · #42 C4 · #61 G2 tutti CHIUSI S1026 · Serie-A #25-#33 CHIUSA S1016/S1018/S1020 · D-74 CHIUSO S1025 · forense 4-fasi COMPLETO S1024/S1025)**

> ℹ️ **Doc note**: `CLAUDE.md` + `README.md` allineati a **v1.0.0 GA** (S958, 2026-06-02 — D-01 risolto). I conteggi headline nei file di progetto sono snapshot di milestone; la verità viva resta questo SOT_STATE. Vedi `DEBT_REGISTER.md` D-01 (risolto).


### Delta S1046 (2026-08-06) — la chiusura smette di agire nel dubbio, e tiene un diario

- **Dottrina del dubbio, una sola per la catena** (commit `f8928e35`). Con il marcatore
  `.session-align.marker` assente (consumato da `align-clones.sh:170`, ricreato *create-if-absent*
  solo al boot successivo di `session-boot.ps1`), i due script rispondevano all'opposto sulla stessa
  informazione mancante: `align-clones.sh` cadeva nel ramo `else` di `--auto-deploy` e faceva
  `DEPLOY=1` («conservative: deploy») — deploy in PROD **anche senza un commit su
  `DEPLOY_PATHS_RE`**, cioè proprio quando la protezione su cui contavamo smetteva di applicarsi —
  mentre `close-propagate.sh` teneva `need_clone=0` e stampava *«no db/migrations|seeds change this
  session»*, un fatto che in quel ramo non può conoscere. Ora: azione a costo basso ⟹ si esegue;
  azione cara o irreversibile (deploy PROD, clone DB) ⟹ **non si esegue, si dichiara `IGNOTO`**, e
  si stampa come forzare. `--deploy` esplicito resta incondizionato.
- **Prova falsificabile** — stesso blocco estratto *as-shipped* da `HEAD~1` e da `HEAD`, valutato
  sugli stessi input: marcatore assente dava `DEPLOY=1 (nessuna ragione registrata)`, ora dà
  `DEPLOY=0 IGNOTO: nessun delta affidabile`; finestra reale che tocca `scripts/` dà
  `DEPLOY=1 misurato`. Custodito da 11 test nuovi in `scripts/test/run-shell-tests.sh` (**115 ok,
  0 failed**).
- **`scripts/close-log.sh`** — rendiconto append-only in `.handoff/close-log.ndjson` (per-macchina,
  gitignored), scritto **dagli script stessi**, quindi non dipende dalla memoria del modello.
  È **rendiconto, non stato**: nessuna decisione lo legge. La distinzione è deliberata — l'analisi
  d'origine proponeva un registro `propagated_head` da cui far dipendere le decisioni, che avrebbe
  duplicato un fatto già misurabile (`ssh <host> git rev-parse HEAD`), contro la regola SoT di
  `CLAUDE.md`, e si sarebbe auto-avvelenato se versionato (`align-clones.sh:57` rifiuta HEAD avanti
  a origin).
- **Skill `handoff` v5.1+** — coperti i casi *nothing to commit* / *nothing to push* (Step 4), prima
  non nominati da nessuna riga; Step 5 impone che un passo saltato sia visibile quanto uno eseguito.
- **Due item di sicurezza recuperati dall'invisibilità**: `Z-261` (7 fattori MFA `e2e-fixture`
  ancora attivi in produzione, repo pubblico) e `Z-262` erano scritti come `### Z-NNN`, formato che
  `parse_register_items` **non** riconosce (vuole `- **#id …** · status:`) — invisibili al menu da
  S1032. Riformattati `#146`/`#147`; menu da 35 a 38 voci. Variante del difetto di S1045 (là la
  sezione sbagliata, qui il formato del blocco).
- **Bug latente nel test harness**: la sezione N di `run-shell-tests.sh` riusava `T`, il tempdir
  globale di riga 39, così il `trap 'rm -rf "$T"' EXIT` eseguiva `rm -rf Bash` **relativo alla root
  del repo** e il vero tempdir non veniva mai ripulito. Invisibile finché nessun test *dopo* quella
  sezione usava `$T`. Rinominato `TOOLN`.
- **Rinviata di proposito** la riscrittura della chiusura in quattro verbi (`registra · verifica ·
  pubblica · propaga`): la statistica che la giustificava mescola ritocchi a un minuto (`S954`) e
  riprese a diciotto ore (`S1041`, `S1045`), quindi la dimensione del problema è ignota. Si decide
  dal rendiconto, dal 2026-08-20 → `#148`. Referto adversarial (7 rilievi) nel design-lab.
- Migrazioni **280 file, max `000282`** (S1047: `000274` — la sentinella dell'organigramma ignora
  le unità ritirate, `#151`; `000275` purga dei 32 fattori MFA residui · `000276` ridà gli stili alle versioni di grafo (`#153`), `#152`, `@migrate: once`,
  eseguita col ripristino pronto e **collaudato** in `.secrets/`; S1048: `000277` riallinea percorsi
  di carriera e obiettivi alla ricostruzione dell'organigramma (`#155`), `@migrate: once`, con
  **rollback integrale versionato** — `staging.storia36_155_rollback()`, collaudato per impronta
  md5 riga-per-riga prima di applicare · `000278` riaggancia gli 8 ruoli critici legacy alle
  posizioni reali (`#160`, aggancio deciso da Enzo; allinea le TRE tabelle che dichiarano la
  criticità, con `staging.storia36_160_rollback()`) · `000279` marca la provenienza delle righe di
  rilevanza create dalla 000278, così la sentinella dell'import torna a vedere solo l'import); tag `v1.0.0`;
  API test file 232. Counts ri-derivati live in S1048: **163 utenti · 314 posizioni · 43 OU
  attive · 158 fattori MFA · 275 migrazioni applicate** (= 275 file `.sql` sul disco);
  percorsi di carriera **231**, obiettivi di carriera **253**, entrambi a **zero** righe su
  posizioni non attive.
- **Fattori MFA, inventario reale post-purga**: **158 TOTP `derived-access`** e nient'altro. Le 32
  righe senza etichetta (26 TOTP mai verificati + 6 WebAuthn verificati su `admin@heuresys.com`)
  sono state rimosse; nessun utente è rimasto senza secondo fattore verificato (misurato).

### Delta S1045 (2026-08-05) — perimetri verdi, cancello per-suite, menu coerente col permesso

- **#115 CHIUSO**: suite intera **1620/1620 verdi** su 232 file (commit `93b9bbfc`). I 24 file di
  perimetro derivano gli attori dall'albero delle **unità organizzative**, indipendente da quello
  che il resolver percorre. Tre run completi: il 1° inquinato da una suite concorrente (→ `#138`),
  il 2° ha scoperto due famiglie di rossi fuori dall'inventario iniziale.
- **Menu e permessi**: `dashboard` era l'**unica** voce riservata su 38 a non dichiarare la coppia
  risorsa+azione che la governa, quindi il filtro ricadeva su un flag generico e offriva il
  cruscotto a chi la pagina nega (mig **000271**). Il test E2E che l'ha scoperto **non** è stato
  adeguato: misurava una proprietà vera.
- **`BRANCH_MANAGER`** (mig **000272**, 14° ruolo): chi regge una **filiale** ha il cruscotto, chi
  guida una **squadra** no — decisione di Enzo. I due gruppi condividono gli stessi tre ruoli
  (`TEAM_LEADER`+`TEAM_MEMBER`+`USER`), quindi il ruolo non li distingue: solo il fatto di reggere
  un'unità `BRANCH`. Assegnato per derivazione, mai per elenco. Esito: 10 capi filiale, 0 capi
  squadra.
- **Pulizia cataloghi** (mig **000273**): 232 righe agganciate a 30 posizioni disattivate —
  nessuna con titolare, nessuna con una viva dello stesso codice, titoli **inglesi** del vecchio
  organigramma contro i titoli **italiani** delle 161 vive, e universo vivo già coperto (157/161).
  Archiviate in `audit.position_requirements_stale_archive` con la riga intera in jsonb:
  re-inseribili. La guardia fallisce se le posizioni vive con requisiti scendono sotto 157.
- **Cancello di verifica**: freschezza **per suite** invece di un'impronta unica. Toccare `apps/web`
  non ricompra più i ~38 minuti di `test-api`, e `git commit` non scade nulla. Corretti anche due
  buchi propri: `git()` perdeva l'output su Windows (cp1252) uccidendo il verdetto **dopo** aver
  eseguito le suite, e `handoff-lint` non era instradato da `db/migrations/` pur verificandone il
  conteggio.
- **`#140` CHIUSO (S1045, 2026-08-06)** — era: «la catena di migrazioni non è stabile sotto
  ri-applicazione». **Causa radice**: `000096` distribuiva i requisiti di competenza a *ogni*
  posizione del ruolo, comprese le **disattivate** (52 righe a ogni giro), e `000273` le
  ri-archiviava — i timestamp dell'archivio lo mostrano (232 + 52 + 52 + 52 = 388). Corretta a
  monte. **Prova**: conteggio esatto delle **227 tabelle** di `sys`+`audit` prima e dopo un giro
  completo → **nessuna differenza** (618.497 = 618.497), più le impronte del contenuto di cinque
  tabelle chiave identiche. Riverificato **dopo un deploy reale**. `#141` chiuso di conseguenza
  (`HS-PROD` = `DIVISION`, unità attive di tipo `TEAM` = 0, misurato dopo il giro di catena).
- **Migrazioni una-tantum (S1045)**: un file può dichiarare `-- @migrate: once` e viene saltato solo
  se **anche l'impronta** corrisponde al registro; senza dichiarazione **nulla cambia**, perché
  **166 dei 271 file** portano post-condizioni che verificano invarianti a ogni deploy — il 61%
  della catena non trasforma il database, lo **controlla**. `MIGRATE_FORCE_ALL=1` riapplica tutto,
  `MIGRATE_DRY_RUN=1` ispeziona senza toccare. `migrate.ps1` allineato allo stesso contratto.
- **Revocare un permesso a `TENANT_ADMIN`** non si fa con una `DELETE` a valle: la `000210`
  riconcede l'allowlist a ogni giro, quindi si toglie il codice dalla sua `VALUES`. Rilevante per
  `#131` (Tenant Builder P1), istruzioni nella testata della `000210`.
- **Direzioni registrate da Enzo**: `#142` cruscotti focalizzati per tipologia di utilizzatore
  (Azienda/Processi/Organizzazione/Filiale/HR/Platform/Tenant/Self-Service, ciascuno con requisiti
  propri incluso il divieto e la granularità) e `#143` la squadra come **progetto**, con il capo
  progetto che ha autorità sullo scopo e non sulle persone.

## Delta S1045 (2026-08-05 → 06) — la catena smette di disfare; i debiti da cinque a uno

HEAD **`4339a3d6`** (7 commit, tutti pushati e deployati). Counts **ri-derivati live**:
**272 file migration** `000001..000274` = **271 applicate** · utenti **163** · posizioni **314** ·
OU **45** · tenant ACTIVE **2** · ruoli **14** · permessi **211** · mapping RBAC **957** ·
skill **14.039**.

**Debiti: 5 aperti → 1.** `D-79` (il budget del cancello CI non attraversava l'SSH: provato live
sulla VM, `2100` arriva, controllo negativo esce 1) · `D-78` (il controllo del clone dichiarava
FATAL su un clone integro — causa: `Environment=PGOPTIONS=-c lock_timeout=30s` **senza virgolette**
nell'unit, e in `Environment=` lo spazio separa due assegnazioni: il server rifiutava ogni
connessione locale; falliva la *misura*, mai il dato) · `D-80` (era già chiuso in codice, mancava
solo nel registro) · `D-81` ex-`D-72` (due denominatori per una sola grandezza: 51 skill su 62
cambiano posizione, 0 fuori dal rapporto atteso). Resta **`D-56`**, che aspetta una decisione
d'ambiente. Corretto anche un identificativo usato **due volte** su debiti diversi.

**Sicurezza**: 7 avvisi pubblicati il 3 agosto, tutti chiusi alzando cinque minimi
(`brace-expansion` ×2, `fast-uri`, `ip-address`, `hono`). Le linee restano **separate per major** —
unificarle rompe ESLint, già misurato in S1038.

**Difetto di presidio**: quattro item `ACTIVE` (`#140`-`#143`) erano **invisibili al menu** perché
scritti fuori dalla sezione che `build_menu.py` legge, e `handoff-lint` non aveva un controllo che
guardasse *dove* vive un item. Rimessi nel registro (21 → 25 ACTIVE) e aggiunto il check **S3**,
bloccante, che usa lo stesso predicato di sezione del generatore.

## Delta S1044 (2026-08-04) — le 13 consegne del lab: l'atterraggio si deriva, la maschera esiste, dieci responsabili diventano quadri

HEAD **`21b1ebc6`** (S1044). Counts ri-derivati live: **163 utenti** (162 attivi) · 161 posizioni attive · 43 OU attive · **26 team** · 2 tenant ACTIVE · RBAC **14 ruoli / 211 permessi / 957 mappature** · **272 file migration** `000001..000274`.

Piano di ciclo: `docs/superpowers/specs/2026-08-04-consegne-lab-13.md` (13 righe, stato per riga).

**#116 — l'atterraggio dopo il login si deriva dal permesso** (`0f619e85`). `landingForPermissions`
in `@heuresys/shared` sostituisce `landingForRoles`: si va al cruscotto se si ha `dashboard:view`,
non se si ha un ruolo fuori da un insieme cablato. Misurato: **6 ruoli su 13** hanno il permesso, e
28 persone su 45 atterravano su una pagina negata (27 erano i responsabili di unità a cui `#113`
aveva appena dato `TEAM_LEADER`). La risposta di login porta ora `permissions` risolti dalla cache
RBAC. **D-68 riletto**: la sua inversione aveva chiuso una caduta e aperto quella speculare.

**#124 — lo stato `mask` esiste** (`f558083e`, **ADR-0032**). Il sistema non è più binario:
`apps/api/src/lib/scope/mask.ts` toglie il campo e lo dichiara in `masked`, applicato a
`COMPENSATION` su `/v1/compensation/recommendations`. **I20 emendato**: `PLATFORM_ADMIN` è un
mandato *tecnico*, non HR — non apre più pay e valutazioni; `TENANT_ADMIN` e `HRMS_MANAGER`
restano. **Voce non chiusa** (INTERRUPTED): resta `EVALUATION` (9 risorse), gli altri endpoint
compensation, gli aggregati e lo strato 1 `IDENTITY_PRO`/`IDENTITY_PRIV`.

**#118 + #120 — dieci responsabili passano a `QD3` e la retribuzione segue** (mig **000264**).
Livello `QD3` (unico livello quadri con coorte reale in questa banca), fascia `MG-2` sulle **otto**
posizioni di comando, retribuzioni collocate per anzianità e valutazione. Totale **592.986 →
729.900 €/anno, +136.914 (+23%)**. I due aumenti fuori scala — `roberta.caputo` +52% e
`martina.sala` +45%, entrambe sotto il pavimento di 65.000 — sono stati **mostrati a Enzo e da lui
approvati prima dell'applicazione**. Esiti: `X2a` 0/119, `X2b` 0/112, `X3a` **0/157**, mediana
`QD3` 73.985 > mediana `3A4L` 62.897.

**Decisione registrata (L5 di #118) — `martina.gentile` resta dov'è.** È Risk Manager dentro
`DIR-RISKM` sotto `alice.esposito` e **non dirige alcuna unità**. Nessuna unità è oggi senza
responsabile, quindi assegnarle un incarico significherebbe crearne una: la decisione di Enzo
(2026-08-04) è di **lasciarla nella posizione attuale**, e non è una dimenticanza.

**Reperto strutturale nuovo (non un difetto retributivo)**: `alice.costa` (Direzione Infrastrutture)
e `pietro.gallo` (Direzione Sviluppo Software) **reggono una direzione senza occupare una posizione
di comando** — sono su `POS-00000300 System Administrator` e `POS-00000324 Software Developer`. La
fascia `MG-2` NON è stata agganciata a quelle due posizioni, perché la fascia descrive la posizione
e non chi la occupa (principio già dichiarato dalla `000262`): agganciarla direbbe che «System
Administrator» è un mestiere manageriale. Le loro retribuzioni nuove cadono comunque dentro `IT-2`,
quindi `X3a` si chiude lo stesso. Il buco è dell'organigramma e si corregge creando le due
posizioni di comando.

## Delta S1041 (2026-08-02 → 03) — batch P2+P3 eseguito: dieci voci chiuse

HEAD **`421d1b32`** (13 commit di lavoro + chiusura). Counts **ri-derivati live**: **241 file
migration** `000001..000263` = **261 applicate** · utenti **163** · posizioni **181** · 2 tenant
ACTIVE · RBAC **13 ruoli / 207 permessi / 923 mappature** (+1 `leads:update`) · tabelle `sys.*`
**208** (+1 `sys_advisor_suggestions`) · voci sidebar **61**, di cui **52 attive** (+3: VRIO,
salute organizzativa, consigli operativi, richieste di contatto) · file di test API **228** ·
spec E2E **96** · test unit **10** (+1).

**Delta S1042** (2026-08-03/04, esecuzione della consegna dalla sessione lab): **241 file migration**
`000001..000243` = **241 applicate** (le 10 nuove: `000234` rimozione validazione presenze ·
`000235` purga della contaminazione da tenant legacy · `000236` indici, vincolo validato e
statistiche · `000237` dedup dei codici percorso + sigillo `UNIQUE` · `000238` traduzioni KPI
orfane · `000239` dedup dei codici competenza + terzo sigillo · `000240` rimozione di 3 righe
di prova lasciate in una tabella terminale · `000241` rimozione del catalogo formativo
alimentare/energetico + 10 percorsi con slug di tenant inesistente nel codice `PATH-` ·
`000242` dichiarazione dell'industry dei due tenant, ancorata ad ATECO ·
`000243` rimozione dei 2 KPI di industry estranea dal catalogo globale). Conteggi che **cambiano per
effetto della bonifica**: percorsi formativi **72** (erano 4.638), moduli **92** (erano 1.032),
obiettivi **2.189** (erano 2.624), skill **14.039** (erano 14.041), KPI **199** (erano 243).
Utenti, posizioni, tenant e RBAC invariati, e **assegnazioni formative 3.061 invariate**: la
bonifica non ha tolto un corso a nessuno.

**Invariante nuovo — I21** (CLAUDE.md): i dati che derivano dall'industry del tenant devono
esserle coerenti (**Heuresys = `MGMT_CONSULTING`/ATECO 70.20 · RTL Bank = `FIN_BANKING`/ATECO
64.19**, dichiarati in `sys_tenancies` da `000242`), mentre le **tassonomie e ontologie** restano
aperte a ogni industry — senza di esse non si creano più blueprint, tenant, strutture o processi.
Il test ha due domande in ordine: la tabella definisce una **classificazione** o dei **contenuti**?
Essere globale non dà diritto di cittadinanza a un contenuto. Tassonomie intatte a valle di tutta
la bonifica: **14.039 competenze ESCO** (incluse le 37 alimentari/energetiche) e **3.257
classificazioni ATECO**.

**Strumenti nuovi in `docs/kb/tools/`**: `db_health.py` (accende le 14 viste sentinella + 14 sonde,
cablato al boot di `session_start` e in `verify_gate` su `db/**`), `check_tenant_contamination.py`
(13 classi, guardia anti-regressione), `exposure_columns.py` e `dead_columns.py`.

**Migration della sessione**: `000224` VRIO · `000225` salute organizzativa · `000226` governance
timeline · `000227` ritiro del peso economico di posizione · `000228` traccia di audit
dell'advisor · `000229` i18n dei permessi timeline · `000230` voce consigli operativi · `000231`
trigger NOTIFY posta in arrivo · `000232` permesso e nav gestione lead · `000233` conservazione
GDPR dei lead.

**Voci chiuse** (dettaglio e prove per ognuna in `docs/superpowers/specs/2026-08-02-p2-batch-execution-plan.md`):
`#84` rules path-scoped · `#56` VRIO · `#57` salute organizzativa · `#87` isolamento del genitore
di unità · `#88` peso economico · `#58` advisor prescrittivo (A+B) · `#38` posta in arrivo in
tempo reale · `#4` GTM W4 · `#9/#10/#11` residuo audit · `#53` fasce retributive · `#45` editing
tenant e blueprint (chiude la serie C).

**Il filo conduttore della sessione**: cinque difetti su dieci non erano nel codice nuovo ma in
**dichiarazioni che il sistema non sosteneva**. L'informativa prometteva 24 mesi di conservazione
e nessun meccanismo li applicava; il "peso economico" era letto da un motore ed era NULL su tutte
le posizioni; una regola dell'advisor collegava due elenchi per nome e non poteva scattare mai;
le fasce retributive avevano il codice al posto del nome su 43 righe; un rilievo d'audit chiedeva
di potare 88 export sulla base di una premessa falsa. Nessuno di questi era visibile da un test
verde: sono emersi misurando ciò che il sistema **afferma** contro ciò che **fa**.

**Difetti trovati dalle guardie del progetto, non da me**: la guardia org-gate (D-51) ha fatto
**rifiutare l'avvio** all'API per una dichiarazione mancante su una rotta nuova; il lint ha colto
una scrittura su ref durante il render e una stringa non tradotta; il gate i18n ha intercettato
l'ultima lacuna EN. Tutte hanno dato fastidio nel momento giusto.

## Delta S1040 (2026-08-02) — batch P2: quattro voci chiuse (cicli organigramma · versioning+export viz · motore premi · storia della persona)

HEAD **`e4acd6d7`** (9 commit). Counts ri-derivati: **221 file migration** `000001..000223` (+3:
`000221` payload degli export, `000222` `sys_user_timeline_events`, `000223` permessi timeline) ·
**207** tabelle `sys.*` (+1) · RBAC **13 ruoli / 206 permessi / 922 mappature** (+2 permessi
`timeline:read` e `timeline:read:self`, +14 mappature) · utenti **163** · posizioni **181** · OU
**28** · team **26** · 2 tenant ACTIVE · skill **14.041** · file di test API **233** (+10) · spec
E2E **86** (+1). Dati nuovi: **2.683 fatti di timeline** su 161 persone · grafi di visualizzazione
**2** (era 1: creata la v2 dell'organigramma RTL) · export **7** (era 3, e i 4 nuovi hanno un
contenuto scaricabile davvero).

**#83 — l'organigramma non può più chiudersi ad anello** (`ffedde40`). `updateOuPartial` scriveva
il genitore senza controlli: la FK garantisce che il genitore *esista*, non che l'albero resti un
albero. Guardia ricorsiva nel repository (clausola `CYCLE` di PG16, così la risalita resta finita
anche su un albero già corrotto) + `409 OU_PARENT_CYCLE`. I test di ciclo sono girati **prima**
della correzione (4 rossi su 9, incluso quello che dimostra il ciclo scritto davvero sul DB).
LIVE su RTL: "Divisione Risk & Compliance" sotto la sua figlia → 409; 0 cicli preesistenti nel dato.

**#36 (B5) — versionamento dei grafi + motore di export** (`3dfbbc5f`, mig `000221`).
`graph_version` esisteva a schema ma nessun endpoint la incrementava; gli export erano righe che
rimandavano a `storage://…`, un archivio inesistente. Ora `POST /:id/versions` crea una copia
indipendente (gli id della copia si generano nell'applicazione: è l'unico modo di rimappare gli
archi senza dedurre la corrispondenza da un join su etichetta) e `GET /:id/download` serve il
documento vero (SVG/Mermaid/GENERIC_JSON/REACT_FLOW_JSON, nessuna dipendenza nuova). PNG e PDF
rifiutati con `409 EXPORT_FORMAT_NOT_RENDERABLE` invece di un file vuoto che sembrerebbe vero.
LIVE: v2 dell'organigramma RTL con 158 nodi / 157 archi / 158 posizioni copiati, 0 archi pendenti,
0 nodi condivisi con la v1; SVG 29.836 byte scaricato via HTTP.

**#37 (B2) — il motore fra KPI e retribuzione variabile** (`2a78c40c` + `de7b3002`). **Il dossier
di linea era stale**: dava `reward_gates`/`gate_results`/`payout_curves` a 0, il DB ne ha
**3283/3283/3**. Mancava il motore, non i dati. `reward-engine.ts` — funzioni pure per le curve
(LINEAR/CAPPED/STEPPED/SIGMOID) e per l'aggregazione dei cancelli; un blocco porta il fattore a
**zero**, non a una riduzione. `GET /v1/compensation/variable-pay/:id/evaluation` espone il
ragionamento accanto all'importo in archivio + pannello "Valuta" su `/compensation-intelligence`.
Prova falsificabile LIVE su Roberta Gallo / CONDUCT_GATE: ALLOW 0.55 → esito BLOCKED registrato →
BLOCK con fattore 0 → deroga motivata → 0.55 → ripristino identico.

**#49 (D5) — la storia della persona** (`66c12f64` + `daad5cad` + `37002011`, mig `000222`/`000223`).
4.641 fatti nel legacy `employee_timeline` → **2.683 importati su 161 persone**, dal 2005-09-13 al
2026-04-15 (le 1.958 non importate sono di dipendenti che in v5 non esistono — atteso, dottrina
D1/D2); registrato nel registry brownfield come **wave-2**; ri-eseguito senza duplicati. Modulo
`user-timeline` + `/v1/me/timeline`: la storia contiene variazioni retributive e valutazioni,
quindi passa dall'**asse organizzativo** (I18) col pubblico ristretto di `leave:read`, e ha il
pavimento self (I17). Un pannello per due superfici (`/users/[userId]` e `/me` → scheda Storia).
**NON è un event-store di dominio** — il commento sulla tabella lo dice esplicitamente perché
nessun modulo cominci a scriverci.

**Metodo — tre prove partite rosse, e due erano difetti della prova**: leggevano la pagina mentre
ancora caricava (pannello premi, timeline). La terza era informativa davvero: `occurredAt` esce in
ISO 8601 (millisecondi) mentre PostgreSQL tiene i microsecondi, quindi rimandare indietro
`lastEventAt` tale e quale come estremo superiore taglia 10 righe su 2664 — scritto nel test.

**Cancello di esposizione** (#79): 73/73 tabelle scritte dal programma sono lette da un modulo API,
0 lacune, verificato dopo ogni voce.

## Delta S1039 (2026-08-01) — adeguamento dell'ecosistema Claude a Opus 5 / Fable 5 + cancello di verifica

HEAD **`40f05555`** (7 commit da S1038). **Sessione di ecosistema, non di prodotto**: nessun file
sotto `apps/`, `packages/` o `db/` toccato. Counts **ri-derivati e IDENTICI a S1038**: 218 file
migration `000001..000220` · utenti **163** · posizioni **181** · RBAC **13 ruoli / 204 permessi /
908 mappature** · **206** tabelle `sys.*` (BASE TABLE) · voci sidebar attive **48** · file di test
API **223** · spec E2E **85**.

**`CLAUDE.md` di progetto: 38.393 → 20.990 caratteri**, 261 → ~160 righe (sotto la soglia di 200
raccomandata dalla documentazione Claude Code). Verificato uno per uno che i **17 invarianti non
negoziabili** (I1..I20, RD-08/09, ADR-0011) siano sopravvissuti **verbatim**, insieme alla
Definition of Done ADR-0026, alla OUTPUT RULE S1011, a "What NOT to touch", alle regole `NEVER` del
Design System e alla dottrina LIVE DATA E2E ONLY.

**Nasce `.claude/rules/`** (4 file, caricati solo sul percorso corrispondente): `api-module-pattern`
(`apps/api/**`, `packages/shared/**` — entry split, catena 13 plugin, pattern moduli in 7 passi),
`security-auth` (auth/middleware/plugins — Argon2id, JWT, refresh rotation, CSRF, 11 ruoli,
personas), `db-migrations` (`db/**`), `tests` (test e spec — Vitest singleThread, isolamento
transazionale D-52, wrapper Node 22 per Playwright D-36). Più la skill `full-alignment-deploy` per
la dottrina di allineamento cloni. **Verificato con `/context`**: all'avvio nessuna rule compare fra
i memory files → il frontmatter `paths` è onorato (se fosse malformato avrebbero aggiunto ~5k token).

**Tagliato perché derivabile o duplicato**: l'albero delle directory (lo mostra `ls`) e la sezione
"Required infrastructure at session start", che ridescriveva ciò che `scripts/session-boot.ps1` già
verifica e stampa a ogni avvio (tunnel, pgpass, DB, branch, dirty, unpushed, lint).

**Cancello di verifica — `docs/kb/tools/verify_gate.py` + hook `Stop`/`SubagentStop`** in
`.claude/settings.local.json`. Chiude un buco strutturale: i tool di verifica esistevano
(`zp_gate`, `zp_zero_check`, `zp_evidence`, `ci-gate.sh`, 10 workflow CI) ma **nessun hook li
chiamava** — erano un'istruzione, non un meccanismo. Principio: il verdetto è funzione dello stato
osservabile, mai della conversazione — `input_hash = sha256(HEAD + git status --porcelain + git diff
HEAD)`; se lo stato cambia il verdetto scade da solo, e rieseguire è idempotente. Sottocomandi
`route` / `run` / `check` (`--hook` emette il JSON di blocco). Routing dal `git diff` che **riusa i
trigger dei workflow CI** perché cancello locale e CI non divergano: `apps/api/`+`packages/shared/`
→ typecheck+test-api · `apps/web/`+`apps/showcase/` → typecheck+lint · `db/migrations/` → migrate +
ri-esecuzione per idempotenza · `scripts/` → shell-tests · `docs/kb/SOT_`/`DEBT_`/`.handoff/` +
`handoff_lint.py` → handoff-lint. Playwright **non** instradato automaticamente (costa minuti): si
chiede con `run --with-e2e`; la prova live resta obbligatoria per chiudere un work-item, ma non è il
cancello di fine turno. Il `check` è istantaneo (confronta hash, non esegue le suite); per
`stop_hook_active` blocca **una volta per turno** — un ciclo di correzione forzato in sessione,
mentre i cicli multipli restano appannaggio del driver esterno. Freno: `.zp/verify-off`. Provato sul
campo su tutte e quattro le vie: routing, verdetto scaduto, freno, ritorno a verde.

**Regola di convivenza Cowork/CLI scritta dove mancava** (§Source of Truth): l'unico writer e
committer di `docs/kb/` è Claude Code CLI; Cowork e Desktop sono read-only e propongono con un
append a `docs/kb/COWORK_INBOX.md`; `cowork_code_exchange/` e `cowork_reserved/` restano archivio
read-only. Il vincolo esisteva **solo** nelle preferenze globali di claude.ai — verificato che il
`CLAUDE.md` non l'avesse mai contenuto, nemmeno prima del refactor.

**Correzione a `scripts/align-claude-ecosystem.sh`**: `PORTABLE_PATHS` non conteneva `reference/`,
la directory nuova a cui il `CLAUDE.md` globale rimanda. Propagare senza la correzione avrebbe messo
sui cloni un file che punta a percorsi inesistenti. Aggiunte `reference` e `rules` al catalogo e al
wipe gestito, più un preflight che fallisce forte.

**Fuori da questo repo, per contesto**: `~/.claude/CLAUDE.md` 29.950 → 8.953 caratteri; plugin
`human-resources-plus` disabilitato (48 skill + 6 agenti mai usati in 50 sessioni); `effortLevel`
`xhigh` → `high` (default Opus 5); nuova regola **R24** (pianificazione, simulazione, chiusura) con
guard-rail negli script di bootstrap che ne verifica la presenza a ogni avvio su tutte e tre le
macchine. Misura `/context`: contesto residente in una sessione su questo progetto **−25.600 token**
(da ~47.500 a ~21.900). Piano completo e paracadute:
`claude_service_workspace/[Plans]/PIANO-adeguamento-ecosistema-claude-opus5-fable5_2026-08-01.md`.

**Nuovi item**: **#84** (verificare che le rules si carichino quando servono) · **#85** WAIT-INPUT
(`AGENTS.md` ora divergente — rigenerazione di competenza del canale Codex) · **#86** WAIT-INPUT
(`claude login` su VM e linux-pc: lo smoke headless fallisce l'autenticazione su entrambi, stato
pre-esistente non causato dall'adeguamento; l'allineamento invece è riuscito ed è mantenuto).

## Delta S1038 (2026-08-01) — l'applicazione smette di essere solo consultabile: si governa dall'interfaccia

HEAD **`26535ec8`** (18 commit). Counts ri-derivati: **218 file migration** `000001..000220`
(gap 000035+000139) = 218 applicate · utenti **163** · posizioni **181** · OU **28** · team **26** ·
tenant ACTIVE **2** · RBAC **13 ruoli / 204 permessi / 908 mappature** · skill **14041** ·
206 tabelle `sys.*` · voci sidebar attive **48** (+2: `job-catalog`, `skill-taxonomy`) ·
file di test API **223** · spec E2E **85** (+10) ·
E2E su build di produzione **274 verdi / 0 rossi / 0 instabili** · parità i18n **2689 × 2**.

**#44 (C1) e #43 (C2) CHIUSI — la serie C dell'editing amministrativo copre tutto tranne #45.**
La piattaforma mostrava l'organizzazione ma non lasciava cambiarla: decine di endpoint di scrittura
esistevano da MVP-1 e **nessuna pagina li chiamava**. Ora si governano dal browser: persone
(anagrafica + assegnazione/revoca ruoli), posizioni (tutti i legami, scelti **per nome** da elenchi
veri), organigramma (creare, rinominare, spostare), e i cataloghi — mansioni, tassonomia
competenze, competenze con sinonimi e legami, KPI, formazione con percorsi e passi ordinati.

**Due pagine NUOVE**: `/job-catalog` (famiglie + ruoli professionali) e `/skill-taxonomy` (famiglie,
categorie, livelli di padronanza). Erano gli ultimi moduli con API complete e **zero interfaccia**.
Ognuna porta la sua migrazione (`000219`, `000220`) perché **il menù vive nel database**
(`sys_ui_interfaces`): una rotta senza la sua riga esiste e non la raggiunge nessuno. Entrambe le
prove E2E includono un **cancello di esposizione** che fallisce se la voce sparisce dal registro.

**Lo stesso difetto scoperto QUATTRO volte, sempre da un collaudo rosso**: elenchi troncati a 200
voci su cataloghi da 14.041 competenze, 1006 moduli, 137 ruoli, 77 famiglie — l'elemento appena
creato finiva oltre la prima pagina della tabella e non si riusciva più a correggerlo. Ora ogni
catalogo filtra **lato server** con i parametri che l'API già offriva.

**#82 CHIUSO — non era una prova instabile, era un difetto vero.** Falliva solo quando l'elemento
compariva: il contatore delle notifiche non lette esiste soltanto se ce ne sono. Contrasto misurato
da axe **2.22** (bianco su verde) e **3.49** (bianco su rosso) contro il 4.5 richiesto, sulle
varianti PIENE del badge di `@heuresys/ui`; e i token cambiano fra tema chiaro e scuro
(`--danger` #DC2626/#F87171, `--success` #16A34A/#22C55E), quindi **nessun riempimento era sicuro
in entrambi**. I 7 badge di stato passano a variante a contorno, usando i token semantici come
testo/bordo — come erano stati tarati in S982. Il referto axe ora salva **quali** nodi violano
(selettore + HTML + motivo), non solo quanti: era il motivo per cui #82 restava indiagnosticabile.
**Nota di sistema**: il progetto `chromium` della config prod **dipende** dai progetti a11y, quindi
un rosso lì impediva l'esecuzione di tutto il resto (80 test su 351 nella corsa che l'ha rivelato).

**Tre debiti chiusi misurando, non ereditando la narrativa scritta.**
- **D-76** (dev server web rotto): la causa NON era il file indicato — l'import incriminato sta in
  101 file. Era la condition di export **`development`**, nome *standard* che i bundler applicano da
  soli: `next dev` agganciava i sorgenti `.ts` invece del compilato, al contrario di quanto
  dichiaravano `next.config.js` e `tsup.config.ts`. Rinominata in **`heuresys-source`** (custom):
  il ramo sorgenti è ora **opt-in esplicito**. Per vitest la strada delle conditions è stata
  provata e **scartata** (sovrascrivere `ssr.resolve.conditions` rompe i pacchetti esterni: 175 file
  rossi) → alias esplicito. Prova falsificabile: bundle tsup costruito anche con `shared/dist`
  nascosto.
- **D-75** (allarme `brace-expansion`): premessa ormai falsa — le retroportazioni **1.1.17** e
  **2.1.3** erano uscite 3 giorni prima. Provato **eseguendo** il payload dell'advisory: 1.1.16 /
  2.1.2 / 5.0.7 crashano per OOM, le nuove reggono. Minimi degli override alzati alla prima
  versione provata sana. Era anche un **doppione di D-77**, ora rafforzato.
- **D-69** (residui brownfield): i numeri erano di un anno prima. `temp_sdbi` **non esiste più**
  (mig `000193`); il residuo `staging.wave1_*` è 18 tabelle **vuote** / 720 kB (non "~43 righe /
  ~13MB") ed è l'area di lavoro dichiarata di un motore che si conserva → **WON'T-DO** motivato,
  con le condizioni per riaprirlo scritte nella riga.

**Registro debiti: da 4 aperti a 1** (resta D-56, bug upstream del plugin claude-mem).

## Delta S1037 (2026-07-31) — la storia si chiude e impara ad andare avanti da sola, la scheda di una persona smette di essere un elenco di codici

HEAD **`f7de70aa`** (9 commit). Counts ri-derivati: **216 file migration** `000001..000218`
(gap 000035+000139) = 216 applicate · utenti **163** · posizioni **181** · OU **28** · team **26** ·
tenant ACTIVE **2** · RBAC **13 ruoli / 204 permessi / 908 mappature** · skill **14041** ·
moduli API **92** · file di test API **223** · spec E2E **75** · suite API **1524 verdi / 0 rossi** ·
E2E su build di produzione **93 verdi** · parità i18n **2428 × 2**.

**storia36 CHIUSO — 13 cluster su 13** (#77 e #80). Lo Step 12.6 aveva una precondizione dichiarata
dal piano («la skill codifica il procedimento ESERCITATO, non quello sperato») che **non era
soddisfatta**: il modo `avanzamento` non esisteva, falliva esplicito con exit 2. Prima il modo, poi
la skill.

- **`db/seeds/storia36/13_avanzamento.sql`** — finestra MOBILE, stesse regole e **stesse chiavi
  naturali** dei cluster di costruzione, così le batterie esistenti coprono da sole le righe nuove
  (anti-drift AP-01). Ciò che è DERIVATO non viene riscritto: si ri-esegue il seed che possiede la
  regola (01 per i saldi ferie, 07 per le approvazioni). **Prova reale**: presenze ferme al 24
  luglio → estese al 30 (624 presenze, 2 richieste di ferie, 1 rinnovo di abilitazione); terza corsa
  **0 righe**. Guardia provata nei due versi: `--window-end=oggi` → rifiutato, exit 3.
- **La custodia ha prodotto 4 rossi, tutti triagati**: 2 difetti miei (un episodio nuovo che
  reclamava giorni già decisi dalla costruzione; un giorno d'aula sorteggiato senza un corso dietro)
  · 1 lacuna vera dell'avanzamento — i check leggono la **frontiera della storia**, non l'orologio,
  quindi spostandola un'abilitazione obbligatoria è risultata scaduta e mai rinnovata, **non per un
  errore ma perché è passato il tempo** · 1 saldo derivato non riallineato.
- **Check nuovo C12c** — calendario denso e coerente, e festività **calcolate** (Pasqua,
  Meeus/Butcher, per gli anni che la lista scritta a mano non conosce) che devono **coincidere** con
  quelle scritte a mano sulla finestra di costruzione. 3 selftest, tutti scattati. Custodia **254
  selftest**.
- **Skill `storia36-custodia`** (`.claude/skills/`): solo ciò che ricorre — custodia, avanzamento,
  triage a tre esiti; la costruzione esclusa. Punta agli strumenti invece di duplicarli; ogni path
  citato verificato esistente. **Limite dichiarato**: non testata con subagenti sotto pressione.

**#81 CHIUSO — la scheda di una persona.** La nota del register diceva «gli endpoint esistono già»:
**misurato dal vivo, non era vero** — molti ignorano il filtro per utente e restituiscono l'intera
popolazione (competenze 14.036, esiti di valutazione 1.560, rischio-uscita 158). Nuovo
**`GET /v1/users/:userId/dossier`** (11 sezioni) che **riusa** le letture del portale personale
invece di riscriverle — una sola implementazione per dimensione. Cancello organizzativo ADR-0027: un
**pari** riceve **404**, non 403 (un 403 confermerebbe che quella persona esiste). 7 test col
negativo. Pagina ricomposta + i due difetti di Fase 4 su di essa chiusi (date formattate, importi in
valuta). **Migration `000218`**: il titolo mostrava `IT_ME_FFDEECF5-name`, residuo pre-D-52 sfuggito
alla bonifica della 000200 (altra famiglia di prefissi).

**Due difetti trovati sbattendoci contro.** (1) **`seed-test-admin.ts` era rimasto indietro rispetto
a Z-262**: scriveva la password grezza invece di quella derivata per-utente e, con
`TEST_ADMIN_RESET_PASSWORD=1`, **rompeva il login di tutte le personas** (misurato:
`verify-derived-login.mjs` → FALLITO; riparato con `db:provision-access --realign`, 159 credenziali).
Ora la derivazione è una sola implementazione, importata. (2) **L'orologio del PC era indietro di 10
ore e 21 minuti**: 14 test MFA rossi con una scadenza «precedente» alla creazione — l'applicazione
gira in locale, il database registra col proprio orologio. La VM aveva ragione (accertato con due
sorgenti indipendenti). Lasciati due presidi: controllo dello scarto in `status_dashboard.py` e
attività pianificata sul PC. **Il solo servizio Windows non basta — misurato**: iniettato uno scarto
di 6 minuti e atteso 3 minuti, non si è corretto; con l'attività pianificata, da −6 min a −1 s.

**Nuovo item #82**: la prova a11y su `/me/inbox` è passata, fallita e poi risultata flaky nella
stessa mezz'ora a codice invariato.

## Delta S1036 (2026-07-29/30) — l'audit di merito su tutte le tabelle, e le date che tornano a raccontare la verità

**Stato ri-derivato**: 206 tabelle `sys.*` · 163 utenti · 181 posizioni · 28 OU · 26 team · 2 tenant ACTIVE · RBAC 13 ruoli / 204 permessi / 908 mapping · 14.041 skill · 218 file migration (max `000220`) = 218 applicate · 6 viste strutturali a 0 violazioni · coverage i18n dati 0 gap · HEAD `598e8768` · tag `v1.0.0`.

**#80 storia36 C12 — Step 12.1→12.5b CHIUSI** (restano 12.6 gated + 12.7).

**Lo strumento.** `db/scripts/audit-storia36-semantic.py`: audit semantico **ri-eseguibile**, perimetro e applicabilità delle regole **derivati dal catalogo di sistema** (AP-03 — una tabella nuova entra da sola), 10 regole per tipo e ruolo della colonna, **604.098 righe coperte**. Esiti a tre vie, tutti espliciti: **riparato** · **spiegato** (`audit-storia36-explanations.txt`, classi con motivo *misurato*) · **dichiarato** (`audit-storia36-empty-tables.txt`, 10 tabelle vuote col perché). Verbale: `docs/kb/storia36/AUDIT_FINALE.md` — **263 aperti / 196 spiegati / 0 tabelle vuote non dichiarate**.

**Il difetto principale.** Otto tabelle registravano fatti distribuiti su **anni diversi** con un timestamp **identico** — il giorno del popolamento (misure KPI 2024 e 2025 «registrate» lo stesso 3 giugno; 1.560 esiti di valutazione tutti il 30 maggio; 902 giudizi di competenza idem; 178 richieste di straordinario su tre mesi tutte il 12 dicembre). È la firma del caricamento in blocco e **si vede nel prodotto** (ordinamenti per recente, «ultimo aggiornamento» uguale su tutto). Riparato in `db/seeds/storia36/12_registration_dates.sql`: la registrazione segue il fatto, ritardo plausibile per tipo d'atto, giorno lavorativo, deterministico dall'hash — **4.847 righe**, twice-run 0 su tre corse consecutive.

**Due difetti secondari emersi guardando le date**: tutti i **178 straordinari erano `PENDING`**, il più recente di sette mesi prima (coda mai lavorata) → 162 liquidati + 16 respinti dal manager gerarchico vero, mai dalla persona stessa; **114 percorsi di studio su 159 iniziavano a gennaio** → riportati a settembre/ottobre (solo il mese: anno, età e anzianità restano quelli verificati in #72).

**Due regressioni auto-inflitte, chiuse portando a coerenza il FATTO** (mai allargando il check): le lauree in sessione estiva rendevano **71 esperienze antecedenti alla laurea** (C5a) → si sceglie la sessione compatibile con la prima esperienza, e quando nessuna lo è **vince la biografia**; le lacune ridatate diventavano «mature» accendendo C4f su **302 azioni ancora proposte** → 307 prese in carico con responsabile e scadenza.

**Un difetto di MISURA scoperto dal selftest**: la completezza di C12a si fidava di `pg_stat_user_tables`, che per una tabella nuova dichiara zero righe — sarebbe stata **cieca proprio nel caso che conta**. Ora il conteggio è reale. Due check nuovi permanenti (**C12a** datazione + completezza del perimetro · **C12b** code di approvazione) con **3 selftest**.

**Rete di sicurezza (12.3)**: vitest **1.517 verdi / 0 falliti** · Playwright prod **254 verdi** · guardia attori **9/9**. Due prove che **fotografavano lo stato** riscritte come invarianti: `/me/approvals` pretendeva l'empty-state (il C7 l'ha popolato) e `surveys-m2` cercava la survey «Q4» per nome (il C8 ha rinnovato i cicli). I 2 file inizialmente rossi erano **saturazione del tunnel** — due suite lanciate in parallelo per errore, deadlock su `sys_auth_mfa_factors`; rilanciati 15/15.

**Dimostrazione (12.4)**: `apps/web/tests/e2e/storia36-demo.spec.ts` (gated `STORIA36_DEMO=1`, fuori dalla regressione), 9 schermate in `qa_artifacts/storia36/demo/`. **Ha trovato ciò che 1.771 prove non vedevano**: l'intestazione mostrava `roles[0]` e presentava `federica.marchetti` (TENANT_ADMIN, CEO) come «**Dipendente**» su ogni pagina, mentre il titolo della stessa pagina diceva «Amministratore tenant» → `apps/web/src/lib/role-precedence.ts` (fonte **unica**, usata anche da `SystemHealthLive` che teneva una lista parziale di 8 ruoli su 13) + regressione `role-badge.spec.ts` che **chiede all'API i ruoli veri** invece di fotografare un'etichetta. **Due falsi indizi evitati** guardando gli artefatti: «48,25 utenti» era l'animazione colta a un terzo (48,25/163 = 29,6%; 54,05/181 = 29,9%; 7,63/28 = 27% — il rapporto costante smaschera l'artefatto); la scheda persona era stata fotografata mentre diceva «Caricamento…».

**Sicurezza**: alert Dependabot ALTO al push (`brace-expansion`, DoS, **CVE-2026-14257**). Tentata prima la strada pulita — override unico `^5.0.8` — e **misurato che non regge**: `minimatch` 3.x in eslint esplode in `pathMatches`, lint rosso su ogni workspace. La soluzione vera non era nel lock: il manutentore ha retroportato il fix il **28-29 luglio** (**2.1.3**, **1.1.17**) e gli override per-major le prendono da soli. **Verificato nel codice installato**, non dedotto: tutte e tre le versioni definiscono `EXPANSION_MAX_LENGTH` citando la CVE. Alert #128 dismesso (`inaccurate`) con la prova nel commento → **alert aperti: 0**. Registrato **D-77** con la condizione di riapertura e l'istruzione di non ritentare l'override unificato.

**Produzione e presidio (12.5 / 12.5b)**: deploy VM **OK** verificato live (`/login` 200 · `/api/readyz` ready); al primo giro il **cancello CI** ha fermato il deploy per timeout a 900s (il commit toccava il lockfile → suite integration ripartita intera) → rilanciato con `CI_GATE_WAIT=2700`. Clone DB del gemello rigenerato (row-counts verificati vs VM) + `heuresys_ci` ricostruito (parità 262 tabelle / 6 estensioni); deploy linux-pc OK. **Presidio settimanale** (`deploy/systemd/heuresys-advanced-storia36-custodia.{service,timer}`, lunedì 04:30, `OnFailure`) installato e `enabled` su **VM e linux-pc**, e **provato nei due versi**: esecuzione reale → `custodia VERDE` in 11s; **caso negativo eseguito** (finestra impossibile `--window-end=2023-01-01`) → il registro degli allarmi, **vuoto prima**, contiene la riga con unità/esito/orario e il journal `err` la riporta.

**Nuovo item #81** (P1): `/users/[id]` mostra **solo anagrafica tecnica** (identificativi UUID grezzi, lingua, fuso, data in formato macchina) e nulla dei 36 mesi — i dati ci sono tutti, è la scheda che non li compone; include due difetti già censiti in Fase 4 su una pagina di vetrina. **Regola nuova di Enzo**: nel deploy la **VM va per prima e fino in fondo**, poi il linux-pc (memoria `feedback_full_alignment_doctrine`).

## Delta S1035 (2026-07-29) — la storia arriva in fondo, e il database torna ricostruibile

HEAD **`d8a45832`** (10 commit; CI verde 5/5 e deploy verificato su VM + linux-pc). **Una migration nuova**: `000217` (vocabolari chiusi sulle tre
colonne «orizzonte» della successione — RD-08). Counts: **215 file migration** `000001..000217`
(gap 000035+000139) · utenti ACTIVE **162** · posizioni **181** · RBAC **13 ruoli / 204 permessi /
908 mappature** · skill **14041** · tenant ACTIVE **2** · moduli API **92** (+`user-target-positions`
+`organization-unit-history`) · file di test API **222** · suite **1517 test verdi**.

**storia36: chiusi la coda C5 e sei cluster** (C6→C11), ~13.000 righe di storia nuove sopra le
~285.000 dei cluster precedenti; **196 tabelle su 206 non vuote**. La batteria `verify-storia36.sql`
conta ora **119 selftest** (erano 46) e resta interamente verde.

- **coda C5** (#78): tre registri di criticità con **zero** posizioni in comune → 8·8·8 concordi ·
  22 successori su 49 senza criterio → 0 · 35 passi di carriera senza origine né destinazione → tutti
  collegati · giorno più frequente delle date d'inizio dal **33% al 7,5%**, mese dal **41% al 12,2%** ·
  9 datori su 9 con più settori → 0 · mobilità interna da 5 a **34 persone** in 36 mesi.
- **C6 riorganizzazione 2025-03**: 6 eventi datati + blueprint con 7 deroghe. Disegno **dedotto da
  tre tracce** nell'organigramma di oggi (due divisioni omonime su «Compliance», un vertice IT più
  leggero di ciò che governa, Operations senza informatica). **Presente invariato verificato**: 0
  unità modificate.
- **C7 approvazioni**: 642 richieste + 751 passi agganciati a fatti già registrati; le ferie ricalcano
  l'assenza vera (approvatore e data presi da lì). 12 pratiche restano **aperte**.
- **C8 engagement**: 6 cicli nuovi, 4.496 risposte, e **la curva del clima registra la
  riorganizzazione** — 7,71 prima, **6,80** il mese dopo, 7,61 a distanza.
- **C9 contenuti**: da **0 documenti pubblicati** a 10, con 24 revisioni **datate sui fatti** (le
  policy organizzative riviste il 2025-03-17, le normative sulla loro norma).
- **C10 coda sensibile**: 641 scelte sui trattamenti facoltativi (l'invariante non è «tutti hanno
  acconsentito» ma «ognuno ha espresso una scelta»), 4 richieste dell'interessato chiuse nei termini,
  2 segnalazioni chiuse dal **custode vero**, 5.633 accessi storici.
- **C11 configurazione**: organigramma con disposizione salvata (158 nodi tipizzati e posizionati
  dall'albero vero) + pipeline di acquisizione **auto-referenziale** — 12 corse che registrano i
  cluster di questo programma con le validazioni vere.

**Cancello di esposizione (#79) — 5 lacune vere trovate e colmate**: storia organizzativa
(`/v1/organization-unit-history`, append-only), registro GDPR che si scriveva e non si rileggeva
(`GET /v1/gdpr/requests`), istruttoria e fonti della pipeline
(`/v1/seed-candidate-records/:id/{validations,evidence}`), revisione degli obiettivi di carriera
(`/v1/user-target-positions`, 6 rotte). E una **tabella morta** in cui avevo scritto per sbaglio
(`sys_auth_sessions`, che il prodotto non usa): 71 righe rimosse invece di essere derogate.

**Il database era smesso di essere ricostruibile.** `pnpm db:validate` — che riapplica tutte le
migration e confronta lo schema — falliva da mesi, e nessuno lo sapeva: il difetto emerge solo
rieseguendo su un database **vivo**. Sei punti, tutti la stessa famiglia (verificavano uno stato
invece di una regola): il vocabolario delle notifiche ricreato con `DROP+ADD` in `000027`/`000132`
mentre `000141` lo aveva allargato · l'asserzione B-50 in `000076` che pretendeva NO_SOURCE su
quattro tabelle che il programma ha popolato · il conteggio dei successori in `000106` · l'EXCLUDE
sulle iniziative formative in `000200`. Ora: **215 migration applicate, viste a zero righe,
twice-run idempotency proven**.

**Undici test riscritti sull'invariante** (stessa classe): sei conteggi di riconciliazione, l'UUID
di una rilevazione scritto a mano nel portale ESS, il registro consensi che pretendeva una persona
senza passato.

**Tre pezzi di piano NON eseguiti, con motivo scritto**: preferenze di notifica per tutti (una
preferenza è una scelta della persona) · cascata KPI sui processi (EXCLUDE riconfermato due volte) ·
crosswalk CP2021↔ISCO-08, dove la scorciatoia strutturale **mapperebbe il mestiere sbagliato su 4
grandi gruppi su 9** (artigiani↔agricoltori) — è una tavola ufficiale ISTAT, va importata.

**C12 iniziato**: batteria intera verde con i 119 selftest, dossier per-entità, `db:validate`
riportato a verde. Restano audit semantico sulle 206 tabelle, E2E, demo live e la coda
infrastrutturale.

## Delta S1034 (2026-07-28) — la storia RTL arriva alla carriera, e nasce il cancello di esposizione

HEAD **`b045872f`** (14 commit S1034 + handoff). **Nessuna migration nuova**: counts di schema invariati
(214 file, max `000216`; RBAC 13 ruoli / 204 perm / **908** map; skill **14041**; tenant ACTIVE **2**;
utenti ACTIVE **162**). Il delta è tutto nel **dato di storia** e in tre esposizioni nuove.

**storia36 C4 «formazione» — ripreso dal congelamento e chiuso in v2** (3.832 righe, twice-run 0). Il
seed ereditato non era eseguibile (puntava a `learning_module_name`, che non esiste) e copriva 5 dei
7 buchi censiti. Riscritto: **15 corsi d'aula** `BANK-CL-*` INSTRUCTOR_LED (il catalogo non aveva
nulla di erogabile in presenza), **62 edizioni mensili** con capienza dichiarata di 25 posti e aule
parallele, **1.180 tracce di frequenza** (una per ogni giornata TRAINING del C1 — erano 7 su 1.180),
**1.071 iscrizioni** che valorizzano per la prima volta `initiative_id` e `deadline` (vuote su 1.990
righe legacy), **1.254 corsi a distanza** di cui 794 coprono gli obblighi di CONTENUTO, **77 rinnovi**
di abilitazioni senza scoperture, ciclo lacuna→azione chiuso. **Review adversarial 3 lenti: FAIL×3**,
4 BLOCKER assorbiti — il fuso orario (99,6% delle lezioni chiuse dopo l'uscita timbrata: il server
gira a `Etc/UTC`, le timbrature sono `time` naive locali), l'obbligo di contenuto non presidiato
(146 attivi su 158 senza antiriciclaggio nel 2026), la finestra legata al mese corrente (la batteria
sarebbe diventata rossa da sola il 1° agosto), i rinnovi ottenuti DOPO la scadenza (75 catene su 78
con 1-11 giorni scoperti). Batteria estesa a **C4a-C4g** con 11 selftest.

**storia36 C4/sicurezza** (`04b_safety.sql`, 362 righe): la cornice D.Lgs 81/08 completa — 116
lavoratori attivi non avevano ALCUN record su un obbligo che l'art. 37 pone per ognuno. Cinque
platee **derivate** (lavoratori 158 · preposti 32 = chi ha riporti diretti · dirigenti 9 =
inquadramento · datore di lavoro 1 = vertice · squadre di emergenza 2+2 per sede via la nuova vista
`staging.storia36_sede_personale`, che mappa tutte e 158 le persone a MI-HQ/MI-OPS/MI-CEN). Cadenze
fissate dalla **norma** e non dalla mediana del dato (`storia36_cert_validity_di_legge`). Check
**C4h** su cinque predicati + 2 selftest.

**storia36 C5 «carriera»** (854 righe, twice-run 0): **255 esperienze precedenti** con le date
vincolate da nascita, fine studi e assunzione · **145 obiettivi di carriera** · **24 candidati** per
le 8 posizioni critiche · **141 valutazioni di prontezza** · **289 variazioni di requisito** di
posizione. Tre riparazioni legacy: 156 titoli di studio senza data d'inizio + 50 date di
conseguimento impossibili ancorate all'età · percorsi di carriera ricostruiti per famiglia
professionale (177 posizioni collegate, erano 40) · 9 bacini di successione che erano gusci vuoti.
**Review adversarial 3 lenti: FAIL×3, 42 rilievi con 8 BLOCKER** — i critici assorbiti in v2: la
**direzione della carriera era invertita** (misuravo la verticalità dalla rarità della posizione e
il minimo di titolari è zero → 150 obiettivi su 150 su una scrivania vuota, 22 direttori di filiale
«aspiranti cassieri», il vertice con obiettivo approvato di diventare cassiere; ora la direzione
viene dalla profondità della catena `reports_to`, con nuovo **C5c(iv)**) e la **norma sorteggiata**
(240 righe su 289 citavano una fonte estranea alla competenza; ora deriva dalla materia). Riparato
anche il danno che la mia prima riparazione aveva fatto: cancellava le 40 righe dell'import senza
filtro di tenant, rompendo un test e lasciando incoerenti 92 piani di carriera — righe recuperate
dal dump pre-storia36, riparazione resa **additiva**. Batteria **C5a-C5e** + 5 selftest.

**⚠ CANCELLO DI ESPOSIZIONE — regola nuova di Enzo, vincolante e RETROATTIVA.** Un dato che nessuna
API espone non è nel prodotto. Strumento: **`docs/kb/tools/check_exposure.py`** — deriva dai seed le
tabelle scritte, dal sorgente dell'API quelle lette, incrocia coi conteggi live e **fallisce (exit 1)**
se ne resta una scoperta; deroghe solo in `exposure_waivers.txt` e solo con motivo. Prima misura:
**30 tabelle scritte, 27 esposte, 3 scoperte** → tre endpoint nuovi: `GET /v1/me/professional-experiences`
(+ scheda «Esperienze» in `/me/profile`), `GET /v1/positions/:id/skill-requirements/history`,
`GET /v1/compensation/payout-curves`. Tutti su permessi RBAC esistenti (nessuna migrazione). Ora **30/30**.

**Difetto sistemico corretto — date senza orario**: le colonne `date` (RD-09) tornano da `pg` come
`Date` a mezzanotte locale, e `toISOString()` le spostava al **giorno precedente** fuori da UTC. Era
in **19 punti di 9 moduli** (compensation, me, positions, organization-units, mentorship, engagement,
training-initiatives, analytics, export) e in uno era perfino documentato in un commento («ignoring
TZ drift»). Ora una sola funzione condivisa `src/lib/date-only.ts`; il test **impone il fuso a
runtime** e pretende che la lettura vecchia e la nuova divergano, così prova qualcosa anche in CI a UTC.

**Difetto di prodotto corretto — `/me/learning`**: quattro colonne su cinque erano vuote (la pagina
leggeva campi mai esistiti nella risposta). L'endpoint ora risolve il NOME di ciò che è assegnato
(`kind`/`title`/`enrolledAt`), la pagina importa il tipo da `@heuresys/shared` e l'E2E asserisce il
contenuto, non la sola visibilità.

**Codex — canale di audit in sola lettura**: identità DB **verificata** (`codex_auditor`: login sì,
superuser/CREATEDB/CREATEROLE/BYPASSRLS **no**; `default_transaction_read_only=on` a livello di
ruolo; timeout 30s/2s/60s; unico privilegio **SELECT** su `sys` 223 tabelle e `audit` 14).
`.codex/`, `.codex-review/` e `AGENTS.md` restano untracked per disegno e non sono di Claude.

Counts di dato ri-derivati 2026-07-28 (S1034): attendance **116.015** · evidenze formative **3.868**
· certificazioni **916** · iniziative formative **62** · esperienze professionali **255** · obiettivi
di carriera **145** · API test file **221**.

## Delta S1033 (2026-07-27) — la CI verde per diagnosi, Z-261 chiuso, gli attori per caratteristica, il mandato storia36

HEAD **`17ac7aca`** (6 commit S1033 + handoff). **CI: da 158 file rossi su 218 a TUTTI i gate verdi** — e la causa non era il codice. Diagnosi per riproduzione (i log dicevano `login <persona>: 400`, cioè «richiesta malformata», e mentivano): sul runner off-prod **`MFA_ENCRYPTION_KEY` non era la chiave con cui i 159 fattori `derived-access` sono cifrati** (hash divergente misurato) e **`DEV_ACCESS_MASTER_KEY_B64` non era mai stata aggiunta all'ambiente** (il fix di S1032 aveva preparato il codice, non il runner). Fix senza toccare `/etc/heuresys-runner.env` (root-only): env file dedicato `/etc/heuresys-runner-crypto.env` + drop-in systemd `zz-heuresys-crypto.conf` (il prefisso `zz-` vince sull'`override.conf` esistente che ricaricava il file principale). **Il sintomo mascherato è ora un difetto risolto**: `MfaCiphertextMalformedError`/`MfaEncryptionKeyMissingError` estendevano `ApiError` generico → l'errorHandler li declassava a **400**; nuova classe **`InternalError` (500)** con `publicMessage` separato dal log (il codice esce nella risposta, il dettaglio che nomina le env var resta nel log), 5 unit test nuovi rossi-prima-verdi-dopo. Sei test residui di Z-262 corretti (password di UN utente riusata per un altro in 2 file · precondizione «senza fattore» ora GARANTITA nel beforeAll · parity-label capovolta: lo skip di ri-cifratura NON deve coprire i fattori derivati).

**`Z-261` CHIUSO (ok esplicito di Enzo)**: i **7 fattori `e2e-fixture` coi segreti TOTP pubblicati su GitHub eliminati da tutti e tre i database** (PROD OCI · linux-pc `heuresys_advanced` · `heuresys_ci`), verificato 0 residui e nessun utente rimasto senza fattore verificato (tutti conservano il `derived-access`). **Playwright verde** dopo due cause proprie: `derive-access.mjs` usava `import.meta` → in CommonJS (transpile Playwright) è errore di **sintassi**, la suite non si caricava e il sintomo era «No tests found» (ora `findRepoRoot()` risale da cwd a `pnpm-workspace.yaml`; impronta della password derivata INVARIATA prima/dopo: `88b75e03db4ee491`); lato web restava la password unica `TEST_ADMIN_PASSWORD` → `passwordFor(email)` dal modulo condiviso, costante eliminata (era la trappola). Due difetti introdotti-e-corretti nella stessa verifica (parametro non risolto allo step-2 TOTP; re-export che non porta il simbolo nello scope del modulo) — trovati eseguendo sulla config della CI (build prod), non dal typecheck.

**Attori dei test per caratteristica** (`test/helpers/actors.ts` + guardia `actors-profile.integration.test.ts`, 9 test): userWithRole (con vincolo tenant) · managerAndReport (coppia reale dall'organigramma) · outsiderOf (I19) · actorWithoutMfaFactor (preparato, non cercato) · **functionalPeers** (I18: stesso team, ZERO legame gerarchico a qualunque profondità — 1553 coppie nel dato) · **hierarchyChain** (I20: tre livelli con esclusione del riporto diretto top→bottom, altrimenti non prova la transitività — 131 catene) · actorWithoutPosition. **Rettifica**: «135 utenti senza posizione» era un artefatto owner/incumbent (I1) — `sys_user_position_assignments` copre 162/162; l'unico senza è l'account di servizio. **Audit di completezza DBMS eseguito live** (catalogo + count reali, non stime): 206 tabelle · **37 vuote** · 16 quasi-vuote · 448.558 righe; coperture per-utente 96-99%; incoerenza misurata buste paga (→2026-06) vs presenze (→2025-12-08). Da qui il **mandato Enzo #77 — storia RTL su 36 mesi** (2023-08-01→2026-07-31, riorg 2025-03-01, crescita moderata, aree delicate a volumi bassi, dump preventivo, popolazione chiusa): piano 13 cluster in `docs/superpowers/plans/2026-07-27-rtl-storia-36-mesi.md`, stato in `.storia36/PROGRESS.md`, register #77. Debito nuovo **D-76** (`next dev` non risolve `./_pagination.js` da `packages/shared` — dev server web rotto, prod/CI ok).

Counts ri-derivati 2026-07-27 (S1033): migration **214** (max **000216**) · utenti **163** (ACTIVE 162) · RBAC **13** ruoli / **204** perm / **908** map · skill **14041** · tenant ACTIVE **2** · API test file **219** · fattori `e2e-fixture` **0**.

## Delta S1032 (2026-07-26) — prima corsa presidiata, il modello organizzativo, e un gate di conformità che non copre

HEAD **`79bb529c`** (12 commit S1032). **Accesso rifondato**: dalle 7 personas fisse a **158 utenti su 158** con identita', password e secondo fattore **derivati** da `.secrets/dev-access-master.key` (gitignored) — identita' **160**, credenziali correnti **160**, fattori TOTP **178**. Provisioning idempotente (`pnpm db:provision-access [--dry-run|--realign]`), consultazione `pnpm dev:whoami <email> [--watch]`, login reale verificato **con caso negativo** (derivata -> 200 + 3 cookie · errata -> 401). I segreti TOTP sono **usciti dal repository**: i due file `mfa-fixture-secrets.ts` derivano dal modulo condiviso `apps/api/scripts/derive-access.mjs`, e il parity test ora fallisce se qualcuno reintroduce un valore scritto. **Z-261 APERTO**: i **7** fattori `e2e-fixture` con i segreti pubblicati su GitHub sono **ancora attivi** (non eliminabili finche' `mfa-enroll-confirm` resta rosso). **Migrazioni 214** (max `000216`, +1 = colonna `gdpr_map_reference_kind` di Z-259, applicata; registro GDPR invariato a **54** righe dopo il rollback di Z-257). Counts invariati: moduli `src/modules` **90** · utenti ACTIVE **162** · posizioni **181** · OU **28** · team **26** · tenant ACTIVE **2**. Piano zero-pendenze: **262 cluster, 43 chiusi** (189 autonomi / 908h · 30 su Enzo).

**`Z-203` chiuso — e la prima corsa presidiata dell'impianto ha ripagato il suo costo.** L'accesso al peer in `semantic-matching` era deciso da una lista di ruoli locale che duplicava `canReadOrgTarget` e lo contraddiceva: per ADR-0027 F1 la managerialità è anche un fatto di DATO (`isOrgUnitManager`), non solo di ruolo. Rimossa dai tre metodi per-bersaglio; decisione in **ADR-0027 §F1-bis**. Ma il valore sta nella review: **tre revisori adversarial hanno demolito l'evidenza, non la modifica** — il test che chiudeva il criterio era una **tautologia** (atteso e osservato costruiti dalle stesse costanti), il fixture sceglieva l'attore col predicato sbagliato e senza `ORDER BY` (poteva passare *contro* il bug), due dei tre metodi corretti erano senza rete, e l'ADR presentava come «chiusura di un falso diniego» quello che è un **allargamento delimitato** (`matching:read` è l'unico permesso F3 concesso a `USER`). Tutto corretto e ri-verificato iniettando la regressione in ciascuno dei tre metodi, uno per volta.

**Modello organizzativo — decisione di Enzo recepita**: la SoT è **l'organigramma**, i ruoli RBAC si popolano da lì e non possono contraddirlo. I ruoli si partizionano in **organizzativi** (derivati dalle catene gerarchiche), **funzionali** (team e processi — `TEAM_LEADER`/`TEAM_MEMBER` inclusi) e **mandati conferiti**. Ricerca sul campo (CCNL credito e terziario, Circolare 285) per fissare chi è manager: **il vertice di una direzione o divisione**, non chi ha riporti — il responsabile della formazione dipende dal Direttore HR, guida impiegati, ed è un quadro direttivo. Programma in `docs/superpowers/specs/2026-07-26-organizational-model-and-role-derivation-design.md` (F0-F5).

**Divergenze misurate**: 11 responsabili di unità su 17 senza ruolo manageriale · 22 su 29 con riporti diretti idem · 1 `MANAGER` che non dirige nulla · `TEAM_LEADER` 5 lo sono senza il ruolo, 1 ha il ruolo senza esserlo · titolarità di processo sana a livello unità (23/23, zero doppioni) e **rumore a livello persona** (3 marcature `OWNER` su 120 sul legame giusto, 20 processi su 23 senza titolare). **Rettifica importante**: l'inquadramento contrattuale era stato dichiarato inesistente — **esiste ed è popolato** in `sys_user_contracts` (160/162 utenti attivi, `CCNL Credito 2024` per RTL Bank con Dirigente/QD4/QD3/3A1L-3A4L, `CCNL Commercio` per Heuresys). Ricerca fatta guardando solo la tabella principale e non i satelliti persona.

**Team di scoperta sul DBMS** (workflow 6 lenti + sintesi, 7 agenti): rilievo ALTA — **il gate di copertura GDPR non copre**. `gdpr.integration.test.ts` dichiara «LIVE fk graph — the SoT is `pg_constraint`, never a hardcoded list» e poi filtra `LIKE 'sys.sys_user\_%'`: le FK verso `sys_users` sono **248 su 135 tabelle**, ne guarda **74**, e 51 tabelle con dati personali veri (`sys_survey_responses` 3.792, `sys_goals` 1.067…) non sono mai confrontate col registro → `Z-257`. Secondo rilievo ALTA: l'**ambito tenant ha tre classi** (142 dirette · 34 transitive · 29 globali) e il codice ne conosce due → `Z-258`. Nuovo registro **`docs/kb/DATA_PATTERNS.md`**: pattern di dati riusabili con regola di manutenzione, prova obbligatoria e anti-pattern (AP-01 stesso fatto dichiarato due volte · AP-02 invariante tenuta dalla prosa · AP-03 predicato ancorato al nome invece che al grafo).

## Delta S1031 (2026-07-26) — i documenti dell'impianto zero-pendenze allineati a ciò che l'impianto fa

HEAD **`a6fe1584`** (3 commit S1031, di cui 2 di correzione documentale + questo handoff). **Nessuna modifica a runtime, schema o API**: counts invariati e ri-derivati oggi (sotto).

**Cosa è stato corretto, e perché conta.** La §9 del README della skill `zero-pending-loop` — riscritta da Enzo dopo la review di S1030 — è stata verificata contro la riconciliazione in `docs/kb/COWORK_INBOX.md` e la sezione `meta` di `zp.config.yaml`: il testo regge, con sette imprecisioni corrette rimisurando. I test automatici sono **15** e il documento ne dichiarava 14; il «pavimento di classe ha alzato 14 cluster» non dichiarava la base di misura (rispetto alla classificazione registrata prima del fix sono 14, ricalcolando il solo pavimento sono 13 — il quattordicesimo, `Z-250`, l'ha alzato la regola sul testo); «il tetto di spesa è reale» diceva più del provato (provata è la *contabilizzazione*: che il tetto fermi la corsa è uno dei 4 controlli che richiedono una sessione viva); le guardie del driver erano elencate come tre e mancava proprio il freno, l'unica oggi attiva.

**Il difetto di fondo non era aritmetico.** Cinque affermazioni promettevano garanzie inesistenti, e due stavano nel codice: **`zp_gate.py tipi` stampava una regola diversa da quella che applica** (raggruppava per «natura» dello strumento mentre il gate decide per livelli dal ridisegno S1030 — chi leggeva concludeva che `staticcheck` + altro chiudesse un cluster, mentre è rifiutato), e `zp_evidence` registrava `natura` nel blocco di evidenza (ora `livello`). Nei documenti: `SKILL.md` non conosceva il freno — il driver esce 3, ma il driver non è l'unica via d'ingresso, quindi una invocazione a mano l'avrebbe scavalcato e «l'impianto non parte» era falso (ora è precondizione della skill, con outcome `blocked`); `blast-radius.md` insegnava a dedurre la classe di rischio dai path dichiarati, cioè il metodo che aveva mandato in corsia non presidiata `Z-153`, che si chiude con un `curl` sul dominio pubblico; `close.md` diceva che il deploy in chiusura «va fatto», mentre in non presidiato è vietato da `HEURESYS_CLOSE_NODEPLOY=1`, veto applicato dentro `close-propagate.sh` dove vince sui flag.

**Design promosso di stato**: da «BOZZA — in attesa di approvazione» a **«IMPLEMENTATO — esercizio non presidiato NON autorizzato»**, con regola di precedenza esplicita (dove il documento diverge dal repo, vale il repo). Il riquadro V6, che è la premessa aritmetica della condizione di fine, è stato **rigenerato dai dati veri** invece che ritoccato: porta entrambe le misure datate (2026-07-25: 218 autonomi/~924h · 30 su Enzo/~446h → 2026-07-26: **183 autonomi/895h · 30 su Enzo/446,5h**) più il comando che le rimisura. Gli autonomi calano perché vengono chiusi, i bloccati su Enzo no.

**Metodo applicato a se stesso**: il primo comando che era stato scritto in `blast-radius.md` (`zp_state.py precondizioni`) non esisteva — scoperto eseguendolo prima di committarlo, e sostituito con i test 5a/5b del self-test, che quel comportamento lo mostrano davvero.

Counts ri-derivati 2026-07-26 (S1031), **invariati rispetto a S1030**: migration **213** (max **000215**) · moduli `src/modules` **90** · API test file **217** · utenti **163** · posizioni **181** · OU **28** · team **26** · tenant ACTIVE **2**. Piano zero-pendenze: **255 cluster, 42 chiusi, 213 aperti** (183 autonomi / 895h · 30 su Enzo / 446,5h).

## Delta S1030 (2026-07-25) — W1 avanzata, la coda Dependabot azzerata, e un fix di ieri che aveva spento i colori in produzione

HEAD **`7881c400`** (12 commit S1030) — **CI 7/7 verde** (typecheck · lint · build-web · test-integration · playwright-smoke · CodeQL · deploy showcase). **Zero PR aperte** (erano 8).

**Coda di sessione — impianto `zero-pending-loop` pubblicato con il freno inserito.** Una sessione Cowork ha consegnato skill + driver + 6 tool (21 file, nessun commit); rivisti qui prima del commit da due revisori ostili, ~25 rilievi verificati eseguendo. Corretti: il lock non era un lock (il driver che rinunciava cancellava quello attivo, e `kill` non lo fermava ma gli faceva mollare il lock — fermarlo era cio' che creava concorrenza); il rito di chiusura deployava la **produzione** fuori da ogni filtro di rischio (ora `HEURESYS_CLOSE_NODEPLOY=1` e' un veto imposto da `close-propagate.sh`); il tetto di spesa era inerte (stderr nel JSON + argv oltre 32k → costo 0); «zero pendenze» era dichiarabile con righe di piano non lette; CI «verde» con zero workflow valutati; API di produzione ferma vista come sana. `meta.autorizzato_non_presidiato: false` → il driver esce 3: versionato e ispezionabile, ma non parte. **I sei punti di ridisegno sono stati chiusi il 2026-07-26** (`8293553f`): la classe si deduce ora dall'azione del criterio di chiusura e non dalla descrizione — sette cluster che toccavano la produzione sono usciti dalla corsia non presidiata, 14 alzati e zero abbassati; le precondizioni di classe C si misurano; il tipo di prova e' confrontato col comando; il gate ragiona per livelli e ammette le coppie che la Definition of Done impone; il self-test rileva le 4 regressioni che prima non vedeva; lo stato si scrive in modo atomico. Il freno resta comunque inserito: toglierlo e' una decisione di Enzo. Il passo che manca e' una **prima corsa presidiata**, che chiude anche i 4 test eseguibili solo con una sessione viva.

**Rettifica su W0**: S1029 la dichiarava completa (11/11) e S1030 lo ha ereditato senza rimisurarlo — segnalato da una consegna Cowork e verificato: l'ondata conta **11 voci, 10 chiuse, 1 aperta** (`Z-034`, segreti TOTP). È classe D (produzione viva), quindi resta in attesa di autorizzazione esplicita di Enzo. Stessa lezione della giornata, applicata a se stessa: un'affermazione di stato ereditata non è una misura.

**W1 — 9 cluster chiusi**: `Z-022` (timer settimanale di refresh del DB clone su linux-pc, con i servizi che ripartono anche se il clone fallisce) · `Z-029` (6 plugin di authoring sempre accesi → spenti, 16→10) · `Z-030` (la doppia esecuzione di handoff_lint al boot **misurata**: 0.4 s, non è il collo di bottiglia → mantenuta e documentata invece che rimossa con una cache rischiosa) · `Z-125` (censimento copertura moduli/test) · `Z-156` (token di tema a fonte unica) · `Z-178` (le release 0.1.8/0.1.9 di `@heuresys/ui` esistevano solo in locale → pushate) · `Z-224`/`Z-225` (due tracker che descrivevano lavoro già fatto) · `Z-234` (i doc del design system descrivevano un repo che non esiste più).

**`Z-004` chiuso**: le 8 PR Dependabot assorbite in un batch unico — 3 major di GitHub Actions (checkout/setup-node v7, cache v6; precondizione verificata: entrambi i runner hanno node24 fra gli externals), 4 major runtime (@fastify/rate-limit 11.1.0, fastify-plugin 6.0.0, dotenv 17.4.2, + fastify 5.10.0 · pg 8.22.0 · argon2 0.45.1), 39 minor/patch. **Non preso** @types/node 26: i tipi devono seguire il major del runtime (Node 22) — `ignore` in `dependabot.yml` con motivazione e condizione di revisione.

**Rettifica di `Z-139` (S1029)**: la sua premessa era falsa — `text-destructive` **era** una utility valida (`--color-destructive` arriva da `@import "@heuresys/ui/styles"`). Il fix di ieri ha sostituito quelle classi con `text-danger`, che nello showcase **non aveva token**: sul sito pubblico le segnalazioni d'errore sono rimaste senza colore fino a oggi. Riparato alla radice (`apps/web/src/app/_theme-tokens.css`, fonte unica copiata dal sync) + gate CI che confronta le classi usate nell'HTML pubblicato con le regole nel CSS pubblicato.

**Review adversarial (3 revisori)** → correzioni applicate lo stesso giorno: `clone-vm-db.sh` usciva **0** con il restore fallito (ora legge entrambi i lati di `PIPESTATUS`, e il sanity check è un gate: `local=? vm=?` non è più «OK») · gli orari dei due timer erano sfasati di **2 ore** (VM in UTC, linux-pc in ora locale: il pull dei backup partiva un'ora *prima* che il dump esistesse) · il censimento copertura passava anche con 217 test **vuoti** (ora richiede un `it(` eseguibile, assegna ogni test al modulo con match più lungo, ignora i commenti, e ha un `--self-test` in CI).

Counts ri-derivati 2026-07-25 (S1030): migration **213** (max **000215**) · moduli `src/modules` **90** · API test file **217** · utenti **163** · posizioni **181** · OU **28** · team **26** · tenant ACTIVE **2** · RBAC **13** ruoli / **204** perm / **908** map · skill **14041**. Stack aggiornato: **fastify 5.10.0** · **pg 8.22.0** · **next 16.2.11** · **react 19.2.8** · **@playwright/test 1.62.0** · **vitest 4.1.10** · typescript 6.0.3 · zod 4.4.3 · `@heuresys/ui` ^0.1.9. Alert Dependabot aperti **1** (brace-expansion = D-75, rischio accettato).

## Delta S1027 (2026-07-22/23) — asse professione Cowork implementato + bilinguismo dati 100% + batch debiti

HEAD **`3596be42`** (14 commit S1029, 2026-07-25) — mandato Enzo: censire tutto il residuo verso «zero pendenze» ed eseguire in autonomia non presidiata con doppia verifica + review adversarial per task. **Censimento**: 14 agenti su 13 fonti → 497 voci → **248 cluster canonici** (dedup verificata: 497/497 mappati, 0 persi, 0 rilievi da 3 verificatori); piano `docs/superpowers/specs/2026-07-25-zero-pending-plan.md`, **~1.370h** totali di cui ~924h senza input di Enzo. **W0 (sblocco) CHIUSA** tranne alerting: **(a)** CI rossa da 2 giorni → verde (run **30143003990**): `pg_stat_statements` non preloaded sul runner (fix su 3 piani: runtime + script di setup + endpoint che DEGRADA con `extensionAvailable` loggato) e marker SDBI truncati dalla 000213 → **mig 000215** che li ri-deriva dai 1490 lineage; **(b) D-55 RISOLTO** — root cause reale = `deadlock detected` (non il pool-jitter registrato): `withTransaction` ritenta 40P01/40001; **(c)** alert di sicurezza da **10 a 0** via `pnpm.overrides` (erano transitivi, irraggiungibili da Dependabot → i run «security_update_not_possible» non erano un guasto); **(d)** backup PROD con **archivio off-host** su linux-pc (`pull-prod-backups.sh` + timer, verifica TOC + restore reale 163 utenti). **W1 avviata**: literal password ritirato da 20 file tracciati, 17 path assoluti resi relativi, CodeQL attivato, agent-gateway nei gate (47 test mai eseguiti), 3 env var portate in EnvSchema (incl. la guardia brute-force del login), pool `max` parametrizzato, kickoff forensi marcati ESEGUITO. **W0 COMPLETATA** con Z-015 (alerting): 4 regole Prometheus live su PROD (`/api/v1/rules` = 2 gruppi), handler `heuresys-unit-failure@` con `OnFailure` su tutti i **9** job schedulati (verificato facendolo scattare con una unit di prova), consegna via nuova sezione del dashboard di sessione — scelta deliberata al posto di un alertmanager, che senza SMTP sarebbe un'altra dashboard non letta. **AIDE**: database mai inizializzato (causa del fallimento quotidiano da settimane) — ora creato, 401 MB. **W1 avviata, 22 cluster chiusi**, fra cui: token colore `text-destructive` inesistente corretto in **17 file** (non solo showcase — anche approvals/insights/me/*, quindi produzione); credenziali API del gateway neutralizzate PRIMA dell'import dell'SDK (erano hoisted → arrivava tardi, fallimento silenzioso con costo); contraddizione MFA risolta sul reale (enforcement OFF, 25 fattori registrati). **Alert di sicurezza**: 10 chiusi, 1 non chiudibile registrato come **D-75** con rischio accettato e motivato (le linee 1.x/2.x di brace-expansion non hanno patch; l'unificazione rompe ESLint). Counts ri-derivati 2026-07-25 (S1029): migration **213** (max **000215**) · unit test **36** (6 file, prima non eseguiti in CI) · integration **1476** (1454 pass, 22 skip) · alert sicurezza aperti **0** · PR Dependabot aperte **8**.

HEAD **`cccaf074`** (21 commit S1028, 2026-07-23) — mandato Enzo “procedi con tutti in autonomia”: batch end-to-end. **P1**: #72 coerenza per-user 5 dimensioni residue (`seed_residual_user_coherence.sql` — 65 education fix con regola ruolo×ateneo, 56 KPI-target da mappa 26-ruoli, 43 overtime esenti QD/Dirigente archiviati, satellite CAP/regioni/familiari/CIE-patente sanati, martina.gentile→Risk Manager QD3 su POS-RISKM-HEAD nuova; post-condition F1-F10) · #47 D2 engagement history (misura preliminare: 3 domini core GIÀ completi vs crosswalk — nota register stale; importati check_ins 1621 `LEGACY_CI::` + wellbeing 480 `LEGACY_WB::` in sys_pulse_checks → 2834 tot; registry wave-2 sanato 0→10 mapping; regression LIVE `POST /v1/insights/recompute` con login TOTP: 158 rescored, avg 39.64→38.38, 25 band flip). **P2**: #62 G3 (cicli IS_A multi-hop → 409 `SKILL_TAXONOMY_EDGE_CYCLE`; 5 `LIMIT 5000` → `guardResultCap` fail-loud; `/v1/leads` filtri+paginazione) · #35 B7 (GET `/v1/observability/slow-queries` su pg_stat_statements + `/request-series` dal ring 24h; 2 sezioni system-health riaccese, LogStream/IncidentTimeline spente per onestà) · #60 G1 retention (**DB 1386→841 MB, -39%**: `audit.import_validation_results` 536MB→24kB con archivio off-DB verificato su VM `dump_archive/` 83MB + restore command in mig 000213; slow-query #1 del cleanup token risolta con 2 indici → 48ms; wave1 vacuum; report claude-align 108→20 con policy N-ultimi permanente; temp_sdbi deferito a D-69/Fase-3) · #48 D3 verificato GIÀ-importato (goals 1067 + updates 1811, registry sanato) · #59 F5 self-view ESS (**ADR-0031** supersede D-6; `GET /v1/me/development` con perm `insight:read:self` mig 000214, evidenze = derivation del modello; web “Il mio sviluppo” coach IT/EN; demo LIVE tommaso 44.42 MEDIUM con engagement primo contributore — alimentato dal D2 di oggi). **P3**: #73 NACE/ATECO legacy DEPRECATI (mig 000211: verifica Eurostat Rev 2.1 → legacy=ibrido Rev 2 con extraterritoriali duplicate; archivio audit.* 3276+5730, ATECO_2025 unico canonico, test aggiornati) · #63 G5 archivio script esausti (`docs/archive/scripts-exhausted/`, cowork-exchange inclusa, 15 entry npm rimossi; `encrypt-totp-secrets` tenuto = runbook vivo) · #74 `GET /v1/notifications/broadcasts` (audit per-evento, I5, gate notification:create) · #75 teams lifecycle (POST/PATCH/membership, modello single-lead, perm `team:manage` mig 000212 con allowlist D-57 estesa). **#66 Dependabot avviato**: 9 PR rebasati, #53 next 16.2.11 MERGED, low-risk lane ai check, 4 major deferiti presidiati; `allow_auto_merge=true` abilitato. **Migration 000211-000214** (tutte idempotenti twice-run). **Fix di metodo**: 2 test hardcoded resi data-derived (pulse 733→live, wave-executor wave=2→max+1); tunnel-drop di metà sessione riconosciuto (201 file rossi = infra, non codice). Counts ri-derivati 2026-07-25 (S1029): migration **213** (max **000215**) · RBAC 13/**204**/**908** (TENANT_ADMIN **183**) · utenti 163 · posizioni **181** · pulse **2834** · wave-2 registry **10** · API test file **217** (4 unit) · DB **841 MB**.

## Delta S1026 (2026-07-22) — batch autonomo P1: 8 item chiusi + fix coerenza #70 + E2E verde

HEAD **`e2cd3735`** (10 commit) — batch Enzo "P1→P2→P3+debiti, commit per fase; tools potenti, model/effort a scelta". **8 item P1 CHIUSI**: **#61 G2** (mig **000199**: 15 permessi dedicati audience-invariata + self-healing anti-000005 — `job_family:*`, `skill_taxonomy:*` matrice-onesta PLATFORM_ADMIN-only, `operating_model:*`, `*_kpi_template:*`; guard test +2 invarianti) · **#68 F4 P2** (label-layer enum: `lib/enum-labels.ts` + `EnumStatusBadge/Pill` + **~57 domini IT/EN** in `common.json:enums` derivati da Zod+DISTINCT live, retrofit ~40 file; fix codici tecnici: `/me/gaps` su `MeGapSchema`+Score, highlight org-chart via `sourceEntityId`, skillName su positions/learning, picker self-assessment senza ESCO-code, URI→basename docs; mig **000200** purge residui test `IT_TI_*` pre-D-52 + registry EXCLUDE/D) · **#24 ADR-0027 F4 COMPLETO** (F4-A/B erano già S1025 — register stale; mig **000201** `approval:read`→TEAM_LEADER, attività §2.3-4; matrice cross-tree F5 provata HTTP su membro derivato live: ACTIVITY ✅/EVALUATION ❌; superfici task respinte = WHAT registrato Enzo) · **#34 B3** (primo flusso approvativo BUSINESS: `POST /v1/me/time-off/requests` → approval TIME_OFF_REQUEST al manager reale via reports-to → effect handler atomico scrive time-off APPROVED-at-birth + balance move guardato + ledger USAGE; pagina ESS `/me/time-off`; mig **000202** `leave:request:self` self-floor + **000203** sidebar; pending=runtime approvals, reject=zero compensazioni) · **#46 D1** (import wave-2 **ESEGUITO** dal PG nativo VM: 1445 righe legacy→**1355 `sys_user_skills`** (158 utenti/58 skill) — non era MAI girato, 0/583 external_code; `/me/skills` riscritta possession-primaria + trail evidenze; dominio i18n `skillSource` coi valori legacy reali) · **#55 F1** (sezione "Capability essenziali" su `/org-director`: formula+pesi dalla response, 9 colonne drill) · **#51 E1** (whistleblowing OPERATIVO: mig **000205** custode reale = andrea.martino Head of Legal&Compliance + sidebar Governance custodian-only; pagina PUBBLICA `/whistleblowing` (form anonimo+tracking-code+status-check, allowlist proxy) + console custode con update; persona E2E custodian provisionata, fixture TOTP parity 2-lati; residuo esplicito: crypto app-level body) · **#42 C4** (retrofit paginazione 9 pagine + fix companion KPI-strip su aggregato server; dedup 10 shared-types in 8 file; **4 STOP onesti** = gap di CONTRATTO registrati: CareerPath.difficulty, BlueprintFamily.industryCode, BlueprintActivation.activatedAt, EnterpriseTypingProfile.status + mismatch economicWeight). **Fix coerenza #70**: mig **000204** — il seed S1025 riattivò le catene di alice(CRO)/alberto(dealer) lasciando gli utenti DEACTIVATED → la 000188 §D le ri-chiudeva a ogni migrate (CRO vacante, 2 gap orfane, `(unassigned)` analytics); ora utente+catena ACTIVE insieme, permanente. **Igiene emersa**: purge `sys_training_initiatives` residuo-test con dichiarazione registry (000200); soglia me-permissions derivata dal vivo (magic <50 rimosso); mappa segreti MFA fixture estesa (custodian) in parity. **Suite API**: 2 run complete → 1433/1434 → verde (unico rosso = parity fixture, fixato). **E2E prod**: 244 pass/10 fail → triage a causa radice (proxy allowlist whistleblowing = fix di LEGGE; RACI: paginazione+nome localizzato S1024; 5 assertion stantie da sessioni precedenti; **`MFA_ENFORCEMENT_ENABLED=false` nell'`.env` locale** — seam S989 spento che accecava le 4 spec MFA, riportato a `true`; EMAIL_OTP → skip motivato #8) → **90 pass / 1 skip / 0 fail**. Lezione trasversale: register/doc sistematicamente STALE vs codice (3 item su 8 erano già ~75-90% backend — il collo era il consumo frontend). Counts ri-derivati 2026-07-22: migration **203** (max **000205**) · RBAC 13/**198**/**889** · `sys_user_skills` **1355** · sidebar **55** voci · API test file **212** · E2E spec **72** · utenti 163 · posizioni 180 · team 26. Cowork inbox: **2 proposte PENDING non processate** (istruzione Enzo — fresh session). 18 doc storici spostati a mano (Enzo) in `docs/kb/xtras/`.

## Delta S1025 (2026-07-22) — D-74 chiuso + #70/#71 + audit per-user + Fase 4 census

HEAD **`5e378c02`** (14 commit) — **PROD deployata su HEAD** (LAST_GOOD `5e378c02`, readyz OK). **D-74 CHIUSO**: mig **000195** (registra `sys_reference_translations`+`sys_skill_groups` nel reconciliation registry — causa dei 5 test rossi CI-only) + **000196** (re-dedup difensivo con repoint esteso, **eredità categoria/gruppo dall'archivio dedup** — 132 evidenze ri-categorizzate, heatmap by-category DENSE 770→902 — e restore `sys_skills_natural_key_uq`); codice conflict-aware (materialize get-or-create per nome; skills 409 `SKILL_NAME_CONFLICT`); **CI integration VERDE** dopo 4 run rosse; **i18n wiring deployato**. **#70 FATTO** (`seed_key_roles_coverage.sql`): 6 ruoli chiave coperti con riassegnazioni interne (alice.esposito→CRO e colombo→dealer erano catene ENDED mai sostituite — causa del bucket `(unassigned)` in analytics; promozioni cattaneo/lombardi/martino/gallo alle 4 posizioni Head; manager OU, lead+membership team, band MG-1). **#71 FATTO**: mig **000197** (purge 6.454 moduli learning junk — fatti legacy ingeriti come moduli, 0 referenze, archiviati) + **000198** (21 career path residuo-test) + `seed_ccnl_floors.sql` (26 contratti RTL sotto il minimo **CCNL Credito 03/2026** sanati; inquadramenti HEURESYS coerenti) + `seed_banking_learning_catalog.sql` (**15 moduli formativi bancari reali** temi ABI + mapping completo skill-richieste→formazione). **Audit coerenza per-user (mandato Enzo) FATTO a livello archetipo** (`seed_user_role_coherence.sql` + report `db-forensics/USER_ROLE_COHERENCE_2026-07-22.md`): 163/163 con ruolo (chiara→Head of Product; fondatore credential-less su CEO&Founder), job_title=posizione, floor età/anzianità per livello, CRO 211k→178k, employment sync contratto (33) — residuo → **#72 (P1)**. **2 bug API reali corretti**: payload gap array→`{skill_gaps}` (500 su `/v1/learning-gaps/analysis-results`) + prune orfani flight-risk/skill-gap su recompute PLATFORM. **Fase 4 (#68)**: census meccanico **COMPLETO** (spec `f4-sweep.spec.ts` gated F4_SWEEP, 68 route × 3 personas, cattura stabilizzata) + **7 fix chirurgici verificati** (F4-01…07: `<dl>` career, contrasto warning inbox, contrasto picker org-chart in dark via token S982, label ruoli header `shell:roles.*`, livelli self-assessment, copy organigramma IT, system-health i18n) + **regressione italiano-di-default** (`default-locale.spec.ts`, suite normale) — worklist P2 clusterizzata in `frontend-forensics/F4_SURFACE_CENSUS_2026-07-22.md`. **Suite test interamente data-derived** (analytics, export, materialization, reconciliation, me-*, psr — mai più conteggi pinnati). Counts ri-derivati 2026-07-22: migration **196** (max **000198**) · utenti **163** · posizioni **180** · team **26** · OU **28** · RBAC 13/182/814 · skills **14041** · learning modules **988** (973 reali + 15 BANK-LM) · career paths **7** · API test file **186** · E2E spec **70**.

## Delta S1024 (2026-07-21) — mandato forense Fasi 1-3 (debiti + DB capillare + coerenza semantica)

16 commit (branch `forense/f2-db-hygiene` → merge). **Fase 1** debiti: register verificato riga-per-riga contro codice/DB, D-61..D-73 nuove, fix parser dashboard. **Fase 2** DB capillare: census 7 analisi + mig **000188** (igiene: indice lineage, 3 FK, 5 indici morti) + **000189** (dedup 24 gruppi skill con repoint 573 referenze + unique index preventivo — poi rimosso da 000194, vicenda D-74) + **bilinguismo IT/EN COMPLETO** (**ADR-0029**: mig 000190-000191, `sys_reference_translations` **29.511 traduzioni** da dataset ESCO ufficiale IT+EN) + **ontologia 100%** (**ADR-0030**: mig 000192, `sys_skill_groups` 640 nodi/4 livelli, 13.647 skill→gruppo) + wiring API i18n (`req.locale` + overlay). **Fase 3** coerenza: mig **000193** (drop 34 residui brownfield morti) + seed banking RTL (792 requisiti + possessi + gap con position_id — F6 chiuso) + comp/date (salari monotoni per banda, 8 executive→Dirigente, 468 pay-slip) + org (OU Tesoreria+Internal Audit, +18 posizioni). **000194**: revert unique index (regressione CI → D-74, chiuso in S1025).

## Delta S1023 (2026-07-21) — epiche GO-BRANCH COMPLETE e LIVE + coda chiusa

HEAD **`fbead146`** (22 commit: branch `d08-f5-offprod-runner` → merge `a101a081` + follow-up; + handoff). **D-08 F2-F5 SHIPPED**: 2° runner self-hosted **`linux-pc-runner`** (off-prod, x64, label `off-prod,linux-pc`, provisioning idempotente `scripts/provision-ci-runner-linuxpc.sh` — lezioni: `.path` snapshot per i job-step, TEST_ADMIN_PASSWORD nel runner-env, node22 symlink stabile) con i 3 workflow pesanti retargati (`test-integration`+`playwright-smoke`+`build-web` → twin; PROD fuori dal loro path CI); **deploy-gate `ci-gate.sh`** fail-closed in `vm-deploy.sh` (**ADR-0028**: required-checks da ruleset incompatibili col direct-to-main; enforcement al consumo) — **primo esercizio reale riuscito** (`[ci-gate] OK — de521620 CI-green` nel log deploy); **resource-slice cgroup** su entrambi i runner (VM 12G/300% drop-in installato live; twin 10G/300% nella unit); timeout CI ricalibrati per il twin (test-integration 25', lint 10'). **D-14 F2-F4 SHIPPED**: provision-engine completeness (role floor TENANT_ADMIN+USER I17, archetipo in-transazione via repo materialize, 409 `TENANT_CODE_EXISTS`, kill-switch `TENANT_PROVISION_ENABLED`) + **GDPR tooling** (mig **000186**: registry `sys_gdpr_data_map` 54 righe classificate da FK reali con strategie DELETE/ANONYMIZE/RETAIN+basi legali, `sys_user_consents` append-only con `consent_seq`, `sys_gdpr_requests` accountability, perms `gdpr:*` + floor self; modulo `gdpr` 4 endpoint + ME `POST /me/gdpr/export`+`GET/POST /me/consents`; **demo LIVE PROD**: login reale federica → export tommaso **54 tabelle/37 con dati** + row EXPORT COMPLETED; erasure E2E provata in suite su soggetto provisionato). **D-09 F5 LIVE**: Prometheus 3.13.1 su VM, `heuresys-prometheus.service` loopback **:9091** (la **:9090 è dello stack monitoring legacy pre-esistente** — api-gateway-evo/exporters — deliberatamente NON toccato), retention 15d/2GB, target `heuresys-api` UP, `PROM_METRICS_ENABLED=true` in PROD. **Coda**: **D-59 RISOLTO** (retention login-events 400gg via framework GDPR) · **#65 RISOLTO** (mig **000187** FK NOT VALID + vista orphans + tripwire 920; gli orfani = scheme ATECO legacy parziale L5-6, crosswalk-support) · **D-11** freeze brownfield (`BROWNFIELD_ENGINE_ENABLED`, PROD=false → 404 verificato live, `docs/brownfield/ENGINE_STATUS.md`) · **4 Dependabot HIGH** chiusi (override patch-scoped brace-expansion×3 + js-yaml). **Riparazioni**: invariante **twice-run** migration ripristinato (000009 amendment + 000185 v2 — la v1 lo rompeva; provato con 2 run consecutive pulite, 0 UNCLASSIFIED) · clone CI **completo** su entrambi gli host (`setup-ci-database.sh` v2: pre-create estensioni superuser, TOC-filter, parity-gate 274 tabelle/6 estensioni) · regressione **`pnpm dev`** (tsx flag order da D-58) · test `me-career-tabs` a derivazione-DB (S1012) · `TEST_ADMIN_PASSWORD` stale nel `.env` VM allineato (rotazione S1014 mai propagata — additive-merge by-design, classe segnalata a Fase-1 kickoff). Counts ri-derivati 2026-07-21: migration **185** (max **000187**) · RBAC **13 ruoli / 182 perm / 814 map** · API test file **209** (+4: gdpr, nace-parent-integrity, brownfield-freeze, +1) · E2E spec **68** · utenti **162** · posizioni **162** · team **24** · 2 tenant ACTIVE · skills **14093**. Suite CI completa **1424 test / 0 fail** sul runner off-prod. **Prossima sessione**: mandato forense 4-fasi (`NEXT_SESSION_DB_FRONTEND_FORENSICS_KICKOFF.md`).

## Delta S1022 (2026-07-20) — mandato forense ESEGUITO + D-58 risolto LIVE + remediation R1-R4

HEAD **post-handoff** (7 commit S1022: `0e55d0d3` R1 D-58 exports · `7f2f1dc5` R3 FK-index · `f1c91567` audit outputs · `4af23064` tsup fix · `8893e560` emit fix · `ce29bb10` R4 dead-schema · + handoff). Migration su disco **181** (`000001..000183`, +000182 FK-covering-index +000183 drop-org-hierarchies). RBAC **13 ruoli / 176 perm / 776 map** (invariato). Moduli API **88**. Test file **204**. **Endpoint reali 516** (git grep route handler — la doc diceva ~407, drift F-A08). 162 utenti, 2 tenant ACTIVE.

- **Audit forense S1022** (`docs/kb/full-forensic-audit/{AUDIT_FORENSE,FINDINGS}_2026-07-20_022239.*`): fan-out multi-agente (14 finder + fp-check adversarial) **fallito su session-limit account** (reset 6:20am) → audit completato in **main thread**, evidence-based (verifica di persona = gate fp-check). **8 finding verificati** (0 Critical · 1 High = D-58 · 3 Medium = F-A01 FK-no-index / F-A02 login-events-91k-unbounded / F-A04 F4-backing-vuoto · 4 Low) + **4 refutati** (XSS themeBootScript, SQL-injection→placeholder, env-boolean-footgun→z.enum, N+1-dashboard→subquery-aggregate) + **7 asset**. Scorecard **arch 83 / security 85 / db 76 / test 80**. I **4 TP storici S1014 tutti chiusi** (F-001 secrets / F-002 open-redirect / F-004 CSV / F-012 fork-runner). 0 vuln pnpm audit. Verdetto **CONDITIONAL-GO** (competenza esecutiva dimostrata; 3 condizioni bloccanti).
- **R1 — D-58 RISOLTO e DEPLOYATO LIVE** (`0e55d0d3`+`4af23064`+`8893e560`): `@heuresys/shared` exports condizionali `development`→`./src/*.ts` (dev tsx + API tsup via `--conditions development`, source-first) / `default`→`./dist/*.js` (web Turbopack) · **root cause `emitDeclarationOnly:true`→`false`** (tsc non emetteva mai i .js; localmente passava per .js stale) · `main`→dist · fuori da `transpilePackages` · build-web hard-fail. **Gate: next build verde in PROD, www.heuresys.com/login 200, /api/readyz 200.** 3 fix in catena (regressione tsup + emit scoperte SOLO al deploy — errore riconosciuto: gate tsup saltato nella verifica locale iniziale).
- **R3 — 17 indici FK applicati LIVE** (mig **000182**): FK di filtro/join scoperte (audit: 261/549 senza indice leading) — tenant_id + risorse joinate + assessor; escluse le audit-actor. EXPLAIN prima/dopo al deploy.
- **R4** (mig **000183**, applicata al deploy): drop dead-schema `sys_organization_hierarchies` (0 righe/0 usi, verify-first). **NACE F-A06**: 920 `parent_code` orfani in `sys_activity_classifications` → qualità dati (decisione), NON tamponato.
- **Doc-tank** (istruzione Enzo, fuori repo): backup completo `../heuresys-advanced-docs-tank/archivio-2026-07-20` (11269 file) + clone rigenerato `clone-2026-07-20` (236 file, README/START_HERE ricreati alla realtà, CLAUDE.md drift-corretto, MANIFEST + validazione link).
- **S1022 2ª parte (fan-out ripristinato)**: **R2 F4 RISOLTO e LIVE** (mig **000184**, derivazione RULE-B decisione Enzo, 1086 righe = 120 OWNER/966 CONTRIBUTOR; DoD: alice.rossi OWNER reale vede 38 partecipanti via asse funzionale) · **audit 8-P1** = pattern "verde≠reale" isolato a **2/8** (F4 fatto + B3 event-driven empty-by-design come E1; altre 6 sane) · **WS-L FATTO** (`WS-L_PLAN.md`+`WS-L_TODO.md`, ecosistema sano post-S1017 + diagnosi D-56) · **triage D-01..D-14 FATTO** (`DOSSIERS_TRIAGE_S1022.md`: 6 DONE / 3 GO-INLINE / 3 GO-BRANCH / 1 DEFER / 1 WON'T).
- **Epiche GO-BRANCH (S1022 3ª+4ª parte)**: **D-09 /metrics FASI 1-4 DONE** (`fa24f7f3`, PROD gated OFF prod-safe; test 4/4). **D-14 F1 provision-engine DONE** (`1eee097a`: `POST /v1/tenants/provision` transazionale admin-gated — tenant+TENANT_ADMIN(identity+argon2id)+role+MFA-policy in withTransaction, rollback atomico; test 3/3 incl. login del nuovo admin LIVE). **D-08 F1 DB-CI isolato DONE** (`91d2a831`: `heuresys_ci` = clone di PROD 162 users via peer-auth; override `POSTGRES_DB` nei 2 workflow → CI **mai più su heuresys_advanced**; `db/scripts/setup-ci-database.sh` idempotente). Spec completa: `EPICS_SPEC_S1022.md`.
- **Residuo epiche (⚠ nuova regola S1022: Claude DECIDE ed ESEGUE le decisioni tecniche, [[feedback_claude_decides_technical]])**: D-08 F2 deploy-gate-CI + F3 cgroup + F4 required-checks + F5 2°-runner off-prod linux-pc · D-14 F2 completezza + F3-4 GDPR (DSR-export/erasure/retention — scope tecnico deciso da Claude) · D-09 F5 collector systemd VM.
- **Residuo**: GO-INLINE (D-03/04/11) + D-59 retention login-events (policy) + #65 NACE 920 orfani + unit-layer.

## Delta S1021 (2026-07-19/20) — batch "tutto P1" 8/9 + ~10 difetti pre-esistenti + mandato forense

HEAD **`6db1b250`** (16 commit `bae0a7ef..6db1b250`; 15 pushati fino a `bfc346b6`, `6db1b250` = D-58 diagnosi). Migration su disco **179** (`000001..000181`, +7: 000175 ESCO-skill-global · 000176 PIP-rank · 000177 delete-perms · 000178 deproxy-perms · 000179 process-participants · 000180 user-skills · 000181 whistleblowing). RBAC **13 ruoli** (+WHISTLEBLOWING_CUSTODIAN) / **176 perm** / **776 map**. Nuovi moduli/tabelle: `sys_process_participants` (F4), `sys_user_skills` (D1, 905 righe import legacy), `sys_whistleblowing_reports` (E1). Nuovo workspace app: nessuno. Nuovi endpoint: `/v1/capability/composition/essential-ranking` (F1), `/v1/learning-gaps/summary` (C4), `/v1/me/skills/possession` (D1), `/v1/whistleblowing/*` (E1, canale pubblico + console custodian).

- **Batch P1 8/9 shipped DoD-complete** (dettaglio → `.handoff/STATE.md` + `SOT_BACKLOG.md`): #42 C4 · #61 G2 · #24 F4 · #34 B3 · #46 D1 · #47 D2 · #55 F1 · #51 E1. **#4 GTM** = solo pricing page WAIT-INPUT.
- **~10 difetti pre-esistenti corretti (R3/R17)**: catalogo skill invisibile a ogni tenant (14036/14093 orfane, mig 000175) · PIP view senza `rank` (000176) · N+1 producer notifiche (emitNotificationsBulk dedupe) · `db:validate` rotto su Windows ($ErrorActionPreference+pg_dump) · badge FALSO "FACOLTATIVO" su ogni percorso (drift `isMandatoryDefault` inesistente) · colonna morta seed-runs (`blueprintVariantId`) · fixture E2E `API_BASE` doppio-/v1 · censimento test congelato (registry) · **D-57** grant-a-tappeto TENANT_ADMIN.
- **Counts verificati**: full API suite **~1394 test / 0 fail** (202 file); typecheck + lint + i18n (1922-1923 chiavi) + state-lint verdi; `db:validate` viste 0 righe + idempotenza twice-run diff vuoto.
- **CI**: tutti i gate VERDI tranne **Playwright-smoke ROSSO = D-58** (web build Turbopack barrel `.js`, pre-esistente `bae0a7ef` 17-lug, NON dei commit S1021 — diagnosi definitiva + 3 opzioni fix nel register).
- **Nessun deploy PROD** (D-08 + D-58 bloccherebbe il build web). **Mandato Enzo fine sessione**: la prossima sessione fresca fa PRIMA una review forense dell'intero progetto + gate adversarial + verdetto finance-readiness → `docs/kb/NEXT_SESSION_FORENSIC_KICKOFF.md` + memoria `project_next_session_forensic_mandate`.

## Delta S1020 (2026-07-18) — Serie-A read-exposure residua CHIUSA (#33 · #32 · #29)

HEAD **`42f4838d`** (3 commit pushati: `a5c8a586` #33 time-off/leave · `c10abc06` #32 comp-read · `42f4838d` #29 talent-review). Chiusa la coda Serie-A che S1018/S1019 avevano lasciato ACTIVE — tutti e 3 read-exposure di dati dormienti già popolati, org-gated (ADR-0027 F3), shipped DoD-complete (API + web + Playwright E2E su login persona reale + dati live). #33 fatto in main context (calibrazione), #32/#29 implementati da subagent con briefing completo + verifica/E2E/commit dal main.

- **#33 A/L8 time-off/leave** (`a5c8a586`): modulo `time-off` — `GET /v1/time-off/{requests,accrual-rules,balance-transactions}` + ESS `GET /v1/me/time-off/requests`. `leave` = PERSONAL (data-classes) → org-gate service (requests/transactions per subject user) + catalog (accrual rules, tenant policy, no person rows). **mig 000173** (`leave:read` 6-role + `leave:read:self` + sidebar `/time-off`). Pagina admin `/time-off` (3 sezioni). Integration 8/8 + E2E verde.
- **#32 A/L7 comp & reward read** (`c10abc06`): estende il modulo `compensation` con 6 GET su tabelle dormienti (variable-pay, recommendations, bonus-pools, objective-reward-rules, position-economic-weight, handoff-records) — **riuso `compensation_intelligence:read`** (nessuna migration). variable-pay/recommendations orgGate service (per user), resto catalog. Chiude l'anomalia `sys_payroll_handoff_records` write-only (0 righe = empty state reale). Pannelli su `/compensation-intelligence`. Integration 8/8 + E2E verde.
- **#29 A/L3 talent-review 9-box** (`42f4838d`): modulo `talent-review` — `GET /v1/talent-review/{nine-box,fit,readiness,succession,critical-positions,critical-coverage}`. `talent` = EVALUATION (data-classes) → org-gate service (4 person-level) + catalog (2 position-level). **9-box derivata da `sys_talent_scores`** (potential×performance a soglie 50/75 → LOW/MEDIUM/HIGH; il fantasma `sys_nine_box_grid` NON esiste — drift Ledger). **mig 000174** (`talent:read` 6-role, **NO `:self`** — self-view OFF per decisione prodotto + sidebar `/talent-review`). NUOVA pagina admin `/talent-review` (griglia 3×3 board-ready + pannelli fit/readiness/succession/critical). Integration 9/9 + E2E verde.

Counts ri-derivati 2026-07-18: migration **172** (max **000174**, +000173 leave/+000174 talent) · RBAC **12 ruoli / 162 perm / 734 map** (+`leave:read`/`:self` +`talent:read` = +3 perm, +20 map) · moduli API **87** (+time-off +talent-review; compensation esteso). Suite API completa **196 file / 1352 test passed** (2 file + 6 test skip pre-esistenti, 0 fail). **Debiti nuovi**: D-55 (flake login MFA intermittente, pre-esistente, path auth), D-56 (claude-mem disabilitato, workaround). **Nessun deploy** (W13).

## Delta S1019 (2026-07-16) — riallineamento post-batch-VM: flotta in parità + piano versionato

HEAD **`5787ef39`** (1 commit S1019: `docs(kb)` piano batch versionato + fix path). Sessione di **riallineamento** dopo S1018 eseguita sulla VM (deroga a local-first, ora ripristinato): i 18 commit del batch confluiti GitHub → locale → linux-pc; **flotta 4 nodi in parità** (locale=GitHub=VM=linux-pc). Suite API completa **verde in locale** (193 file passed / 2 skip · **1327 test / 0 fail**, ~29min via tunnel) + typecheck/lint verdi + `db:validate` pulito (twice-run idempotency). **DB linux-pc ri-clonato 1:1** dalla VM (`clone-vm-db.sh` eseguito SUL linux-pc — da Windows non funziona, script sudo/binari Linux; row-counts identici). `.env` locale: +`MATCHING_FREETEXT_ENABLED=true` +`COOKIE_SECURE=false` (topologia dev; su linux-pc il flag matching resta OFF — denylist by-design di `env-key-merge.sh`). **Piano batch S1018 versionato**: `docs/kb/PLAN_S1018_BATCH.md` (era solo in `~/.claude/plans/` sulla VM) con nota adattamento local-first; riferimenti nel RESUME corretti a path repo-relativi. Counts ri-derivati 2026-07-16: migration **170** (max 000172) · API test file **195** (il "172" del delta S1018 era sotto-derivato) · E2E spec **65** · pagine web **102** · utenti **162** · posizioni **162** · team **24** · 2 tenant ACTIVE · RBAC **12 ruoli / 159 perm / 714 map** (+provenance/evidence S1018) · skills **14093**. **Nessun deploy** (W13) → PROD www ancora pre-batch.

## Delta S1018 (2026-07-16) — batch autonomo full-scope: W0 + Serie-A P1 (parziale, congelato per fresh session)

HEAD **`ee3b0558`** (batch S1018, ~16 commit pushati). **Congelato per ripartenza fresh session** (richiesta Enzo) — punto di ripartenza autoritativo: **`docs/kb/RESUME_S1018_BATCH.md`** + piano `docs/kb/PLAN_S1018_BATCH.md` (versionato in S1019; origine: plan-file di sessione VM). **Nessun deploy** (scelta Enzo: deploy a fine batch W13) → **PROD www gira ancora la versione pre-batch**.

Counts ri-derivati 2026-07-16: migration **170** (max **000172**, +000170 inbox-purge/+000171 provenance/+000172 evidence) · API test file **172** · utenti **162** · 2 tenant ACTIVE · RBAC role-permission estese (+provenance:read, +evidence:read/:self).

**FATTO** (tutti DONE con API+web+E2E verdi, register aggiornato): **W0** — register serie C-G→#42-#63, **D-08 core** (probe-as-gate + `vm-rollback.sh` + LAST_GOOD), fix test-harness PROD-VM (rate-limit pre-dotenv), **C4-mini** (usePaginatedList + EntityTable server-side), verifica 4 TP audit (già fixati). **W1 Serie-A P1**: **#40** free-text search · **D-54** inbox-orphans (mig 000170, **D-54 RISOLTO**) · **#26** goal/OKR life (helper `canReadGoal` = contratto F4) · **#27** evidence layer ⭐ (UNION 9 tabelle + provenance, mig 000172) · **#28** trust-ledger ⭐ (/provenance, mig 000171) · **#30** gap-closure · **#31** KPI-metrology. Twice-run repair (assertion grant-count → FLOOR in 000060/000145).

**RESIDUO** (fresh session, riprendi da W2): **Serie-A P2/P3 residua NON fatta** — **#29 talent-review (9-box), #32 comp-read, #33 time-off** restano ACTIVE (il commit ee3b0558 ha sovra-dichiarato "Serie-A tutta chiusa": #26/#27/#28/#30/#31/#40 sì, #29/#32/#33 no). Poi **W3 Serie B** (#34-#38) → W4 GTM → W5 G → W6 C → W7 D → W8 F → W9 F4 → W10 E1 → W11 E5 → W12 audit-100X → **W13 deploy finale + handoff**. Dettaglio wave: RESUME doc + piano.

## Delta S1017 (2026-07-08) — forensica + ottimizzazione session-start + fix integrità inbox

HEAD **`667d776c`** (2 commit pushati: `cee92c7e` session-start opt · `667d776c` cleanup DB inbox; + handoff). Counts **invariati** (sessione infra/tooling, nessuna migration/modulo/dato tracciato): migration **167** (max 000169) · utenti **162** · posizioni **162** · RBAC **12 ruoli / 156 perm / 698 map** · skills **14093** · 2 tenant ACTIVE.

- **Forensica session-start** (workflow 4 profiler + verificatore avversariale, `docs/kb/SESSION_START_FORENSICS.md`): "avvia sessione" >20 min. Root cause = **prodotto (N round-modello imposti dalla doctrine session-start) × (decode a xhigh/ultracode)** — NON gli MCP (serena/claude-mem/playwright = addendi one-shot; serena `start-mcp-server` mai misurato, solo 3,4s su `--help`; spawn probabilmente parallelo). Script deterministici ~12s (irrilevanti). Attribuzione dominante *inferita* (manca la misura chiave: timestamp per-round del harness).
- **Fix #1+#3 doctrine** (`cee92c7e`): nuovo **`docs/kb/tools/session_start.py`** = `build_menu` (menu) + `status_dashboard` (salute) in **un processo, un round**, `--no-net` al boot (git-fetch/CI/PROD → on-demand via `pnpm status` / `session_start.py --net`). CLAUDE.md "Session start" riscritta: lancia lo script, **NON leggere raw** SOT_BACKLOG/SOT_STATE/DEBT al boot (drill-down solo sull'item scelto). Boot view live: 2978 ms.
- **Fix #6 slim CLAUDE.md** (`cee92c7e`): **270→236 righe** (−5,8KB). Estratti in doc linkati: Design-System-X18 narrativa → `docs/kb/DESIGN_SYSTEM_UI.md`, R23-project → `docs/kb/AUTONOMY_R23_PROJECT.md`; MVP-2a/2b compattato con pointer a `docs/archive/NEXT_SESSION_MVP_2A.md` (**corretto pointer stale** "repo root"). Regole operative tenute inline (perdita-zero verificata).
- **Fix #2+#4+#7 config GLOBALI fuori repo** (backup `.bak-forensic-20260707`, effetto **prossima sessione CLI**): `~/.claude/settings.json` enabledPlugins **40→17** (disabilitati 6 voltagent + 11 trailofbits + codebase-audit-suite + optimization-suite + example-skills + karpathy come inerti su stack TS/Fastify; serena+playwright **lazy-load**); `~/.claude-mem/settings.json` `CLAUDE_MEM_CONTEXT_OBSERVATIONS` **50→12**. Raccomandazione #5 (boot a effort ridotto) = abitudine `/effort`, non forzabile → documentata.
- **Fix integrità DB** (`667d776c`): `status_dashboard` ha scoperto **4 righe in violazione** di `sys.v_inbox_resource_consistency` — 4 notifiche inbox UNREAD di `paolo.caputo@rtl-bank.org` (tenant RTL_BANK, create 2026-07-05) di tipo ASSESSMENT che puntavano a 3 assessment inesistenti (ref polimorfico, no FK/cascade). Snapshot + DELETE mirato con rollback-guard → **tutte 6 le viste strutturali = 0**. Artefatto + reversal + rischio-recidiva in `qa_artifacts/inbox-orphan-cleanup-20260707.{md,csv}`; recidiva registrata come **D-54**.
- **Propagazione**: `cee92c7e` + `667d776c` pushati; **VM + linux-pc allineati** (align-clones all: git + payload + memoria; deploy skipped = nessun codice runtime). Refresh DB linux-pc via `clone-vm-db.sh` (rimuove le 4 orfane anche dal twin locale).

## Delta S1016 (2026-07-05/06) — atlas conoscenza + portafoglio dev-lines + #25 live

HEAD **`2397eb0a`** (10 commit pushati: `e7aeae45` build_atlas · `9ee2cff9` ATLAS_CURATED+INDEX · `a7c7170d` nota-grafo · `05232c5c` dossier-A · `cf7a09bd` dossier-B..G · `34f7752d` register #25-#40 + fix build_menu UTF-8 · `e22dcdb7` **#25 feature** · `2397eb0a` register-#25-DONE; + deploy VM verde verificato live). Counts ri-derivati live 2026-07-06: migration **167** (max 000169) · API test file **189** (+1 `positions-learning`) · E2E spec **60** (position-sub esteso) · pagine web **101** · RBAC **12 ruoli / 156 perm / 698 map** · utenti **162** · posizioni **162** · skills **14093** · 2 tenant ACTIVE.

- **Atlas cross-layer (nuova infrastruttura di conoscenza)**: `docs/kb/tools/build_atlas.py` ri-deriva pagina→endpoint→permesso/orgGate→tabella→dati live in `docs/kb/atlas/{atlas.yaml,ATLAS.md}` (100% generati, idempotenti — timestamp=commit HEAD; `--no-db`; psql batch UNION ALL + retry su drop tunnel; ~7s). **Ruolo SoT**: atlas autoritativo; grafo graphify + grafo wiki = viste esplorative parallele. Sintesi semantica curata in `ATLAS_CURATED.md`: 193 rilievi del full-sweep 19-agenti (9 API chunk + 3 web + shared + DB live + ops + legacy×2 + wiki + design-system) — mappa 67 tabelle vuote, 41 tabelle popolate mai referenziate (15k righe; 86k+ col secondo anello), capacità dormienti, residuo legacy (65 tabelle >1k non importate; cantiere `heuresys.com.evo` con 12 macro-aree assenti), ~60 componenti @heuresys/ui mai usati, drift documentali (§11: CLAUDE.md "11 ruoli"→**12**, Design System `^0.1.1`→**`^0.1.9`**, filtro PET ritirato S1009; Ledger pre-Gap#1 → VRIO/OHI/Ranker sbloccati; `sys_nine_box_grid` NON esiste).
- **Grafo graphify rigenerato full-codebase**: 13.777 nodi / 18.774 edge (era DB-health stale 06-17), GraphML + sub-viste `graph-{api,web,shared}.html` fresche. **Parziale onesto**: 26/52 chunk semantici freschi (limite spesa mensile colpito durante il run) → top-up in `graphify-out/PENDING_SEMANTIC_TOPUP.md` (register **#41 GATED**).
- **Portafoglio prodotto**: 7 dossier `docs/product/DEVELOPMENT_LINES_{A..G}.md` (41 linee evidence-based con webapp impattate/nuove, effort, sequenza; indice nel README prodotto). Selezione Enzo: **serie A+B** → Action register **#25-#40** (14 ACTIVE: P1 #25-28+#34 · P2 #29-30+#35-37 · P3 #31-33+#38; 2 GATED: #39 EMAIL su #8, #40 free-text su decisione Voyage).
- **#25 DONE + LIVE** (`e22dcdb7`): `GET /v1/positions/:id/{learning-requirements,learning-modules}` — espone `sys_position_learning_requirements` (1791, JOIN learning_paths) + copertura moduli delle skill richieste (`sys_skill_learning_mappings` 635 JOIN learning_modules/skills); pattern sub-resource (scope via getById, `position:read`; risorsa non-sensitive → nessun orgGate richiesto dal gate D-51, coerente coi sibling). Pagina `positions/[id]/learning`: da proxy gap-only a 3 sezioni, tipi shared, i18n IT/EN (parity **1745**). Integration **5/5** (attese derivate live, zero hardcoded) + E2E position-sub **8/8** + **CI 7/7 verde** + **deploy VM** (readyz OK; endpoint 401 su www = live). Chiude il gap-DATI S1004 della pagina.
- **Fix minori**: `build_menu.py` stdout UTF-8 (crash cp1252 sui glifi) · lezione E2E locale: rate-limit login 10/5min esauribile da run ripetuti del setup (attendere finestra).

## Delta S1015 (2026-07-05) — D-51 + D-52 chiusi (batch "2+3"): gate prescrittivo data-class + suite ermetica

HEAD **`04a4037d`** (2 commit pushati: `d9caf7bd` D-51 · `04a4037d` D-52). Migration **167** (invariato, max 000169). API test file **188** (+1 `org-gate.integration.test.ts`). RBAC **698 map** / utenti **162** (invariati — S1015 non tocca dati). CI attesa verde sui 2 commit (il vitest CI gira sotto isolamento con DB localhost → overhead savepoint trascurabile).

- **D-51 RISOLTO** (`d9caf7bd`, 35 file): ADR-0027 **F2 da descrittivo a prescrittivo**. Nuovo `apps/api/src/lib/scope/gate.ts`: collector `onRoute` + assertion `onReady` — il boot **fallisce** (`ORG_GATE_MISSING`) se una route read (`read`/`view`/`list`, non-`:self`) su risorsa sensibile della tassonomia non dichiara `config.orgGate` ∈ {`service`,`catalog`,`aggregate`}; `requirePermission` espone il permission code sull'handler (mapping route→risorsa RBAC). **76 route annotate** in 28 file routes, marker verificati handler-per-handler. Il gate ha subito reso: **mentorship `listSessions`/`getSession` erano solo tenant-gated** mentre il pairing padre era org-gated (residuo I18 fuori dalla mappa F3) → org-gate esteso alle sessions (stesso predicato mentor/mentee). Test `org-gate.integration.test.ts` 4/4 (superficie reale 0 violazioni, boot-refusal negativo, esenzioni self/write/unclassified — attese derivate dalla tassonomia, zero liste hardcoded).
- **D-52 RISOLTO** (`04a4037d`, 4 file): **isolamento transazionale per-FILE** — ogni file di test gira in UNA transazione reale rollbackata a fine file (`test/helpers/tx-isolation.ts` + wiring `setup.ts`): zero residui sul DB VM condiviso, niente coupling inter-file; fixture `beforeAll` e CRUD-flow sequenziali intra-file preservati. `pool.connect` → facade savepoint per il `withTransaction` dell'app; `pool.end` intercettato (110 file chiamano `closePool`); scritture dirette (INSERT/UPDATE/DELETE/MERGE + CTE scriventi) in savepoint per-statement **serializzati** (mutex) così gli errori SQL intenzionali (FK/unique → 404/409) mantengono la semantica autocommit senza abortire la tx di file. Iterazione: v2 savepoint-su-tutto = race `Promise.all` + 3× RT tunnel (2078s) → v3 writes-only (1442s, +20% vs baseline). **Evidenza live**: suite completa **186 file passed / 2 skip pre-esistenti · 1285 test · 0 FAIL**; rollback provato 2× (`sys_auth_login_events`: 0 righe committate durante run con centinaia di login). Escape hatch `TEST_TX_ISOLATION=0`; delta noti (now() congelato per file, DELETE legacy ridondanti-innocui) nell'header dell'helper + CLAUDE.md §Tests.
- **Nota flakiness pre-esistente**: la baseline autocommit (pre-D-52) ha mostrato 1 fail non riproducibile su 1291 (order-coupling) — esattamente la classe che l'isolamento elimina.
- **Fix ambiente (fuori repo)**: hook claude-mem che bloccava le Read su Windows — worker morto con socket `:37778` orfano tenuto dalla catena chroma superstite → catena killata, worker riavviato healthy, timeout hook `UserPromptSubmit` 60→180s nella cache plugin. Diagnosi+rimedio in memoria `reference_claude_mem_mcp_flakiness`.

## Delta S1014 (2026-07-03/04) — Audit forense read-only + remediation security live F-001…F-013

HEAD **`bf7327e0`** (6 commit: `0b24b136` F-001/F-013 · `8b237ef3` F-002/F-004/F-005/F-007/F-010/F-009 · `ace64930` F-008/F-011/F-012 · `d1aa8bbb` D-51/D-52/D-53 register · `efb71d5b` fix-CI playwright-config · `bf7327e0` D-53-RESOLVED+audit-outputs). Migration **167** (invariato, max 000169). API test file **187** (+`csrf-origin` + `csv-formula-injection`; il resto = 4 sibling scope-suite S1014-2026-07-02, vedi D-50). RBAC **698 map** (invariato — rotazione = solo credenziali). Nuovo helper `apps/api/test/helpers/personas.ts` (password personas env-driven).

- **Audit forense READ-ONLY** (`/full-forensic-audit`, intero repo): orchestrazione multi-agente (10 finder WS1-WS4 + gate fp-check adversariale). **63 grezzi → 53 verificati** (10 falsi positivi scartati): 1 Critical · 5 Medium · 19 Low · 28 Info. Poi **skill fp-check formale** (7-fasi + 6-gate) sui 21 finding security → **4 TRUE POSITIVE** (F-001 Critical, F-002/F-004 Medium, F-012 Low) + 17 FALSE-POSITIVE-come-vuln (hardening). Output in `docs/kb/full-forensic-audit/` (report + FINDINGS.json + FP_CHECK_VERIFICATION + INDEX). Scanner: `pnpm audit` 0 vuln/1359 dep; semgrep/codeql non disponibili (SAST via LLM). Scorecard: arch 82 · security 55 · db 83 · test 90.
- **F-001 Critical CHIUSO live** (`0b24b136` + rotazione): la password personas (il literal ora ritirato, non ripetuto qui — S1029) era committata in ~150 file + pubblicata in CLAUDE.md/README → l'admin internet-facing era prendibile da chiunque leggesse il repo pubblico (verificato live nell'audit). Fix: helper `personas.ts` env-driven fail-closed, literal rimosso da **139 file test** + Playwright + 3 seed + 5 script gateway/wave (+F-013), doc redatte. **Rotazione live** di tutte le **11 login-personas** (6 seed-test-admin + 5 R2) → `TEST_ADMIN_RESET_PASSWORD=1`/`R2_RESET_PASSWORD=1`; **verificata prod** (vecchia→401, nuova→200 PLATFORM_ADMIN) + **linux-pc** (old→401). Nuova password in `.secrets/test_admin_password.txt` (gitignored). Secret CI provisionato in `/etc/heuresys-runner.env` (runner restart) + GitHub-secret mechanism valutato.
- **F-002/F-004/F-005/F-007/F-010/F-009** (`8b237ef3`): open-redirect login chiuso · CSV formula-injection neutralizzata (+unit test) · dashboard N+1→baseline+running-sum · CSRF exact-origin (+integration test) · token tema. **F-008/F-011/F-012/F-013** (`ace64930`): error boundaries Next · nvm checksum fail-closed · pnpm `onlyBuiltDependencies` (allowlist argon2/esbuild/sharp/unrs-resolver) · creds hardcoded rimosse dai wave-script.
- **F-003/F-006 registrati** (D-51/D-52): non forzati — chiusure = refactor architetturali dedicati (auto-gate dichiarativo su 15 moduli; isolamento transazionale suite). **D-53** (rotazione) → RISOLTO.
- **2 regressioni CI colte+fixate** (`efb71d5b`): (a) R2 personas non ruotate da seed-test-admin (`alberto.rossetti` in auth-mfa-* → 401) → `seed-r2` reset; (b) `playwright.config.ts import.meta` incompatibile col config-loader CommonJS di apps/web → parse `.env` via `fs`. **CI 6/6 verde**.
- **Deploy prod** (`vm-deploy.sh` detached) verde (readyz + login 200). **linux-pc** allineato (pull `bf7327e` + secret + rotazione DB, old→401). VM+linux-pc su HEAD corrente.
- **Gate**: typecheck src+test · lint · i18n parity 1728 · web build · suite API completa **1281 pass / 0 fail reali** (locale) · **CI 6/6 verde** (test-integration, playwright-smoke, build-web, typecheck, lint, i18n) su `efb71d5b`.
- **Note operative**: la password `#` in dotenv va quotata (troncava a `Admin`, lezione S1014). Residuo onesto: TOTP fixture ancora committate (2° fattore F-001) — non attivo perché MFA OFF; se MFA riattivato, ruotarle. Git-history con vecchia password = worthless (ruotata).

## Delta S1013 (2026-07-01) — Batch F3+I21+F5+F6 (ADR-0027): D-50 RISOLTO

HEAD **`3f89874`** +handoff (16 commit: 11×F3 per-modulo `c6f193a`..`28d0452` · I21 `a8d315e` · F5 `9a53ba2` · F6 `d557d71` · de-hardcode `16748ea` · docs `3f89874`). Migration **167 file** (max **000169** = I21 grant HRMS_MANAGER). API test file **181** (+14: 11 `*-scope` F3 + `hrms-plenipotentiary` + `scope-peer-isolation` + `scope-audit`). RBAC **698 map** (+9 I21). **D-50 RISOLTO**: **F3** org-axis enforcement su **11 moduli sensibili** (assessments, capability-composition, successor-readiness, compensation, learning-gaps, goals, insights, semantic-matching, mentorship, okrs, predictions) — list→`resolveOrgReadScope.userIdAllowList`, per-target→`canReadOrgTarget`; `paolo→tommaso` 200 / `paolo→antonio` 404, 11 test scope red→green. **I21**: HRMS_MANAGER plenipotenziario dati (mig 000169, 9 grant business, tecnologici esclusi; live E2E 6/6). **F5**: peer-isolation I19 bidirezionale (paolo⊥claudia, sub-tree 31 disgiunti, intersezione 0). **F6**: scope-access audit centralizzato nel resolver (`lib/scope/audit.ts`, asse autorizzante, 0 wiring moduli). semantic-matching per-target ora org-gated (frontend usa solo `/me/*` → invisibile); test `matching:admin` de-hardcodato (regola S1012, invariante non lista). Suite API **1248 test 0 fail**, typecheck src+test verdi. **F4** (asse funzionale/attività) DEFERITO → HOLD (sessione dedicata, dipende da decisione prodotto Enzo su activity entities; la metà cross-tree di F5 vive in F4).

## Delta S1012 (2026-07-01) — 2 CI rosse + D-49 + Playwright + modello authz ADR-0027 F0-F2 (F3 rimandato)

HEAD **`0dfda86`** (commit S1012 pushati: fix-CI `4d9e6cc` · D-49 `33cdfb5`+`0be1f64` · Playwright `e5980da` · ADR-0027 accept `4b7dcda` · F0 `92f784d` · F1 `bb435fe` · F2 `2af08e1`+`8eaa3f6` · I21+F3-map `0dfda86`). Migration **invariate** (166 file, max 000168). API test file **167** (+5: `scope-org`/`scope-resolver`/`scope-data-classes` + refactor me-interfaces/users). **Nuova lib `apps/api/src/lib/scope/`**: `org.ts` (helper org ricorsivi transitivi), `resolver.ts` (`resolveOrgReadScope`/`canReadOrgTarget` + `HR_MANDATED_ROLES` + `MANAGERIAL_ROLES` + vincolo gestionale Enzo), `data-classes.ts` (tassonomia sensibili→asse org, ACTIVITY→asse funzionale; 20 risorse riservate). **ADR-0027** bi-axial authz ACCEPTED + invarianti **I16-I21** in CLAUDE.md (I21 HRMS plenipotenziario). **D-49 RISOLTO** (vm-deploy detached + poll); **D-50 aperto** (leak sensibili cross-utente, chiusura=F3). Users: reports-to **transitivo** (era one-hop). CI-verde in PROD (lib+refactor; comportamento esterno invariato salvo transitività manager). **F3 rimandato** (opzione A Enzo): buco D-50 in molti moduli (non 4) → mappa `docs/superpowers/specs/2026-07-01-f3-sensitive-modules-map.md`, sessione dedicata.

## Delta S1011 (2026-06-29) — batch menu: D-48/D-46/F3b/F4/GTM (LIVE PROD) · F5 residuo

HEAD **`a280de3`** (8 commit pushati: `5e52f0a` D-48 · `d6e6f46` D-46 · `dc5d04f` F3b · `556e82b` F4 · `86bbf2a` GTM · `615ef1d` fix-000142 · `adffc61` CLAUDE.md-no-PII · `a280de3` F5; **deploy VM verde, live www verificato**). Migration su disco **166 file** (`000001..000168`; +`000166` me-goals-self, +`000167` user-pay-slips, +`000168` me-f5-personal-area). DB: `sys_user_pay_slips` **42** (14 utenti); `goal_subject_user_id` backfillato **632**/1067; RBAC **155 perm / 685 map** (+`goal:read:self`×4). Mappatura iniziale via **workflow 5-agenti** (F3b/F4/F5/GTM/D-46 contro il codice reale). Batch delegato da Enzo ("affrontali tutti esclusi wait-input", ordine D-46→F3b→F4→GTM→F5).

- **D-48** (`5e52f0a`): `vm-deploy.sh` verify `curl /readyz --retry 45 --retry-connrefused --retry-all-errors` → niente più falso-negativo sul boot ~40s.
- **D-46** (`d6e6f46`): CHECK `sys_ui_interfaces_perspective` ristretta ai **5 valori-sezione**. Le 22 migration interface upstream (+CHECK inline 000050) riscritte per seedare DIRETTAMENTE il valore-sezione finale (script content-based, 47 token); 000163 stringe la CHECK. **Verificata su DB scratch fresco** sulla VM (interface-chain ×2 = rerun OK, end-state 47 righe/5 sezioni, PET `ENTERPRISE` rifiutato). Inerte sui DB deployati (ON CONFLICT DO NOTHING). Bijection route→sezione confermata (0 orfani).
- **F3b** (`dc5d04f`): `/me/career` (pagina flat rotta) → **3 sub-tab** [Obiettivi | Percorsi | Rischio & Successione] via `ProfileTabs`, 3 endpoint self read-only (`/v1/me/{goals,risk,career-paths}`). **Obiettivi NON era bloccato** (la mappa non aveva testato la chiave I14): mig **000166** backfilla `goal_subject_user_id` via `'LEGACY_EMP::'||goal_metadata->>'legacy_employee_id'` (632→159 utenti) + seed `goal:read:self` (4 ruoli ESS). Rischio = flight-risk latest + succession DISTINCT ON position; Percorsi = career-paths dalla posizione PRIMARY. integration 5/5 + Playwright verde (Tommaso 4 goal, flight LOW, 3 succession).
- **F4** (`556e82b`): tab **Cedolini** in `/me/profile` (5° tab). Mig **000167** `sys_user_pay_slips` (mirror 000165, status varchar+CHECK RD-08) + generator `gen-pay-slips-seed.sql` + seed `16_user_pay_slips.generated.sql` (66 legacy `employee_pay_stubs` → **42 righe / 14 utenti** via I14; v5-PK su id legacy + ON CONFLICT DO NOTHING = empty-diff). `GET /v1/me/pay-slips` (riusa `user_profile:read:self`). integration 3/3 (Antonio 3 cedolini, Tommaso empty) + Playwright verde.
- **GTM #4** (`86bbf2a`): la mappa ha rivelato i deliverable **già implementati** (one-pager `/investors` + demo `/demo` + F1 `lead_source` + F2 platform-stats + 2 E2E) → **verify + riconciliazione**: STATIC_FACTS aggiornati (modules 85/endpoints 466/migrations 165/apiTests 1098/e2e 123/pages 98, provenance S1011); moat skills **21939→14093** corretto nel doc-spec (il tile è live da F2, auto-corregge); spec → **IMPLEMENTED**. i18n 1693×2 + investors/demo E2E 4/4 verdi.
- **Regressione 000142** (`615ef1d`): `goal:read:self` (resource `goal`) ha rotto l'asserzione esatta `<> 8` di `000142` al 1° deploy (DB già con la perm = 9) → relax a **floor `< 8`** (le aggiunte successive sono legittime).
- **Deploy + live www**: `vm-deploy.sh` (2° run dopo il fix 000142) verde — readyz OK + /login 200. Verifica LIVE su www.heuresys.com: `/investors` 200, `platform-stats skills=14093`, `/api/v1/me/{goals,risk,career-paths,pay-slips}` = **401** (deployate), CHECK perspective = **solo 5 valori**, `goal:read:self` grant 4, pay_slips 42.
- **Correttivo memoria S1011**: bandito dall'output il qualificatore "no-PII/synthetic/ADR-0023" (Enzo stufo, ricorrente).
- **CLAUDE.md no-PII** (`adffc61`, stessa sessione su richiesta Enzo): OUTPUT RULE in §*Data provenance* + I12/I15 ritirano il qualificatore come descrittore, **sostanza intatta** (no anonymization layer, treat-as-real, ADR-0023 = data-source).
- **F5 COMPLETO** (`a280de3`) — chiude #23: mig **000168** (3 nav PERSONAL + `approval:read:self` granted ×4) + fix `000133` a floor (regressione perm-count come 000142). 3 pagine: **/me/analytics** (`GET /v1/me/analytics` aggregator: attendance-trend `date_trunc` + KPI goals/skills/rating/leave, EChartsCard) · **/me/org-chart** (page-only, riusa grafo ORG_CHART live + `/v1/me/positions` per highlight nodo proprio; USER ha `visualization:read`) · **/me/approvals** (track-only `GET /v1/me/approvals` `created_by=self`; 0 richieste → empty-state reale; Enzo: solo-consultazione). integration 3/3 + Playwright 3/3 + typecheck/lint/i18n verdi.
- **Incidente deploy (→ D-49) RISOLTO**: il 1° `vm-deploy.sh` è andato in timeout sul client SSH (10min, SIGTERM) interrompendo il deploy → build web incompleto (`.next` senza prerender-manifest) + API non riavviata (restava su dist vecchio). Recovery: restart API (nuovo dist, mappingsLoaded 689) + rebuild web con `NEXT_PUBLIC_API_PROXY_BASE_URL=http://localhost:8013` (vm-deploy lo setta inline a riga 133; un build env-less inlina :3001 → ECONNREFUSED) + restart web. **Live www verificato**: `/api/readyz` 200 `database:ok`, route `/v1/me/{goals,risk,career-paths,pay-slips,analytics,approvals}` = 401, pagine F5 = 307, skills 14093.

## Delta S1010 (2026-06-29) — Personal area /me a navtab + import anagrafica dal legacy (F1+F2+F3a LIVE PROD)

HEAD **`2db2757`** (5 commit pushati: `1950a7b` F1 · `a426516` doc-bias · `b538aae` test-S1009-debt · `568ab92` F2 · `2db2757` F3a; **3 deploy VM**). Migration su disco **163 file** (`000001..000165`, gap 000035+000139; +`000164` 6 satelliti anagrafici, +`000165` `sys_user_contracts`). DB: nuove tabelle persona popolate da import legacy — demographics **161** · identity-docs 328 · addresses 169 · family 150 · bank 156 · employment 161 · contracts 158. API: modulo `me` **38 route** (+4: `/profile/full`, `/contracts`, `/performance`, `/attendance`). Programma "portale legacy → /me", decisione IA **Ibrido** (Enzo: profilo a tab per i dati fermi; aree dinamiche come sub-tab nelle voci).

- **Codebase legacy chiarito**: il portale a 10 tab live (`:3012/portal/*`) gira da **`/home/ubuntu/heuresys.com.evo`** (Next 16 + api-gateway Express dockerizzato `heuresys_evo_api_gateway` :8012 + DB container `heuresys_evo_platform_db` pgvector). I cloni `D:\evo.heuresys.com` + VM `heuresys-evo` sono **S63 vecchi** (route `(app)/me/*`, no `/portal/`) — codice fantasma. Schema `employees` 95-col (VIEW su core/pii/hr/payroll post vertical-split) confermato dai dati reali nel browser.
- **F1** (`1950a7b`): mig 000164 — 6 satelliti `sys.sys_user_{demographics,identity_documents,addresses,family_members,bank_details,employment}` (I3/I4 + RD-08/09, idempotenti). Import via `db/scripts/gen-anagraphic-seed.sql` (`format(%L)` legacy → seed `db/seeds/rtl-rebuild/14_*`, match `'LEGACY_EMP::'||employees.id`, ON CONFLICT/delete-then-insert idempotente). Endpoint `GET /v1/me/profile/full` (aggregato read self) + schemi `@heuresys/shared`; UI `/me/profile` a tab **Panoramica** + **Organizzazione** via `ProfileTabs` (`apps/web/src/components/profile-tabs.tsx`, `?tab=`+lazy-mount), Impostazioni preservate.
- **F2** (`568ab92`): mig 000165 `sys_user_contracts` (1:N) + import dal legacy `contracts` (158, seed 15_*); endpoint `/v1/me/contracts`; tab **Contratti** + **Documenti** (wiring `/v1/me/documents`, 657 doc).
- **F3a** (`2db2757`): My HR (`/me`) a sub-tab **Riepilogo** (landing preservata) + **Performance** (`/v1/me/performance`) + **Presenze** (`/v1/me/attendance`) — read-only consultazione, dati già nel DB. Fix `potentialRating` varchar (HIGH/MEDIUM/LOW) colto da ResponseSerializationError in test.
- **Correttivo bias no-PII** (`a426516` + memoria): doc `EMPLOYEE_CENTRIC_MAPPING_DOCTRINE.md:70` corretto (satelliti-da-implementare vs I8 payroll-execution) + memoria `data-treatment-no-privacy-concerns` con blocco anti-trigger. **Debito S1009 colto** (`b538aae` → D-47): `me-preferences.spec` aggiornato al toggle-lingua header (CI smoke `smoke-5-personas`-only non lo copriva).
- **Gate** (per fase): typecheck 4/4 · i18n parity (1533→1649) · vitest `me-profile-full` 5/5 · next build prod · E2E `me-profile-tabs` 10/10 + `me-myhr-tabs` + `me-pages`/`me-preferences` no-regressione 17/17. **3 deploy VM** + smoke www (`/me/profile/full`·`/contracts`·`/performance`·`/attendance` = 401 live). **Residuo**: F3b (career sub-tab) · F4 (payroll consult) · F5 (IA+route residue) → backlog.

## Delta S1009 (2026-06-28) — sidebar IA redesign: 5 sezioni + lingua header + tab-merge (LIVE PROD)

HEAD **`c4b5424`** (2 commit `120071a` redesign + `c4b5424` fix-idempotenza, pushati; **CI 7/7 verde**, deploy VM verde). Migration su disco **161 file** (`000001..000163`, gap 000035+000139; +`000163` sidebar-5-sezioni). DB/API/web counts invariati salvo il registro `sys_ui_interfaces`: **38 voci sidebar attive** (8 Panoramica + 8 Governance + 9 Forza-lavoro + 4 Intelligence + 9 Area-personale) + **9 pagine assorbite** `is_active=false` (route vive). Continuazione richieste Enzo dopo #21.

- **Modello**: le 3 prospettive PET (PROCESS/ENTERPRISE/TALENT) sostituite da **5 sezioni** (OVERVIEW/GOVERNANCE/WORKFORCE/INTELLIGENCE/PERSONAL) — `sys_ui_interfaces.perspective` riusato (minimal blast-radius), response-key `perspectives` invariata. `PerspectiveCodeSchema` (shared) 5 valori; `me/service` `UI_PERSPECTIVES` 5 sezioni; il web layout raggruppa per sezione (collassabili) senza filtro.
- **Req 1 — Dashboard prima**: dashboard è `perspective=OVERVIEW order=0` → primo item del primo gruppo (verificato live `nav-dashboard` first).
- **Req 2 — lingua nell'header**: il toggle `DashboardHeader` (esisteva ma no-op, nascosto da G-03) è ora cablato (`onToggleLanguage` → `setLocale` + PATCH `sys_user_preferences`); `LanguageSwitcher` rimosso dalla sidebar + CSS-hide G-03 rimosso. Ereditarietà tra pagine già garantita da `PreferencesApplier` (server source-of-truth + cookie). Verificato live: it→en persiste dopo reload.
- **Req 3 — prospettiva**: selettore PET **ritirato** (le 5 sezioni sono gruppi collassabili sempre visibili, stato open/closed già in localStorage `heuresys-sidebar-groups`). Chiavi i18n `nav.perspective` rimosse.
- **Merge = navigazione** (decisione Enzo, non content-rewrite): 6 voci-merge (Analisi Organizzativa, Analisi presenze, Analisi Skill, Analisi retributiva, Analisi KPI, Carriera & successione) inglobano 9 pagine come **tab in-page** (`apps/web/src/components/section-tabs.tsx`, montato 1× nel layout, auto-determina il gruppo dal pathname con boundary-match, null fuori dai gruppi); le 9 route restano vive (`is_active=false` le toglie solo dalla sidebar). Rinominate: Organizzazione→Analisi Organizzativa, Copertura competenze→Analisi Skill, Obiettivi→Analisi Obiettivi, OKR→Analisi OKR; `/content`→Intelligence (decisione Enzo).
- **Intoppo idempotenza-chain (→ D-46)**: la 000163 ristringeva il CHECK `perspective` ai 5 valori, ma le mig interfaces 000050+ re-inseriscono valori PET su ogni re-run prima del remap (la riga è validata prima di ON CONFLICT) → il deploy VM falliva a 000050. Fix: CHECK allargato a **unione PET∪5-sezioni** (le righe finali sono tutte 5-sezioni; PET solo transitorio upstream).
- **Gate**: typecheck 5/5 + typecheck:test + i18n parity 1533 + `me-interfaces` integration **5/5** (5 sezioni, gating, dashboard-first, tab esclusi) + a11y E2E **40 verde** (`/dashboard` `/skills` `/career-succession` axe 0, TabNav a11y-clean). **CI 7/7 verde**. Deploy: push → `vm-deploy.sh` (×2: 1° fallito a 000050 pre-fix, 2° verde) → **verifica LIVE www** con login reale: 5 sezioni in ordine, dashboard prima, lingua header persiste, TabNav 6 tab su Analisi Skill, no selettore prospettiva.

## Delta S1008 (2026-06-27) — #21 residuo tail audit: a11y shell + perf subpath split (LIVE PROD)

HEAD **`dd8deb8`** (1 commit advanced pushato + handoff; lib `ux-design-shared` commit `c31e4c7` + `@heuresys/ui@0.1.9` pubblicato npm). Counts DB/API/web **invariati** (nessuna migration/modulo/endpoint nuovo): 160 file migration (max 000162), 162 utenti, RBAC 12/154/681. Cambia solo la dep design-system: **`@heuresys/ui` 0.1.8 → 0.1.9** (root + web + showcase).

- **Gate sciolto alla radice** (era #21 GATED su `ux-design-shared`, lib-blocked): misurate le violazioni reali dal report axe S1006 (`audit/a11y/dashboard-admin-axe.json`) invece di assumere — il gate era anche **parzialmente obsoleto** (la 0.1.8 già consumata aveva 2 fix a11y dentro: scrollable-region 0.1.5/0.1.6).
- **a11y shell** (`@heuresys/ui@0.1.9`, lib `c31e4c7`): (1) `DashboardShell` scroll-container `<main tabIndex=0>` → `<div tabIndex=0>` — elimina 3 violazioni axe insieme (`landmark-no-duplicate-main` + `landmark-unique` + `landmark-main-is-top-level`), perché ogni pagina rende già il proprio `<main>` e lo shell ne annidava un secondo; (2) `sidebar-group-toggle` `min-h-6` (era 23px → 24px, WCAG 2.5.8 target-size, ×9 toggle); (3) `AuditFeed` `<ul>` + `LogStream` `<ol>` scroll-region `tabIndex=0` + `aria-label` (`scrollable-region-focusable`, serious). +5 regression-guard `dashboard-a11y-21.test.tsx`.
- **perf chunk-split** (#21): il barrel `@heuresys/ui` esponeva solo `.` → il `next/dynamic(() => import("@heuresys/ui"))` in `apps/web/_charts-client.tsx` tirava l'intero index (469 export) in un unico chunk on-demand ~1.68MB. Aggiunti **subpath exports** `./charts` (entry-charts.ts → EChartsCard/echarts) + `./markdown` (entry-markdown.ts → MermaidDiagram/mermaid) via **tsup multi-entry**; `_charts-client.tsx` rewire a `@heuresys/ui/charts` + `/markdown`. Risultato: echarts (1.1MB), mermaid (3.9MB), cytoscape (420KB) ora in **chunk separati** — ogni route carica solo la libreria pesante che usa.
- **Gate**: lib build (tsup) + typecheck + **116 test** verdi · advanced typecheck 5/5 ws + web build (subpath risolti) + a11y E2E **42/42** prod-build su DB reale (`/dashboard` axe 0 viol.) + echarts subpath E2E (`analytics-kpi`). Bump + rewire `dd8deb8`.
- **Close**: push `origin/main` → **CI 9/9 verde** → `vm-deploy.sh` (VM ha 0.1.9 + subpath dist, systemd active, `/readyz`+`/login` 200) → **verifica LIVE www.heuresys.com** con login reale `admin@`: `/dashboard` axe **0 violazioni** (critical/serious/moderate/minor tutti 0), `<main>`=1 (era 2), `sidebar-group-toggle` height **24px** (era 23), feed scroll `tabindex=0`. DoD ADR-0026 soddisfatto (live su dati reali). #21 → DONE.

## Delta S1006 (2026-06-24) — audit forense QA E2E 74 pagine + ~20 fix live PROD + clean + G-01 i18n-dati

HEAD **`1ddee96`** (~20 commit pushati incl. continuazione #21, 3× `vm-deploy.sh` + CSP nginx). Migration su disco **160 file** (`000001..000162`, gap 000035+000139; +8: `000155` grant-USER-functional, `000156` KPI-IT, `000157` process-IT, `000158` non-ESCO-skill-IT, `000159` ESCO-skill-IT 13k, `000160` clean-junk-skills, `000161` clean-skill-code, `000162` ESCO-skill-IT-LLM 817). DB: **skills 14093** (era 21939, -7846 junk archiviati `audit.skills_junk_archive`), RBAC **681 map**, users **162**. Audit consegnato: `audit/{FINDINGS.md, FORENSIC-NOTES-S1006-cli.md, _visual-confirmed.json}`.

- **Audit forense QA E2E** (74 pagine autenticate, PROD dati reali): batch headless CORE1-6 + ri-verifica con re-login (anti token-TTL 15min) + pass ESS persona dipendente reale (paolo.caputo) + scansioni perf/a11y/security + **workflow visivo multi-agent** (17 unità, 98 finding confermati su 104 con verifica adversariale). 58/74 pagine con ≥1 finding.
- **Fix 🔴 (deployati+verificati live PROD)**: **B-01** `/brownfield-adaptation` mis-wired al contratto (type locali inventavano campi → crash + colonne vuote; riallineato ai type condivisi). **B-03** ESS lockout RBAC: dipendenti con ruoli funzionali (MANAGER/TEAM_*) senza `USER` → 403 sul proprio ESS; fix additivo (ogni dipendente porta anche USER, mig **000155**, decisione Enzo). **B-04** gauge KPI 4.3 mld = bug formato (dato 91.84% reale; arrotondato). **B-05** chart dark-theme neri = CSS-var `hsl(var(--x))` non risolvibili nel canvas echarts → wrapper unico che risolve/unwrap (7 pagine, no lib publish). **B-06** brownfield/learning/positions contract-drift + JOIN nomi skill/KPI. **B-07** org-chart `label.show:true`.
- **Fix 🟡**: **B-02** login `method=post` (no creds in URL su submit pre-hydration). **G-03** toggle lingua duplicato (header lib no-op nascosto via CSS, sidebar funzionale + E2E intatti). **CSP + Permissions-Policy** su nginx live (erano MISSING).
- **G-01 i18n-dati** (decisione Enzo = tradurre nel DB, IT-canonical): KPI 14 (000156), processi BPM 23 (000157), skill non-ESCO reali 41 (000158), **skill ESCO 13.032** via API ESCO ufficiale `language=it` (000159; 817 senza label IT = residuo). Skill code legacy `OLDDB::esco_skills::`→`ESCO::` / `OLDDB::competencies::`→`COMP::` (000161).
- **Clean**: 7846 record junk in sys_skills (`OLDDB::<table>::<uuid>` = entità non-skill mis-importate, `Qx Skills Analysis`, `5-Point Scale`) orfani (0 ref nelle tabelle d'uso) → archiviati in `audit.skills_junk_archive` (reversibile) + rimossi (mig **000160**); catalogo `/skills` 21939→14093.
- **G-02**: org PARENT (nome OU padre) + gaps (user/position/skill name) via correlated-subquery sui list-endpoint; schemi shared estesi (nullable).
- **DATA = NON-BUG** (verificato su dati reali): dashboard "—0.0% vs settimana prec." = trend reale piatto (dati synthetic statici); insights "tutti Medio" = distribuzione reale (flight-risk 120 LOW + 39 MEDIUM, nessun HIGH).
- **Deploy**: push → `vm-deploy.sh` (build api/web + restart, API boot ~12s) → CSP applicato al file nginx live (`nginx -t` + reload, backup) → tutto ri-verificato su `www.heuresys.com` (login 200, 0 errori JS/CSP). I fix-DATI (G-01/B-03/clean) erano già live su PROD (DB condiviso). **MFA resta OFF** (decisione Enzo 2026-06-24).
- **Residuo tail #21 — risolto (continuazione S1006)**: ✅ **career-succession G-02** (join user/pool name + fix contract `successionPoolId`→`poolId`, deployato+verificato PROD) · ✅ **817 stale-ESCO** tradotti via **workflow LLM parallelo** (mig 000162; URI ESCO 404 = concetti stale, no label ufficiale IT → LLM-assisted, decisione Enzo) → **ESCO IT 14002/14002 = 100%**. ⛔ **a11y + perf = LIB-BLOCKED** (`@heuresys/ui`/ux-design-shared, fuori da questo repo): a11y dashboard — tutte le violazioni (tap-target/landmark/scrollable) sono nei componenti shell del lib; perf — il chunk è il barrel `@heuresys/ui` (echarts+three+d3), espone solo `.` (no subpath) → split richiede subpath exports nel lib + publish. Backlog **#21 → GATED** (unblock = sessione su ux-design-shared). HEAD post-#21 **`1ddee96`** (160 migration, max 000162).

## Delta S1005 (2026-06-23) — cleanup doc is_synthetic + verifica users-page + Wave-3 L1 già-fatto

HEAD **`10449b2`** (2 commit `c3ac0cc`/`10449b2` + handoff). Migration **invariate** (152 file, max `000154`; nessuna nuova — sessione doc + verifica + register). Counts DB/API/web/CI invariati da S1004. Batch menu #18/#19/#17 (delega Enzo).

- **#18 doc-cleanup `is_synthetic`** (`c3ac0cc`, DEBT **D-44 RISOLTO**): 8 doc design/plan (`TARGET_SCHEMA_DESIGN`, `MIGRATION_IMPLEMENTATION_PLAN`, `BOOTSTRAP_EXECUTION_PLAN`, brownfield {IMPORT_PLAN, ADAPTATION_MAP, TABLE_CLASSIFICATION}, `MVP_4_ROADMAP`, `AUTH_SECURITY_PLAN`) allineati a ADR-0026/mig 000154: colonna `user_is_synthetic` rimossa, `SYNTHETIC_REFERENCE`→`STANDARD` (persone importate = membri reali del tenant, ADR-0024/0026) / `GENERATED_INCUMBENT` (placeholder tenant-materialization), vista `v_synthetic_user_flag_consistency` tolta dalle liste di validazione live (DDL storico di 000023 con nota di ritiro, storia non falsificata). Grep verde (restano solo menzioni-di-ritiro). **Marginali fuori-scope segnalati e non toccati**: `wave_runners/{wave_2,wave_3}_runner.md`, `due-diligence/WS-T5.md` (cita 7 viste → ora 6).
- **#19 E2E users-page post-synthetic** (verifica, no artefatto): confermato LIVE che il ritiro colonna non rompe `/users` — login reale `admin@heuresys.com` → `GET /v1/users` **200** con utenti RTL reali, `type:STANDARD`, **zero campo `is_synthetic`** nella response; DB census (colonna assente, 162 utenti tutti `STANDARD`); codice TS **zero-ref** alla colonna. Prod-E2E UI (`admin-lists.spec`) coperta da CI, non rieseguita in locale (P3, thin-client dell'endpoint verificato).
- **#17 Wave-3 L1** — discovery: il **L1 (fix Heuresys System) era già eseguito** in S987/S988 (mig `000110` remap `d5855519`→HEURESYS + `000111` import `chiara.spenuso` WITH login; tenant HEURESYS live: 4 utenti / 4 pos / 3 OU, mapping corretto). La premessa del menu derivava dal dossier `POST_V1_ROADMAP §1.B` (pre-S987, stale). **Nessuna scrittura su dati prod** (R14 anti-bias: verificato prima di EXEC). Register #17 aggiornato: residuo = onboarding **L2/L3 multi-industry** (SmartFood/EcoNova, non-banking) = decisione di prodotto (autorità Enzo), resta HOLD per scelta esplicita di Enzo ("solo aggiorna register"). **TODO doc** (next handoff): aggiornare `POST_V1_ROADMAP §1.B` per non riproporre L1 come da-fare.

## Delta S1004 (2026-06-22) — DBMS health-check + ADR-0026 doctrine + retire is_synthetic

HEAD **`6169ee0`** (3 commit pushati `a51fb88`/`72cd9ae`/`6169ee0` + handoff). Migration su disco **152 file** (`000001..000154`, gap 000035+000139; +`000154` retire-synthetic-flag). Viste sys **12** (era 13: `v_synthetic_user_flag_consistency` ritirata). ADR **24** (+`0026`). user_type values ora **STANDARD / GENERATED_INCUMBENT / SERVICE**.

- **Health-check forense DBMS**: census esatto 263 tabelle (188 sys + 62 staging + 8 brownfield + 5 audit) + completezza per-colonna 1853 + matrice copertura **73 pagine web** (workflow 30-agenti map+verify+adversariale): **46 LIVE / 19 PARTIAL / 8 EMPTY**; viste-guardrail tutte 0 (integrità pulita). Distinzione **gap-DATI** (serbatoio vuoto: me/career·approvals·me/handbook·compensation-intelligence·seed-acquisition·positions/[id]/learning) **vs gap-CODICE** (bug binding API↔frontend: brownfield-adaptation crash · tenants typing-tab · positions/[id]/kpis · me/kpis · me/learning · me/gaps · blueprints Industry). Artefatti in `qa_artifacts/dbms_health_2026-06-22/` (FINAL_REPORT + 4 CSV).
- **Flusso graphify ripetibile**: `scripts/db-health-graph.sh` (tunnel-check → census CSV → `_export.sql` esteso `05_data_health`+`06_webapp_coverage` → grafo). Grafo-conoscenza DBMS in `graphify-db-input/graphify-out/` (265 nodi/498 edge/27 community, gitignored, dir-isolata dal codebase-graph). Refresh: `cd graphify-db-input && /graphify . --update`.
- **ADR-0026** (`a51fb88`): un solo ambiente = **produzione**; RTL Bank (customer-example) + Heuresys System (platform/system) = tenant di produzione correnti, NON "di test"; dati synthetic-by-provenance (no-PII, ADR-0023) **trattati come reali**; 2 percorsi (prospect pubblico→demo / utente→login per profilo). **CLAUDE.md invariante I15** + DoD riformulata ("tenant di TEST"/"mai produzione" **ritirati**). **Audit forense 12-agenti**: codice **già conforme** (0 NEEDS_REFACTOR, nessun gate test/prod sui dati); 7 contraddizioni doc terminologiche allineate (WORKITEM_GAP1_PHASE0/PERSPECTIVES, agent_sdk_plan, D-14 ×5, D-11, FINAL_REPORT). Memoria `project_production_grade_two_tenants`.
- **Refactor A — retire is_synthetic** (`6169ee0`, decisione Enzo): la colonna tracciava una dicotomia synthetic-vs-reale inesistente (il legacy è esso stesso synthetic) e non era mai un gate → rimossa (colonna+vista+CHECK consistency+index, mig **000154**); user_type `SYNTHETIC_REFERENCE`→`GENERATED_INCUMBENT` (placeholder generati da blueprint). `000004`/`000023`/`000111` resi twice-run idempotenti (CHECK user_type transition-tolerant). Shared+API users+tenant-materialization+web users-detail+i18n+validate/seed scripts. Gate: typecheck 5/5 · 26 test API · migrate ×2 idempotente · db:validate (6 viste=0, empty diff) · **deploy VM PROD live verde** (`/users` 401 non-500, `/readyz` 200). NB sequenza: drop-schema sul DB condiviso applicato PRIMA del deploy-codice VM → finestra di incoerenza chiusa col `vm-deploy` (lezione DEBT D-45).

## Delta S1003 (2026-06-22) — #4 go-to-market deliverable 2+3: investor one-pager + demo guidata (LIVE)

HEAD **`102d49c`** (13 commit `fa9ffa4`→`102d49c`, pushati al handoff). Counts ri-derivati: migration **151 file / 000153 max** (+000153 lead_source enum) · DB ledger **151** · moduli **85 register** (+`public-stats`; ~80 business) · web **98 page.tsx** (+`/investors` +`/demo`) · API test **160 file** (+`public-stats.integration`) · Playwright **55 spec** (+`investors.spec` +`demo.spec`; `capture-demo.gen.ts` fuori-suite) · i18n **10 namespace** (+`investors` +`demo`). Stack invariato (zero nuove runtime dep). Brainstorm→spec→plan: `docs/superpowers/{specs,plans}/2026-06-22-gtm-investor-onepager-and-guided-demo*`.

- **Fondazione (API-first)**: (F1) `lead_source` enum WEBSITE/INVESTOR/DEMO (mig **000153** idempotente, RD-08 varchar+CHECK) + `LeadCreateSchema.source` + `LeadForm` prop (default WEBSITE = landing behavior-preserved) → segmenta la pipeline lead; leads suite 9/9 (3 nuovi con `remoteAddress` distinti vs rate-limit 5/min). (F2) nuovo modulo `public-stats` → **`GET /v1/public/platform-stats`** pubblico (no-auth/no-CSRF, rate-limit 30/min, TTL-cache 5min, aggregate-only no-PII): count live skills/edges/users/positions/OU/teams/RBAC/UI-interfaces/tenancies; integration 2/2.
- **Deliverable 2 — investor one-pager** (`/investors`, public): metriche **live** da F2 (`usePlatformStats`) + 6 fatti codebase dichiarati con provenance + 3 wedge + traction onesta + roadmap qualitativa + soft-CTA `LeadForm source=INVESTOR`. **Export PDF** = print-CSS (`window.print()` + `@media print`, la pagina È il PDF, zero dep). **Decisioni Enzo**: ask=teaser-no-numbers · tono=onesto-sotto-promessa. i18n `investors` it+en. E2E `investors.spec` 2/2 live (render + metrica live + INVESTOR lead reale).
- **Deliverable 3 — demo guidata** (`/demo`, public): tour scriptato 10 tappe, **screenshot reali** catturati dal vivo su RTL_BANK via `capture-demo.gen.ts` (project Playwright on-demand `capture-demo`, riusa persona storageState post-MFA; `.gen.ts` fuori dal testMatch default → mai in CI/full-suite). Sequenza: landing→org-chart(158 pos)→positions→skills-heatmap→succession(462)→**maturità L0-L5 (wow)**→RACI→matching ESCO→team→dashboard. CTA `LeadForm source=DEMO`. i18n `demo` it+en. E2E `demo.spec` 2/2 live.
- **Fix collaterale (R3/R17c — debito pre-esistente, D-43)**: `organization-unit-processes-raci-demo.integration.test.ts` era stale dopo il refactor RACI demo→produzione S1002 (cercava 12 demo tag `S994-#11`, trovava 0 → file rosso nella full-suite, già rosso pre-sessione) → ri-puntato al seed produzione (tag `S1002-#5/#11`), 4 assert role ri-verificati live (DIV-RISK/02 OWNER→INFORMED · DIV-LEGAL/11 CONSULTED→OWNER · DIR-RISKM/10 CONTRIBUTOR→OWNER · DIV-OPS/12 INFORMED inv.). 6/6.
- **Full-close**: 13 commit pushati + **CI 9/9 verde** (test-api/playwright-smoke/typecheck/lint/i18n/build-web/showcase) + **deploy PROD** `vm-deploy.sh` exit 0 (000153 registrata, build shared→api→web, restart active/active, readyz OK) + **verifica live**: `/investors` 200 · `/demo` 200 · `platform-stats` JSON live · screenshot serviti · **lead INVESTOR+DEMO reali creati su PROD e rimossi** (DoD live-data soddisfatto).

## Delta S1002-GTM (2026-06-21) — #4 go-to-market primo deliverable SHIPPED + #13 + (c)

HEAD **`19b6d2f`** (17 commit non pushati al momento del brief; pushati al handoff). Counts ri-derivati: migration **150 file / 000152 max** (+000151 registry-reconcile, +000152 leads) · DB ledger **150** · moduli business **79** (+`leads`; 84 register incl. infra) · web **96 page.tsx** (+`/privacy`; la landing sostituisce la root esistente) · RBAC **12 ruoli / 154 perm / 681 map** (+`leads:read`→PLATFORM_ADMIN-only) · API test **159 file** (+leads.integration +capability-maturity-rubric) · i18n **8 namespace** (+`landing`) · nuova tabella `sys.sys_leads`. Stack invariato.

- **#13 B-50 bridges CHIUSO** (`52a5fd1`, mig 000151): i due bridge erano già risolti (location→org_unit = `sys_branches` S982; job→position = 8 tabelle popolate via derivazioni S960-982; il crosswalk 1:1 pulito è muro ESCO terminale). Residuo = doc-drift: 28 righe `sys_reconciliation_registry` con `declared_status=NEEDS_DECISION` stale (la vista `v_reconciliation_status` già le risolveva POPULATED via has_rows) → 000152... err 000151 flippa le 28 a IMPORT (lista esplicita verificata has_rows + assert), registry coerente con vista (**0 NEEDS_DECISION**). Idempotente, chain D-38-safe (full migrate verde), suite reconciliation 59/59. Esce da HOLD.
- **(c) rubrica Maturity versionata** (`64dbf80`): estratta la ladder L0-L5 da numeri-magici hard-coded in `deriveLevel` a un **registry config versionato** `apps/api/src/modules/capability-maturity/rubric.ts` (`MaturityRubric` + `RUBRICS["v1-full"]` verbatim + `ACTIVE_RUBRIC_VERSION` + `getRubric`). Reviewable (soglie in un blocco nominato) + versionabile (aggiungere `v2-*` + flip; gli score registrano `rubric_version`). **Behavior-preserving** (v1-full identica → distribuzione live invariata L2/L3/L5 = 1/8/11). Unit test `capability-maturity-rubric` 4/4 (extraction-faithful + explicit===default) + integration 9/9. Chiude il Gap#1 follow-up al 100%.
- **#4 GO-TO-MARKET — primo deliverable SHIPPED** (13 commit GTM `fcdc61f→19b6d2f` + spec/piano): **front-door landing pubblica** su `www.heuresys.com/` (sostituisce `redirect("/login")`) + **lead capture GDPR**. Brainstorming→spec→piano→esecuzione **subagent-driven** (11 task API-first, fresh subagent/task + 2-stage review + final whole-branch review opus). Componenti: mig `000152` `sys_leads` (real opt-in PII + consenso `consent_at`/`consent_version`, registry EXCLUDE bucket-D); perm `leads:read` **PLATFORM_ADMIN-only**; modulo `/v1/leads` (**POST pubblico** no-auth/no-CSRF + honeypot-swallow + rate-limit 5/min / **GET gated**, CSV-exportable); shared schema `@heuresys/shared/schemas/leads`; landing page (5 sezioni, namespace i18n `landing` it+en); lead form react-hook-form (consenso `z.literal(true)` + link `/privacy` via `<Trans>` + honeypot); pagina pubblica `/privacy`; `proxy.ts` apre `/`+`/privacy`. **4 bug reali intercettati dal processo** (typecheck/eslint NON li prendevano): 000005 `CROSS JOIN ... NOT IN` re-grant leakava `leads:read` a TENANT_ADMIN su chain re-run → fix 000152 autoritativa (DELETE non-PLATFORM_ADMIN + assert exactly-1-role); honeypot schema `.max(0)` rifiutava a validation → rimosso (service ingoia); GET unauth è 401 non 403 (+caso non-admin→403); form `companySize` `<select>` vuoto=`""` ma Zod enum vuole `undefined` → `setValueAs` (scovato dall'**E2E live**). Final review opus needs-fixes→**I3** (notice GDPR era testo non link) fixato (consent link + pagina /privacy). Gate: typecheck all-ws + i18n parity 8ns + integration leads 6/6 + full API suite **1094 pass** (3 fail = 1 registry-count fixato + 2 flaky D-20/D-37 pass-isolate) + **landing E2E 2/2 live** + **prova live esplicita** (POST reale→lead salvato `consent_version=2026-06-21-v1`, honeypot→200/0-store). Spec+piano: `docs/superpowers/{specs,plans}/2026-06-21-gtm-front-door-landing-lead-capture*`. **#4 resta ACTIVE** come programma (prossimi deliverable = autorità Enzo).
- **Close**: 17 commit pushati + handoff + close-propagate (align VM+linux-pc + **deploy PROD GTM**). Post-deploy: verifica landing live + submit lead reale + **C1 deploy-verify** (rate-limit per-IP dietro 2 hop → nuovo debt).

## Delta S1002 (2026-06-21) — Gap#1 follow-up CLASS-A: 3/4 sotto-task chiusi live

HEAD **`9ce9a92`** (4 commit `fcdc61f→9ce9a92`: i 3 Gap#1 follow-up + il post-handoff RACI `9ce9a92`; pushati). Migration su disco **148 file** (`000001..000150`, +`000150`; gap 000035+000139), DB ledger **148 applied**. Moduli business **78**, RBAC **12 ruoli / 153 perm / 680 map**, API test file **157**, web **95 page.tsx** — tutti **invariati** (nessun nuovo modulo/endpoint/pagina; 2 pagine modificate). Sessione di prodotto leggera sull'unico item ACTIVE del menu (Gap#1 follow-up CLASS-A). Decisione Enzo: "procedi con tua raccomandazione" → 3/4 sotto-task chiusi, ciascuno **live su dati reali** (DoD).

- **(b) Porta-1 RACI drill-down** (`fcdc61f`): l'endpoint `GET /v1/organization-unit-processes/by-process/:id` (`listForProcess`, perm `organization_unit_processes:read`) **esisteva già** ma la console `/process-owner` era un catalogo piatto (il commento nel codice lo flaggava "next layer"). Wiring drill-down: selezionando un processo carica le OU assegnate col ruolo RACI tone-coded (OWNER·A/CONTRIBUTOR·R/CONSULTED·C/INFORMED·I), empty-state reale a ogni livello. Composto da `@heuresys/ui` (PageHeader+EntityTable) + tipo shared `OrgUnitProcessForProcess`. i18n it+en. E2E: drill di *Human Capital Management* (2 OU OWNER+CONTRIBUTOR su RTL_BANK) → count+righe live.
- **(a) ORG_DIRECTOR holder-of-record** (`184a5fe`, mig **`000150`**): il ruolo funzionale `ORG_DIRECTOR` (mig 000145) era **holderless** by design (D1-B). Nominato l'holder reale = **Valentina Conti** (`valentina.conti@rtl-bank.org`, HR Director + manager "Divisione Human Resources") — best-fit sulla struttura org live (la capability/maturity-oversight delle OU è il remit dell'HR Director; scelta sopra la CEO Federica già TENANT_ADMIN). **Grant SEMANTICO, non access-change**: Valentina vedeva già la Porta-2 via HRMS_MANAGER (`org_director:read` mappato a HRMS_MANAGER+TENANT_ADMIN+PLATFORM_ADMIN+ORG_DIRECTOR). Resiliente (join email+role_code) + idempotente (NOT-EXISTS guard, fixed `granted_at`); applicato live (1° INSERT 0 1 + assert; 2° INSERT 0 0); chain `migrate.sh` ri-verde ("148 migrations applied", **D-38-safe** — assert scoped, non resource-wide). Valentina ora = HRMS_MANAGER+ORG_DIRECTOR+TEAM_LEADER.
- **(d) radar maturità Org-Director** (`473f848`): la console `/org-director` aveva una tabella capability/maturity piatta. Aggiunto il `CapabilityRadar` (**@heuresys/ui**, SVG — no echarts/SSR-wrapper) drill-down: selezionando un'OU mostra il radar delle 5 dimensioni maturity (composite / skill coverage / KPI achievement / readiness / **gap-freeness** = 1−majorGapRatio) lette dai `criteria[]` della response `/v1/capability/maturity` e normalizzate 0-100 (no nuovo endpoint). Azione disabilitata sulle OU senza score. E2E: drill prima OU scorata → radar SVG da dati live.
- **(c) rubrica `rubric_version` rivedibile** = **DEFERITO → HOLD** (basso valore + scope-decisione: `RUBRIC_VERSION='v1-full'` è costante hard-coded; renderla parametrizzabile ha senso solo quando Enzo vuole soglie diverse, e la v1-full l'ha già scelta lui).
- **Gate**: E2E `gap1-consoles` **9/9 verde** (Node22 wrapper D-36, dev-mode per-spec, dati reali OCI) · typecheck web pulito · eslint pulito · i18n parity **1403 chiavi**.
- **Counts S1002 (ri-derivati psql/ls/git)**: migration **148 file / 000150 max** (+1) · DB ledger **148** · moduli business **78** (invariato) · RBAC **12 ruoli / 153 perm / 680 map** (invariato; +1 user-role grant a Valentina, non conta su role/perm/map) · web **95 page.tsx** (invariato) · API test file **157** (invariato). Stack invariato.
- **Post-handoff — #5/#11 RACI di produzione** (`9ce9a92`, esce da HOLD): Enzo ha sbloccato il HOLD chiedendo a Claude di *proporre* il mapping RACI OU↔processo; proposta evidence-based su org+processi reali → approvata. Seed `db/seeds/reconciliation/54_raci_*.sql` riscritto come SoT dichiarativa (clean-slate DELETE all-RTL + INSERT): **105 assegnazioni** su **23/23 processi** (1 OWNER + 30 CONTRIBUTOR / 29 CONSULTED / 23 INFORMED), OWNER al livello più specifico (DIR-* dove esiste, es. KYC/AML→DIR-AML; HQ possiede governance+audit). Idempotente (v5 PK + timestamp fissi → empty twice-run diff), RTL-only. Fix test collision **D-23**: `organization-unit-processes.integration.test.ts` ora snapshot-restore di DIR-AML/DIR-BACKOFF (owner della mappa) intorno al wipe pristine. Applicato **live sul DB VM condiviso** → PROD mostra già le 105 righe (no re-deploy: nessun runtime code cambiato, solo seed+test). La Porta-1 RACI drill-down (b) ora rende la mappa reale completa. Gate: seed DELETE 13→INSERT 105 + assert; 2° run idempotente; integration **9/9** + post-run count 105 (restore provato); typecheck:test pulito; E2E gap1-consoles **9/9**.
- **Close**: 3 commit Gap#1 follow-up + handoff `b7c703a` pushati + close-propagate full (align VM+linux-pc + deploy PROD verde) — poi post-handoff RACI `9ce9a92` push + propagate **light** (`--no-deploy --clone-db`: source sync + refresh DB linux-pc, no PROD rebuild).

## Delta S1001 (2026-06-21) — design handoff-rigor IMPLEMENTED end-to-end + close-flow live

HEAD **`6bb83d8`** (10 commit `545ec8a→6bb83d8`, tutti pushati). Migration su disco **147 file invariate** (`000001..000149`, gap 000035+000139). Moduli business **78**, RBAC **12 ruoli / 153 perm / 680 map**, API test file **157** — tutti **invariati** (infra pura, nessuna migration/modulo). Lavoro: verifica del design `2026-06-20` (risultava un *subset pragmatico*) → chiusi i 9 gap + implementati §11 P1-P9. `handoff_lint.py` portato a **10 check** (D1-D4·S1-S2·H1-H2·A1-A2), **bloccante di default**, H2 esteso alle sezioni attive del backlog; skill `handoff` → **v5.1** (gate bloccante + push rebase-safe P5); nuovi tool `build_menu.py` (P2) + `journal-append.sh` (P4) + workflow `state-lint.yml` (P6) + boot reality-check (P7); register rinominato `🗂 Action register` (store canonico, P1); `vm-deploy.sh` rende **TUTTE** le unit con `SERVICE_USER` (entrypoint unico VM+linux-pc, fix guard placeholder) + test close-propagate §12.5; `align-claude-ecosystem` `project_memory` parity verify (§13). **Close-flow live** (close-propagate) **exit 0**: VM + linux-pc PROD ri-deployati e verde (`/readyz` + `/login 200`), skill v5.1 + memorie (42) propagate sui 3 host. `claude-mem` enabled=**false** (decisione Enzo) reso **durevole** su vm+linux-pc+mac. **D-40 + D-41 RISOLTI in-session**: D-40 = `reapply_enabled_plugins()` ri-applica gli `enabledPlugins` di Windows dopo il reinstall (i valori Windows vincono, `extraKnownMarketplaces` intatte) → ogni plugin disabilitato resta tale a ogni align; D-41 = `project_memory` verify reso resiliente (sentinel `MEMCOUNT=` + SKIP su host lento, niente più falso `remote=0`). Verify ecosystem ora **CLEAN** su vm+linux-pc (mac live `42=42 OK`). Design doc → **§14 IMPLEMENTED**.

## Delta S1000 (2026-06-20) — infra: handoff-rigor design + 3 bugfix ecosystem + FASE 4 align

HEAD **`5f3e376`** (8 commit `fa9591d→5f3e376`, tutti pushati). Migration su disco **147 file invariate** (`000001..000149`, gap 000035+000139). Moduli business **78** (invariato). RBAC **12 ruoli / 153 perm / 680 map** (invariato). API test file **157** (invariato). Sessione di **infra pura** — nessun modulo prodotto, nessuna migration nuova.

**Handoff-rigor design §3-§5/§12-§13** (pre-compaction):
- `fa9591d` feat(handoff): `docs/kb/tools/handoff_lint.py` — gate deterministico (lint H1/H2/S2/D2/D3) + design spec `2026-06-20-handoff-rigor-and-hold-lane-design.md` §4.
- `6ac3869` docs(handoff): status vocabulary (`ACTIVE`/`GATED`/`WAIT-INPUT`/`HOLD`/`INTERRUPTED`) + HOLD register in `SOT_BACKLOG.md` + menu render §3 + CLAUDE.md §Session start revisionato.
- `0ec57e4` fix(deploy): `vm-deploy.sh` env-parametrico (`SERVICE_USER`/`GROUP`, `PUBLIC_HOST`) per linux-pc twin — D-39 RISOLTO.
- `2af84c6` feat(close): `scripts/close-propagate.sh` — orchestratore canonico two-channel close (repo+payload+deploy via `align-clones` + ecosistema Claude via `align-claude-ecosystem` + clone-db condizionale) + `align-claude-ecosystem.sh` marketplace-SHA verify (design §12.2/§13).
- `0022cc8` docs(deploy): D-39 resolution note + `deploy/README.md` §close-flow.

**3 bug fix in align-claude-ecosystem.sh + close-propagate.sh** (trovati durante FASE 4):
- `936a690` fix(close): rimosso `export MSYS_NO_PATHCONV=1` globale da `close-propagate.sh` — rompeva i path `jq` (POSIX path `/tmp/...`) nei subshell di `align-claude-ecosystem.sh`; il globale era ereditato e convertiva i path in-staging.
- `db3c237` fix(ecosystem): `backup_remote` zsh word-split — su Mac (zsh login shell) `$existing` non viene word-splittato (SH_WORD_SPLIT off di default in zsh) → `tar` riceveva la stringa intera come un argomento; fix: array `to_tar=()` + `${to_tar[@]}`.
- `5f3e376` fix(ecosystem): `cleanup()` trap exit-0 — la funzione terminava con `[ -n "$STAGE" ]` (rc=1 quando STAGE="") sostituendo il codice di uscita del script via `trap cleanup EXIT`; fix: `return 0` esplicito. + `close-propagate.sh` → `--skip-smoke` permanente (Mac SIGILL, vedi sotto).

**Scoperta permanente**: `claude` CLI su Mac Ivy Bridge 2012 (Core i7) dà **SIGILL** rc=132 — il binario Mach-O x86_64 usa istruzioni CPU non supportate dal Core i7 di terza generazione. Documentato in `memory/reference_remote_ssh_deploy_ops.md`. `close-propagate.sh` passa sempre `--skip-smoke`; i file config (CLAUDE.md/skills/settings) arrivano comunque; i plugin non si installano (warning, non FATAL).

**FASE 4 COMPLETO** (EXIT=0 verificato via task output `b327t4kkc`): Mac + VM + linux-pc ricevono payload ecosistema (CLAUDE.md, skills, commands, settings, SDK parity `@anthropic-ai/claude-agent-sdk@0.3.179`/`anthropic==0.109.2`); VM/linux-pc 16 plugin installati; Mac config files pushati, plugin SIGILL-skipped con WARN. Drift segnalato (`drift-{mac,vm,linuxpc}-*.md`): SHA marketplace drift informativo (Opzione C, update manuale per-macchina).

- **Counts S1000 (ri-derivati psql/ls/git)**: migration **147 file / 000149 max** (invariato) · moduli business **78** (invariato) · API test file **157** (invariato) · web pages **95 page.tsx** (ri-derivato via `find`) · RBAC **12 ruoli / 153 perm / 680 map** (invariato) · DB ledger **147 applied** (sys.sys_schema_migrations). Stack invariato.
- **Close**: 8 commit pushati + handoff commit (questo). No align-clones (no nuovo codice da propagare); ecosystem già allineato in-session.

## Delta S999 (2026-06-20) — Gap#1 programma-faro CHIUSO end-to-end + goals/okrs

HEAD **`f133b04`** (6 commit Gap#1 `3d2b0a7→f133b04` + i 12 commit goals/okrs `72fd218→787621f` dello stesso giorno, tutti pushati). Migration su disco **147 file** (`000001..000149`, gap 000035+000139). Moduli business **78** (+`capability-composition` +`capability-maturity`; goals/okrs vivono dentro moduli esistenti). Enzo: "parti autonomamente e porti tutto a conclusione". Decisione di prodotto sciolta: rubrica Maturity = **v1-full** (bande + gate).

- **Step 1 RBAC** (`3d2b0a7`, mig 000145): ruolo `ORG_DIRECTOR` (functional holderless, D1-B) + 4 perms snake_case (`process_owner:read`/`org_director:read`/`capability:read`/`capability:admin`) + 14 map + ORG_DIRECTOR in `UI_ADMIN_ROLES` (service+layout). Fix `8956cbc`: ORG_DIRECTOR aggiunto al `RoleCode` union shared (la cache RBAC lo skippava — verificato live PROD 12 ruoli/680 map/0 skip).
- **Step 3 MLCE** (`b6e54d3`, mig 000146): engine `capability-composition` — composite 0..100 deterministico bottom-up **employee→position→org-unit→org** (roll-up gerarchico pesato = media flat sottoalbero; recursive su `parent_id`, closure D-35 vuota; `COALESCE(economic_weight, criticalityFactor, 1.0)` per il gap NULL) + lineage append-cohort. `sys_capability_scores`+`sys_capability_score_lineage` (registry bucket-D). **317 score live RTL** (148 emp/148 pos/20 OU/1 org), determinismo PASS + D-18 bounded. capability-composition.integration **11/11**.
- **Step 4 Maturity** (`ff89051`, mig 000147): engine `capability-maturity` L0-L5 v1-full — bande composite + gate-criteri SQL-auditable (skill-coverage da MLCE, KPI-achievement da `sys_user_kpi_evidence`, readiness da succession-scores, major-gap da skill-gap), aggregati su incumbent del sottoalbero OU; gated-downgrade. `sys_capability_maturity_scores` (org_unit FK, `capability_ref` future-grain, registry bucket-D). **20 OU live → L2(1)/L3(8)/L5(11)** (composite 92.9 declassato a L2 su KPI debole = audit-defensibile). capability-maturity.integration **9/9** (incl. ponte composite==MLCE).
- **Step 2+5 console UI** (`8638e76`, mig 000148/149): `/org-director` (Porta 2 — capability+maturity per OU dai motori live) + `/process-owner` (Porta 1 — catalogo blueprint-processes; RACI drill-down = layer successivo). Sidebar DB-driven (org_director:read ENTERPRISE / process_owner:read PROCESS). i18n it+en. **E2E gap1-consoles 7/7 live** (federica/TENANT_ADMIN).
- **WI-C demo agente** (`f133b04`, `live-tenant-materialization-acceptance.ts`): agente PLATFORM_ADMIN su **subscription MAX** → gateway SSE → **HITL write-gate** (approval_required `hrx_tenant_materialize`) → allow → `POST /v1/tenant-materialization` mode=plan → MaterializeResult (7 OU/11 pos archetipo RETAIL_BANK_REFERENCE), read-only, zero mutazioni. PASS live.
- **goals/okrs** (stesso giorno, `72fd218→787621f`, mig 000142/143/144): moduli read-only full-stack (shared→repo→service→route→test→pagina→E2E), gruppo sidebar `intelligence`. Pre-Gap#1, non in delta precedenti.
- **Counts S999 (ri-derivati psql/ls/git)**: migration **147 file / 000149 max** · moduli business **78** · **157 API test file** · web pages **74** · RBAC **12 ruoli / 153 perm / 680 map** (+ORG_DIRECTOR +4 capability/console perm; +goals/okrs 8) · `sys_capability_scores` 317 + `sys_capability_maturity_scores` 20 · reconciliation **0 UNCLASSIFIED** (3 nuove tabelle bucket-D EXCLUDE) · stack invariato. CLI nuovi: `capability:recompute`, `capability-maturity:recompute`.
- **Close**: 6 commit pushati + **VM PROD deploy verificato** (`/api/readyz` ok, `/v1/capability/composition`+`/maturity` 401-live, `https://www.heuresys.com/login` 200; boot lento ~45s recuperato da retry D-20) + **Mac allineato** (align-clones mac). CI sul runner VM in drenaggio al handoff (typecheck success; integration/build/smoke serial). **RESIDUO** (autorità Enzo / non bloccante): #4 go-to-market (il *cosa*) · #5 RACI produzione · #13 B-50 bridges · #17 Wave-3 · #8 EMAIL (app-password) · D1-A holder reale ORG_DIRECTOR · Porta-1 RACI drill-down.

## Delta S998 (2026-06-19) — batch autonomo "chiudere tutto": menu 1/2/6/15 + #7 + dispozioni rimanenti

HEAD **`85d6418`** (5 commit local non ancora pushati al momento della scrittura: `b082336` docs pre-esistente + `d0b1105` #15/#6 + `36daa47` #1 slice-3a + `ee266ea` #2 slice-2a + `85d6418` #7 D-08; push in questo handoff). Migration su disco **139 file invariate** (`000001..000141`, gap 000035+000139 — **nessuna nuova migration**: slice-3a riusa il `resource_type/resource_id` polimorfico di 000132, slice-2a scrive su tabelle esistenti). Moduli business **76** (invariato — slice-3a aggiunge `approvals/effects/`, slice-2a estende `tenant-materialization`, dentro moduli esistenti).

- **#15 D-36** (`d0b1105`, doc): già RISOLTO S992 (wrapper `e2e-node22.mjs`, `addb19a`); riconciliato il bullet stale pre-fix in `SOT_BACKLOG.md:137` → terminale.
- **#6 R5/R7** (`d0b1105`): già FATTO S987 (`fdd412b`); **verificato LIVE su VM** — `backup.timer`+`reindex.timer` active, backup eseguito 2026-06-19 01:33Z (7 dump 204-217M, retention OK), reindex embedda (58 occupations + 156 user_profiles). + `.env.example` knob R5 (BACKUP_DIR/RETENTION/OFFHOST) documentati.
- **#1 BPM slice-3a effect-wiring** (`36daa47`): nuovo `apps/api/src/modules/approvals/effects/{registry,tenant-activation,index}.ts` + dispatch in `service.applyRequest`. `applyRequest` ora esegue `markApplied` + handler registrato **nella stessa `withTransaction`** (atomico; throw → `APPLY_EFFECT_FAILED` 409 rollback; `resource_type` unknown/null → pure marker backward-compat). Primo handler reale **`TENANT_ACTIVATION`**: flip guarded `sys_tenancies.tenant_status` PENDING_ACTIVATION→ACTIVE (transizione reale pre-esistente). approvals **19/19** (16+sla 2 + effects 3). Spec aggiornata (3a SHIPPED). Decisione tecnica (PM-owns-WHAT): Option A (target reale tenant-activation, disaccoppia da #2 → WI-C diventa un 2° handler senza rework).
- **#2 WI-C slice-2a** (`ee266ea`): `tenant-materialization` ora materializza **1 incumbent SYNTHETIC_REFERENCE per posizione + assignment PRIMARY ACTIVE** (oltre a org-units+positions di slice-1). `blueprints.archetypeUsers()` deterministico (nome da pool per indice, email su position-code → unica per tenant, `user_external_code='SYN_<positionCode>'`, **MAI** `LEGACY_EMP::` — I14/ADR-0024; keying già deciso/enforced da mig 000004). `MaterializeCounts` +users +assignments (shared schema + service totals). Idempotente (ON CONFLICT tenant+lower(email); assignment existence-checked; partial-unique `sys_upa_one_primary_active_per_user`). tenant-mat **9/9** (apply 11 users + 11 assignment su RTL, re-apply idempotente, plan-count, tenant-isolation HEU+SUSPENDED untouched). **slice-2b SHIPPED → WI-C COMPLETO** (`4e85940`, aggiunta a context-budget dopo il close): catalogo skill/ranked-KPI tenant-scoped (8 `sys_skills` RBR-SK-* + 4 `sys_kpi_definitions` RBR-KPI-*) + evidence per-incumbent **set-based** (unnest + NOT EXISTS anti-join: 2 statement non ~130 round-trip; apply RTL = 8 skill + 4 KPI + 88 skill-evidence + 44 KPI-evidence, idempotente; `measured_value` → ranking; tutto sintetico ADR-0023, keying SYN_). `MaterializeCounts` +skills/kpis/skillEvidence/kpiEvidence. tenant-mat 9/9. Resta gated solo la demo agente live E2E (MAX rate-limit); il registry `effects/` di #1 permette un handler `TENANT_MATERIALIZATION` apply-effect senza rework.
- **#7 D-08 pre-deploy snapshot** (`85d6418`): step 2a in `vm-deploy.sh` — `pg_dump -Fc` PRIMA della migration (2b) in `pg_dump_snapshots/pre-deploy/` (dir dedicata, distinta dai backup R5 daily); fail-loud su dump vuoto (aborta il deploy), retention keep-newest-N (`PREDEPLOY_RETAIN`=10), bypass `PREDEPLOY_BACKUP=skip`. Il re-exec guard esistente lo attiva già al 1° deploy. **Verificato LIVE** (dump 217M su VM) + `bash -n`. Deep D-08 (2° runner SPOF, rollback automatico) resta dossier infra.
- **Gate**: **full API suite 1063 pass / 6 skip / 0 fail** (1069, 153 file, 881s, exit 0 — zero regressione) · typecheck **5/5 ws** + api typecheck:test puliti.
- **Dispozioni rimanenti (Enzo confermato in blocco, autorità *cosa*)**: **terminali** — #12 B-10b-m2b normalized surveys → WON'T-DO (modello JSONB canonico, no cluster parallelo) · #5c process_kpi crosswalk → terminale EXCLUDE (muro B-42) · #14 matching PSR → terminale (già S978, PSR 844) · #18 linux-pc → terminale (policy refresh on-demand, align già B-52) · #19 B-30 2° runner → WON'T-DO desktop (security). **Programma-faro schedulato** (rendi-dimostrabile→GTM): #3 Gap#1 + #4 go-to-market + #5-RACI prod + #13 B-50 bridges + #17 Wave-3. **Blocked-on-Enzo**: #8 EMAIL (app-password Outlook → attivo live) · #16 SuccessFactors (sandbox).
- **Counts S998 (ri-derivati psql/ls/git)**: migration **139 file / 000141 max** (invariato) · moduli business **76** (invariato) · **153 API test file**, full suite **1063 pass / 6 skip** · RBAC invariato (slice-3a riusa `approval:create`, slice-2a service-gate) · stack invariato. CI girerà al push.

## Delta S997 (2026-06-19) — batch autonomo 1-6+10: 5 item pieni + BPM slice-3b SLA live

HEAD **`8df493b`** (8 commit `b7bf710`..`8df493b` + handoff, pushati). Migration su disco **139 file** (`000001..000141`, gap 000035 + 000139). Moduli business **76** (invariato — slice-3b aggiunge `approvals/sla.ts` dentro il modulo esistente). Batch decision-authority session-scoped ("da 1 a 6 e poi 10, tutto in autonomia").

- **#6 Dependabot** (`b7bf710`): nodemailer `^8.0.10`→**9.0.1** (alert #82+#84, message-level raw → file-read+SSRF; `SmtpMailer` non usa `raw`, 0 API-surface change) + dompurify override `>=3.4.8`→**3.4.11** (alert #83). Gate: typecheck 5/5 + smtp-mailer 6/6.
- **#3 prodotto** (`752eed4`): `docs/product/` **adottata SoT del dominio prodotto** (CLAUDE.md §Source of Truth, disgiunta da docs/kb + docs/due-diligence) + 5 deliverable Cowork + DD scorecard. **Fase-0 LIVE** (`docs/product/WORKITEM_GAP1_PHASE0_VERIFICATION.md`): count reali su :5433 → MLCE/maturity/event-store **0 tabelle** (assenti); ESCO occupation-skill 126051, PIP VIEW, requirements 844/172/1791, insights 159/462/154, org 26/162; RACI 13 demo RTL; catalogo latente **sovrastima** event-sourcing/maturity (legacy evo). COWORK_INBOX 2026-06-17 riconciliata.
- **#4 QW de-dup FieldGrid** (`58eb98c` + upstream `0860151`): primitive identica web+showcase → **`@heuresys/ui@0.1.8`** (pubblicata npm); 2 file → shim re-export (showcase = dead-code). Gate: typecheck 5/5 + web/showcase build.
- **#5 QW residui CLASS-A** (`3d52551`+`a145fc6`): **QW-SEC5** audit `MFA_FAIL` su authn-failure 2° fattore (no migration, type già nel CHECK 000005; +test auth-mfa 13/13, no plaintext) · **QW-J3** TRUST_PROXY governance (`vm-bootstrap`=1 / `provision-linux-pc`=false; shell-tests 46/0) · **TODO_100X riconciliato** (era stale pre-S994).
- **#1 BPM slice-3b SLA** (`e327312`, mig **000141**): SLA/escalation scheduler — colonne `approval_step_{due_at,reminder_count,escalated_at}` + `approval_request_sla_hours` + due-index + inbox CHECK widened; `NotificationType` **7→9** (+APPROVAL_REMINDER/OVERDUE); `runApprovalSla` (reminder dedupe + escalate al 3° a created_by) + sla-cli + 2 systemd units + vm-bootstrap wiring. **approvals-sla.integration 2/2 LIVE su RTL_BANK**. (Timer inerte fino a vm-bootstrap re-run, D-17; runtime provato live.) slice-3a effect-wiring = residuo.
- **test-fix** (`8df493b`): notification-preferences count 7→9 (regressione latente catturata dalla full-suite, R3/R17). Verde isolato 10/10.
- **Counts S997 (ri-derivati psql/ls/git)**: migration **139 file / 000141 max** (gap 000035+000139) · moduli business **76** · reconciliation registry **111**, 0 UNCLASSIFIED · RBAC **630 map / 11 ruoli / 137 perm** (invariato) · NotificationType **9** · full API suite verde-modulo-flaky-D-20 (regressione 7→9 fixata; gli altri fail = timeout tunnel sotto carico, file isolati 42/42 verdi). Stack: **+nodemailer 9.0.1 · +dompurify 3.4.11 · +`@heuresys/ui@0.1.8`** (era 0.1.7).
- **Full-close**: 8 commit pushati + **align Mac/VM/linux-pc** (`align-clones all --deploy`, 0 errori, 139 migrations applied incl. 000141) + **deploy PROD verificato live** (`https://www.heuresys.com/login` 200, `/api/readyz` `{"status":"ready","database":"ok"}`, VM api :8013 ready). **RESIDUO** (autorità Enzo / multi-sessione): #1 slice-3a effect-wiring (serve natural target = WI-C activation) · #2 WI-C (mini-modulo materialization + agente gated) · #10 3 decisioni prodotto (Surveys full-fleet / RACI prod / process_kpi crosswalk = blocked-on-Enzo) · Gap #1 (Porte UI + MLCE/Maturity).

## Delta S996 (2026-06-18) — batch autonomo #4-#14 (intervista-guidata): 8 item full-stack live

HEAD **`1efeafd`** (8 commit feature `bd1d5eb`..`1efeafd` + handoff, pushati). Migration su disco **138 file** (`000001..000140`, gap 000035 + **000139** — slice-3 SLA non creata, spec-only). Moduli business **76** (+`tenant-materialization`). Batch decision-authority session-scoped — intervista iniziale (nessuna credenziale abilitata · Surveys=mirror · RACI/process_kpi=keep · MFA=slice), poi esecuzione senza interruzioni. Design-mapping workflow (6 Explore agent → blueprint) poi impl in-loop con gate + commit atomico per item.

- **D-37 ✅ RISOLTO** (`bd1d5eb`): `reference-sync.integration` hook-timeout sotto full-suite era latenza DB/login (NON HTTP esterno — i fixture sono già iniettati); fix = per-hook 60s sui 3 beforeAll + stub difensivo `escoSkillFetcher`/`escoSkillUriSource`. Test-only, 14/14 isolato.
- **#4 WI-D2** (`c58a505`, mig **000137**): `rank smallint` su `sys_position_kpi_requirements` + PIP view proietta rank (ORDER BY rank NULLS LAST, weight DESC) + write API POST/PATCH/DELETE `/v1/positions/:id/kpis` (riusa `position:update`, **no nuovo RBAC**). positions 11/11.
- **#7 B-10b ✅ CHIUSO** (`a60b339`, doc-only): verifica evidence-based → le 3 macro-aree (mentorship/surveys/predictions) erano GIÀ shipped end-to-end S970-S988; `SDBI_PHASE2_CLOSURE.md` aggiornato; `engagement_pulse_configs`→WON'T-DO. Registry 0 UNCLASSIFIED, 15 tabelle B-10b classificate.
- **#6/#10 Surveys-templates** (`d69de4e`, mig **000140** + seed **55**): `sys_survey_templates` mirror completa il cluster m2b normalized (000097). Legacy `surveys` NON ha template_id (verificato live) → catalogo standalone. Import 2 template (RTL+HEU, gli unici legacy-tenant mappati in `tenant_id_mappings`). + GET `/v1/engagement/templates` (riusa surveys:read). engagement 8/8.
- **#5 BPM slice-2** (`8cc2e46`, mig **000138**): ordered multi-level chains — `approval_step_level_policy` (per-livello, nullable→request policy; varchar+CHECK) + active-level partial index; `deriveChain` level-aware; createRequest emette inbox solo al livello-1; decideStep guard `LEVEL_NOT_ACTIVE` + apertura livello successivo (dedupe). Backward-compat coi 10 test slice-1. approvals 14/14. **slice-3** (effects registry + SLA scheduler) = spec `docs/superpowers/specs/2026-06-18-bpm-approval-slice2-3-design.md` (`9d090fb`), residuo multi-sessione.
- **#4 WI-C** (`e31835a`): nuovo modulo **`tenant-materialization`** (76°, prefix `/v1/tenant-materialization`) — generatore archetipo→org-units+positions, plan/apply idempotente (ON CONFLICT), **M-1** tenant ACTIVE-validation (tenantId input-supplied → 404/403), PLATFORM_ADMIN service-gate (no nuovo perm) + CSRF. Archetipo `RETAIL_BANK_REFERENCE` (codici `RBR-`, namespace-safe). MCP `hrx_tenant_materialize` (mcp-tools + WRITE allowlist deny-by-default). 9/9 live. Residuo: users/assignments/skills/ranked-KPI + demo agent live (rate-limit MAX).
- **test-fix** (`1efeafd`): reconciliation-registry A26→27 (+`sys_survey_templates`) + me-interfaces +`me-surveys` (**regressione latente S995** mig 000136 mai catturata — la suite S995 era pending; R3/R17). Sole assertion, no src.
- **Counts S996 (ri-derivati psql/ls/git)**: migration **138 file / 000140 max** (gap 000035+000139) · moduli business **76** · reconciliation registry **111** (A27/B16/C23/D45), **0 UNCLASSIFIED**, POPULATED 150 · RBAC **630 map / 11 ruoli / 137 perm** (invariato — WI-D2 riusa position:update, WI-C service-gate) · **151 API test file**, full API suite **1064 pass / 6 skip / 0 fail** · agent-gateway **47/47** · i18n **1328×2×7**. Stack invariato. CI girerà al push.
- **+ continuazione post-handoff (go Enzo "pubblica @heuresys/ui 0.1.7")**: **#8 QW-E5 ✅ FATTO** — `@heuresys/ui@0.1.7` **pubblicato su npm + deployato live su PROD** (VM ui=0.1.7). StatusPill/StatusBadge/statusTone promossi upstream (type `StatusPillTone`, evita collisione StatusIcon); ux-design-shared `a815421` (source+dist); consumer shim+pin `^0.1.7` `cdff66c` (−156 LOC, i 2 file app→re-export). Gate: upstream vitest 111/111 + build + publish:dry · consumer typecheck 5/5 + web+showcase build + clean install (D-15). **fix deploy-blocker `000078`** (`ac4b723`): assert surveys-perm reso re-run-safe (scope-by-code) — break **latente S995** (mig 000135 aggiunse `surveys:respond:self`) emerso al 1° deploy post-S995; full migrate chain ri-verde ("138 migrations applied"). Stack: **+`@heuresys/ui@0.1.7`** (era 0.1.6). **RESIDUO** (autorità Enzo / multi-sessione): BPM slice-3 · WI-C residuo · 2° dup `detail-panel` FieldGrid (follow-up QW).

## Delta S995 (2026-06-18) — batch P1: #1 BPM approval-flow 3.3 slice-D + #2 Surveys-M2 (full-stack live)

**4 commit LOCALI non pushati** (`c582109`/`90d03dc` #1 + `fc47eee`/`8e69a28` #2). Stop ai commit per scelta Enzo (NO push/align/deploy).

**#1 BPM approval-flow 3.3 slice-D (DONE-LIVE)** — primo runtime BPM eseguibile sul layer catalog/modeling:
- mig **000132** (`sys_approval_requests` + `sys_approval_steps`, state-machine varchar+CHECK PENDING/APPROVED/REJECTED/APPLIED, quorum ALL_OF/ANY_OF, inbox CHECK widening +APPROVAL_REQUEST/+APPROVAL_STEP, registry EXCLUDE) + **000133** (perm `approval:{create,decide,read}`, +19 mapping) + **000134** (sidebar operations/PROCESS).
- modulo `/v1/approvals` (5 endpoint create/list/detail/decide/apply; decide state-guarded + data-layer own-step authority; emit inbox + mark-read in `withTransaction`). **10 integration test live** RTL.
- web: `/approvals` admin-track + `/approvals/[id]` decision-page + `/me/inbox` inline approve/reject (sezione additiva). i18n it+en. **E2E live verde** (federica self-approve → APPROVED → APPLIED).

**#2 Surveys-M2 (DONE-LIVE)** — decisione Enzo **M2** (audience esplicita per-utente, su M0):
- mig **000135** (`sys_survey_assignments` targeting+completion + perm `surveys:respond:self` a 11 ruoli + registry EXCLUDE) + seed 8 assegnazioni RTL + **000136** (sidebar engagement+me-surveys).
- ESS `/v1/me/surveys` (list/detail/submit; `withTransaction`; dedup natural-key→**409**, type↔column→**422**, not-assigned→**404**). **6 integration test live**. + `errors`: `NotFoundError` code-param (backward-compat) + nuovo `UnprocessableEntityError`(422).
- web: admin `/engagement` (overview + pulse `EChartsCard` + per-survey results bar) + ESS `/me/surveys` (form compilazione rating/text). i18n it+en. **E2E live verde** (tommaso compila → completed via re-fetch).

**Counts ri-derivati**: migration **136** (135 file, gap 000035; `000001..000136`) · RBAC role-perm mapping 600→**630** (+19 approval +11 surveys) · reconciliation bucket-D 42→**45** (+2 approval +1 survey_assignments) · API moduli +1 (approvals; `me` esteso) · web pagine +6 (`/approvals`×2, `/me/inbox` ext, `/engagement`×2, `/me/surveys`×2) · i18n parity **1328×2×7**.

**#3 triage 14 dossier Fase-C** consegnato (tabella decision-ready) → decisioni per-riga + epic S-100X-E = **sessione dedicata** (scelta Enzo). **#5 audit ecosistema Claude S-100X-A-L** = **sessione dedicata** (scelta Enzo).

**Verifica finale**: full suite API lanciata a fine S995 (background); gate mirati tutti verdi (typecheck 5/5 ws, 10 approvals + 6 me-surveys live, reconciliation D45, E2E live verdi). Esito full-run da controllare next session (eventuali fail = flaky infra noti D-20/D-37).

## Delta S994 (2026-06-17) — batch 13-item intervista-guidata (QW CLASS-A + SEC6 + B-50 close + RACI demo + design-spec)

HEAD **`c89c749`** (10 commit `7dbfa96`..`c89c749`, pushati `042e386..c89c749` + handoff). Migration su disco **130 file** invariato (`000001..000131` — **nessuna nuova migration**: #15 è app-layer, #12/#11 sono seed). Moduli business **75** invariato. Batch scelto via **intervista preventiva** (discovery 6-agenti read-only → opzioni evidence-based; ultracode multi-workflow). Commit atomici per item, gate per ondata.
- **#4 refactor backend** (`fd18629`): `withTransaction` auth→`db/client.ts` (QW-B6) · shared `ActorContext`+`actor()`+`isPlatform()` in `src/lib/actor.ts` (QW-4/B4, ~73 service de-dup) · `paginationSchema(max)` factory `packages/shared/_pagination.ts` (QW-B5, ~71 cap preservati) · teams my-team N+1 → 1 query batch (QW-B3). 216 file, **net −626 LOC**. Behavior-preserving: full vitest **1012 pass / 6 skip / 0 fail** (144 file).
- **#2/#6 frontend** (`0a56d4c`): QW-D1 chart code-split (9 analytics + Mermaid via `_charts-client` next/dynamic, echarts fuori chunk iniziale + SSR-crash guard) · **QW-E1** 13 `text-destructive` + 1 `bg-destructive`→`danger` (token non registrato in globals.css → errori renderizzati invisibili, bug confermato) · QW-E2 4 branch `isError` con UI errore + i18n parity · QW-D2 `optimizePackageImports`. Gate: web typecheck 0, **i18n 1220×2×7**, next build **76/76**.
- **#15 SEC6** (`00a6d7b`): `secret-crypto.ts` AES-256-GCM (`enc:v1:iv:tag:ct`, IV random/record) al boundary `mfa-repository`; **key-presence-gated + retro-compat** (key set→ciphertext, unset→plaintext non-breaking; decrypt legge sempre legacy plaintext); `env.ts` consuma `MFA_ENCRYPTION_KEY` (D-30 "inert" corretto); lazy re-encrypt su verify con **skip-guard e2e-fixture**; bulk script `db/scripts/encrypt-totp-secrets.ts` (`db:encrypt-totp`) **READY-BUT-UNRUN**. mfa vitest 65/65; **DB live verificato 6/6 fixture intatti (enc=0)**. NB: i 6 secret sono tutti e2e-fixture → restano plaintext by-design (l'encryption vale per future enrollment reali).
- **#5 CI** (`81978ce`): cache pnpm + `.next/cache` · SHA-pin **26 action** (+`# vN`, Dependabot preservato) · showcase drop sister-repo checkout + `npm --legacy-peer-deps` (verify-first `@heuresys/ui@^0.1.6` registry) · `ms-playwright` cache · agent-gateway `build`+`lint` (ora in `pnpm -r`).
- **#14 dump** (`7dbfa96`): `scripts/archive-dumps.sh` (scp `--ignore-existing` idempotente) → **27 snapshot/3.7G live su `oracle-vm-default:/home/ubuntu/dump_archive`** (elimina SPOF disco Windows) + runbook + retention manual-only.
- **#7 dead-deps** (`c89c749`): drop `@tanstack/react-query-devtools`+`supertest`+`@types/supertest` · `pino` dev→deps · clean install (lock −203 righe). Gate: typecheck 5/5 + web build 76/76 + full vitest 994 pass (reference-sync hook-timeout = flake esterno ESCO, 14/14 isolato).
- **#1 anti-drift doc** (`afd8cc8`): de-hardcode counts README/CLAUDE→puntatori `SOT_STATE` (anti-drift strutturale, D-01 era ri-driftato) · fix persona cancellata in README · supertest/error-envelope (QW-B7: requestId = header `x-request-id`, non body) · **INDEX rigenerato** (`build_index.py`, 75 mod/130 mig) · FINDINGS register WS-A..K DONE · **handoff skill wired** (`build_index` conditional al close).
- **#12 B-50** (`9fcea0d`): **chiuso terminale** (`v_reconciliation_status` **148 POPULATED / 21 NO_SOURCE / 9 EXCLUDE / 1 REFERENCE_ONLY / 0 UNCLASSIFIED**). Import `process_kpi_templates` ri-misurato verify-first → **muro B-42 ri-confermato** (KPI side 81/81 ma process FK overlap **0/25 code, 1/25 name**); tenuto EXCLUDE, no crosswalk fabbricato; evidence-seed `53_*` (idempotente ×2) + test 14/14 live.
- **#11 RACI** (`46b2ce4`): demo live su **RTL_BANK** (crosswalk autorato 12 righe banking-plausible, 8 OWNER/2 CONTRIBUTOR/1 CONSULTED/1 INFORMED), seed `54_*` idempotente (run1 +12/run2 +0, uuid_v5 RFC-4122, fixed ts), **0 HEURESYS (I5-enforced)**; verifica live via integration test (RTL admin login → GET by-ou/by-process + POST→GET→DELETE round-trip, 6/6); T2.5 9/9.
- **#3/#9/#10 design-spec** (`a4ee936`): **14 dossier Fase-C** `DOSSIERS/D-01..D-14` (option-triad conservativo/evolutivo/radicale + raccomandazione, sintetizzati da FINDINGS WS-*) + **2 design-spec** (`docs/superpowers/specs/2026-06-17-bpm-approval-flow-design.md` slice-D approval-flow riusa notification center · `...-surveys-engagement-ui-design.md` admin read-only + ESS self-response).
- **Counts S994 (ri-derivati psql/ls/git)**: migration **130 / 000131 max** · moduli **75** · RBAC **600 map / 11 ruoli / 137 perm** · route-method **~424** · **148 API test file** · **14 dossier Fase-C** (16 doc design totali) · stack: **−3 dead-dep**, `pino` runtime-dep, +`secret-crypto.ts`. Nuovi script: `archive-dumps.sh`, `encrypt-totp-secrets.ts`. `.env` locale: **`MFA_ENCRYPTION_KEY` attiva** (gitignored; placeholder in `.env.example`; propaga via align — non denylistata). CI girerà al push.

## Delta S993 (2026-06-16) — workaround mail/MFA senza SMTP + agente MAX stabilizzato + 100X A4/WS-C + 3.2 ASVS + QW live

HEAD **`eabae4a`** (6 commit `59ecb75`..`eabae4a`, pushati al handoff). Migration su disco **130 file** (`000001..000131`, +3: `000129` auth-audit prune · `000130` tenant-FK indexes · `000131` drop dead lineage idx — **già applicate live al DB PROD** via tunnel, ledger 130). Moduli business **75** invariato (auth-housekeeping è uno script, non un modulo). Batch decision-authority session-scoped (Enzo: "risolvi i 2 aspetti poi procedi con tutti i punti, chiudi tutto il possibile").
- **Aspetto-1 mail/MFA senza SMTP** (`59ecb75`+`ce52e41`): `IMailer.productionCapable` (SmtpMailer/InMemoryMailer=true, ConsoleMailer=false); `enrollableKinds()` gate EMAIL_OTP su mailer reale (anti-lockout, mirror SMS_OTP); `enrollEmailOtp()` → `EMAIL_NOT_CONFIGURED` su POST diretto. **MFA pienamente operabile su TOTP+WebAuthn senza email**; 3.4 digest = dormiente-opzionale (non più blocked). `.env.example`: guida "no SMTP?" + ricette gratuite Outlook/Gmail app-password. Unit 5/5 (`mfa-email-otp-gating`), MFA integration 22/22.
- **Aspetto-2 agente #9 MAX** (`ce52e41`): `.env.example` documenta `AGENT_GATEWAY_SUBSCRIPTION_AUTH=1` come modo canonico (no API key, subscription MAX, verificato S990/S991) + memoria `project_agent9_subscription_max`. Chiuso, non più gated.
- **100X A4 / WS-C** (`7fadeea`, doc-only): `FINDINGS/WS-C.md` 0 CRIT / 3 HIGH (F-WS-C-1 243 FK no-index/56 tenant_id · F-WS-C-4 auth-audit unbounded · reconciles WS-G F-10 backup=SHIPPED) + ASSET (0 dead-schema, RD-08 perfetto, D-18 verificato). **3.2 ASVS** mapping (`FINDINGS/3.2_ASVS_MAPPING.md`): 24 PASS/14 PARTIAL/2 GAP.
- **QW chiusi live**: QW-1(drizzle già S989)/QW-3+G3(7 env var→.env.example)/QW-5(package.json desc) · **QW-SEC2 HSTS** al TLS edge nginx (`Strict-Transport-Security max-age=31536000`, applicato live VM + verificato curl HEAD+GET) · QW-SEC1 verificato (`TRUST_PROXY=1`+`COOKIE_SECURE=true` su VM) · **QW-C1** 6 indici tenant-FK (mig 000130, EXPLAIN refresh-token→Bitmap Index Scan) · **QW-C2** auth-audit prune (mig 000129: refresh-token **46.348→37.028**, −9.320 revoked+expired, 0 prunable; i 37k attivi scadono e li raccoglie il **job ricorrente** `scripts/auth-housekeeping.sh` + systemd `heuresys-advanced-auth-housekeeping.{service,timer}` daily 02:00 + wiring vm-bootstrap) · **QW-C4** drop dead `sys_source_lineage_records_natural_key_idx` (mig 000131, 70→60MB). SEC3/4/7 = già fatti S989 (QW-H).
- **Gate**: typecheck **5/5 ws**, MFA unit 5/5, MFA integration 22/22, auth+refresh+sessions 29/29 (post-prune), HSTS curl-verified live, EXPLAIN refresh-token usa il nuovo idx. Full API suite NON ri-run end-to-end (solo i subset toccati). CI girerà al push.
- **Counts S993 (ri-derivati ls/git/psql)**: migration **130 file / 000131 max** · ledger **130** · moduli business **75** · refresh-token **~37k** (post-prune) · 6 nuovi `*_tenant_idx` · stack invariato. Nuovi file: `scripts/auth-housekeeping.sh` + 2 systemd unit.
- **+ via libera (stessa sessione S993) — programma 100X FASE A COMPLETA**: fan-out di 7 sub-agent read-only **A5-A11** → FINDINGS `WS-B/A/D/E/J/K/I` scritti (A1-A11 ora tutti DONE). Headline: **CRITICAL B-1** broadcast `POST /v1/notifications` N+1 (loop emitNotification ~1000-1500 query serial su 500 utenti → **QW-B1 FIXED** set-based `emitNotificationsBulk`, 3 query, notifications 13/13) · HIGH WS-D chart code-split incoerente + app `(authenticated)` client-side · HIGH WS-I doc-drift (README/CLAUDE 60→75 mod/55→130 mig/586→600 map) · HIGH WS-K `.next` 29G no-clean. **QW chiusi LIVE**: QW-C3 (dr-drill strict-mode + weekly timer) · QW-C4 (drop dead idx, lineage 70→60MB) · QW-J1/J2 (env footgun fail-open `API_DOCS_ENABLED`/`MATCHING_FREETEXT` + denylist) · QW-2 (`scripts/clean.sh`, `.next` 29G→0) · QW-B1 (broadcast set-based). Nuove migration **000129/130/131** (auth-prune/tenant-idx/drop-idx, già live). HEAD finale **`a2b9d4c`** (ri-derivati: **130 migration / 000131 max** · ledger 130 · **11 ruoli / 137 perm / 600 map** · **146 API test file** · **11 audit WS** fase-A completa in `FINDINGS/` · 0 UNCLASSIFIED). typecheck 5/5. **QW-C3 DR-drill verificato LIVE end-to-end** (restore 162/162/3180/12 OK, RTO 93s, PASS exit 0) dopo aver scoperto+fixato 2 bug DR reali (ok-flag subshell → false PASS; pg_restore path `Permission denied` backup sotto /home/ubuntu → **restore via stdin**: il backup "shipped" ora è provatamente restorabile). Le 3 systemd unit nuove (notifications-digest/auth-housekeeping/dr-drill) installate live (self-modify-buffer D-17 workaround manuale). **Residuo QW (CLASS-A chiudibili)**: A1/A2/A4 · **B2✅** B3-B7 · D1/D2 · E1-E5 · I1-I4 · J3 · K3 + dossier anti-drift + agent-gateway-CI. (QW-B2 chiuso S993: `LIMIT 5000` cap difensivo su insights×3 + listSurveys, 34/34.) **Residuo prodotto/multi-sessione** (autorità Enzo): #5 BPM 3.3 · #6 m2b Surveys · #7 RACI · #8 B-50 · #10 Fasi 4-8 · S-100X-A-L (ecosistema Claude) · S-100X-C (dossier → decide Enzo per-finding).

## Delta S992 (2026-06-16) — batch 1-4 + P3 ondata-1: 3.5 reporting/export + 3.4 notification center

HEAD **`93e5791`** (pre-handoff; 11 commit `69f77fb`..`93e5791` pushati `c92c3a9..93e5791`). Migration su disco **128 file** (`000001..000128`, gap 000035; +3: `000126` notification_preferences · `000127` notification_broadcast_permission · `000128` notification_preferences_registry). Sessione lunga delegata per fasi; ogni fase chiusa con gate verde + push + align/deploy(VM/Mac/linux-pc) + CI 6/6.
- **Batch 1-4**: #1 Dependabot già chiuso da `c92c3a9` (verificato `pnpm audit`=0, 0 alert) · #3 AI-matching P1b già fatto `664588e` (E2E `/me/matching` ri-verde live, 16/16) — entrambi backlog stale, no lavoro inventato · **#2 reporting export** (`69f77fb`): `GET /v1/analytics/:view/export?format=csv|json` per le 9 viste, flattener generico multi-section, 15 test · **#4 D-36** (`addb19a`+`cbfff34`): wrapper `apps/web/scripts/e2e-node22.mjs` (Playwright 1.61 = latest stable → no bump; pin Node 22 riproducibile, passthrough su Node ≤22 → CI/Mac/VM invariati) + fix lint scoped-globals; **D-36 RISOLTO**.
- **P3 · 3.5 reporting/export** (`03b46d3`): exporter generico **`?format=csv|xlsx|pdf` su tutti gli ~85 endpoint list `{items,total}`** via hook `onSend` globale zero-touch (RBAC/scope già applicati dall'handler; `format` da raw URL — 0 schemi querystring strict). CSV (riusa `csvCell`) + XLSX (`exceljs`, già dep) + PDF (`pdfkit`, **nuova dep**, tabella landscape). Coesiste con l'export analytics nested. 15 test (8 unit + 7 integration live). Design: `docs/superpowers/specs/2026-06-16-reporting-export-design.md`.
- **P3 · 3.4 notification center** (6 commit `6134002`..`90d8d77` + fix `bc100d7`): **chassis** (mig 000126 `sys_notification_preferences` nullable-tenant + `lib/notifications/emit.ts` con dedupe + API `/v1/me/notification-preferences` GET/PATCH) + **6 producer** (GAP_CLOSURE_DUE via insights recompute hook · ASSESSMENT_REQUEST + MANAGER_FEEDBACK_READY via assessments/results hook · TRAINING_DEADLINE + CAREER_TARGET_STATUS via me enroll/career hook · SYSTEM broadcast `POST /v1/notifications` mig 000127 `notification:create`) + **digest scheduler** (`IMailer.sendNotificationDigest` su tutti gli impl · `lib/notifications/digest.ts` · CLI `notifications:digest` · systemd `notifications-digest.{service,timer}` daily 02:45 + vm-bootstrap wiring). **In-app pienamente LIVE**; **digest EMAIL = blocked-on-Enzo (SMTP creds)** — chassis completo, no-op finché non configurate. mig 000128 registra `sys_notification_preferences` nel reconciliation registry (D/EXCLUDE) → 0 UNCLASSIFIED (fix dei 6 fail full-suite). Tutti e 6 i producer dimostrati live (test 5/5 producer + 2 broadcast + 5 preferences/emitter + 1 digest).
- **75° modulo**: `notifications` (broadcast). Lavoro VM divergente **preservato** (assorbito in origin pre-push: commit `f278ede` playwright root pin + bump non committato `@anthropic-ai/claude-agent-sdk ^0.3.178` → commit `93e5791`). Ecosistema Claude VM non toccato dall'align (sync ristretto a `memory/`).
- **Counts S992 (ri-derivati psql/ls/git)**: migration **128 file / 000128 max** · moduli business **75** (+`notifications`) · route prefissi `/v1` **76** · RBAC **11 ruoli / 137 perm / 600 map** (+`notification:create`) · **full API suite 1007 pass / 6 skip / 0 fail** (145 file) · reconciliation registry **107 righe** (A26/B16/C23/**D42**), **0 UNCLASSIFIED** · stack: **+`pdfkit`** (PDF export), **+`playwright` root** (1.61.0 pin), agent-sdk `^0.3.178`. CI 6/6 verde su `93e5791`. PROD verificato: export+notification endpoint 401/400 (route live), `/login` 200, `/readyz` OK su VM+linux-pc.

## Delta S991 (2026-06-15) — batch delega: #1 agente LIVE + fix sicurezza write-gate · #2 Skills-Group-Share full-stack

HEAD **`088cccb`** (2 commit `ce7e2bd`+`088cccb` pushati `bd156cd..088cccb`; + handoff). Backup PROD pre-batch `/home/ubuntu/pre-s991-batch.dump` (216M, CUSTOM, rollback point). Migration su disco **124 file** (`000001..000125`, gap 000035/000076/000103; +1: `000125` skills-group-share UI interface).
- **#1 agente #9 — DA "blocked-on-credential" a DONE-LIVE**: lo S990 lasciava WI-B.3/M-2 come `blocked-on-Enzo (out_of_credits)`; verificato live S991 che la **subscription MAX risponde** (smoke `query()`→PONG, `rate_limit five_hour=allowed`, NO out_of_credits). **WI-B.3 LIVE** (`ce7e2bd`): le 3 skill `/hr` (skills-taxonomy/skill-gap/people-analytics) girano live — `Skill` tool + tool MCP `mcp__heuresys__*` su `/v1` reale, is_error=false. **M-2 write-gate dimostrato LIVE** su RTL_BANK come `federica` (TENANT_ADMIN): nuovo `scripts/live-write-acceptance.ts` — ALLOW→`approval_required`→approve→`POST /v1/kpi-definitions 201`→re-fetch riga su tenant 86ba7a65 · DENY→fail-closed (write mai arriva, 0 righe) · rollback DELETE 204. Audit M-4 (`agent-audit.jsonl`): WRITE_HUMAN_APPROVED/WRITE_DENIED/READ_AUTO_ALLOW (args sha256, no PII raw). **🔴 difetto sicurezza scoperto+corretto via DoD live**: `sdk-agent.ts` aveva `allowedTools:["Skill","mcp__heuresys__*"]` → per l'SDK `allowedTools`="auto-allowed without prompting" → **i WRITE tool bypassavano canUseTool** (il gate HITL non veniva MAI esercitato sui write). Fix: `allowedTools:["Skill"]` → tutti i tool MCP cadono in canUseTool (read auto-allow+audit, write HITL+audit); built-in (Bash/ToolSearch) restano governati da settingSources. Gateway test **47/47**. Detector credential-block in `live-skills-acceptance.ts` reso specifico (un `401` nudo matchava dati di business). WI-B.4 dev page `/dev/agent` già esisteva (S990).
- **#2 Skills-Group-Share (T3.8) + clustering (T2.6) — full-stack LIVE** (`088cccb`): ora che il backfill S990 ha popolato `skill_group_uri` su 12892/21939 skill (400 gruppi), i gruppi ESCO = cluster naturali. **Backend** (module pattern): `SkillsGroupShareAnalyticsResponseSchema` (shared) + `repository.getSkillsGroupShare` (GROUP BY skill_group_uri, top-25 + "other" che riconcilia al grouped-total; `escoGroupCode()` estrae il segmento vocabolario `skill/S2.8.1` dal concept-URI a doppio `/esco/`) + `analyticsService.skillsGroupShare` + **9° endpoint** `GET /v1/analytics/skills-group-share` (`analytics:view`). **4 test integration** (401/403/PLATFORM/TENANT) verdi su DB reale — anchor: 21939 skill, 12892 grouped, 400 gruppi, max `skill/S2.8.1` (314); **analytics suite 32/32**. **Frontend** (live-data E2E only): pagina `/analytics/skills-group-share` (3 StatsCard + bar chart gruppi + tabella share) + sidebar **mig 000125** (gruppo intelligence, ENTERPRISE, icona PieChart aggiunta a ICON_MAP) order 42 + i18n it/en (parity **1216 keys**) + **E2E Playwright verde** (login admin reale → naviga → conteggi grouped/groups + chart canvas + tabella top-group dai dati live).
- **#3 T2.5** verificato già-fatto (modulo `organization-unit-processes` + test + 1 demo row RTL, S990); mapping RACI reale = decisione business Enzo (non inventato). **Convergenza**: m2b/reporting/RACI richiedono decisione prodotto/modellazione Enzo → residuo onesto.
- **Infra E2E locale**: Playwright 1.61 + **Node 24 di sistema** rompe il loader (`context.conditions?.includes`); workaround = Node 22 portable isolato (memoria `reference_local_e2e_node22_playwright`). CI (VM Node 22) non affetta.
- **⚠ 2 alert Dependabot NUOVI** (1 high + 1 moderate, segnalati al push S991, non dai commit S991 — 0 cambi dipendenze): da verificare/chiudere (backlog).
- **Counts S991 (ri-derivati ls/git; full suite NON ri-run end-to-end — solo analytics 32/32 + gateway 47/47)**: migration **124 file / 000125 max** · web pages **85** (+`/analytics/skills-group-share`) · endpoint analytics **8→9** (stesso prefisso `/v1/analytics`) · i18n **1216 keys × 2 × 7** · typecheck **5/5 ws** · web prod build OK. Stack invariato. DB: nav interface `analytics-skills-group-share` registrata; nessun dato business mutato in modo persistente (M-2 demo + rollback puliti, 0 righe `M2-%` residue).

## Delta S990 (2026-06-15) — batch menu 1→11 (ESCO data + agent-gateway M-2 + A3 audit + doc-fix)

HEAD **`913a07e`** (11 commit `ce558d3`..`913a07e` + handoff, pushati). Migration su disco **123 file** (`000001..000124`, gap 000035/000076/000103; +6: `000119` enterprise-typing test-tenants · `000120` skill_kind · `000121` OU↔process schema · `000122` OU↔process perms · `000123` occupation-skill-requirements · `000124` reference-sync ESCO_SKILL_HIERARCHY watermark). Batch decision-authority session-scoped (Enzo), hybrid main-loop + 4 sub-agent delegati, ognuno verificato indipendentemente prima del commit.
- **#3 ESCO/tenant-onboarding** (spec #03/#04 di Enzo, committate `913a07e`): **T1.3** typing-profiles dei 2 tenant TEST (mig 000119, asse ATECO_2025: RTL_BANK→64.19/RETAIL/M/HIGH/158, HEURESYS→62.10/B2B_SERVICES/S/MEDIUM) · **T2.4** dimensione `skill_kind` da ESCO skill_type (mig 000120, **14036/21939** popolate; layer hard/soft DEFERRED autorità Enzo) · **T2.5** modulo `organization-unit-processes` (74° modulo, mig 000121/122, RACI OU↔processo, 9 test, perms `organization_unit_processes:{read,create,delete}`) · **T1.2** import occupation→skill (mig 000123 + seed 52, **126051 righe** — ESSENTIAL 67600/OPTIONAL 58451, risoluzione 100% via `skill_code='OLDDB::esco_skills::'||id`) · **T1.1** connector skill-hierarchy (mig 000124, EscoSkillFetcher seam + source ESCO_SKILL_HIERARCHY + jsonb-merge; **backfill LIVE eseguito post-close: skill_group_uri 0→12892**, broader_uri 5007, 828 URI stale-404 saltati su 14011; connector reso skip-tolerant, fix `c343380`).
- **#9 agent-gateway code-slice** (mock-first, live blocked-on-Enzo = credenziale agente `out_of_credits`): **M-2** (`004e581`) write-gate completo — `redact.ts` (PII) + `audit-sink.ts` (seam AuditSink, FileAuditSink JSONL, redact→sha256) + M-3 deny-by-default allowlist + approval bridge reale (`approval-bridge.ts` + `POST /agent/approve`) wirati in makeCanUseTool; **47/47** test. **WI-B.3/B.4** (`338c604`) dev page `/dev/agent` (flag `NEXT_PUBLIC_ENABLE_AGENT_DEV`, @heuresys/ui) + harness `live-skills-acceptance.ts` (3 skill, fail-loud su credential-block).
- **#7 audit A3/WS-F** (`89d0bb4`, doc-only read-only): `FINDINGS/WS-F.md` 19 finding (3 HIGH: no-full-E2E-in-CI · no-unit-layer · tunnel-coupling; 6 QW-F + 6 DOSSIER + 3 ASSET); `TODO_100X` A2+A3 → DONE.
- **#4/#5 doc-fix**: RBAC baseline CLAUDE.md 8/394→**11/586/133** (`ce558d3`, +CEO/TEAM_LEADER/TEAM_MEMBER) · D-30 MFA_ENCRYPTION_KEY claim corretti (inert/reserved, `b1574f0`).
- **Regressione D-23-class catturata nel PROD-verify + RISOLTA** (`5e3e56f`): l'`enterprise-typing` pipeline test upsertava+cancellava il profilo **reference** RTL_BANK (innocuo pre-T1.3, distruttivo post-T1.3) → fix **snapshot-restore** in afterAll; dato ripristinato (re-apply 000119). **5 PR dependabot → `defer-major`** per fermare il **CI-race** (old-test sul DB PROD condiviso = istanza WS-G F-3): #33/#34/#36 test-integration fail (defer reale), #32/#35 CLEAN (Enzo: un-defer+merge).
- **Counts S990 (ri-derivati psql/ls/git)**: migration **123 file / 000124 max** · moduli business **74** · RBAC **11 ruoli / 136 perm / 598 map** · **full API suite 963 pass / 6 skip / 0 fail** (135 file) · route prefissi `/v1` **76** · web pages **84** (+`/dev/agent`) · reconciliation registry **106 righe** (A26/B16/C23/D41), 0 UNCLASSIFIED · DB: typing wired **2** · skill_kind 14036 · `sys_occupation_skill_requirements` **126051** · `sys_organization_unit_processes` (RACI, 1 DoD row RTL) · stack invariato (agent-gateway usa `@anthropic-ai/claude-agent-sdk` già da #9). **Gate pre-deploy verde** + PROD verificato live (commit `913a07e` deployato / servizi+timer active / nuovo-codice 401-non-404 / dati live / `/login`+`/api/readyz` 200).

## Delta 2026-06-15 (#9 Agent SDK+MCP integration — PLAN prodotto + review/amendment riconciliati, doc-only)

Riconciliata la proposta `COWORK_INBOX` `2026-06-14 | #9` (Agent SDK + layer MCP del plugin `human-resources-plus`, pilota blueprint-builder banca retail). **Read-only sul codice**: nessun cambio a src/db/config/CI/deploy → **counts DB/API/web/CI/migration INVARIATI da S989** (114 migration `000001..000115`, 73 moduli business, full API suite 918/0/21). Deliverable = **PLAN** `docs/integrations/agent_sdk_mcp_integration_plan_2026-06-15.md` (5 work-item a/b/c/d + pilota, evidence-based via Explore agent + letture mirate; invarianti I3/I4/I5/I8 + GDPR Art.22 + R11). 3 scoperte design-changing: variant=header catalogo · activation=1 riga link · archetipo da seed deterministico (no recommender). Nessuna migration creata/applicata (DDL=PROPOSED). Backlog → `SOT_BACKLOG.md §🔌 #9`. **Review Cowork = GO-con-modifiche + amendment §7-chiuso riconciliati** (stesso giorno, 2 entry inbox): (a) MFA **A1 flag-DB `sys_auth_mfa_exemptions`** (I7) + A3 tenant-null · (b) service user tenant-null · (c) `apps/agent-gateway` TS · (d) **D2 abilitato / D1·D3 rinviati** · (e) KPI per-position con rank (edge I9 VIEW PIP) + **7 hardening M-1..M-7** nel PLAN §9. **WI-A ✅ DONE** (go "procedi", re-review M-8/V-1/V-2 recepita): mig **000116** (esenzione `sys_auth_mfa_exemptions`, auth-only I7) + **000117** (M-8 trigger eligibility PLATFORM_ADMIN-only) **APPLICATE** (default-safe, tabella vuota = login invariato) + guard `auth/service.ts §3b` + repo `isUserMfaExempt` + `seed-service-user.ts` scaffold opt-in (commit `1a8738b`/`9005adc`/`ca7c193`). Test **auth+mfa 83/83**, typecheck verde. **Migration su disco ora 116** (`000001..000117`, gap 000035). **WI-B (mock-first) ✅ SLICE** (`33fd9b7`): nuovo workspace **`apps/agent-gateway`** (4° app; `@heuresys/agent-gateway`) — `write-gate` (HITL deny-by-default M-2) + `heuresys-client` (auth-ibrido/CSRF/single-flight-refresh M-5) + `mcp-tools` (catalogo `/v1`, permission reali) + `sdk-agent` (query, SDK `@anthropic-ai/claude-agent-sdk@^0.3.177`); test **11/11**, **root -r typecheck 5/5**. Stack: +`@anthropic-ai/claude-agent-sdk`. **WI-B.2 READ-LIVE ✅ DONE-LIVE** (`61645e9`, trigger DoD): `apps/agent-gateway/src/server.ts` (POST /agent → SSE, sessione forwarded, write-deny) + `scripts/live-read-acceptance.ts`. Evidenza live: login reale → `GET /v1/organization-units` **200/26 righe reali** RTL_BANK via gateway; SDK+SSE E2E. **§0 DoD persistita** (`2f47ef2`, CLAUDE.md §Definition of Done = regola vincolante cross-sessione). **WI-B.2 AGENTE ✅ DONE-LIVE** (§A.1 subscription auth): unset `ANTHROPIC_API_KEY` → SDK usa subscription Claude (flag `AGENT_GATEWAY_SUBSCRIPTION_AUTH=1`); domanda reale → agente `claude-opus-4-8` → tool MCP `hrx_org_units_list` → `/v1` live → **26 org-unit reali** → SSE (`is_error=false`). ⚠ subscription rate-limit five_hour/out_of_credits → query agente con parsimonia; PROD-port = API key reale/Bedrock/Vertex (annotato). Stack: +`@anthropic-ai/claude-agent-sdk`. **§B migration-apply autorizzato** (additive/reversibili): **M-8b ✅ DONE-LIVE** (`4d8d041`, mig **000118** applicata): esenzione MFA SERVICE-only (PLATFORM_ADMIN umano non esentabile) + audit trail `sys_auth_mfa_exemption_audit`; test 4/4 + psql admin→BLOCKED. **Migration su disco 118** (`000001..000118`). **Residuo (milestone, prossime sessioni)**: WI-C (generatore materializzazione), M-2 (write-gate+write-live gated), WI-D/WI-D2 (pilota 8-step + rank KPI); WI-B.3/B.4 agente-bound (rate-limit subscription `out_of_credits`). PROD-port agente = API key reale/Bedrock/Vertex.

## Delta S989 (2026-06-14) — MFA/SMTP neutralization + 6 QW-H WS-H + D-29 (chiusura pending/debt)

7 commit (`b34b4bd`..handoff). **Counts DB invariati** (sessione code+test, 0 migration/seed). **Migration su disco: 114 file `000001..000115`** (lo snapshot pre-S988 diceva 108/000109 — sanato qui). **Stack**: `drizzle-orm`/`drizzle-kit` **RIMOSSI** (dead-dep, 0 importatori del `db` export) + override `esbuild`→`>=0.28.1` → **lone Dependabot alert chiuso, `pnpm audit`=0**. ADR-0003 (metà Drizzle) marcato superseded.
- **MFA/SMTP neutralization** (`b34b4bd`): nuovo env `MFA_ENFORCEMENT_ENABLED` (enum-transform anti-footgun, default **true** → PROD/VM/linuxpc invariati). `=false` bypassa l'intero gate login `§3b` (mfa_required + mfa_enrollment_required) → dev/test senza 2° fattore, capability + policy DATA intatte. DI seam (`BuildAppOptions.mfaEnforcement` / `AuthRoutesOptions`); `buildTestApp` pinna true (suite esistente invariata). `.env` dev locale = false. **Denylist in `env-key-merge.sh`** (+ shell-test C5) → un `false` dev NON si propaga a un remote (no silent PROD downgrade). Test `auth-mfa-enforcement-switch.integration` (2). SMTP/EMAIL_OTP/SMS_OTP/TOFU = già neutrali by-design (factory Console, mai throw) → 0 codice.
- **6 QW-H (S-100X-A2 WS-H) chiusi**: H1 drizzle+esbuild · H2 media magic-byte sniff (`media-service` + test negativo) · H3 rate-limit keyGen dead (era sempre `req.ip`, dead branch rimosso) · H4 `requirePermission` sulle 13 write-route skill-taxonomy (reads aperte by-design; service ensurePlatformAdmin resta autorità) · H5 ConsoleMailer `devOnlyCode` droppato (chiude raw-console redaction-bypass) · H6 cap per-route `/matching/search` 30/min + `/reindex` 5/h.
- **D-29 RISOLTO** (`79cde3b`): Playwright `globalTeardown` (`apps/web/tests/e2e/global-teardown.ts`) cancella via psql le righe `E2E Test Cert%` (best-effort, host/port/db/user dal `.env`, password da `~/.pgpass` — nessun secret letto/loggato); wired nel base config, ereditato dal prod.
- **Open-question S988 risolte** (CLASS-A, veto Enzo): R3 KEEP `sys_job_families` (27 famiglie, no duplicazione); R2 KEEP crosswalk bidirezionale (NARROWER 2865 + BROADER 2865 = 5730); ESCO 11 low-conf già flaggati `metadata.low_confidence` (design corretto — 5/11 in realtà validi, 6/11 trgm-noise).
- **Gate**: full API suite **918 pass / 0 fail / 21 skip** (133 file; 1 file-flake `content.integration` afterAll = DB-timeout jitter tunnel **D-20**, riconfermato verde isolato 15/15), typecheck 4/4, web build, i18n **1177×2×7**, web lint, shell-tests 43/43, `pnpm audit` 0. **Nuovo debt minore D-30**: `MFA_ENCRYPTION_KEY` validato in `env.ts` ma mai consumato dal codice (TOTP secrets = base32-plaintext); claim `.env.example` "throws MFA_ENCRYPTION_KEY_MISSING" stale.

## 0-terdecies. Delta S979 (2026-06-08) — dottrina full-alignment (infra deploy)

**Dottrina di allineamento totale (v1+v2) shipped** (HEAD `e6dc503`, pushed origin/main; counts DB/API/web/CI **invariati** da S978 §0-duodecies). Mac+VM = **cloni effettivi** del repo locale, inclusi i gitignored. Nuovi/modificati `scripts/`: `align-clones.sh` (orchestratore `mac|vm|all` — modo `--deploy` full / modo close `--delta --resilient --auto-deploy`), `env-key-merge.sh` (.env key-merge additivo, topology per-macchina intatta), `sync-memory-tree.sh` (memorie Claude, slug per-macchina via `find`), `db/scripts/migrate-if-pending.sh` (migra solo se uno sha256 manca dal ledger `sys_schema_migrations`). `vm-deploy.sh` **hardened**: re-exec self-modify-buffer, clean-reinstall su cambio ABI Node, guard lockfile forte, migrate-if-pending. Marker delta `session-boot.ps1` → `.session-align.marker` (gitignored). **Chiusura automatizzata**: skill `handoff` **Step 4b** lancia `align-clones all --delta --resilient --auto-deploy` (propaga delta + deploy se codice + skip host giù). Verificato **live**: PC=Mac=VM @ `e6dc503`, PROD readyz/login 200, migrate-if-pending → skip. Dottrina → `memory/feedback_full_alignment_doctrine`; ops → `deploy/README.md` §"Full alignment". (Path `--delta` E2E live verde al handoff S979 successivo: deploy=1 delta=1.)

## 0-quaterdecies. Delta S979 (2026-06-09) — linux-pc gemello PROD autonomo (ISOLATO)

Nuovo ambiente **PROD autonomo** oltre alla VM: **`linux-pc`** (192.168.1.11, Zorin OS 17.3 = Ubuntu 22.04, **x86_64**, user enzo, `/home/enzo`). Server completo LAN-accessible: api :8013 + web :3013 (bind 0.0.0.0), systemd `heuresys-advanced-{api,web}` + timer scraping/insights (User=enzo), **DB PostgreSQL 16 LOCALE :5432** = clone 1:1 di `heuresys_advanced` dalla VM (1.1GB; users 161/positions 162/attendance 3180/esco-emb 3040 = VM). **Runtime indipendente dalla VM.** **ISOLATO** dalla dottrina align-clones (mac+vm invariati). Nuovi script (committati): `scripts/{provision-linux-pc,setup-local-pg,clone-vm-db}.sh`. Ecosistema Claude clonato da Windows (overwrite+backup, auth preservata). Deps installate post-1°-align: `postgresql-16-pgvector`, `libpq-dev`. Dottrina/gotcha → `memory/reference_linux_pc_prod_twin`. Counts DB/API/web/CI del progetto **invariati** (linux-pc usa una copia del DB VM). Residuo: integrare linux-pc nelle regole align/deploy (decisione Enzo, backlog).

## 0-quindecies. Delta S980 (2026-06-09) — batch #1-5: 4/5 shipped (CMS P3 + MFA §2.5)

HEAD **`8c9c2c1`** (4 commit `46cadc9`→`8c9c2c1`). Migration su disco: **101 file** (`000001..000102`; +000100/101/102 questa sessione). Nuovo modulo API registrato: **`content-blueprint-links`** (`/v1/content-blueprint-links`). Nuove dep: `@simplewebauthn/server@^13.3.1` (api) + `@simplewebauthn/browser@^13.3.0` (web).
- **DB**: +2 tabelle business `sys.*` — `sys_content_blueprint_links` (mig 000100, tenant-scoped cross-link doc↔blueprint-process) + `sys_auth_mfa_webauthn_credentials` (mig 000102, credenziali passkey); entrambe registry **D/EXCLUDE** → **registry 96 righe, split A23/B16/C23/D34**, 0 UNCLASSIFIED. La 000101 è solo permission-seed (`me:sessions:manage`, 11 ruoli).
- **API endpoint nuovi (+13)**: cross-link 5 (POST/DELETE/by-document/by-process/by-variant) · ESS sessions 3 (`/v1/me/security/sessions`) + `GET /v1/auth/sessions/current` · WebAuthn 4 (`/v1/auth/mfa/webauthn/{registration,authentication}/{options,verify}`).
- **Auth core**: access JWT ora porta il claim **`fam`** (refresh-token family) → `req.user.familyId` (`middleware/auth.ts`) — abilita il current-device ESS attraverso il proxy `next.config /api/:path*` (il refresh-cookie path `/v1/auth` non lo raggiunge). Additivo, **0 regressioni** (auth/mfa/me 79/79).
- **Test**: integration **+19** (cross-link 8 + me-sessions 6 + webauthn 5); E2E **+4 spec** (`content-search`, `content-blueprint-links`, `me-sessions`, `webauthn` — quest'ultimo con virtual-authenticator CDP); `reconciliation-registry.integration` aggiornato 95→96/D34.
- **Web**: `/content` search box · `/content/[id]` linked-blueprints panel + `/blueprints/[variantId]` documentation tab · `/me/security` sessions section + passkey enroll (secure-context gated). i18n parity **1108×2×7**.
- **Non ri-derivati** (full-suite non ri-eseguita: finestra tunnel degradata + chiusura context-budget): API total stimato **~856** (S978 837 +19); CI non ri-eseguita (i 4 commit erano locali fino al push handoff S980). Dettaglio → `SOT_BACKLOG.md` §S980 · priorità/#4 → `.handoff/STATE.md`.

## 0-sedecies. Delta S981 (2026-06-10) — batch 11 item (mandatory-MFA keystone + cap④ P3 close)

HEAD **`4d0b731`** (10 commit S981, tutti pushati, CI verde sul HEAD). Migration su disco: **104 file** (`000001..000105`, gap 000035; +3: `000103` mfa-policy schema+CHECK-extension · `000104` mfa_policy perms · `000105` content-media). `migrate.sh` idempotente ×2 ri-provato (fix D-12(a)-class: il re-add del CHECK login-events in `000005` ora è guarded e `000103` è content-aware — la replay non clobbera mai constraint più nuovi).
- **MVP-4 §2.5 #4 mandatory-MFA SHIPPED** (`0c7d7dc`+`4d0b731`): modulo **`mfa-policy`** (72° modulo, `/v1/mfa-policy` GET/PUT, perms `mfa_policy:{read,manage}` PA+TA) · terzo stato login `mfa_enrollment_required` con sessione ristretta (JWT claim `enr`, roles [], NO refresh) contenuta da **allowlist positiva** nel middleware auth (fuori da mfa/login/logout/health → 401 `MFA_ENROLLMENT_ONLY`) · enrollment pre-sessione in login page (TOTP QR + EMAIL_OTP + passkey) con auto re-login · **WebAuthn authentication ceremony LIVE**: il verify completa il login (cookie + bundle via `completeMfaLogin`) e la login page ha "Usa una passkey". Default OFF (0 policy row → comportamento pre-#4 byte-identico). Review adversarial 19-agenti: tutti i finding confermati fixati in-commit (allowlist, csrfStore sul passkey login, rami exhaustive, tenantExists archived, commenti stale D-21, TOFU documentato). Eventi: +`LOGIN_MFA_ENROLLMENT_REQUIRED`.
- **cap④ CMS P3 — COMPLETO** (`f0f6cf3`+`45f6ebc`): **media object-store** (mig 000105 `sys_content_media`, seam `ObjectStore` local-disk default `MEDIA_STORAGE_DIR=.data/media`, S3/MinIO=driver futuro; 4 route su /v1/content, multipart 10MiB, allowlist mime, download attachment+nosniff; pannello allegati su `/content/[id]`; +`@fastify/multipart`) + **rich markdown nel handbook** (`MarkdownView` GIÀ in `@heuresys/ui@0.1.4` Tier 10 — solo wiring consumer + `@tailwindcss/typography`). Residuo P3: solo ESS-media serve.
- **B-52** (`5b9068a`): `linuxpc` terzo target di `align-clones.sh` (strict da solo, resilienza FORZATA dentro `all`; deploy leg = vm-deploy con `REPO_DIR=/home/enzo/heuresys-advanced`).
- **D-19/D-20/D-21 RISOLTI** (`1613535`/`913b383`/in `0c7d7dc`): shell-test gate (`scripts/test/run-shell-tests.sh` 39 check + workflow CI `shell-tests.yml`, shellcheck 0.9.0 installato sul runner; `env-key-merge.sh` rifattorizzato a funzione unica `declare -f` + CRLF-safe) · boot retry RBAC cache (5 tentativi backoff, 3 unit test) · commenti stale mfa.
- **a11y slice §2.7** (`8251515`): audit completo 35 route ×3 personas — serious = solo 2 regole; **`list` AZZERATA** (1 fix al perspective-switcher del layout → 28 route pulite, probe verde); residuo `color-contrast` 28r/179n = palette brand (decisione Enzo), moderate/minor 0, critical 0.
- **Dependabot** (`fb5bf49`): #31 minor group + #29 faker 10 adottate (override `@types/react` 19.2.17 + clean install D-15-discipline); **#30 @types/node 25 defer-major** (i types seguono il floor engines ≥20).
- **Dossier decision-ready** (`495caff`): `docs/kb/B50_DEFER_UNBLOCK_PACKAGE.md` + `WAVE2_UNBLOCK_PACKAGE.md` (misure live; la sola Decisione #1 sblocca 14-15 pool + 18-24 candidati + 6 branch).
- **Counts S981 (ri-derivati)**: moduli business **72** · API test file **123**, **full suite 877 pass / 6 skip / 0 fail** · web pages **82** · Playwright spec **44** · i18n parity **1136×2×7** · registry **99 righe** (A23/B16/C23/D37), vista: POPULATED 138 · NO_SOURCE 21 · EXCLUDE 8 · NEEDS_DECISION 3 · REFERENCE_ONLY 2, 0 UNCLASSIFIED · `sys_ui_interfaces` 38 · `sys_auth_mfa_policies` 0 righe (default OFF) · stack: +`@fastify/multipart` (api), +`@tailwindcss/typography` (web dev), env +`MEDIA_STORAGE_DIR`.
- **Incidente sessione**: hook claude-mem PreToolUse rotto (worker zombie, `printf Permission denied`) → Read tool bloccato per l'intera sessione; lavorato via Bash/cat + patch python/perl. Mitigazione pending lato setup Claude (vedi STATE open questions).

## 0-septdecies. Delta S982 (2026-06-10) — mega-batch: Wave-2/B-50 close + §2.5 MFA complete + attivazione PROD

HEAD **`3d361b0`** (8 commit `9dd7c68`..`3d361b0`, tutti pushati, **CI 100% verde** sul push). Migration su disco: **107 file** (`000001..000108`, gap 000035; +3: `000106` Wave-2 registry close · `000107` nav mfa-policy UI · `000108` TOFU CHECK ext). Seeds reconciliation numerati fino a **50** (49 succession, 50 branches). `migrate.sh` ×2 idempotente + `db:validate` 7/7 (empty schema diff).
- **Wave-2 / B-50 CHIUSI** (`9dd7c68`, decisioni PM in-session D1=B/D2/D3): `sys_branches` **6** (àncora-OU, 5 RTL+1 HS) · `sys_succession_pools` **17** (15 incumbent-anchor + 2 title-match CEO/CFO) · `sys_successor_candidates` **25** (plan-cascade; FALSE-FRIEND corretto: legacy `succession_candidates.critical_role_id` → FK DDL verso `succession_plans`). talent_pools+members = WON'T-DO esplicito; career cluster + 86 orfani = out-of-scope terminale. **Registry 100 righe (A23/B16/C23/D38)**; vista: **POPULATED 141 · NO_SOURCE 21 · EXCLUDE 8 · REFERENCE_ONLY 2 · NEEDS_DECISION 0 · 0 UNCLASSIFIED**. `000076` assert reso state-aware (pattern D-12) per l'invariante twice-run.
- **MVP-4 §2.5 COMPLETO code-side**: **SMS_OTP code-only** (`b3ac899`: seam `ISmsSender` Console/InMemory + 4 route `/v1/auth/mfa/sms-otp/*` + login step-up + UI; `enrollableKinds()` provider-aware → in PROD resta invisibile finché manca un provider reale) · **TOFU v2** (`6b3dc56`, mig 000108: confirm out-of-band email sul PRIMO fattore self-owned, env `MFA_ENROLL_CONFIRM=auto|on|off` — auto=ON solo con mailer reale ⇒ dormiente in PROD fino alle creds SMTP; + notifica "nuovo metodo MFA" + evento `MFA_FACTOR_ENROLLED` + hardening webauthn: la ceremony filtra `verified`) · **UI admin `/admin/mfa-policy`** (`65e4172`, nav mig 000107 governance/order 51 — primo editor config per-tenant).
- **🔐 ATTIVAZIONE REALE mandatory-MFA su RTL Bank**: `enabled=true, roleCodes=[BLUEPRINT_MANAGER,HRMS_MANAGER,PROCESS_OWNER,READ_ONLY]` — **LIVE IN PROD** (verificato su 8013: alberto READ_ONLY → `mfa_enrollment_required`; personas E2E fuori scope → suite intatta). I 3 test policy-touching (2 integration + spec E2E `login-mfa-enrollment`) ora fanno **snapshot-restore** (il blanket-disable della spec aveva cancellato la prima attivazione → fix `3d361b0`, provato: l'attivazione sopravvive alla suite). `sys_auth_mfa_policies` = 2 righe (RTL on / HS off).
- **cap④ CMS CHIUSA** (`e4eeff8`): ESS-media serve published-only sotto `/v1/me/content` (immagini inline + nosniff, draft/cross-tenant 404 no-leak), handbook embed con rewrite URL admin→ESS + card Allegati + bottone copy-markdown.
- **a11y §2.7 — color-contrast RISOLTO** (`9d48ff5`, delega Enzo + checkpoint visivo): 179 nodi → **0** con 3 ritocchi token app-level (semantici dark `--info/--danger/--success` step-400 · `--color-primary-fg` scuro sul primary dark · label sidebar a token pieno); full audit 35×3 = **0 critical/serious/moderate/minor**; **gate CI alzato a serious=0**. Residuo §2.7 = SOLO mobile sweep.
- **Infra/deps** (`1c91450`): engines floor **≥22** + `@types/node` 22.19.20; PR **#30 chiusa** not-adopted; label defer-major ripristinata su #28. Fix R3 latente: `sys_auth_mfa_factors` non era MAI nel registry (mascherata da has_rows sui residui test) → registrata D/EXCLUDE con amendment in `000062` (deve precedere l'assert per i rebuild freschi).
- **Counts S982 (ri-derivati)**: full suite **899 pass / 6 skip / 0 fail** (125 file run / 127 su disco, +4 file: mfa-sms, mfa-enroll-confirm, me-content-media, reconciliation-wave2) · Playwright spec **46** (+2: mfa-policy-admin, handbook-media) · web pages **83** (+ `/admin/mfa-policy`) · i18n parity **1177×2×7** · `sys_ui_interfaces` **39** · env nuove: `MFA_ENROLL_CONFIRM`, `SMS_PROVIDER`/`SMS_FROM`. Align: PC=Mac=VM=linuxpc @ `3d361b0`→`d2dd02f`; vm-deploy eseguito (readyz/login smoke ok, timer attivi). **Coda S982**: refresh DATI del twin linux-pc (`clone-vm-db.sh` + restart api, sanity counts VM=local) — il DB locale del twin ora include Wave-2 (6/17/25) e la policy MFA RTL attiva, gate verificato live su :8013 (il deploy porta codice+schema; i dati del twin si rinfrescano solo col clone on-demand, B-52 by-design).

## 0-octodecies. Delta S983+S984 (2026-06-11/12) — batch "consuma tutte le code": mobile-a11y zero · ISTAT/ATECO · mandatory-MFA TOTALE

S983 fu interrotta da un riavvio PC (mai handoffata); S984 l'ha recuperata e chiusa. HEAD **`472a91f`** (S983: commit `cc7eb41` TS6 · `9c58f0a` vite8 · `13e73e6` WS-B · `509cad7` WS-D · `0761ca2` WS-C · `5c64f63` WS-E E1-E4 · S984: `df21cb2`/`8f7b13d`/`472a91f`), tutto pushato, **CI 100% verde sul HEAD**. Migration su disco: **108 file** (`000001..000109`, gap 000035; +1: `000109` watermark row `ATECO_2025`). Piano S983: `~/.claude/plans/superpowers-consumiamo-tutte-le-atomic-piglet.md`.
- **WS-A (S983)**: B-40/B-41 → WON'T-DO/OBSOLETE · B-52 riga sanata · dependabot major adottati: **TypeScript 6.0.3** (4 workspace, `cc7eb41` — typescript-eslint 8.61.0 supporta `<6.1.0`, 0 `baseUrl` nei tsconfig) + **vite 8.0.16** (`9c58f0a`). 0 PR dependabot aperte.
- **WS-B/D mobile a11y §2.7 (S983)**: project Playwright **`mobile-a11y`** (Pixel 7, chromium) su `a11y.spec.ts` con output JSON namespaced per project (`13e73e6`); census **ZERO violazioni di OGNI severità su 35 route** → gate reso **incondizionato** (`509cad7`). **§2.7 CHIUSO** (era l'ultimo tail a11y).
- **WS-C cap⑤ P2 ISTAT/ATECO (S983, `0761ca2`)**: 2ª sorgente reference-sync — dispatch generalizzato su `body.source`, connector `istat-ateco-connector.ts`, scheme **`ATECO_2025`** (3257 righe live, generazione parallela: le 3276 legacy `ATECO`/`NACE` intatte), mig 000109 watermark, unit+integration fixture-based. Chiude l'ultima riga cap⑤ (ToS sign-off PM in S983).
- **WS-E mandatory-MFA copertura totale (S983 E1-E4 `5c64f63` + S984 E5/E6)**: fixture set **6 personas** (+ `marco.rinaldi`), secrets single-source con parity-test api↔web; helper dual-mode `loginRaw`/`completeApiLogin` (success | mfa_required→TOTP step-2 | enrollment_required→fail loud); codemod su 85 file + 9 suite MFA adattate (delete scoped `label<>'e2e-fixture'`, snapshot-restore); seeder `db:seed-test-admin` += `ensureTotpFactor`; knob `AUTH_LOGIN_RATELIMIT_MAX` (vitest 10000 / CI+local 200 / PROD 10). **E5 (S984)**: seed 6 fattori + flip psql (`qa_artifacts/s984_mfa_flip.sql`; rollback `s983_mfa_rollback.sql`) → **RTL e HS entrambe `enabled=t, role_codes=NULL`**. **E6 (S984)**: login TOTP headless completo su PROD HTTPS (admin/HS + tommaso/RTL-USER), `/me` 200. Verifica adversarial pre-mutazione (workflow 4-agenti): seeder/flip/E6 ok + **1 BREAKING trovato e fixato** (wave2 raw-login → `loginRaw`, `472a91f`).
- **Fix S984 CI-green (`df21cb2`/`8f7b13d`)**: 3 import `TEST_PASSWORD` morti (lint) · **smoke CI silenziato da app estranea**: `lalibraiascalza.com` (nuovo next-server sulla VM, 2026-06-11) aveva occupato :3100 → il `curl -sf` passava contro l'app sbagliata; porta → **3187** + guard pre-bind fail-loud + identity-check `grep heuresys` su /login.
- **Addendum S984b (post-handoff, `9900ccd`, CI 4/4 verde)**: chiusura di CLASSE del conflitto porte sull'host VM multi-app — guard pre-bind + identity-check anche sul boot **API :3001** del job smoke (simmetrico al web :3187) + **"Mappa porte VM"** in `deploy/README.md` (census S984: advanced/evo/Grafana/lalibraiascalza/vibe-kanban/exporters; :3001 e :3187 RISERVATE CI; regola consulta-prima-di-deployare). **Mac riallineato** via watcher (era irraggiungibile al close S984): clone pieno, memorie incluse.
- **Counts S984 (ri-derivati)**: full suite **912 pass / 6 skip / 0 fail** (127 file run / 129 su disco; +2: `mfa-fixture-parity`, `istat-ateco-connector`) · Playwright spec **46** (file invariati; progetto mobile-a11y nuovo) · `sys_auth_mfa_policies` 2 righe **t/ALL + t/ALL** · `sys_auth_mfa_factors` **9** (6 fixture + 3 throwaway unverified di tommaso, residuo E2E cosmetico) · tabelle `sys.*` con righe: **144** · gates post-flip: vitest full + smoke CI dispatch + Playwright composita (main 123 pass; rerun prod-build **100 pass/7min**; finale 1 pass — i 97 fail del main run = artefatto finestra-sessione, vedi **D-24/D-25**).

## 0-novodecies. Delta S985 (2026-06-12/13) — D-24/D-25 risolti · D-26 scoperto · dossier roadmap post-v1.0

HEAD **`a629844`** (2 commit: `41e01ed` test-infra+a11y · `a629844` docs/kb; + handoff). Migration su disco: **108 file** (invariate). Discovery via workflow ultracode 7-agenti (`s985-discovery`).
- **D-24 RISOLTO**: diagnosi corretta — la sessione E2E muore a **15 min** (TTL `hrx_access`; il refresh non attraversa MAI il proxy `/api` → D-26), il "45min" era mascheramento da audit a11y **vacui** sulla login page. Fix: **`apps/web/playwright.prod.config.ts`** = entrypoint full-suite canonico (`pnpm test:e2e:prod`: next build + `next start` fail-fast; catena `setup → mobile-a11y + a11y-desktop → setup-refresh → chromium` con re-login mid-suite; showcase gated su `NEXT_PUBLIC_ENABLE_SHOWCASE`; catena nel SOLO config prod per non gravare le iterazioni single-spec dev) + **assert anti-vacuità URL-based** in `runAxeOnRoute` + riga dottrina CLAUDE.md. Audit desktop ora nel project **`a11y-desktop`** (JSON in `test-results/a11y-audit/a11y-desktop/`).
- **D-25 RISOLTO**: snapshot sanitizzato in `i18n-en.spec.ts` (readback 'en' = sempre leftover → normalizzato 'it'; chiuso bug latente `{locale:null}`→400) + afterAll loud con SQL recovery + **guard pre-run in `auth.setup.ts`** (PATCH `locale='it'` per persona, 0 login extra) — crash-resilienza provata con doppia simulazione poison→heal.
- **🔴 D-26 SCOPERTO+REGISTRATO** (non fixato, scope discipline): `hrx_refresh` path-scoped `/v1/auth` mai inviato su `/api/v1/auth/refresh` + `proxy.ts` redirige su cookie scaduto → **utenti reali PROD sloggati ogni 15 min** con re-login TOTP. Fix = design decision, sessione dedicata (~2-4h).
- **a11y**: l'anti-vacuità ha smascherato 2 violazioni serious REALI (`scrollable-region-focusable`) nascoste dai census storici vacui → fixate: **`@heuresys/ui@0.1.5`** (upstream `a0aacd2` + npm publish: `<main>` shell `tabIndex=0`) consumato `^0.1.5` ×3 package.json; matrice `/admin/roles` `tabIndex/role/aria-label`.
- **Dossier roadmap post-v1.0** → `docs/kb/POST_V1_ROADMAP_DOSSIER.md` (decision-ready: 3 candidate storiche misurate, 12 residui in 3 fasce, 9 direzioni nuove, conteggi pinnati). **F7 = label stale, terminale** (3 chiusure verificate WS-6d/S961/S965-S969).
- **Counts S985 (ri-derivati 2026-06-12/13)**: route registrations API **407** (`grep app.<verb>` su src) · API test file **129** su disco / 889 `it()` (suite API non ri-eseguita — cambi web-only) · Playwright spec **46** · **full suite E2E prod 200 pass / 1 flaky-retry-green in 9.6min** (gate D-24) · typecheck **4/4 ws** · vista reconciliation: **POPULATED 143 · NO_SOURCE 21 · EXCLUDE 6 · REFERENCE_ONLY 2** (drift fisiologico: `sys_auth_mfa_factors` D/EXCLUDE ora has_rows→POPULATED con i 6 fattori fixture) · `sys_auth_mfa_factors` **6** (i 3 throwaway di tommaso autopuliti dal run suite, verificato) · stack: `@heuresys/ui` 0.1.4→**0.1.5**.

## 0-vigesimo. Delta S-100X-0 (2026-06-13) — apertura programma RELEASE 100X (doc-only, read-only sul codice)

Sessione di apertura del programma **100X** (audit forense QA E2E + miglioramento radicale). **Read-only sul codice**: nessun cambio a src/db/config/CI/deploy → **counts DB/API/web/CI/migration INVARIATI da S985** (108 migration `000001..000109`, 72 moduli, ~407 endpoint, full suite 912/6, web pages 83, Playwright 46). HEAD = commit di handoff (post `eb98acc`).
- **Deliverable doc-only** in nuovo albero `docs/kb/improvement/`: `MASTER_PLAN_100X.md` · `TODO_100X.md` · `BASELINE_METRICS.md` · `INTERVIEW_LOG.md` · `AUDIT_PROTOCOL.md` · `FINDINGS/` (README + `S-100X-0_recon.md`) · `DOSSIERS/` (register D-01..D-14) + epic 100X in `SOT_BACKLOG.md`. Commit `eb98acc`.
- **Recon 3-agente read-only**: leve top = CI runner self-hosted unico = VM prod (SPOF, 7/8 workflow) · `vm-deploy.sh` distruttivo senza rollback · suite 901 `it()` single-worker serial · repo on-disk **31G** (24G `apps/web/.next` + 3.7G dumps, rigenerabili) · `drizzle-orm`/`-kit` dead-dep · 8 env var non documentate. Asset confermati: raw SQL (0 N+1), boot hardened, design-system pulito (0 primitive in-repo), auth hardened, git-history sano (.git 23M).
- **Intervista** (gate): asse dominante **robustezza/operability** · breaking **aperto/radicale via dossier** · appetite **evoluzione selettiva** · scope **audit completo poi decido**. 6 default accettati (`INTERVIEW_LOG.md`).
- **WIP D-26 scoperta nel working tree** (NON committata, fuori da questo handoff): fix parziale silent-refresh — vedi `DEBT_REGISTER.md` D-26. Resta uncommitted per la sessione D-26 dedicata.
- **Incidente d'ambiente** (NON repo): hook `claude-mem` worker down tutta la sessione → plugin disabilitato in `~/.claude/settings.json:156` (reversibile, backup `settings.json.bak-100x-*`). Tracked come item WS-L del programma 100X.
- **Prossimo**: S-100X-A1 (audit WS-G CI/CD & deploy). Stack invariato (TS 6.0.3 · Next 16.2.7 · Fastify 5.8.5 · Zod 4.4.3 · vitest 4.1.8 · node dev-host v24.3.0).

## 0-unvigesimo. Delta S987 (2026-06-13) — programma post-v1.0 avviato: Fasi 0-2 + #8a

HEAD **`a589a6e`** (5 commit `fa564fe`..`a589a6e`, pushati al handoff). Migration su disco: **109 file** (`000001..000110`, gap 000035; +`000110` fix mapping HS). API test file **130** (+`auth-refresh-cookie`), Playwright spec **47** (+`session-refresh`). Moduli business **72** (invariato — R6 non è un modulo). Avviato il **programma post-v1.0 8-fasi** (decisioni in `memory/project_post_v1_program_s987` + `POST_V1_ROADMAP_DOSSIER §5` + `SOT_BACKLOG §S987`).
- **Fase 0 — D-26 silent-refresh ✅ RISOLTO** (`fa564fe`, DEBT_REGISTER D-26): opzione (a) `REFRESH_COOKIE_PATH='/'` + clear legacy path + single-flight + Web Lock cross-tab + proxy resumable. Gate: typecheck 4/4 + vitest auth 53/53 + E2E `session-refresh` verde (isolato `--no-deps`, la catena mobile-a11y→chromium era bloccata da D-27).
- **Fase 1 — Audit 100X-A1 WS-G ✅** (`0ba0eb4`, doc-only read-only): `FINDINGS/WS-G.md` 30 finding — **1 CRITICAL nuovo** (repo PUBBLICO + runner CI su host PROD + trigger pull_request → fork-PR ACE su prod), 10 HIGH, +5 QW-G. Insight: wall-clock CI ~80-90% **coda** sul runner unico (typecheck 14m wall/76s compute); `main` 0 required-status-checks; PROD traccia main HEAD non un tag. **D-08 → security-priority**.
- **Fase 2 — Quick-wins CLASSE A ✅** (`fdd412b` + `a279f5c`): **R5** backup DB schedulato (`scripts/backup-db.sh` pg_dump -Fc + retention + off-host opt-in) + systemd `backup.{service,timer}` 01:30 · **R7** reindex embeddings weekly (systemd `reindex.{service,timer}`, CLI `embeddings:backfill`; real reindex ⛔ VOYAGE_API_KEY su VM) · **3.7** DR drill (`scripts/dr-drill.sh` restore+RPO/RTO) · **R6** OpenAPI/Swagger (`/docs` + `/openapi.json` 255 path, gated `API_DOCS_ENABLED` default OFF). Timer installati al prossimo vm-deploy/bootstrap (loop esteso).
- **Fase 3 — #8a mapping HS ✅** (`a589a6e`, mig `000110` LIVE): `brownfield.tenant_id_mappings` `d5855519` corretto RTL→HEURESYS (`8bc5bc59`); 000033 `ON CONFLICT DO NOTHING` non si auto-curava. Idempotente. Backup pre-op VM `pre-fase3-s987.dump` (204M).
- **Stack**: +`@fastify/swagger` ^9.7.0 (era transitivo) + `@fastify/swagger-ui` ^6.0.0; env +`API_DOCS_ENABLED`. **Debiti**: D-26 RISOLTO; **D-27 NUOVO** (🟡 a11y mobile `scrollable-region-focusable` /me/certifications + /kpis). **Residuo**: Fase 3 (#8b chiara · R1 feedback · R2 crosswalk · R3 cleanup+families+ESCO) + Fasi 4-8 + audit 100X A2..A11. claude-mem resta disabilitato.

## 0-duovigesimo. Delta S988 (2026-06-13) — batch autonomo item 1-6: Fase 3 dati COMPLETA + D-08 mitigato + D-27 risolto + A2 audit

HEAD **`cb880de`** (8 commit `dfd664d`..`cb880de`, pushati al handoff) + sibling ux-design-shared `c7832eb` + **`@heuresys/ui@0.1.6` pubblicata** (npm). Migration su disco: **114 file** (`000001..000115`, gap 000035; +5: `000111` chiara · `000112` ATECO↔NACE crosswalk · `000113` engagement-feedback schema · `000114` engagement-feedback perms · `000115` job_roles cleanup/families/ESCO). API test file **131** (+`engagement-feedback`). Moduli business **73** (+`engagement-feedback`). Batch eseguito con decision-authority session-scoped (Enzo).
- **#1 D-08 CRITICAL MITIGATO** (`dfd664d`): fork-guard job-level `if: (event_name != 'pull_request' || head.repo.full_name == github.repository) && !defer-major` sui 7 workflow self-hosted → una fork-PR è skippata dal controller GitHub PRIMA di qualsiasi checkout sul runner PROD; push/PR-same-repo/Dependabot preservati. Robustezza D-08 più ampia (SPOF/rollback/off-prod) resta dossier 100X. WS-G F-WS-G-1 annotato MITIGATO.
- **#2 Fase 3 dati COMPLETA**: **#8b** chiara.spenuso importata (`ce6023b`, mig 000111: employee-centric `LEGACY_EMP::2b1cc664`, HEURESYS, USER, LOCAL+ARGON2ID login; override soft-delete legacy; spen.zosky verificato NON-distinto, escluso) → **users 161→162** (headcount test bumpati `80c61ec`). **R2** crosswalk ATECO↔NACE (`a6fd8cf`, mig 000112): **5730 righe** (NARROWER 2865 + BROADER 2865 reciproco, confidence 1.0, deterministico parent_code, 0 unresolved); tabella REFERENCE_ONLY→POPULATED. **R1** modulo `engagement-feedback` (`b3256dc`, mig 000113/114 + seed 51): 2 tabelle (`sys_engagement_feedback` anonimo + `sys_engagement_action_plans`), import RTL **400 feedback + 6 action-plan** (reviewer/owner via LEGACY_EMP::, 0 unresolved; submitter anonimo), 13 endpoint `/v1/engagement-feedback/*`, perms `engagement_feedback:{read,create,update,delete}`, 11 integration test; registry 100→102 (A25/B16/C23/D38). **R3** (`f37adcd`, mig 000115): DELETE 91 OLDDB job_roles (227→136), wire 25 RTL-ROLE a `sys_job_families` + seniority, ESCO enrich 25 (offline catalog + pg_trgm + acronym-expand, floor 0.70 → 14 auto + 11 `low_confidence` flag). **⚠ DEVIAZIONE R3** (CLASS-A, veto Enzo): usato il catalogo esistente `sys_job_families` invece di creare `sys_job_role_families` (no duplicato/FK-retarget).
- **#3 D-27 a11y RISOLTO** (`9e097cb` + ux `c7832eb`): `@heuresys/ui` `DataTableWithCrossHair` scroll-region `tabIndex=0`+`role=region`+`aria-label` → `@heuresys/ui@0.1.6` pubblicata + consumata `^0.1.6` ×3. Verifica: **mobile-a11y prod gate 42/42** (incl. /kpis + /me/certifications). 8 righe E2E cert residue pulite; spec teardown documentato (no DELETE endpoint).
- **#5 S-100X-A2 audit** (`cb880de`, doc-only): `FINDINGS/WS-H.md` (5 sub-agent, 761k token). 🔴 HIGH `TRUST_PROXY=false` rate-limit per-IP (→ **D-28**), 🟠 MED rate-limit keyGen/skill-taxonomy-no-requirePermission/matching-reindex/media-no-sniff, supply esbuild-HIGH-tenuta-aperta-da-override + drizzle dead-deps, 🟢 LOW mailer redaction-bypass. Asset: SQL param 60 mod, Zod 415/415, 0 secret in response/log, `pnpm audit --prod`=0. **Doc-fix flaggato**: RBAC reale **11 ruoli / 586 mapping / 133 permessi** (questo SOT + CLAUDE.md dicevano "8/394" — stale post-R2).
- **Items #4 (Fasi 4-8) e #6 (B-10b m2b) NON eseguiti**: feature major multi-sessione `design→spec→ok→implementa` (autorità *cosa* = Enzo) → deferiti onestamente (`.handoff/STATE.md` top-priorities).
- **Counts S988 (ri-derivati)**: migration **114** (000001..000115) · moduli business **73** · API test file **131** · **full API suite 925 pass / 6 skip / 0 fail** (R3 gate) · route registrations **418** · web pages **83** (engagement-feedback è API-only) · Playwright spec **47** · typecheck **4/4** · DB: users **162** · positions 162 · tenants ACTIVE 2 · sys tables con righe **140** · engagement_feedback 400 · action_plans 6 · ateco↔nace crosswalk **5730** · job_roles **136** · registry view POPULATED **145** / NO_SOURCE 21 / EXCLUDE 7 / REFERENCE_ONLY 1, 0 UNCLASSIFIED · stack `@heuresys/ui` 0.1.5→**0.1.6**. **Gate pre-deploy** (CLASS-A): full chromium prod E2E NON girato (cambi web = solo bump a11y-additivo + API/DB) → coperto da typecheck+API-925/0+mobile-a11y-42/42+build+CI-smoke-on-push.
- **Coda S988 (2026-06-14, richiesta Enzo)**: **D-28 RISOLTO+deployato** (`79b7a97`) — verifica `.env` VM ha rivelato il footgun `z.coerce.boolean("false")===true` (era effettivo `trustProxy=true`/spoofabile, non `false`); fix `apps/api/src/config/trust-proxy.ts parseTrustProxy` (hop-count/CIDR, chiude il footgun) + `env.ts` + unit test 4/4; VM `.env TRUST_PROXY=1` (1 hop nginx, anti-XFF-spoof), linuxpc/local `false` (bind diretto, ora corretto). + **linuxpc DB ri-clonato dalla VM** (`clone-vm-db.sh`: crosswalk 1840→**5730**, chiara/engagement-feedback live, 162/162/3180 = VM). 2 re-align full `all --deploy` (memorie 39 propagate, VM+linuxpc deployati, `.env` per-host preservato da env-key-merge). PROD www/login+api/readyz 200. Migration su disco invariate (114, `000001..000115`).

## 0-bis. Delta S963 → S966 (refresh 2026-06-05)

Lo snapshot §0 e i conteggi §1-§9 erano scritti a S957/S962. Questo blocco riconcilia i cambiamenti di stato fino a **S966**. Tutto **pushato su origin/main, CI 6/6 verde** (HEAD `c4c6363`).

- **🔧 D-15 RISOLTO + hardening pubblicato (S966)**: era un pnpm **stale virtual-store** (orphan `@types/react@19.2.14` lasciato dal flip override S965 *senza clean install*; il source `.tsx` di `@heuresys/ui/brand/candidates` cadeva sul hoist-root stale → "two unrelated types"), NON un problema upstream come ipotizzato. Fix **locale** (`@types/react` override → 19.2.16 + clean install, `5c59295`) + **hardening upstream** `@heuresys/ui@0.1.3` (`@types/react`/`-dom` optional peerDep, pubblicato npm + consumato `^0.1.3`, `a4abb44`). Single physical instance, 0 unmet-peer warning, typecheck 4/4 + build web/showcase verdi. Pulito anche `ui/tsconfig.tsbuildinfo` tracked in `ux-design-shared` (`c974988`). Vedi `DEBT_REGISTER.md` D-15.
- **🚀 Deploy PROD + allineamento workstation (S966)**: Mac (`mac-local`) + VM (`oracle-vm-default`) allineati a `c4c6363`; `vm-deploy.sh` eseguito (frozen install `@heuresys/ui` 0.1.2→0.1.3, build api tsup + web next 66 pagine, restart, **api/readyz OK · web/login 200**). `https://www.heuresys.com` live (TLS 200).

- **🌐 Milestone i18n (IT default + EN) CHIUSA** (S962→S965). Era italiano-hardcoded (70/71 pagine). Ora **bilingue reale**: 7 namespace (common/shell/analytics/admin/blueprints/hr/ess) IT+EN, **45 pagine app migrate** (Fasi 2-5 fan-out 4 agenti S965: ess 16/admin 17/hr 7/blueprints 5) + analytics (Fase 1 S964) + shell/login (Fase 0). **IT byte-identico** (behavior-preserving). Locale persistente in `sys_user_preferences` (mig `000069`) + `PreferencesApplier`. **EN gate** `i18n-en.spec.ts` (server-pref locale, 6 ns). **Guardrail ESLint `i18next/no-literal-string` = `error`** su `apps/web/src/app/**` (`showcase/**` escluso §10). Parity **802×2×7**. Design: `docs/superpowers/specs/2026-06-04-i18n-milestone-design.md`.
- **📊 Capability ① BI analytics COMPLETA (P1→P3)**: P1 workforce/kpi (S958) + P1b frontend (S961) + **P2** attendance/compensation/skills (S962) + **P3 org-network** (S965, `GET /v1/analytics/org-network` — span-of-control/depth/reach via recursive CTE su `position_reports_to_position_id`, mig `000070`). 6 viste `/analytics/*` live-data, scope PLATFORM/TENANT/TEAM, `analytics:view`. Anchor org-network: 162 pos / 3 root / 28 mgr / span 5.68 / depth 4.
- **🤖 AI ② substrate P0** (S961): pgvector (mig `000060`) — extension + 4 sidecar embedding tables `vector(1024)` + HNSW + `matching:read/admin`. **② P1 backfill gated su `VOYAGE_API_KEY`** in `.env` VM (azione Enzo).
- **🔧 Dependabot sweep** (S964): applicati **pino 9→10.3.1**, **upload-artifact v4→v7**, **#26 minor group** (react 19.2.7, @tanstack/react-query 5.101, i18next 26.3.1, vitest 4.1.8, lucide-react 1.17, tsx 4.22, typescript-eslint 8.60, react-hook-form 7.77). **3 major DEFERITI** (`defer-major`): next 16, typescript 6, vite 8. 0 security alert.
- **⬆️ next: 15.5.18 → 15.5.19** (S965, backport patch, opzione A). **next 16 (16.2.7 = latest) = opzione B in STAND-BY** fino a un driver (feature/perf necessaria o EOL linea 15): bump dedicato = rimuovi `eslint` key da `next.config.js` + `eslint-config-next@16` + decisione `middleware.ts`→`proxy.ts` + full E2E re-validation prod-mode. Pre-validato verde-in-isolamento S964 ma da ri-validare col codice i18n attuale. Backlog B-23 / PR #21.
- **🗄️ Reconciliation registry** (S960/S961): `sys.sys_reconciliation_registry` (mig `000058`) POPULATED 112/147 · 0 UNCLASSIFIED. Migration su disco ora **`000001..000070`** (gap `000035`): +`000059` analytics-nav · `000060` pgvector · `000061..000065` reconciliation/SDBI · `000066..000068` BI-P2-nav · `000069` i18n-locale · `000070` org-network-nav.
- **🌐 PROD live `https://www.heuresys.com`** (TLS, S962): nginx → web `:3013` → api `:8013`, `COOKIE_SECURE=true`, nginx conf versionata `deploy/nginx/` (S964). Deploy = `scripts/vm-deploy.sh`. **PROD aggiornata a `c4c6363` (S966)** — `vm-deploy.sh` eseguito: build api+web verdi, restart, api/readyz OK, web/login 200; Mac + VM allineati.
- **CI**: 6 workflow self-hosted (typecheck/lint/test-integration/build-web/playwright-smoke/i18n-parity) + showcase deploy. **6/6 verde** su `7c2f4ee` (S965). Playwright ora **~29 spec**.
- **F7 showcase** (S965): tokenize colori già completo (0 raw); split `SystemHealthDashboard` + extract `DashboardShell` (lib-owned `@heuresys/ui`) segnalati → decisione Enzo.
- **✅ Debito react override RICONCILIATO (S965)**: `pnpm.overrides` react/react-dom allineati **19.2.5→19.2.7** (peer-warning react = **0**, dedupe runtime OK, typecheck 4 ws verde). **D-15 RISOLTO (S966)**: `@types/react` override → **19.2.16**. La diagnosi "doppia istanza via lib pubblicata" era errata; root-cause reale = pnpm **stale virtual-store** dopo il flip override S965 *senza clean install* (orphan `19.2.14` + stale hoist-root). Fix locale (override 19.2.16 + clean install, commit `5c59295`): single instance, 0 warning, typecheck 4/4 + build web/showcase verdi. Hardening upstream **pubblicato**: `@heuresys/ui@0.1.3` (optional `@types/react`/`-dom` peerDep, upstream `e63f3fe`) + consumer bumpato `^0.1.3` (commit `a4abb44`) — la lib ora forwarda il `@types/react` del consumer (subtree → 19.2.16), trappola stale eliminata alla radice. `@types/react-dom` 19.2.3 = latest corretta.

## 0-ter. Delta S966 → S968 (refresh 2026-06-05)

Sessione ultracode di **chiusura terminale** (4 item del menu + dead-weight removal). Tutto pushato, `main` synced, CI verde.

- **🧩 F7 — `/system-health` wire-to-live (mock eliminato)** (commit `29f27da`, CI 5/5 verde): la pagina produzione PLATFORM_ADMIN ora legge dati 100% live da `GET /v1/observability/system-health` (pool/RBAC/tenant-fleet/auth/schema/audit/request-metrics) + `GET /v1/auth/role-permissions` (RBAC matrix pivot granted/denied). Nuovi `apps/web/src/lib/api/observability.ts` (`useSystemHealth`/`useRolePermissions`) + `apps/web/src/components/SystemHealthLive.tsx`. 4 sezioni senza backend droppate (LogStream/SQLSlowQuery/IncidentTimeline+Alert/sparkline) con rationale documentata. Showcase mock (`SystemHealthDashboard.tsx`) invariato. `system-health/layout.tsx` → pass-through (chrome authenticated standard, niente takeover demo). E2E `system-health.spec.ts` ora asserisce su dati live (tenant reali; mock GENESIS_DEMO/ACME_CORP assenti; RBAC matrix live). Chiude la violazione live-data-doctrine di `/system-health` (obs 15317/15372).
- **📊 Viz renderers (tappa B) → CHIUSO** (commit `0d09b0d` + `8d0231c`): subsystem già completo (Mermaid `visualizations/[id]` + ECharts force-graph `organization/org-chart` + ECharts bars `analytics/org-network` + lib Cytoscape `NetworkGraph`/`KGGraphCanvas`); 9 `graph_type` a stato terminale (bucket-map: ORG_CHART rendered+seeded · 6 renderer-ready/seed-deferred · SUCCESSION_MAP no-source · PIM ridondante), brand-gate SOLLEVATO. **`reactflow` dead-weight rimosso** dalla lib → **`@heuresys/ui@0.1.4`** (npm public, ux-design-shared `047d6b9`); consumer bumpati `^0.1.4`, lockfile rigenerato (`reactflow@` 0 entries). Mappa: `docs/kb/VISUALIZATION_RENDERERS_CLOSURE.md`.
- **🗂️ SDBI Phase 2 (B-10) → CHIUSO-as-umbrella** (commit `7b0ef9e`): 5/8 macro-aree terminali (PerformanceReviews/Feedback360 via slice `000065`; Documents/Compensation-history/TalentPool assorbite o nel reconciliation registry); 3 reali (Surveys/Mentorship/PredictionsML, schema target mancante) scorporate in **B-10b** (deferred modeling stream P3, ~22-27h/3 sessioni). ADR-0014 footnote di implementazione. Mappa: `docs/kb/SDBI_PHASE2_CLOSURE.md`.
- **🔧 B-31 ssh-agent → DONE** (commit `5032c1c`): era già realizzato da ADR-0021 (task `HeuresysTunnel5433` live, verificato), solo doc allineata in `SOT_BACKLOG.md` (era stale in 3 punti).
- **Stack**: `@heuresys/ui` ^0.1.3 → **^0.1.4** (−reactflow). Sibling ux-design-shared HEAD `047d6b9`. DB/migration/API counts invariati (sessione no-DB).
- **PROD aggiornata a `36676f0`** (S968): Mac (`mac-local`) + VM (`oracle-vm-default`) allineati; `vm-deploy.sh` eseguito (build prod api tsup + web next, restart systemd, verify **api/readyz OK · web/login 200 0.01s · heuresys.com TLS /login 200**). **F7 (`/system-health` live) ora in produzione.**

## 0-quater. Delta S968 → S969 (refresh 2026-06-06)

Sessione ultracode: aggregato 7-item aperto con **discovery 7-agenti evidence-based** (note backlog in parte stale). **4 commit pushati su `origin/main`, CI 7/7 verde** (HEAD `3f9c694` pre-handoff).

- **B-20b zod4 deprecation sweep** (`84a6042`): **644 edit-site** in `packages/shared`+`apps/api` forme v3→canoniche zod4 (`.string().uuid()`→`z.uuid()` ×477, `.string().datetime()`→`z.iso.datetime()` ×151, `.email()` ×8, `.url()` ×2, `.strict()`→`z.strictObject()` ×2, refine `{message}`→`{error}` ×4). Sono **alias che forwardano agli stessi validator** su zod 4.4.3 → behavior-preserving (no runtime change, deprecation hint silenziati). `dist/schemas/analytics.d.ts` riallineato (era stale `ZodString`→`ZodUUID`). Suite API 657/0.
- **BI ① P2-ext `/analytics/overtime`** (`5aec451`): **7ª vista analytics** full-stack — `GET /v1/analytics/overtime` (request/approval lifecycle su `sys_overtime`, **distinta** dalle ore di straordinario lavorate in attendance: ~77% disgiunte, sommarle double-counta). byStatus/byType/monthly/byOrgUnit, scope PLATFORM/TENANT/TEAM, `analytics:view`. Schema shared + repo raw SQL (scope subject-centric) + service + route + **4 integration test** (anchor live: 221 req / 676.00 h / €34.267 / tutte PENDING / 4 type / 4 mesi / 22 OU) + pagina `@heuresys/ui` (donut status + bar type + line mese + bar OU) + nav mig **`000071`** (order 40, icon `Clock` aggiunta a ICON_MAP) + i18n it/en (incl. label localizzate status/type). E2E live exit 0. **#8b category heatmap = NON sbloccato** (`skill_category_id` 0/21939, serve data-task).
- **B-23 Next 15.5.19 → 16.2.7** (`5721ca6`): web+showcase. **`middleware.ts`→`proxy.ts`** (export `proxy`, runtime nodejs — verificato build `ƒ Proxy (Middleware)` + prod `/`→307 redirect); rimosso `eslint:{ignoreDuringBuilds}` da 2 `next.config.js`; **`eslint.config.mjs` migrato al flat-config di eslint-config-next@16** (import diretto `eslint-config-next/core-web-vitals`, filtrato l'entry `next/typescript` per evitare redefine `@typescript-eslint`, stripped il babel-parser che crashava `no-unused-vars` — FlatCompat non più compatibile → "circular structure"); 2 fix `react-hooks/set-state-in-effect` (login dead-effect rimosso; `data-table-panel` adjust-state-during-render). **Gate**: typecheck 4/4, lint clean, build web + showcase-static, API 661/0, prod-smoke (/login 200, /→307), **Playwright-smoke CI verde** (gli 8 fail E2E **prod-mode** = 7 showcase-smoke flag-off + 1 login-mfa flaky → non-regressioni verificate).
- **F7 split/extract** = verificato **già-fatto/cosmetico** (DashboardShell lib-owned in `@heuresys/ui@0.1.4`; mock `SystemHealthDashboard` = fixture showcase intenzionale, prod usa `SystemHealthLive`). Nessun codice.
- **Prune mirror showcase** (`3f9c694`): `apps/showcase/src/components/data-table-panel.tsx` rimosso — `sys.sync-showcase.sh` prune i componenti i18n non-portabili (ADR-0013 R2); il file committato era residuo pre-i18n.
- **Stack**: **next 16.2.7** (web+showcase), **eslint-config-next 16.2.7**.
- **🚀 Deploy PROD + allineamento workstation (S969)**: Mac (`mac-local`) allineato `36676f0`→`2e42c2a` (frozen install); **`vm-deploy.sh` eseguito** su `oracle-vm-default` (reset hard origin/main, frozen install, build prod api tsup + **web next 16 ARM64** `ƒ Proxy (Middleware)`, restart systemd `active`/`active`, verify **api/readyz OK · web/login 200 0.009s**). Public TLS `https://www.heuresys.com`: `/login` 200, `/`→307, **`/analytics/overtime`→307 (route live)**, `api/readyz` 200. **next 16 + vista overtime ora in produzione.**
- **Pending decisione Enzo** (non autonomi — autorità semantica): #9 bridge job→position (sblocca 3 tab) · #5 SDBI B-10b · #2 LOOKUP_FK crosswalk vs out-of-scope · #8b skill-category data prereq. Dettaglio `SOT_BACKLOG.md`.

## 0-quinquies. Delta S969 → S970 (refresh 2026-06-06)

Sessione ultracode, aggregato 7-item un-punto-alla-volta. **4 commit pushati su `origin/main`** (HEAD `5f164f2` pre-handoff). I 4 "pending decisione Enzo" del S969 risolti dall'utente in-session.

- **#1 bridge job→position — KPI leg** (`6365097`): seed `41` popola `sys_position_kpi_requirements` **172 / 43 pos / 1 tenant** via legacy `tenant_job_kpis` (employee-mediated I14: position→legacy_employee→assignment corrente→KPI; `job_kpis` ESCO = dead-end misurato, `tenant_jobs.source_template_id` 0/20). `kpi_definition_id` per code (KPI1..4 4/4). Dedup `DISTINCT ON (position,kpi)`. **Leva successione DEFERITA** (decisione Enzo): `succession_plans.position_id` 100% NULL → 9/31 anchor → gap-esplicito (seed `42` registry `DEFER S970`). Test 4/4.
- **#4 LOOKUP_FK `sys_process_kpi_templates` → OUT-OF-SCOPE** (`6b33d0b`, chiude **B-42**): card brownfield `table_mappings` IMPORT→EXCLUDE + registry `declared_status`→EXCLUDE (`card_classification` precede `declared_status` in `fn_reconciliation_status`). Razionale: registry v5-native (23 process bancari 00..22) vs legacy `business_processes` multi-industria (12 banking/7 energy/7 food, 0 code-overlap) = mismatch tassonomico, non code-bug. Test 3/3.
- **#5 skill-category mapping** (`47d9932`, sblocca **#8b**): seed `44` +**7ª categoria** 'Technical / Domain Expertise' + `UPDATE` 31 skill referenziate → 23 behavioral + 8 technical. `skill_category_id` nullable (additivo). Test 3/3. Heatmap per-categoria data-ready.
- **#2·m1 SDBI Mentorship** (`5f164f2`, 1ª delle 3 milestone **B-10b**): mig `000072` 4 tabelle `sys_mentorship_programs`/`sys_mentorships`/`sys_mentorship_sessions`/`sys_mentor_match_scores` (conv 000065) + mig `000073` RBAC `mentorship:{read,create,update,delete}`. **Modulo API `/v1/mentorship/*` 17 endpoint** (programs/pairings/sessions full CRUD + match-scores read-only), scope tenant-only, shared Zod v4 + repo/service/routes. Seed `45` import RTL **5/63/150/30** (employee-centric LEGACY_EMP:: 100%; skill via **ESCO-URI bridge** 30/30 — correzione adversarial). Test **22** (15 API + 7 data-level). 3 rettifiche in-volo: registry bucket `varchar(1)`, `node-pg` date→Date (normalizzato), test registry count A10→A14.
- **#6 reconciliation umbrella** = stato terminale: registry **POPULATED 118** · NEEDS_DECISION 7 · NO_SOURCE 17 · EXCLUDE 6 · REFERENCE_ONLY 3 (**151 tabelle, 0 UNCLASSIFIED**; bucket A14/B16/C23/D25 = 78 righe).
- **Counts**: migration **000001..000073** (72 file, gap 000035); recon seed **45**; API test file **101**; suite API **686 green**. Stack invariato (next 16.2.7, @heuresys/ui ^0.1.4, zod 4.4.3).
- **🚀 Deploy PROD + allineamento (S970)**: Mac + VM allineati a `origin/main`; `vm-deploy.sh` eseguito; **modulo `/v1/mentorship` live** (DB centrale già migrato 000072/073 + seedato 45 via tunnel in-session → deploy ricostruisce il codice, RBAC cache ricarica `mentorship:*` al restart).
- **Pending prossima sessione**: **#2·m2 Surveys + #2·m3 PredictionsML** (2 milestone B-10b residue) · ② AI P1 (gated key) · #7 MVP-4 · #8 cap ③④⑤.

## 0-sexies. Delta S970 → S971 (refresh 2026-06-06)

Sessione ultracode: capability **② AI Semantic Matching — P1** consegnata e **live in PROD**. **6 commit pushati** (`6fcfca0`→`fab74ce`), CI verde, `vm-deploy.sh` eseguito.

- **Gate `VOYAGE_API_KEY` SBLOCCATO**: Enzo ha aggiunto un metodo di pagamento Voyage (dashboard Billing → Preferences: company/personal + indirizzo, prerequisito che attiva "Add payment details") → **Usage Tier 1** (16M TPM / 2000 RPM, da 3 RPM/10K TPM del free-tier). Costo reale **$0** (~1,2M token entro i 200M gratis). Budget limit impostato come safety. La chiave vive solo nel `.env` locale (gitignored) dove gira il backfill; **la VM/serving NON la riceve**.
- **Pipeline embedding** (`6fcfca0`): client REST Voyage `voyage-4-lite` (1024-dim) dietro interfaccia `Embedder` iniettabile + `FakeEmbedder` deterministico per CI; script `pnpm embeddings:backfill` (`apps/api/src/modules/semantic-matching/backfill.ts`, hash-skip sha256 idempotente) embedda skill/occupazioni/ruoli e deriva i profili via SQL `avg(vector)` su `DISTINCT (user, skill)` (no double-count). `VOYAGE_API_KEY` = env opzionale (assente ≠ server-break; serve solo al backfill).
- **Modulo `semantic-matching`** (`1b7c419`): `/v1/matching/{me/occupations, users/:id/occupations, skills/:id/similar}` — 3 endpoint read-only, kNN cosine `<=>` su pgvector, `matching:read`, no CSRF. Pattern 7-step. **Substrato = mig `000060` (già live, NESSUNA migration nuova)**. Shared schema `semantic-matching` (+1).
- **Backfill PROD** (via tunnel sul DB centrale): `sys_skill_embeddings` **21939** · `sys_esco_occupation_embeddings` **3040** (URI con label embeddabile; 4605 URI senza label saltati) · `sys_job_role_embeddings` **227** · `sys_user_profile_embeddings` **156** (media **5.78** skill distinte/profilo = 902/156). Tutti `model_id=voyage-4-lite`. **kNN reale validato**: MANAGER→business/strategic-planning manager; CEO→strategic-planning manager; bank USER→corporate/cybersecurity risk manager (sim 0.64–0.78).
- **Review adversarial 3-lenti** (workflow correctness/scope-security/conventions): **1 HIGH I5** (similar-skills leakava skill privati cross-tenant — mancava `(skill_is_global OR skill_tenant_id=$t)` che ogni altra read del modulo skills applica) → **fixato** (`79f651a`, filtro su input-existence + result-set, PLATFORM_ADMIN unfiltered) + mean-pool dedup + voyage batch-length guard. **Opzione-b** (`eae62c1`): `/users/:id/occupations` peer-fit ammesso solo a ruoli elevati; **self-only = `{USER, TEAM_MEMBER, READ_ONLY}`** (tutto il resto vede i peer in-tenant); plain USER → 404 su peer, 200 su self.
- **Test backfill-robust** (`0be92a1`): la suite integration `semantic-matching` usa **entità throwaway** (users/occupations/skills create+distrutte) per le asserzioni di contenuto → corretta sia a tabelle vuote (CI fresca) sia popolate (post-backfill); persona-login solo per status/scope. **Fix post-push** (`fab74ce`): il test substrate S961 D7-P0 asseriva le 4 tabelle embedding *vuote* ("P0 ships no data") → rosso dopo il backfill → aggiornato a "esiste + queryabile" (`count >= 0`, robusto a vuoto/popolato).
- **Counts S971**: migration **000001..000073** (72 file, gap 000035, **nessuna nuova**). Route module **62** (+semantic-matching). API test file **103** (+2: `semantic-matching.integration` + `semantic-matching-backfill`). Endpoint live **~289** (+3 matching). Schema shared **67** (+semantic-matching). Stack invariato (next 16.2.7, @heuresys/ui ^0.1.4, zod 4.4.3) + `VOYAGE_API_KEY` env opzionale.
- **🚀 Deploy PROD (S971)**: `vm-deploy.sh` su `oracle-vm-default` — build prod api tsup + web next (ƒ Proxy middleware), restart systemd `active`/`active`, verify **api/readyz OK · web/login 200 0.009s**; smoke `/v1/matching/{me/occupations,skills/:id/similar}` → **401** (route registrate + auth-gate attivo, non 404). `/v1/matching` **live in produzione**. **Mac NON allineato in-session** (ad-hoc o prossimo handoff).
- **Pending prossima sessione**: **② P1b** (pagina ESS `/me/matching` + Playwright E2E live) · **② P2** (reindex endpoint `matching:admin` + ricerca free-text + Fase 2 person→job_roles/person↔person) · **#2·m2 Surveys** · **#2·m3 PredictionsML** · #7 MVP-4 · #8 cap ③④⑤.

## 0-septies. Delta S971 → S972 (refresh 2026-06-07)

Sessione ultracode "chiudi tutte le 9 action" (discovery 9-agenti evidence-based → 6 implementazioni full + 1 chiusura terminale + 2 deliverable design). **10 commit** (`5ec1c6d`→`beef87f`) pushati su origin/main, **CI 6/6 verde**, `vm-deploy.sh` eseguito (PROD live).

- **Capability ② AI — COMPLETA** (P1b + Fase3 + P2):
  - **P1b** (`664588e`): pagina ESS `/me/matching` (occupazioni ESCO, DataTablePanel score%, empty-state su `evidenceCount`, nav mig `000075`), E2E live (tommaso match reali).
  - **Fase 3** (`79c63c8`, opzione C read-only): `/v1/matching/{me/positions, users/:id/positions}` — kNN profile→job_role-embedding fan-out via `position_job_role_id`, I5 tenant-scoped + ESS positions panel. NESSUNA migration; **PSR-population disaccoppiata** (`sys_position_skill_requirements` resta 0 — positions senza ESCO-URI/crosswalk). 8 test.
  - **P2** (`830641c`): `POST /v1/matching/reindex` (`matching:admin`+CSRF, backfill via DI seam non-distruttivo `BackfillSummary`) + `/me/job-roles` + `/users/:id/job-roles` (person→roles) + `/users/:id/similar` (person↔person, elevated-only, self-excluded) + `GET /search?q=` dietro **`MATCHING_FREETEXT_ENABLED` default OFF** (→404 `MATCHING_FREETEXT_DISABLED`; ON embedda la query at-request via Embedder seam/FakeEmbedder). ESS job-roles panel (3 pannelli). 16 test. **Embedding reali invariati** (21939/3040/227/156, verificati 3×).
- **BI ①·#8b skills-by-category** (`5ec1c6d`): 8ª vista `/v1/analytics/skills-by-category` (heatmap categoria×proficiency, 902/902 evidenze dense, clone-swap-axis), nav mig `000074`. 4 test.
- **B-50 muri residui → stato terminale** (`9567381`, mig `000076`): 4 tab → NO_SOURCE (payout_curves/reward_gate_results/successor_readiness/user_target_positions), 3 → DEFER (branches/succession_pools/successor_candidates, gated su bridge PM/Wave-2). NEEDS_DECISION (view) 7→3. 0 dati fabbricati.
- **B-10b stream COMPLETO** (m1+m2+m3):
  - **m2 Surveys/Engagement** (`921012e`, mig `000077`/`078`): modulo `/v1/surveys` 12 endpoint (3 tab JSONB engagement) + import RTL **5 tmpl / 6 surveys / 862 risposte** (LEGACY_EMP:: 0 unresolved; cluster `survey_*` normalizzato → **m2b** follow-up). 9 test.
  - **m3 PredictionsML** (`827720f`, mig `000079`/`080`): modulo read-only `/v1/predictions` 4 endpoint (2-table generic) + import **4 models / 468 predictions** (267×3 fold, RTL slice; `mv_talent_signals` skipped = recompute-view, `sys_talent_scores` 154 intatta). 8 test.
- **MVP-4 EMAIL_OTP** (`3d39cf9`, mig `000081`): 2° fattore MFA (`sys_auth_mfa_otp_challenges`), 6-digit CSPRNG, Argon2id hashed-at-rest, TTL 10min, max-attempts 5 + lockout, resend-cooldown, single-use, CSRF, log-redacted, mai-in-response; login step-up riusa la state-machine TOTP (TOTP+password+JWT+refresh **0-diff**). UI `/me/security` + E2E. 13 test (5 TOTP + 8 EMAIL_OTP). Wave-2 + Mobile/WCAG annotati deferred.
- **Cap ③④⑤ — 3 design spec a gate-Enzo** (`14bb949`+`beef87f`): `docs/superpowers/specs/2026-06-07-{data-mining,cms,scraping}-design.md`, open-decision risolte best-practice (implementation-ready). Residuo umano: solo pesi derivation-rule data-mining + sign-off legale per-source scraping. Roadmap status table allineata (① COMPLETE, ② live PROD).
- **Counts S972**: migration **000001..000081** (80 file, gap 000035; +8: `000074..000081`). Moduli business **64** (+surveys+predictions). Endpoint live **~317** (+28; grep handler app.METHOD = 344). Schema shared **69** (+2). Web pagine **75** (+2). Playwright spec **33**. API test file **105**; **full suite 756 pass / 6 skip / 0 fail**. Stack invariato (next 16.2.7, @heuresys/ui ^0.1.4, zod 4.4.3) + env `MATCHING_FREETEXT_ENABLED` (default off).
- **Reconciliation registry**: POPULATED **127** · NEEDS_DECISION **3** · NO_SOURCE **21** · EXCLUDE 3 · REFERENCE_ONLY 3 (**157 tabelle registrate / 158 base, 0 UNCLASSIFIED**; +6 nuove: 3 surveys + 2 predictions + 1 mfa_otp).
- **🚀 Deploy PROD (S972)**: `vm-deploy.sh` su `oracle-vm-default` — build prod api tsup + web next 16 (`ƒ Proxy`), restart systemd active/active, verify **api/readyz OK · web/login 200 0.009s**. Smoke nuovi endpoint (`/v1/matching/{me/occupations,positions,job-roles}`, `/v1/analytics/skills-by-category`, `/v1/surveys`, `/v1/predictions`) → **401 (live + auth-gated)**. DB centrale già migrato/seedato via tunnel in-session → deploy ricostruisce solo il codice. Mac + VM allineati a `origin/main` (handoff).
- **Pending prossima sessione**: B-10b **m2b** (cluster survey_* normalizzato ~6-8h) · cap **③④⑤** plan→impl post-greenlight (③ data-mining = pesi derivation-rule; ⑤ scraping = ToS per-source) · MVP-4 residuo (Wave-2 / Mobile-WCAG, multi-sessione) · B-50 3 tab DEFER (bridge/Wave-2). (D-16 hygiene `analytics.d.ts` RISOLTO S972 `26dfbe9`.)

## 0-octies. Delta S972 → S975 (refresh 2026-06-07)

Sessione ultracode "aggregato #1+#3+#6+#8" (4 item del menu) + review adversarial + fix deploy. **5 commit pushati su origin/main** (`86ad0cf`→`ef13856`), **CI 6/6 verde**, `vm-deploy.sh` eseguito (PROD live + smoke-verificato).

- **#6 free-text matching → LIVE in PROD** (config-only, no commit): `MATCHING_FREETEXT_ENABLED=true` + `VOYAGE_API_KEY` aggiunti al `.env` VM (gitignored, valore via stdin mai loggato; backup `.env.bak.s974`). `GET /v1/matching/search?q=` ora **200 con kNN Voyage at-request** (verificato live: "software engineer" → software developer 0.73 / analyst 0.72 / architect 0.71). Prima era 404 (flag OFF). La chiave query-time vive ora anche sulla VM serving.
- **#8 mailer SMTP reale (CODE-ONLY)** (`86ad0cf`): `SmtpMailer` nodemailer provider-agnostic + factory `makeMailer` env-driven (SMTP_HOST+MAIL_FROM → SMTP, altrimenti ConsoleMailer = **no-regressione**). Campi env `SMTP_*`/`MAIL_FROM` opzionali + `.env.example`. `buildApp` cabla `makeMailer(app.log)` al posto di `new ConsoleMailer`. OTP-code mai loggato (SmtpMailer senza logger). **+nodemailer ^8.0.10**. 5 unit test (capturing transport, no rete). **Attivazione PROD = follow-up** (creds SMTP reali nel `.env` VM → SmtpMailer auto-selezionato).
- **#1 cap③ data-mining (insights/flight-risk slice A)** (`630c068`): modulo `/v1/insights` 7-step. Tabella **DEDICATA** `sys.sys_flight_risk_scores` (mig `000082`) — NON sovrascrive il talent-grid 9-box live `sys_talent_scores` (tutte 0 job_role; band varchar+CHECK LOW/MED/HIGH/CRITICAL, value numeric(5,2), payload jsonb provenance, FK tenant+user I14). Registrata **EXCLUDE** nel reconciliation registry (in-platform-derived). Permessi `insights:{view,admin}` (mig `000083`): view = 6 ruoli non-leaf (admin/manager-only, D-6 no-ESS), admin = PLATFORM/TENANT_ADMIN/HRMS_MANAGER. Regola **weighted-linear deterministica** (pesi confermati Enzo: tenure .15/attendance-OT .20/KPI .25/engagement .25/comp .10/promo .05) + re-normalize-on-missing + payload esplicabile per-feature. Feature-extraction SQL su tenure/attendance/KPI-evidence/engagement-JSONB/comp-band-percentile/promo-recency, scope PLATFORM/TENANT/TEAM (I5). `GET /flight-risk` + `GET /users/:id/flight-risk` + `POST /recompute` (insights:admin+CSRF, append-latest-wins). **11 test** (RBAC/CSRF/recompute deterministico/I5/esplicabilità/D-6/404 + regression jsonb-guard).
- **#3 cap⑤ scraping (reference-sync/ESCO P1)** (`4306839`): modulo `/v1/reference-sync` 7-step. Connector ESCO (`fetch` paginato `ec.europa.eu/esco/api/search` → `_embedded.results` uri/title/code → normalize + dedup, seam `EscoFetcher` iniettabile → fixture in CI, no HTTP live). Upsert idempotente in `sys_esco_occupation_mappings` su chiave naturale `(job_role_id NULL, esco_uri)` NULLS-NOT-DISTINCT — refresh label/ISCO/metadata, **mai delete**; insert-vs-update via xmax=0. Lineage run-level riusa `brownfield.source_exports` (upsert-by-name) + `brownfield.import_runs` (append, scope `reference_sync`). `GET /sources`/`/runs`/`/runs/:id` + `POST /runs` (CSRF). Permessi `reference_sync:{read,trigger}` (mig `000084`) **PLATFORM_ADMIN esplicito** (NB: TENANT_ADMIN eredita via catch-all 000005 — vedi backlog/STATE). **8 test** (RBAC via MANAGER, CSRF, insert→update→idempotenza 0-net-new, lineage, 404 — fixture sintetica, catalogo reale 7645 intatto). Watermark/scheduler = **P2** (non in P1).
- **Review adversarial 6-agenti** (SQL/scope-I5/idempotenza/contract → find→verify): **1 bug confermato** → engagement CTE faceva `jsonb_array_elements(response_answers)` non-guardato, ma la colonna è `jsonb DEFAULT '{}'` (oggetto) → un singolo row non-array abortiva l'intero recompute. **Fix** (`b98f9c1`): guard `jsonb_typeof='array'` + regression test. Stessa commit corregge la premessa RBAC del test reference-sync (TENANT_ADMIN ha il perm via catch-all → negativo spostato su MANAGER).
- **Fix deploy `vm-deploy.sh`** (`ef13856`): il primo deploy è fallito al `next build` (`@heuresys/shared has no exported member AttendanceAnalyticsResponse`) — root-cause = **stale `packages/shared/tsconfig.tsbuildinfo`** (D-16 aveva untrackato `analytics.d.ts`; l'incrementale tsc lo credeva emesso → skip; CI passava perché checkout fresco). Risolto in-deploy (clean rebuild shared) + reso durevole: vm-deploy ora fa `rm -rf packages/shared/{dist,tsconfig.tsbuildinfo}` + clean build shared **prima** di api/web.
- **Counts S975**: migration **000001..000084** (83 file, gap 000035; +3: `000082` flight-risk-scores · `000083` insights-perms · `000084` reference_sync-perms). Moduli business **66** (+insights+reference-sync). Schema shared **71** (+insights+reference-sync). Endpoint live ~**324** (+7: 3 insights + 4 reference-sync). API test file **108** (+3); **full suite 780 pass / 6 skip / 0 fail**. Stack: **+nodemailer ^8.0.10** (api); resto invariato (next 16.2.7, @heuresys/ui ^0.1.4, zod 4.4.3) + env nuovi `SMTP_*`/`MAIL_FROM` (opzionali). Capability program: **① BI COMPLETE · ② AI COMPLETE · ③ data-mining P1 SHIPPED · ⑤ scraping P1 SHIPPED · ④ CMS = mai iniziata**.
- **Reconciliation registry** (vista live): POPULATED **128** · NEEDS_DECISION 3 · NO_SOURCE 21 · EXCLUDE 3 · REFERENCE_ONLY 3 (158 tabelle, **0 UNCLASSIFIED**). `sys_flight_risk_scores` registrata bucket-D/EXCLUDE (split registry A19/B16/C23/D27); appare POPULATED nella vista perché i test recompute hanno popolato righe derivate.
- **🚀 Deploy PROD (S975)**: push → CI 6/6 verde → `vm-deploy.sh`. **Primo run fallito** (stale tsbuildinfo, vedi sopra) → recovery (clean shared rebuild) + api tsup + web next 16 + restart systemd active/active. Smoke autenticato PROD: readyz 200 · login 200 · **`/v1/insights/flight-risk` 200 · `/v1/reference-sync/sources` 200 · `/v1/matching/search` 200 (#6 vivo post-restart) · `/v1/predictions` 200**. Mac NON allineato in-session (irraggiungibile in sessioni recenti — prossimo handoff/ad-hoc). VM allineata `ef13856`.
- **Pending prossima sessione**: cap③ **P1b** (frontend) + **P2** (slice B/C) · cap⑤ **P2** (watermark+scheduler+2ª sorgente) · cap④ **CMS** (mai iniziata) · B-10b **m2b** · **#8** attivazione SMTP (creds) · **reference_sync** decisione strict-PLATFORM_ADMIN · MVP-4 residuo.

## 0-novies. Delta S975 addendum (post-handoff: RBAC strict-only + CMS P1-API)

Dopo l'handoff S975 (`931b6cd`), Enzo ha evaso le 3 open-question → 3 commit aggiuntivi pushati, CI verde, 2° deploy PROD. **(1) #8 mailer**: lasciato ConsoleMailer (no SMTP reale — sviluppo/test senza email; nessuna azione). **(2) reference_sync RBAC** (`f3f2217`): Enzo ha delegato la scelta tecnica → **strict PLATFORM_ADMIN-only** (ESCO refresh muta dati globali; coerente con `brownfield_adaptation:approve`). 000005 denylist + mig `000085` revoke TENANT_ADMIN; test reference-sync ora ha TENANT_ADMIN come negativo (403). Verificato live PROD (TENANT_ADMIN 403 / PLATFORM_ADMIN 200). **(3) cap④ CMS — P1 API SHIPPED** (`ac3ac6c`): modulo `/v1/content` (CRUD documents + categorie + version history) su 3 tabelle `sys_content_{categories,documents,versions}` (mig `000086` tenant-FK-RESTRICT I5, author-FK-SET-NULL I14, kind/status/format varchar+CHECK, markdown body D-1, flat taxonomy D-4, row-per-version immutable, FK circolare documents↔versions via ALTER, 3 registry EXCLUDE) + perms `content:{read,create,update,delete,publish}` (mig `000087`; read=6 ruoli, write+publish=PLATFORM/TENANT_ADMIN/HRMS_MANAGER). withTransaction per create=head+v1 e edit=append-version+repoint. D-5 delete (published→archived soft / draft→hard). **NO publish-workflow/restore/ESS (P2)**. 8 test (RBAC, CSRF, create→v1, edit→v2, categorie, I5, delete). **cap④ frontend = layer successivo (P1)**.
- **Counts (post-addendum)**: migration **000001..000087** (85 file su disco, gap 000035; +3 vs §0-octies: `000085` reference_sync-revoke · `000086` content-schema · `000087` content-perms). Moduli business **67** (+content). Schema shared **72** (+content). Endpoint live ~**335** (+11 content). API test file **109** (+1 content). **full suite 789 pass / 6 skip**. Reconciliation registry split A19/B16/C23/**D30** = 88 righe (+3 content EXCLUDE), 0 UNCLASSIFIED. Capability program: **①② COMPLETE · ③ P1 · ⑤ P1 (strict-PA) · ④ P1-API (frontend next)**.
- **🚀 Deploy PROD (S975 addendum)**: 2 deploy via `vm-deploy.sh` (il fix D-17 clean-shared-build ha tenuto al 2°). Smoke autenticato: `/v1/content` 200 · `/v1/content/categories` 200 · `/v1/insights/flight-risk` 200 · `/v1/reference-sync/sources` 200 (TENANT_ADMIN 403) · `/v1/matching/search` 200. VM allineata `ac3ac6c`.

## 0-decies. Delta S975 → S976 (refresh 2026-06-07)

Sessione ultracode marathon "aggregato #1+#3+#2+#4", ordine **domain-complete** (Enzo). **7 commit pushati** su `origin/main` (`a8c247b`→`8852ee0`, HEAD pre-handoff `8852ee0`), full API suite verde. **DEPLOY PROD differito** (VM su codice pre-sessione; DB centrale già migrato `000088..000092` via tunnel — old code + new tables = nessuna rottura).

- **🔴 Bugfix `resolveWriteTenant`** (`a8c247b`): il modulo content (S975) faceva **500** sul create di un PLATFORM_ADMIN (JWT `tenantId=null` → INSERT `document_tenant_id` NULL). Aggiunto `resolveWriteTenant` (mirror surveys) → **403 `TENANT_ID_REQUIRED`** + regression test. Gap non testato a S975 (il test creava solo come TENANT_ADMIN).
- **🟣 Cap④ CMS frontend P1** (`6c7ee15`): pagina admin `/content` (list+filtri kind/status/categoria/q · create markdown-textarea+categorie · edit append-version · delete · cronologia versioni · category CRUD), `@heuresys/ui` via DataTablePanel, i18n IT+EN (admin ns), nav mig `000088`, Playwright E2E live (author→edit→version-bump→delete, persona tenantAdmin). **Nota tecnica**: `apps/web` può importare **solo `type`** da `@heuresys/shared` (il barrel `.js`→`.ts` non risolve sotto Next/Turbopack `transpilePackages` per i valori runtime) → enum option-list come literal locali in-sync con CHECK.
- **🟣 Cap④ CMS P2 API** (`9fa8021`): restore versione + publish-workflow **catena in_review** (submit-for-review draft→in_review · return-to-draft · publish · unpublish, invalid-transition→409) + ESS `GET /v1/me/content`+`/:id` (published-only, tenant-scoped, effective-window, no-leak) + perm `me:content:read` mig `000089` (tutti i ruoli). +6 integration test.
- **🟣 Cap④ CMS P2 UI** (`d707e99`): `/content/[id]` workflow-bar gated (content:update vs publish via `/v1/me/permissions`) + restore per-versione · ESS **`/me/handbook`** "Manuale del dipendente" (list+detail, body preformatted safe — rich markdown render = P3) · nav mig `000090` me-handbook · E2E 2-context (admin pubblica → employee legge, draft nascosto).
- **🟣 Cap③ insights P1b** (`705293f`): pagina admin `/insights` flight-risk (tabella subject/score/band + explainability per-feature dal payload · recompute gated insights:admin), nav mig `000091` (ENTERPRISE/intelligence, `insights:view`), i18n admin ns, E2E live (count = API total).
- **🟣 Cap③ data-mining P2 slice B/C** (`8852ee0`): estende `insights` con 2 famiglie di score in-platform-derived su substrato ② (mig `000092`, **tabelle dedicate** `sys_succession_readiness_scores` + `sys_skill_gap_scores`, NON reuse del seed legacy). **B succession-readiness**: per (subject, top-3 best-fit target position) cosine-fit .50 + KPI .30 + tenure .20 → horizon READY_NOW..NOT_READY. **C skill-gap**: current-role cosine-distance .70 + evidence-sparsity .30 → ALIGNED..MAJOR_GAP. Pesi **delegati a Claude** (best-practice): seniority-gap droppata (data-sparse 49/227, scala indefinita), peso redistribuito; regole in spec §9.3/§9.4. Endpoint `GET/POST /v1/insights/{succession-readiness,skill-gap}{,/recompute}`. +6 test. Registry +2 bucket-D EXCLUDE.
- **🔴 fix test me-interfaces** (`e12a55e`): `me-handbook` (ESS always-visible) entra nel set TALENT del pure USER → assert exact-match aggiornato (regressione latente da `d707e99`, intercettata dalla full-suite di #4 — il gate #3 girò E2E+web ma non l'API suite).
- **Counts S976**: migration **000001..000092** (91 file su disco, gap 000035; +5: `000088` content-nav · `000089` me:content:read · `000090` me-handbook-nav · `000091` insights-nav · `000092` insights-P2-score-tables). Route module registrati **70**. API test file **110** (+2: insights-p2 + content/me invariati-count; content esteso). Schema shared **72** (content.ts + insights.ts estesi, no nuovi file). Web pagine **80** (+5: content×2, insights, me/handbook×2). Playwright spec **36** (+3). `sys_ui_interfaces` **36** (+3). Stack invariato (next 16.2.7, @heuresys/ui ^0.1.4, zod 4.4.3, nodemailer 8). **full API suite 802 pass / 6 skip**.
- **Reconciliation registry** (vista live): **POPULATED 130** · NEEDS_DECISION 3 · NO_SOURCE 21 · EXCLUDE 6 · REFERENCE_ONLY 3. Registry tabella 90 righe, split **A19/B16/C23/D32** (+2 D/EXCLUDE: succession_readiness + skill_gap; +3 D content erano già a S975). 0 UNCLASSIFIED.
- **Pending prossima sessione**: **deploy PROD S976** (vm-deploy + Mac align) · **#5 scraping P2** (watermark+timer, VM-only) · **#6 m2b** surveys normalized (modeling, spec→sign-off) · **#4 frontend opzionale** (`/insights/succession-readiness`+`/skill-gap`) · MVP-4 residuo. Debito nuovo: **D-18** (insights recompute append-accumulation — vedi DEBT_REGISTER).

## 0-undecies. Delta S976 → S977 (refresh 2026-06-08)

Sessione ultracode, aggregato **#1+#2+#4+#3** — 4 item, tutti shippati + live PROD. **3 commit pushati** (`5bf046d`, `f7d5fb3`, `d320246`), CI verde per ciascuno, deploy via `vm-deploy.sh` + Mac/VM allineati. (Counts re-derivati: HEAD `d320246`, migration **94 file** `000001..000095` gap 000035, web pages **82**, Playwright spec **38**, `sys_ui_interfaces` **38**, full API suite **805 pass / 6 skip**.)

- **#1 deploy PROD S976**: VM `a1b4f02`→`4d62677`, dominio CMS + data-mining (S976) live, verificato 404→401 sugli endpoint S976.
- **#2 cap③ insights P2 frontend** (`5bf046d`): 2 pagine admin `/insights/succession-readiness` + `/insights/skill-gap` (DataTablePanel + badge horizon/segment + explainability per-feature), nav DB-driven mig **000093** (intelligence/ENTERPRISE, requires_admin+insights:view), i18n it/en (`insightsReadiness`/`insightsSkillGap`), 2 E2E live. Solo `type` import da `@heuresys/shared`. me-interfaces 5/5 (le +2 interfacce admin-gated escluse per pure USER).
- **#4 D-18 RISOLTO** (`f7d5fb3`): le 3 score-table insights crescevano monotone (append-latest-wins). Fix = **delete-then-insert atomico** (CTE `WITH del AS (DELETE … WHERE user_id = ANY($1)) INSERT`) nelle 3 upsert → una riga attiva per soggetto, scope-safe; + **mig 000094** collapse one-time del backlog (flight-risk 10428→**158**, succession 11088→**462**, skill-gap 1848→**154**; idempotente). Test idempotenza in `insights{,-p2}.integration`. DEBT_REGISTER D-18→RISOLTO.
- **#3 cap⑤ scraping P2** (`d320246`): **`brownfield.source_watermarks`** (mig **000095**, delta/HWM §3.3, brownfield-infra non-registry) + advance watermark **in-transazione** (mai su rollback) + **lock FETCHING** per-source (acquire/staleness-30min/no-overlap → 409 SYNC_IN_PROGRESS) + `markWatermarkFailed` condizionale (no-clobber) + presence-guard skip + **CLI-bypass** `sync-cli.ts` (`reference-sync:run`, run di sistema=initiator NULL) + systemd `scraping.{service,timer}` (**weekly Sun 03:30**, Persistent) installati da **vm-bootstrap E vm-deploy** (no silent-miss). Shared: watermark schema + `skipped`. Hardened da **review adversarial 4-lente** (11 finding assorbiti: in-flight lock, no-clobber FAILED, presence-guard, vm-deploy install-gap HIGH, FAILED-path coverage). **One-shot ESCO verificato live in PROD**: run-1 (post-reset) STAGED/updated 100, run-2 (identico) **UNCHANGED/skipped** — short-circuit provato. Timer schedulato (next Sun 2026-06-14 03:32 UTC). **2ª sorgente ISTAT/ATECO differita** (⛔ ToS D-4).
- **🔎 Follow-up P1 emerso (cap⑤)**: il connector ESCO reale tira **100** occupazioni (endpoint *search* ritorna `total=100`), non l'intero catalogo (~3040) — rischio P1 noto (spec §6.2). Refresh parziale; catalogo già 7645 dal rebuild. → backlog cap⑤ P1.
- **🚀 Deploy PROD (S977)**: 3 deploy `vm-deploy.sh` (#2, #4, #3) — build prod api tsup + web next, restart systemd active/active, readyz/login 200, TLS 200. Per #3 le unit scraping installate manualmente sulla VM (il 1° deploy gira lo script vecchio in buffer; deploy futuri auto-installano via il nuovo `vm-deploy.sh`). Mac+VM+Windows allineati a `d320246`.

## 0-duodecies. Delta S977 → S978 (refresh 2026-06-08)

Sessione **ultracode**, aggregato **item #1-8** dal menu eseguiti in autonomia (decisioni delegate da Enzo, solo questa sessione). **7 commit pushati** (`0ea045b`..`385e74e`, HEAD **`385e74e`**), full API suite **837 pass / 6 skip** (114 file +2 skip; +32 test nuovi), web typecheck+build verdi, secret-scan clean. Migration **94→98** (`000096`..`000099`). Moduli API **70** (nuovo `engagement`).
- **#4 category-heatmap skills** — GIÀ shippato (`5ec1c6d`, pre-S978); verificato verde (analytics 28/28). Nessun commit; doc-drift chiuso qui.
- **#3 cap③ insights scheduled-recompute** (`0ea045b`, NO migration): CLI `insights:recompute` (system PLATFORM actor, le 3 recompute delete-then-insert D-18) + systemd `heuresys-advanced-insights.{service,timer}` (daily 02:15, off-peak vs scraping Sun 03:30); `vm-bootstrap.sh`+`vm-deploy.sh` installano+enable entrambi i timer. Test 3/3 + live entrypoint EXIT=0 (158/462/154 stabili).
- **#1 cap⑤ ESCO full-catalogue** (`3663371`): root-cause = `full=true` rompe la risposta + offset-paging muore oltre offset≈500. Fix = probe `total` (limit=1) → single large page (limit=total, `full=false`) + guard truncation/ceiling fail-loud. Live: **2942** (era 100), run-1 STAGED → run-2 UNCHANGED, catalog 7645→7650 (no-delete). 6 connector unit test. **Chiude il follow-up P1 cap⑤.**
- **#5 cap② Fase 3 PSR-population** (`93e4b45`, mig `000096`): `sys_position_skill_requirements` 0→**844** (148 pos / 31 skill / RTL) via derivazione **peer-group-prevalence-v1** (job_role→skill, tenant-local I5, ≥34% prevalence & ≥2 holders, modal proficiency, criticality banded). UUID v5 deterministici (uuid_generate_v5) + delete-derived-then-insert idempotente. Registry PSR → **POPULATED**. 6 PSR test + positions count-relative fix + 7/7 validation views (tenant-boundary clean).
- **#2 cap② B-10b m2b normalized cluster** (`afdfd64`, mig `000097` + seed `48` + generatore `_gen_m2b.sql`): 4 tabelle `sys_surveys`/`sys_survey_questions`/`sys_survey_responses`/`sys_pulse_checks` + modulo read-only **`/v1/engagement`** (`surveys:read`, GET surveys/results/pulse, tenant-scoped 404). Import RTL: **8 surveys / 21 questions / 3792 responses / 733 pulse** (0 unresolved I14, LEGACY_EMP::). Registry **+4 A/IMPORT** (90→94). engagement 6/6 + registry 11/11.
- **#6 cap④ CMS P3 full-text search** (`4e85281`, mig `000098`): `document_search_tsv` generated (title-A + body-B) + GIN; **GET `/v1/content/search`** (`websearch_to_tsquery('simple')` + `ts_rank` + `ts_headline`, content:read, scope I5). content-search 5/5. **Residuo P3** registrato (backlog): rich-text primitive (upstream `@heuresys/ui`), media object-store (infra/PM), BPM cross-link, search-UI box.
- **#7 MVP-4 §2.5 MFA recovery codes** (`9ba2fba`, mig `000099`): `sys_auth_mfa_recovery_codes` (10 codici 64-bit SHA-256 single-use); **POST/GET `/v1/auth/mfa/recovery-codes`** (CSRF); login bypass integrato in `verifyLoginChallenge` (length-16 guard, normalizzazione); `mfaCode` max 16→40. Registry **+1 D/EXCLUDE** (94→95, D33). mfa-recovery 6/6 + auth-mfa/mfa non-regressivi. **Residuo §2.5** registrato: WEBAUTHN, SMS_OTP (⛔ PM), session-enum UI, mandatory-MFA.
- **#8 MVP-4 §2.7 WCAG 3.1.1** (`385e74e`): root layout async → `<html lang>` dal cookie `NEXT_LOCALE` (era hardcoded `it`, errato per utenti EN su ~47 route) + `setLocale` sync client. next build verde (route → ƒ Dynamic). E2E `html[lang=en]` in `i18n-en.spec` (CI). **Residuo §2.7** registrato: axe serious/moderate/minor tail per-route + mobile sweep (~37-62h, critical=0 già gated).
- **Reconciliation registry**: **95 righe** (A23/B16/C23/D33), vista `v_reconciliation_status`: POPULATED **135** · NO_SOURCE 21 · EXCLUDE 7 · NEEDS_DECISION 3 · REFERENCE_ONLY 2 · **0 UNCLASSIFIED**.
- **Pending close S978**: vm-deploy PROD + Mac/VM align a `385e74e` (in corso); CI da confermare verde.

## 1. Git / release

| Item | Valore |
|---|---|
| Repo Windows | `D:\heuresys-advanced` |
| Repo OCI VM | `/home/ubuntu/heuresys-advanced` |
| Remote | `https://github.com/Spen-Zosky/heuresys-advanced` (public) |
| Branch / HEAD | `main` synced origin — **S1006** (HEAD `d261e74`); 1001 commit |
| Working tree | pulito (verificato S1007) |
| Tag corrente | **`v1.0.0`** (GA baseline, S957) — predecessori sotto |
| Tag (12 totali, incl. `v1.0.0`) | v0.2.0-mvp2 · v0.2.1-mvp2a-final · v0.3.0-mvp3 · v0.3.1-mvp3-final · v0.3.2-mvp3-full · v0.3.3-preflight-partial · v0.3.4-p0-closed · v0.4.0-brand-v1 · v0.4.0-mvp4-ready · v0.4.0a-s937-partial-checkpoint · v0.4.1-housekeeping-closed |
| Pre-commit hook | `.git/hooks/pre-commit` warn-only → `scripts/cowork-exchange/validate-naming.mjs` su cowork_code_exchange |
| Sibling repo | `ux-design-shared` (HEAD `047d6b9`, pushato) = sorgente di `@heuresys/ui` (npm `^0.1.4`, −reactflow S968) |
| Legacy read-only | `D:\evo.heuresys.com` (Win) + `/home/ubuntu/heuresys-evo` (VM) — enrichment source, mai committare path assoluti |

## 2. Stack (versioni verified)

- **Root**: pnpm 9.15.0 (pinato), Node ≥20.11, ESLint 9.39.4 flat, typescript-eslint 8.60.1, **eslint-config-next 16.2.7** (B-23 S969; flat-config consumato via import diretto `eslint-config-next/core-web-vitals` in `eslint.config.mjs`). `pnpm.overrides`: react/react-dom **19.2.7** (S965 reconcile), @types/react **19.2.16** (S966), vite ^6.4.2, postcss ^8.5.10, esbuild ^0.25.0, qs ≥6.15.2.
- **apps/api**: fastify 5.8.5, @fastify/{cookie 11,cors 11.2,helmet 13,jwt 10,rate-limit 10.3}, fastify-type-provider-zod 6.1.0, zod 4.4.3, pg 8.13.1, drizzle-orm 0.45.2 (solo pool wrapper; query business = raw SQL parametrizzato), argon2 0.41.1, otpauth ^9.5.1, **pino 10.3.1** (S964), **vitest 4.1.8** (singleThread), tsx 4.22.4, typescript 5.7.2, supertest 7.
- **apps/web**: **next 16.2.7** (B-23 bump S969 §0-quater; `middleware`→`proxy` rename), **react/react-dom 19.2.7 + `@types/react` 19.2.16** (override riconciliati S965/S966, §0-bis), @heuresys/ui ^0.1.4 (npm, NON link:; optional @types/react peerDep; `reactflow` dead-weight rimosso in 0.1.4 — S968), @heuresys/shared workspace:*, @tanstack/react-query 5.101, react-hook-form 7.77, **i18next 26.3.1 + react-i18next 17**, tailwindcss 4.3.0, @playwright/test 1.55.1 + axe.
- **apps/showcase**: **Next 16.2.7** static export → GitHub Pages, consuma `@heuresys/ui`.
- **TS invarianti** (`tsconfig.base.json`): `strict`, `noUncheckedIndexedAccess` (narrow `T|undefined` mandatory), `noUnusedLocals/Parameters` (unused → prefix `_`), `exactOptionalPropertyTypes:false` (intenzionale).

## 3. API (apps/api) — heart MVP-1→4

- **Plugin chain fisso 13-step** (non riordinare; vedi `docs/api/API_IMPLEMENTATION_PLAN.md` §3.2): zod compilers → requestId → helmet → cors → cookie → JWT RS256 → rate-limit → auth (decode-only) → CSRF (opt-in) → tenantContext → errorHandler → /healthz+/readyz → module routes.
- **85 route module business** (`/v1/` prefix in `app.ts`; 96 `app.register` totali incl. infra, S1007), **~458 route handler** (grep `app.{get,post,put,patch,delete}` nei moduli, S1007) (BI analytics ① P1→P3 + P2-ext: `/v1/analytics/{workforce,kpi}` S958 + `{attendance,compensation,skills}` P2 S962 + `org-network` P3 S965 + **`overtime` P2-ext S969**; migration nav `000059`,`000066..000068`,`000070`,`000071`). Pattern modulo a 7 step (schema shared → repository raw SQL → service + ActorContext → routes con `requirePermission` + `verifyCsrf` → register → integration test → atomic commit). **Non deviare.**
- **Test API**: **160 file** `apps/api/test/*.test.ts` (hit DB live via tunnel, no mock). Conteggio pass live ri-derivato dal CI `test-integration` (§7), **non hardcodato** (S1007: fix assertion skill-count → derivata live dal DB, anti-staleness).
- **Shared**: `packages/shared` — **87 schema module** Zod in `src/schemas/` (S1007) + subpath exports.

## 4. Database (PostgreSQL 16 native — ADR-0004 NO Docker)

> ✅ **EMPLOYEE-CENTRIC ingestion correction DONE 2026-06-01 (S954, ADR-0024 / I14).** Le metodiche di popolamento dal SoT legacy erano **user-centric** (key `LEGACY:<users.id>`, `users → sys_users`); la realtà strutturale è **employee-centric** (207 FK → `employees` vs 45 → `users`). Corretto: `sys_user*` ⟸ legacy `employee*`, `users` → solo `sys_auth_*`, key `LEGACY_EMP::<employees.id>`. **3 fasi tutte chiuse**: Fase 1 doc (`2d87b77`: ADR-0024 + `EMPLOYEE_CENTRIC_MAPPING_DOCTRINE.md` + I14 + correzione plan/map/SOT), Fase 2 re-key 5 seed `rtl-rebuild` via email-join (`10d9923`), **Fase 3 migration `000046` ESEGUITA live** (`10c0cd1`: 160 `LEGACY:`→`LEGACY_EMP::`, relabel puro 0-FK, idempotente, API 359/0 verde, backup `pre-000045-rekey`; il file era inizialmente `000045`, rinominato `000046` per collisione con `000045_rbac_role_category_and_ceo.sql`). Dottrina: `docs/brownfield/EMPLOYEE_CENTRIC_MAPPING_DOCTRINE.md`; debito D-13; backlog B-50(e).

> 🔄 **RTL tenant rebuild EXECUTED 2026-05-30 (S950)** — DB collassato a **161 utenti / 2 tenant ACTIVE** (RTL_BANK ex-`86ba7a65` + nuovo HEURESYS): 158 rtl-bank.org + 3 heuresys.com. Org reale wired: **162 posizioni** (157 owned-by-manager, I1), 160 assignment PRIMARY, 26 OU, 3180 attendance IMPORT, comp/skills/certs/RBAC importati. 272 utenti out-of-scope + scaffold sintetico (161 pos / 6 OU) eliminati (single-tx, assertito). Backup: `pg_dump_snapshots/…pre-09-collapse_a892b81…` + `…pre-rtl-rebuild_eb55058…`. **6 commit PUSHATI** su origin/main (`3df1f6a`/`a892b81`/`db3104b`/`b4f56a0`/`b62b441`/`bad989f`, HEAD `bad989f`); **CI 5/5 verde** (test-integration + playwright-smoke, entrambi rossi a S949, ora verdi). API 354/0, prod E2E 138/0. Inclusi 2 fix pre-esistenti emersi dalla validazione: D4 sidebar (hybrid gate `hasAdminRole`) + login rate-limit env-tunable. I conteggi pre-rebuild qui sotto sono SUPERATI. Dettaglio: `memory/project_rtl_tenant_rebuild.md`.

- **Runtime**: ADR-0010 Option B — PG16 native su `oracle-vm-default`, raggiunto da Windows via tunnel `ssh -L 5433:localhost:5432 oracle-vm-default` (RD-25). Connection: localhost:5433, db `heuresys_advanced`, user `heuresys`, schema `sys`.
- **160 file migration** idempotenti `000001..000162` (**gap `000035`+`000139` cosmetici**, documentati; aggiornato S1007; storico S972 +8: `000074` skills-by-cat-nav · `000075` matching-nav · `000076` B-50 terminal-annotation · `000077`/`078` surveys schema+perms · `000079`/`080` predictionsml schema+perms · `000081` mfa-email-otp). Ultime: `000054` teams/roles (R1b) · `000055` team UI interface · `000056` reclassify activity_classification_mappings (ADR-0025 §5.4) · `000057` analytics:view permission (BI P1) · **`000058` reconciliation registry — `sys.sys_reconciliation_registry` + vista `sys.v_reconciliation_status` (S960, F1 del reconciliation-closure cycle; status live query-abile per ogni tabella sys, 0 UNCLASSIFIED)**. Contract: `pnpm db:migrate` ×2 → pg_dump diff vuoto + `db:validate` 7/7 (verificato S957; 000056 idempotente ×2 S958).
> 🧹 **DB sanitization 2026-06-01 (S954, commit `169c999`).** Pass di igiene del bare-metal (census→clean, gated da backup `pre-cleanup-s954_20260601_0410`). Fatto: TRUNCATE `staging.rtl_*` + `legacy_rtl_occupations` (14 tab, ~6k righe leftover S950, dati già in `sys.*`); DROP 2 tabelle leftover `temp_sdbi` (pilota SDBI, non migration-managed); import_run orfano RUNNING (fermo 21/05) → CANCELLED (74k validation_results preservati, UPDATE non DELETE) — B-50(c); VACUUM FULL audit+legacy_mirror (compattato ~21 MB bloat reale). **ANALYZE full-DB** (stime planner ri-allineate). **Correzione census (R5)**: `legacy_mirror` (586 MB) e `audit.import_validation_results` (528 MB) NON erano "bloat 0-righe" come erroneamente classificato da `pg_stat.n_live_tup` stale — sono **dati reali** (1.522.455 validation results dei 21 import COMPLETED + cache ESCO 14k skills/pgvector). Nessun ~1 GB da liberare. Verifiche post: db:migrate ×2 idempotente (45 mig), API 359/0, schema intatto. Dettaglio: `qa_artifacts/_census_CORRECTION.md`.
> ✅ **2 tenant ACTIVE — case-study scope enforced (S954, commit `42b4c80`)**: **RTL_BANK** (158 utenti, operativo, rebuild S950) + **HEURESYS** (3). Decisione prodotto (Enzo): il case study è RTL Bank; gli unici tenant accettati sono RTL_BANK + HEURESYS. **Eliminati** (migration `000047`): (a) tenant scaffold `RTL_BANK_REFERENCE` (0 dipendenze su 92 tabelle FK, `qa_artifacts/_rtlref_deps.txt`) — `000021` §7 INSERT rimosso così `db:migrate` non lo ricrea; il blueprint FIN_BANKING + 7 reward gates + 23 processi (catalogo GLOBALE non tenant-scoped) restano, rilevanti a RTL Bank; (b) schema `legacy_mirror` (115 tab, cache rigenerabile dal wave-executor loader, conteneva anche SmartFood 82 + EcoNova 26 estranei); (c) mapping SmartFood/EcoNova da `brownfield.tenant_id_mappings` (`000033` ri-scoped ai soli 2 legacy in-scope via JOIN per codice). **Errore intercettato+corretto (R3)**: il primo tentativo ruppe `db:migrate` (DELETE tenant → subquery NULL in 000033) → fix re-scoping 000033. Verifiche: db:migrate ×2 verde (46 mig), API 359/0, blueprint intatto. **DB 1304 → 719 MB** (−585).

- **DB size**: **1257 MB** (live S1007; risalito da 719 MB S954 per backfill embeddings pgvector + catalogo ESCO 14093 skill + 1.5M validation results audit). Breakdown: `audit` 528 MB (1.522.455 validation results dei 21 import — dato reale), `sys` 167 MB, `staging` 2 MB (svuotato), `brownfield` 1.5 MB. **`legacy_mirror` rimosso** (era 586 MB).
- **Schemi**: `sys` (188 base tables + 12 views per I3/I4, `sys.sys_<plural>`; aggiornato S1007), `staging` (wave1_* 18 tab migration-managed + rtl_* svuotate), `brownfield` (registry), `audit`, `temp_sdbi` (schema da migration 000036; tabelle pilota rimosse). **`legacy_mirror` DROPpato (S954)** — cache rigenerabile on-demand dal wave-executor loader quando servirà un import.
- **Counts verified live S1007** (real count(\*)): **sys_users 162**, **positions 162**, **org_units 26**, **assignments 160 PRIMARY**, **2 tenant ACTIVE** (RTL_BANK 158 + HEURESYS 3 — case-study scope), **sys.* populated 65/134**. Lineage: **69.450** rows. Brownfield registry: 93 source_tables, 1164 source_columns, 94 table_mappings, **1271 column_mappings**, 18 staging.wave1_*, tenant_id_mappings 2 (in-scope). import_runs: 21 COMPLETED + 1 CANCELLED + 1 FAILED. **55 file migration** (000048 B-51 titles, 000049 R2 role grants, 000050 U1 ui-interfaces, 000051 skill_categories nullable, 000053 sys_user_preferences, 000054 teams, 000055 team-ui, 000056 reclassify activity-class card). **`sys_activity_classification_mappings`** = empty-by-design (S958: card mis-authored → REFERENCE_ONLY). Data-reconciliation in **`docs/kb/DATA_RECONCILIATION_PLAN.md`**: **catalogo KPI unificato `sys_kpi_definitions` 0→243** (S958: process_kpis 81 + job_kpis 45 + org_unit_kpis 100 + employee_kpi_targets 17 — KPI a 4 livelli org, 1:1) + **`sys_kpi_targets` 0→248** (employee-level, FK user via `LEGACY_EMP::` 138 utenti RTL, tenant dal sys_user). Seed in `db/seeds/reconciliation/01-03`, idempotenti. **(S1007: i conteggi puntuali sotto — `55 file migration`, `job_roles 227` — sono storici S954/S955; live = 160 migration, job_roles 136.)** Restanti cat(i) = **NEEDS-DECISION** (muri: org-unit template-vs-instance + bridge job→position assente + learning catalog event-sourced — plan §7). Esecuzione gated su decisioni di modellazione. **job_roles 227** (+25 `RTL-ROLE-*` da B-51, S955); **162 positions** titolate+wired 162/162. **RBAC R2 ✅** (4 ruoli holderless assegnati per funzione + login) + **U1 ✅** (`sys_ui_interfaces` 23 interfacce + `GET /v1/me/interfaces`, hybrid gate, test 5/5) + **U2 ✅** (`(authenticated)/layout.tsx` sidebar DB-driven da `/v1/me/interfaces` via hook `useMyInterfaces` + filtro perspective PET, gating ora server-side, E2E 76/76 PROD). Vedi `RBAC_UIX_PERSPECTIVES_PLAN.md`.
- **Brownfield Wave 1**: 13/19 IMPORT targets popolati (~34509 upserted); 3 reclassed REFERENCE_ONLY (ADR-0020); 3 silent-skip mitigati (CW-B61 observability).

## 5. Auth / Security

- Argon2id 64MiB/3/4 (ADR-0005, auto-rehash). JWT RS256 15min HttpOnly+SameSite=Lax; refresh 30d single-use + replay detection (`401 REFRESH_REPLAY_DETECTED`); keys `.secrets/jwt_*.pem` gitignored. Login **200 con body** (no 204). CSRF double-submit opt-in. MFA TOTP (otpauth, AES-256-GCM `MFA_ENCRYPTION_KEY` ≥32). RBAC **12 ruoli × 154 permessi × 681 mapping** (S1007) caricati a server start (`RBAC_NOT_LOADED` se pre-cache). Error classes typed in `src/errors/index.ts`. Logger redaction `LOG_REDACT_PATHS`.
- 12 ruoli: PLATFORM_ADMIN, TENANT_ADMIN, BLUEPRINT_MANAGER, HRMS_MANAGER, PROCESS_OWNER, MANAGER, USER, READ_ONLY + CEO, TEAM_LEADER, TEAM_MEMBER (S953/R2 holderless) + **ORG_DIRECTOR** (org-design).
- Personas seed (`pnpm db:seed-test-admin`, password dall'env `TEST_ADMIN_PASSWORD` — nessun default committato, F-001): dopo il rebuild RTL (S950) sono utenti **reali** `@rtl-bank.org` — `admin@heuresys.com` (PLATFORM_ADMIN), `federica.marchetti@` (TENANT_ADMIN), `paolo.caputo@` (MANAGER), `tommaso.fiore@` (USER, report di paolo), `antonio.parisi@` (USER, esterno alla catena). Le vecchie `*_test@rtl-bank.test` sono state cancellate dal rebuild.

## 6. Web (apps/web) — MVP-2a/2b shipped

- **Next 16** App Router + React 19 + Tailwind 4 + @heuresys/ui. **98 route `page.tsx`** (S1007): admin + analytics (**9**: workforce/kpi/attendance/compensation/skills/org-network/overtime/skills-by-category + index) + ESS `/me/*` (21, ADR-0011 MVP-2b) + GTM (`/`,`/demo`,`/investors`) + system-health + auth + showcase. Tag MVP-2a `v0.2.1-mvp2a-final`. **Auth gate = `src/proxy.ts`** (ex-`middleware.ts`, next 16 rename S969).
- **55 Playwright spec** E2E (login reale + assert su dati seed) (S1007). Dottrina LIVE DATA E2E ONLY (vedi `NEXT_SESSION_MVP_2A.md`): no mock/fixture/placeholder; ogni cella da `/v1/*` reale; nessun page commit senza E2E verde.
- **/showcase**: riabilitato (CW-B59 RESOLVED) via `apps/web/src/app/showcase/_ui-client.tsx` (`next/dynamic ssr:false`); deploy GitHub Pages verde.
- **/system-health** (S968 F7): produzione PLATFORM_ADMIN ora **100% live** (`GET /v1/observability/system-health` + `/v1/auth/role-permissions`) via container `SystemHealthLive.tsx` + hook `lib/api/observability.ts`; mock confinato a `/showcase/system-health`. 4 sezioni senza backend droppate (rationale nel component header). E2E `system-health.spec.ts` live.

## 7. CI / Infrastructure

- **8 workflow CI self-hosted** su OCI VM runner: typecheck, lint, test-integration (DB+JWT+RBAC), build-web, playwright-smoke (5 personas, web :3187), i18n-parity, **state-lint** (handoff/SoT coherence), **shell-tests** (deploy/align). **+ showcase deploy** (GitHub Pages) + Dependabot. Stato letto live via `gh run list` (cfr. `status_dashboard.py`); al refresh S1007 tutti verdi **tranne `test-integration`**, riparato in S1007 (fix assertion skill-count → derivata live dal DB) → verde post-push.
- Runner systemd EnvironmentFile `/etc/heuresys-runner.env` (JWT PEM `\\n` double-escape; ADMIN_ORIGIN; NEXT_PUBLIC_API_BASE_URL). Setup: `docs/ci/self-hosted-runners-setup.md`.
- **Host topology**: Windows primario (`DESKTOP-KH728P2`, PS 5.1, Git Bash, `C:\Git\cmd\git.exe`); OCI VM `oracle-vm-default` 80.225.82.207 (runtime+CI); linux-pc 192.168.1.11 (PROD twin). **Mac (`mac-local` 192.168.1.4) RITIRATO da `all`/close-propagate (S1007 — peso morto: CLI SIGILL su Ivy Bridge, ecosystem channel falliva 16 plugin+drift); resta target on-demand `align-clones.sh mac`.** Chiavi SSH in `C:\Users\enzospenuso\.ssh\` (`oci_recovery_ed25519` passphrase-protected, mac_local, github).
- **Deploy/bootstrap (B-44, 2026-05-28)**: idempotent per-OS scripts in `scripts/` + `deploy/`. `vm-bootstrap.sh` = Linux server (systemd units `heuresys-advanced-{api,web}`, public **:8013/:3013**, Node 22 via nvm, DB local :5432). `dev-bootstrap.sh` (Mac/Linux-desktop) + `dev-bootstrap.ps1` (Windows) = workstation, on-demand `pnpm dev` (:3001/:3000) against the VM DB via tunnel :5433. See `deploy/README.md`. heuresys-advanced **also runs live on the VM** (8013/3013) alongside legacy evo.

## 8. Prerequisiti autonomia unattended (checklist boot CLI)

```bash
ssh -o BatchMode=yes oracle-vm-default 'echo OK'   # key in agent? altrimenti load manuale (CW-B62)
nc -z localhost 5433 && echo tunnel-up             # auto cross-reboot via task HeuresysTunnel5433 (ADR-0021); fallback hook session-boot.ps1
git -C D:/heuresys-advanced log origin/main..HEAD --oneline   # vuoto = synced
pnpm install -r && pnpm --filter @heuresys/web build          # exit 0
gh run list --limit 6                              # main CI verde
```

- **Stato S1007 boot**: tunnel 5433 UP ✓, DB reachable ✓, working tree pulito ✓ (post-commit), handoff-lint coerente ✓, drift §0-vs-live = 0 (via `status_dashboard.py`).
- **Push policy**: commit locali su `main` pre-autorizzati (project rule). **Push solo su ok esplicito di Enzo**; l'autorizzazione vale per la sessione fino a revoca, nuova sessione riparte da "ask".
- **SSH tunnel hands-off** (ADR-0021, chiude CW-B62 / B-31): tunnel `:5433` automatico cross-reboot via scheduled task At-Logon `HeuresysTunnel5433` + hook di sessione fallback `scripts/session-boot.ps1`, su service-account key no-passphrase ristretta (`heuresys_tunnel_ed25519`, `permitopen="127.0.0.1:5432"` + forced command, no shell). Setup one-shot idempotente: `scripts/setup-tunnel-automation.ps1`. La chiave admin passphrase-protected resta per il solo accesso interattivo (helper `s937-ck1-load-ssh-key.ps1`).

## 9. Invarianti non negoziabili (override "common patterns")

I1 Position-centric (owner ≠ incumbent) · I3/I4 schema `sys.sys_<plural>` (aux: staging/brownfield/audit) · **I5 tenant isolation = FK + middleware, MAI RLS** · I7 auth separato (`sys_auth_*`) · I9 PIP = VIEW (ADR-0008) · **I13 PostgreSQL 16 NATIVE, NO Docker** (ADR-0004; runtime only — legacy Docker = read-only source) · RD-08 categorical = `varchar+CHECK` (mai ENUM) · RD-09 `date` vs `timestamptz` · **I12 brownfield/legacy = authoritative no-PII data source** (ADR-0023; `sys.*` = structural authority, no-PII global) · **I14 legacy ingestion = EMPLOYEE-centric** (ADR-0024): la persona legacy è `employees` (207 FK), NON `users` (45 FK, auth shell); `sys_user*` ⟸ legacy `employee*`; `users` → solo `sys_auth_*`; key `LEGACY_EMP::<employees.id>`, mai `users.id`; vedi `docs/brownfield/EMPLOYEE_CENTRIC_MAPPING_DOCTRINE.md` · **I15 ambiente-unico-prod-grade, 2 tenant correnti** (ADR-0026): un solo ambiente = produzione; RTL Bank (customer-example) + Heuresys System (platform) = tenant di produzione correnti, NON "di test"; dati synthetic-provenance **trattati come reali** (no PII mai) · ADR-0011 ESS = MVP-2b. Conflitto con nuovo requisito → **fermarsi e chiedere**.

## 10. Bias / lessons (assorbito da bias_registry.md)

`cowork_reserved/bias_registry.md` = SoT bias storica (read-only archive da ora). **62 catalogati** (CW-B17→B63, B57 withdrawn), **45 mitigated**, **0 deferred-fix** (CW-B59 RESOLVED). Next `CW-B64`. Lezioni meta più importanti per il CLI autonomo: empirical test matrix > narrative diagnosis (B58); time-box 60-90min/8-10 iter su bisect prima di reframe (B59); automazione "shipped" non eseguita NON è validata — il primo run reale è parte del deliverable (B63); pre-flight `git ls-tree HEAD` per evitare spec staleness (B52). **D'ora in poi il CLI emette eventuali nuovi bias direttamente qui (`docs/kb/`), non più via claim race Cowork** — vedi `DEBT_REGISTER.md` per la policy di numerazione post-Cowork.
