# Fase 2 — Census forense DB (mandato S1023) · S1024 · 2026-07-21

> Output del fan-out F2.1-F2.7 (7 analisi read-only, DB live :5433, HEAD `fa125fc6`).
> Fase 1 (census debiti) chiusa in `fa125fc6`. Questo doc è l'artefatto di sintesi; i piani d'intervento in §8.

## §1 Completezza funzionale (F2.1) — modulo → tabelle → dati

- **89 moduli totali**: 62 pienamente popolati · **24 con gap** · 3 non valutabili (brownfield-*, operano su staging).
- **14 moduli VUOTO-TOTALE** (tabella primaria a 0 righe): approvals (requests+steps), leads, whistleblowing, process-kpi-templates, blueprint-activations, blueprint-overrides, successor-readiness (0 assessment per 25 candidati — famiglia succession popolata: pools 17, candidates 25, readiness_scores 462, succession_scores 90), seed-acquisition-runs / seed-candidate-records / seed-approval-decisions, visualization-{exports,layouts,node-layouts,styles} (il grafo core È popolato: 1 graph, 158 nodi, 157 edges).
- **10 moduli PARZIALE-scarno**: me (approvals/notification_preferences/target_positions = 0), auth (webauthn/recovery/otp_challenges/exemptions MFA = 0), compensation (reward_gates/gate_results/payroll_handoff = 0), gdpr (consents 0, requests 1), content (categories 0, media 0), dashboard (blueprint_activations 0), training-initiatives (1), content-blueprint-links (1), blueprint-families/variants (1/1).
- **35 tabelle sys.* a 0 righe** (confermate con count esatto), di cui **8 orfane** (0 riferimenti in tutto apps/api/src): sys_auth_sessions, sys_auth_mfa_exemption_audit, sys_organization_unit_history, sys_position_skill_requirement_history, sys_payout_curves, sys_seed_source_evidence, sys_seed_validation_results, sys_user_professional_experiences.

## §2 Dati sporchi (F2.2) — violazioni semantiche oltre le 6 viste

| # | Violazione | Count | Severità |
|---|---|---|---|
| 3c | **Skill duplicate per nome normalizzato** (24 gruppi, 76 righe; mix ESCO::/COMP:: sullo stesso nome — es. "problem solving" ×4 GLOBAL, "leadership" ×4 RTL) — double-count in matching/gap/succession | 24 gruppi | MEDIUM |
| 8a | **Utenti DEACTIVATED con assignment ACTIVE** (offboarding non propagato): alice.esposito, alberto.colombo (RTL), platform.admin (HEU) | 3 | MEDIUM |
| 4 | **`user_employment_status`/`user_contract_status` = 'active' minuscolo, SENZA CHECK** (convenzione schema = MAIUSCOLO; filtri `='ACTIVE'` mancano tutto) | 161+158 | LOW-MED |
| 5b | Posizioni ACTIVE senza incumbent (POS-00000001 CEO, POS-00000003 — tenant HEURESYS) | 2 | LOW/INFO |
| 5a | Utenti ACTIVE senza assignment (chiara.spenuso, admin@ — tenant HEURESYS) | 2 | LOW/INFO |

Tutto il resto **ZERO violazioni**: date impossibili, updated<created, orfani logici, duplicati su natural key canoniche, cicli reports-to/OU, range quantitativi, enum nei CHECK (277 CHECK, 0 NOT VALID), cross-tenant leak. Nessun overlap con le 6 viste esistenti.

## §3 FK mancanti (F2.3)

Solo **3 FK genuine mancanti, tutte a 0 orfani → FK piene** (niente NOT VALID):
1. `sys_goal_templates.template_role_id` → `sys_job_roles` (nullable, ON DELETE SET NULL)
2. `sys_goal_templates.template_org_unit_id` → `sys_organization_units` (nullable, SET NULL)
3. `sys_auth_mfa_exemption_audit.*_user_id` → `sys_users` (audit trail → RESTRICT) — priorità media-alta
Scartate con motivo: ~40 natural-key `*_code` proprie, 9 polimorfiche by-design (pattern D-54), id esterni/legacy, ISO literal, 2 grouping-key (`refresh_token_family_id`, `org_unit_template_blueprint_id` = legacy group documentato in 000064 — NON è FK benché 225/225 "orfani").
Adiacente segnalato: `auth_mfa_exemption_audit_actor` (uuid → users, fuori pattern *_id).

## §4 Ontologia (F2.5)

| Pilastro | Verdetto | Numeri cardine |
|---|---|---|
| Skills taxonomy | **GAP** | 14093 skill; **8408 (59.7%) ISOLATE** (0 edges); categorie 31/14093 (7 categorie totali); alias 80/14093. Edges 11965 (0 pendenti). skill_kind 99.6% ok (57 NULL su COMP::) |
| ESCO | SANO (+gap minore) | group_uri 12892, broader_uri 5007, esco_uri 14011/14093; occupation_skill_requirements **126051 esatte**, 0 non-risolte. Gap: job_role→ESCO curato 25/136 |
| Embeddings pgvector | **SANO** | 4 tabelle vector(1024) al 100%: skills 14093, occupations 3045, job_roles 136, user_profiles 156. Usati in prod (semantic-matching kNN `<=>`) |
| Knowledge graph | **GAP** | kg_nodes/kg_edges legacy (139k) NON migrati; il "grafo" advanced = 1 organigramma (158 POSITION / 157 REPORTS_TO, integro) |
| Catene trasversali | SANO/GAP ampiezza | position_skill_requirements 844 (148/162 pos, 0 pendenti); **sys_user_skills 905 righe ma SOLO 31 skill distinte** (le stesse 31 categorizzate = seed demo); gap_analysis 270 righe con `position_id` TUTTO NULL |

Interventi proposti: derivare edges/categorie dai group/broader URI ESCO (collassa le 8408 isolate senza dati esterni); promuovere job_role→ESCO via embedding top-1; ampiezza user_skills + position_id in gap → **Fase 3 (seeding)**; KG legacy → decisione deprecate-vs-materialize (item #50 D4).

## §5 Bilinguismo IT/EN (F2.6) — survey

- **Zero infrastruttura i18n nel DB** (0 tabelle translation, 0 jsonb multilingua). Reference data **IT-ificato distruttivamente in-place** (mig 000156-000162, decisione Enzo G-01/S1006): gli EN originali NON sono più nel DB (vivono solo nei file migration/git history).
- Uniche 4 colonne locale-aware, **nessuna consumata** per selezione testo: users.user_locale (160×'it-IT'), user_preferences.locale, skill_aliases.locale (alias ricerca, non traduzioni), org_unit_templates.**name_en** (225, unica coppia IT+EN, mai letta dall'API).
- Frontend: i18next IT-default con parity check — **UI statica già bilingue**; il gap è al 100% sui dati dinamici (nessun endpoint locale-aware, il testo DB bypassa i18next).
- Superfici che mordono: (1) **descrizioni skill 100% EN su 14093** (nomi IT/corpi EN); (2) liste MISTE EN/IT su kpi_definitions (243) e job_roles (136); (3) cataloghi governance interamente EN: permissions 182, roles 13, skill_families 77, categories 7, proficiency_levels 6, goal_templates 40, operating_model 6; (4) descrizioni sistematicamente in ritardo sui nomi; (5) nessun fallback EN per utente EN (reference IT-only).
- Harvest EN→IT: praticabile — le mig 000156-000162 contengono le mappe (CTE code→it; l'EN è il pre-update, recuperabile da git history/seed originali; per le skill ESCO esiste l'API multilingua).

## §6 Pulizia/performance (F2.7)

- DB 1359 MB / 281 tabelle. Top: audit.import_validation_results **547 MB** (confermato #60), skill_embeddings 372 MB (220 = HNSW, atteso). Nessuna crescita anomala. XID age 0.05%, sequenze ok, connessioni ok, autovacuum in pari (nessuna tabella oltre soglia bloat; refresh_tokens 19.9% dead borderline).
- **Unico problema di performance reale MISURATO**: manca l'indice su `sys_source_lineage_records(source_record_id, target_table_name)` — lookup per-riga del brownfield import: **280.010 seq_scan / 10,9 MILIARDI tuple lette**, EXPLAIN conferma Seq Scan. Fix: indice composito + INCLUDE(target_record_id).
- Indici droppabili (idx_scan=0 lifetime, stats_reset NULL → segnale forte): `audit_import_validation_results_source_table_idx` (**11 MB**), sys_skills_skill_kind_idx, sys_attendance_unvalidated_idx, sys_user_certifications_expires_idx, sys_viz_nodes_source_entity_idx. NON droppare HNSW job_role/user_profile + trigram/FTS (feature-backing senza traffico).
- Tuning proposto: autovacuum per-tabella su import_validation_results e skill_embeddings (scale_factor 0.05/0.02); `pg_stat_reset()` post-interventi per ri-giudicare su finestra pulita; VACUUM ANALYZE refresh_tokens facoltativo.

## §7 Tabelle morte (F2.4)

- 281 tabelle in scope: 182 protette relazionalmente (FK/viste/registry) → VIVE per costruzione; 99 passate al vaglio codice.
- **MORTE nette**: `staging.legacy_rtl_occupations` (0 righe, 0 ref ovunque) + 29 `staging.tmp_*` (scratch delle wave-migration) → drop con migration (D-69/Fase 3).
- **DORMIENTI-by-design** (→ drop/archive in Fase 3, D-69): 18 `staging.wave1_*` (engine FROZEN D-11), 14 `staging.rtl_*` (landing raw), 4 `temp_sdbi.pf_*`; `audit.skills_junk_archive` (7846 righe = archivio rollback di 000160, droppabile a retention chiusa).
- **VIVE non-ovvie**: `brownfield.column_mappings/source_watermarks/tenant_id_mappings` (pipeline import attiva, NON frozen); `audit.import_validation_rule_codes` (lookup canonico).
- ⚠ **`audit.user_self_service_actions`: VIVA ma trail VUOTO** — letta da `observability/repository.ts:178`, MAI scritta dai path `/v1/me/*` → gap di strumentazione/conformità (audit mutazioni ESS non popolato). Candidato debito/backlog, NON drop.
- Cautela: gli scan-count sono gonfiati dall'health-check (registry 1.6M seq) → segnale-1 pesato meno di code-refs/relazionale.

## §8 Piano d'intervento (ordine di esecuzione, branch dedicato)

1. **MIG hygiene** (~000188): indice lineage §6 (ALTA) · 3 FK §3 · normalizzazione 'active'→'ACTIVE' + CHECK su user_employment/user_contracts · chiusura assignment di utenti DEACTIVATED (regola generica idempotente) + vista integrità `v_deactivated_user_active_assignment` · drop 5 indici morti · autovacuum reloptions.
2. ✅ **MIG dedup skill `000189` FATTO** (D-72): 24 gruppi collassati sul canonico ESCO-preferred, 573 referenze repointate (conflitti: 4 learning-map + 1 self-loop edge assorbiti), archive `audit.skill_dedup_archive` (52), unique index `sys_skills_natural_key_uq`. 14093→14041 skill, 0 dup residui, 0 ref orfane, twice-run OK. NB: prima misura "loser orfani" era errata (footgun `IN(tuple)`+NULL su scope GLOBAL) — corretta con window COUNT.
3. **i18n dati (Fase 2.6 APPLY)** — ADR-0029, decisione Enzo S1024 = bilinguismo COMPLETO:
   - ✅ **STRUTTURA `000190`**: `sys.sys_reference_translations` (polimorfica D-54) + registro `sys_translatable_field` (22 campi) + vista integrità generica `v_reference_translation_orphans`.
   - ✅ **GOVERNANCE `db/scripts/populate-reference-translations-governance.sql`** (idempotente, generato da fan-out di 4 traduttori HR/banking): 358 voci IT in-row + EN→translations su 8 cataloghi (roles 13, permissions 182, skill_families 77, goal_templates 40, job_families 27, skill_categories 7, proficiency 6, operating_model 6). Verificato live: 498 righe EN, 0 orfani, nomi/descrizioni IT in-row.
   - ✅ **PULIZIA `000191`** (D-73): sentinella sporca `""` (stringa 2-char) su 218 righe description (permissions 181, skill_families 24, proficiency 6, operating_model 6, skill_categories 1) → NULL; census dinamico su tutte le colonne testuali sys.* (BASE TABLE); 0 residui.
   - ⏳ **SKILL 14k EN→IT** via ESCO IT (connector esistente, `language=it`) = blocco successivo.
   - ⏳ **wiring API** overlay `x-locale` + fallback IT = blocco successivo.
4. ✅ **ONTOLOGIA `000192` + loader FATTO** (ADR-0030): dataset ESCO ufficiale v1.2.0 IT (i `skill_group_uri` in metadata erano stale, 0 match — usato il dump `broaderRelations` come autorità). Nuova tabella `sys_skill_groups` (640 gruppi, gerarchia 4 livelli aciclica) + `sys_skills.skill_group_id` FK; 13.647 skill→gruppo + 6.456 edge IS-A. **Copertura 99,4%** (13.952/14.041 collegate; 89 scollegate = custom COMP:: senza URI ESCO). Idempotente, UUID v5 deterministici.
5. **→ Fase 3**: seeding (user_skills ampiezza, successor_readiness, gap position_id, moduli vuoti §1 con criteri di coerenza bancari) · residui staging/temp_sdbi (D-69) · chiusura brownfield.
6. **→ Register**: nuovi debiti dal census (vedi DEBT_REGISTER aggiornamenti S1024-bis).
