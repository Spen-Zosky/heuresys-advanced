### api:c1  {'modules': 10, 'endpoints': 73, 'tables_distinct': 40}
Inventariati i primi 10 moduli API (activity-classification-mappings → blueprint-families): 73 endpoint totali, 40 tabelle sys.* distinte, tutti con schema condiviso in packages/shared/src/schemas/ tranne auth core (schema Zod locale in modules/auth/schema.ts). Auth è il modulo più grande (31 endpoint su 2 route-file, 16 tabelle, connettori SMTP/SMS/WebAuthn); analytics è il più largo in lettura (13 tabelle, solo GET). Gli orgGate config sono presenti solo sul cluster assessment (catalog/service); pattern di visibilità: global-catalog PLATFORM_ADMIN-write (activity-*, blueprint-families), tenant-scoped (assessments, approvals, blueprint-activations), scope-tiered PLATFORM/TENANT/TEAM (analytics). Nessuno schema brownfield/staging/audit referenziato in questi moduli.
- Naming mismatch: il modulo activity-classification-mappings esporta activityMappingsRoutes (apps/api/src/app.ts:77,395) — unico modulo del batch con export name divergente dal dir name
- Permission semantics: DELETE su activity-classifications e activity-classification-mappings usa enterprise_typing:update, non un :delete dedicato (activity-classifications/routes.ts:36; activity-classification-mappings/routes.ts:32)
- Split permission incoerente in blueprint-families: POST=blueprint:activate ma PATCH/DELETE=blueprint:override (blueprint-families/routes.ts:24,31,35) — in blueprint-activations tutte le write usano blueprint:activate
- GET /v1/auth/role-permissions (read-only matrix) gated dalla permission auth:revoke_user riusata (auth/routes.ts:309) — semantica revoke usata per una read; commento dice 'full role CRUD lands in MVP-3' (routes.ts:305) — CRUD ruoli mai shippato = capability latente
- TODO vivo in auth: 'Cross-tenant target check is tracked as a follow-up' per POST /admin/revoke-user (auth/routes.ts:249-251) — verificare se il service ora lo enforcea
- SMS_OTP è stub code-only: enrollment gated server-side 404 SMS_NOT_CONFIGURED, nessun provider reale (auth/mfa-routes.ts:339-340; auth/sms-sender.ts) — feature senza infrastruttura
- Commento STALE in mfa-routes.ts:482-483: 'unusable on the current plain-HTTP PROD origin until TLS lands' — PROD è HTTPS da S962 (nginx TLS), commento da aggiornare
- orgGate coverage nel batch: solo assessment-methods (catalog), assessments e assessment-results (service) dichiarano config.orgGate; analytics/approvals/activity-*/blueprint-* nessuno (verificato su tutti i routes.ts letti) — da incrociare con la taxonomy D-51 per confermare che sia intenzionale
- Approvals effects registry ha UN solo effect implementato (tenant-activation → UPDATE sys.sys_tenancies, approvals/effects/tenant-activation.ts:26) — extension point sottopopolato, opportunità per nuovi approval-driven side effects
- approvals/sla-cli.ts è un entrypoint CLI per escalation SLA (candidato automazione cron/systemd timer se non già schedulato)
- Deviazione dal module pattern in auth: schemi Zod core in modules/auth/schema.ts locale invece che @heuresys/shared (auth/routes.ts:16-29); solo gli schemi MFA/WebAuthn stanno in shared
- Analytics export route senza response-schema (raw CSV/JSON passthrough, analytics/routes.ts:117-127) — deviazione documentata dal pattern Zod-typed response
- Immutabilità by-design: assessment-results senza update/delete (routes.ts:3) e assessments senza DELETE (status=CANCELLED, routes.ts:3) — coerenti col dominio ma da riflettere nell'atlante come vincolo
- Endpoint auth pubblici senza CSRF per design (challengeToken come proof): /mfa/verify-login, /mfa/*-otp/resend-login, /mfa/webauthn/authentication/* (mfa-routes.ts:271-289,407-448,530-588) — superficie non-cookie-auth da tenere mappata per security review

### api:c2  {'modules': 10, 'endpoints': 39, 'tables_distinct': 33}
Inventariati i 10 moduli assegnati (blueprint-overrides/processes/variants, brownfield-import-runs/source-exports/table-mappings/wave-executor, capability-composition/maturity, career-path-steps): 39 endpoint totali, 33 tabelle statiche distinte piu tabelle dinamiche del wave-executor (staging.wave<N>_*, legacy_mirror.*, sys.sys_<target> data-driven). Tutti i moduli seguono il pattern routes+repository+service con requirePermission per route e verifyCsrf su tutte le write; i moduli brownfield e i write blueprint sono platform-only, i capability-* usano scope org (orgGate service/aggregate) con CLI di recompute per systemd timer, career-path-steps eredita la visibilita dal path padre (orgGate catalog sulle read). Il wave-executor e il modulo piu complesso (11 file: engine data-driven, state machine 6 stati, transform-compiler a 12 codici, loader legacy_mirror).
- Permission-semantics riusata in modo grossolano sui blueprint: POST create di processes/variants gated da 'blueprint:activate' e update/delete da 'blueprint:override' (blueprint-processes/routes.ts:24,31,35; blueprint-variants/routes.ts:24,31,35) — non esistono permessi blueprint:create/update dedicati, opportunita di raffinamento RBAC
- transform-compiler.ts supporta solo i 12 transform code meccanici; JSON_EXTRACT e LINEAGE_SOURCE_NK dichiarati non supportati nel codice (brownfield-wave-executor/transform-compiler.ts:8-10) — gap documentato per wave future
- Executor wave-agnostico ma dati fermi a Wave 1: loader.ts:4-7 carica '88 Wave 1 source tables' da db/seeds/brownfield/wave1/legacy_data; Wave-2/3 import deferred a v1.x — feature senza dati
- Commento stale in test: apps/api/test/brownfield.integration.test.ts:2-4 afferma 'brownfield.* tables are empty in CI' — post-rebuild RTL/waves le tabelle brownfield sono popolate sul DB condiviso; il commento descrive uno stato non piu vero
- capability-composition esclude esplicitamente l'ESS self-view (capability-composition/routes.ts:8 'no ESS self-view') — possibile linea di sviluppo: esporre lo score capability del dipendente su /v1/me/*
- Prefix non uniforme: brownfield-wave-executor registrato su /v1/brownfield/wave-executor (nested, app.ts:409) mentre gli altri brownfield-* usano /v1/<nome-modulo> flat; capability-composition e capability-maturity condividono lo stesso prefix /v1/capability (app.ts:428-429)
- brownfield-import-runs non ha DELETE e brownfield-source-exports e read-only puro (2 endpoint): i run/export si creano solo via pipeline o trigger — nessuna via API per pulizia/archiviazione dei run storici
- orgGate presente solo sulle route GET (capability-*: service/aggregate; career-path-steps: catalog); nessun orgGate su blueprint-* e brownfield-* (platform-gated a monte) — coerente con taxonomy D-51
- I due recompute-cli.ts (capability-composition, capability-maturity) sono entrypoint systemd-timer in-process con synthetic system actor senza HTTP/auth — pattern replicato da insights/recompute-cli.ts, dipendenza d'ordine: maturity va eseguito DOPO il recompute MLCE (capability-maturity/recompute-cli.ts:7-9)

### api:c3  {'modules': 10, 'endpoints': 63, 'tables_distinct': 35}
Inventariati i 10 moduli del batch C3 (career-paths, compensation, content [+media plugin], content-blueprint-links, dashboard, engagement, engagement-feedback, enterprise-size-bands, enterprise-typing-profiles, goals): 63 endpoint totali con permission/CSRF/orgGate per route, 35 tabelle sys.* distinte, schema shared e test file per modulo. Pattern rilevanti: orgGate presente solo su read di career-paths (catalog), compensation (catalog/service/aggregate) e goals (service); content è il modulo più ricco (20 endpoint, workflow publish + versioning + media object-store); engagement è un read-model READ-ONLY che riusa surveys:read; enterprise-* sono reference-data con PUT-upsert. Tutti importano dal barrel root di @heuresys/shared, non dai subpath.
- sys.sys_payroll_handoff_records è write-only: INSERT in apps/api/src/modules/compensation/repository.ts ma nessun SELECT in tutto apps/api/src (verificato con grep 'FROM sys.sys_payroll_handoff_records' = 0 hit) — ledger scritto mai esposto, manca endpoint di lettura/audit
- sys.sys_compensation_recommendations è letto solo come count(*) dal dashboard (apps/api/src/modules/dashboard/repository.ts:149) — nessun endpoint di list/detail delle recommendation salvate
- media object-store: driver S3/MinIO dichiarato come config-swap futuro via MEDIA_STORAGE_DRIVER ma non implementato — solo LocalDiskStore (apps/api/src/modules/content/media-store.ts:3-6), TODO architetturale in attesa decisione PM
- incoerenza granularità permission: DELETE usa ':update' invece di ':delete' in career-paths (routes.ts:49-50) e enterprise-size-bands/typing-profiles (routes.ts:27-28), mentre content/engagement-feedback/goals hanno ':delete' dedicato
- orgGate (asse bi-assiale ADR-0027) applicato solo a 3 moduli su 10 del batch (career-paths, compensation, goals); assente su engagement/engagement-feedback/content — verificare se intenzionale (dati non-sensitive) o gap di copertura
- engagement è READ-ONLY e riusa la permission m2 'surveys:read' (apps/api/src/modules/engagement/routes.ts:4) — nessuna resource permission propria: eventuale write-path futuro richiederà nuova resource
- dashboard espone un solo widget aggregator GET /widgets su 11 tabelle (apps/api/src/modules/dashboard/routes.ts:14) — candidato naturale per estensioni (widget per-modulo, drill-down)
- tutti i 10 moduli importano dal barrel '@heuresys/shared' e non dai subpath './schemas/<x>' previsti dal module pattern step 1 (grep import in tutti i *.ts dei moduli) — i subpath export esistono ma sono inutilizzati lato API

### api:c4  {'modules': 9, 'endpoints': 43, 'tables_distinct': 22}
Inventariati 9 moduli API (insights, job-families, job-roles, kpi-definitions, leads, learning-gaps, learning-modules, learning-path-steps, learning-paths): 43 endpoint totali su 22 tabelle sys.* distinte. Insights e' il modulo piu' ricco (7 endpoint, 13 tabelle, pgvector cosine, CLI systemd-timer per il recompute schedulato, orgGate=service); i moduli learning-* e kpi-definitions seguono il pattern catalog global+tenant con orgGate=catalog sui GET; leads ha un POST pubblico (rate-limit + honeypot) e un GET PLATFORM_ADMIN esportabile CSV/XLSX. Frammento YAML scritto in atlas_fragments/api_c4.yaml.
- job-families: nessuna requirePermission su nessuna delle 5 route — job_family CRUD non esiste nella permission matrix del seed; read aperta a qualsiasi autenticato, mutazioni gated solo nel service a PLATFORM_ADMIN (apps/api/src/modules/job-families/routes.ts:6-8). Opportunita': aggiungere job_family:* alla matrix per uniformita' RBAC.
- job-roles: manca il DELETE endpoint perche' job_role:delete non e' in seed (apps/api/src/modules/job-roles/routes.ts:3) — lifecycle CRUD incompleto sul catalogo ruoli.
- learning-gaps: DELETE riusa gap_analysis:update perche' gap_analysis:delete non e' in seed (apps/api/src/modules/learning-gaps/routes.ts:5) — semantica permesso imprecisa.
- job-families e job-roles sono gli unici cataloghi del batch senza alcuna declaration config.orgGate sui GET, mentre i sibling kpi-definitions/learning-* dichiarano orgGate:'catalog' (confronta job-families/routes.ts:26 vs kpi-definitions/routes.ts:23) — passano il boot-assert D-51 (che copre solo data-class sensitive) ma la copertura tassonomica e' incoerente.
- leads: listLeads senza paginazione ne' filtro (SELECT completo ORDER BY created_at, apps/api/src/modules/leads/repository.ts:23-27) e repository che bypassa il pattern DbConnector usando il pool singleton (riga 4) — accettabile ora (PLATFORM_ADMIN only) ma crescera' monotono col traffico del form pubblico.
- insights: flight-risk e' deliberatamente senza ESS self-view (D-6, apps/api/src/modules/insights/routes.ts:8-9) — possibile linea di sviluppo prodotto: insight self-service per il dipendente (con framing appropriato).
- insights: la CTE engagement scarta i response_answers con shape object (default '{}' delle survey create via API) e conta solo la shape array legacy (apps/api/src/modules/insights/repository.ts:132-148) — finche' il dual-shape non e' unificato, le survey API-created non contribuiscono MAI alla feature engagement del flight-risk.
- insights: read scores con LIMIT 5000 hardcoded (repository.ts:289,581,623) — silent truncation potenziale quando la popolazione scored superera' 5000 subject.

### api:c5  {'modules': 9, 'endpoints': 83, 'tables_distinct': 61}
Inventariati 9 moduli API (me, mentorship, mfa-policy, notifications, observability, okrs, operating-models, organization-unit-kpi-templates, organization-unit-processes) per un totale di 83 endpoint e 61 tabelle distinte. Il modulo me domina (44 endpoint ESS self-scope, 45 tabelle toccate direttamente) con deleghe cross-module a teams/content/auth; mentorship (17 endpoint) e okrs sono gli unici due con config.orgGate (catalog/service sulle sole GET). Tre moduli riusano permission di altre risorse (observability usa tenant:create come proxy platform-only, operating-models usa enterprise_typing:*, ou-kpi-templates usa bpm_process:*). Notifications e write-only (broadcast) con digest CLI systemd che resta un chassis senza SMTP; observability e alimentato da un ring in-memory 24h piu probe live DB/pool/RBAC. Frammento YAML scritto in atlas_fragments/api_c5.yaml.
- observability gated con perm proxy 'tenant:create' perche non esiste una perm observability:* dedicata (apps/api/src/modules/observability/routes.ts:19) — opportunita: perm dedicata se la superficie observability cresce
- operating-models riusa enterprise_typing:read/update e il DELETE gira sotto :update non :delete (apps/api/src/modules/operating-models/routes.ts:16,24,28) — nessuna perm operating_model:* dedicata
- organization-unit-kpi-templates riusa bpm_process:read/update (apps/api/src/modules/organization-unit-kpi-templates/routes.ts:17,25) — perm non allineata alla risorsa
- PATCH /v1/me/notification-preferences scrive sotto la perm notification:read:self, nessuna perm write separata nel catalogo (apps/api/src/modules/me/routes.ts:244-247; F-031 confermato by-design ma incoerente col pattern read/write)
- notification digest CLI e un 'chassis until SMTP is provisioned': ConsoleMailer logga l'intento senza inviare (apps/api/src/modules/notifications/digest-cli.ts:8-11) — feature schedulata senza effetto reale finche SMTP non e configurato
- commento service mfa-policy dichiara validazione roleCodes contro '8 canonical RoleCode values' ma i ruoli sono 11 dal S953/R2 (apps/api/src/modules/mfa-policy/service.ts header) — possibile validazione/doc stale che esclude CEO/TEAM_LEADER/TEAM_MEMBER dalle policy MFA
- asimmetria orgGate D-51: mentorship (8) e okrs (3) dichiarano config.orgGate, gli altri 7 moduli del batch zero occorrenze (grep -c orgGate sui 9 routes.ts) — verificare se operating-models/ou-kpi-templates/ou-processes sono esenzioni intenzionali dall'assertion boot org-gate
- modulo me e un god-module: 44 endpoint e 45 tabelle referenziate direttamente nel repository (grep FROM/JOIN/INTO su modules/me) piu deleghe a teams/content/auth — candidato a split per manutenibilita
- POOL_MAX_CLIENTS=20 duplicato hardcoded in observability/service.ts:23 a specchio di db/client.ts (pg.Pool non espone il max configurato) — rischio drift dichiarato nel commento
- notifications non ha superficie di lettura admin (solo POST broadcast; routes.ts:13-22): nessun modo per un admin di consultare/auditare i broadcast inviati — opportunita: GET /v1/notifications storico broadcast
- okrs: key-results sono read-only (GET /:id/key-results, nessuna route di write; apps/api/src/modules/okrs/routes.ts:27-31) — gestione key-results assente dalla API surface
- observability espone solo /system-health JSON, nessun endpoint Prometheus /metrics (routes.ts:17-22) — opportunita integrazione monitoring standard

### api:c6  {'modules': 9, 'endpoints': 44, 'tables_distinct': 27}
Inventario cross-layer completato per i 9 moduli assegnati (organization-units, position-career-paths, position-succession-relevance, positions, predictions, process-kpi-templates, public-stats, reference-sync, seed-acquisition-runs): 44 endpoint totali su 27 tabelle distinte (24 sys.* + 3 brownfield.*). Tutti i moduli seguono il pattern routes/service/repository con Zod da @heuresys/shared (schema file 1:1 col nome modulo) e test integration dedicati. Peculiarita mappate: public-stats e l'unico endpoint pubblico (no auth, rate-limit 30/60s, cache TTL 5 min); predictions e un read-model READ-ONLY con orgGate service/catalog; reference-sync ha 2 connettori HTTP esterni (ESCO + ISTAT ATECO) + CLI systemd (sync-cli.ts) ed e PLATFORM_ADMIN-only globale; positions espone il PIP via VIEW (ADR-0008).
- predictions e import-as-read-model READ-ONLY: nessun endpoint di write/recompute, valori legacy precomputati esposti as-is — opportunita: pipeline di ricalcolo predizioni (apps/api/src/modules/predictions/routes.ts:3-5, service.ts:2-3)
- seed-acquisition-runs.trigger inserisce SOLO il run record, nessuna esecuzione di acquisizione in-process — l'executor reale e esterno/assente dal modulo (apps/api/src/modules/seed-acquisition-runs/service.ts:31-42)
- riuso permission-verb non dedicate: DELETE career-paths e succession-relevance usano career_succession:update (routes.ts:42 e :40), seed-acquisition POST/PATCH/DELETE tutti sotto seed_acquisition:trigger (routes.ts:25-37), process-kpi-templates riusa bpm_process:* — nessuna permission :delete dedicata in questi 4 moduli
- process-kpi-templates: doppio gate ridondante — RBAC bpm_process:update + check isPlatform hardcoded nel service (PLATFORM_ADMIN required); il vincolo catalogo-level non e espresso nel permission model (apps/api/src/modules/process-kpi-templates/service.ts:3)
- header comment stale in positions/routes.ts:3 dichiara '10 endpoints' ma le route reali sono 13 (KPI sub-resource WI-D2 aggiunto dopo)
- public-stats: cache in-process TTL 5 min non invalidabile cross-istanza — con piu repliche API i tiles possono divergere fino a 5 min (apps/api/src/modules/public-stats/service.ts:8-16)
- reference-sync dispatch ora copre tutte e 3 le sorgenti (ESCO/ATECO_2025/ESCO_SKILL_HIERARCHY, service.ts:54-59 + 142-162) — il gap 'ESCO-hardcoded' osservato 2026-06-11 risulta risolto
- organization-units e positions non hanno alcun config.orgGate (esenti dalla boot assertion D-51 perche data-class non sensitive); i gate catalog/service compaiono solo su career-paths, succession-relevance e predictions

### api:c7  {'modules': 9, 'endpoints': 39, 'tables_distinct': 17}
Inventariati i 9 moduli del cluster skill-taxonomy + seed-acquisition-viewer + semantic-matching: 39 endpoint totali su 17 tabelle distinte sys.*. Il cluster taxonomy (families/categories/edges/aliases/proficiency-levels) segue il pattern read-open + write PLATFORM_ADMIN gated nel service; skills e semantic-matching sono gli unici con orgGate (catalog/service) e permission esplicita sulle read. semantic-matching e' il modulo piu' ricco: pgvector kNN su 4 tabelle embedding, connettore esterno Voyage AI (backfill CLI + free-text query-time flag-gated), DI seam per i test, rate-limit per-route dedicati. I moduli seed-* sono viewer/ledger read-mostly della pipeline seed-acquisition. Ogni modulo ha test integration dedicato (semantic-matching ne ha 4).
- Incoerenza permission-gating nel cluster skill: le read di skills richiedono skill:read + orgGate catalog (apps/api/src/modules/skills/routes.ts:23-33) mentre le read di skill-aliases/categories/families/taxonomy-edges/proficiency-levels non hanno requirePermission ne' orgGate (es. skill-aliases/routes.ts:22-28) — stessa classe di dato catalogo, gate diversi
- skills non ha DELETE: soft-deactivation dichiarata post-MVP e mai shippata (skills/routes.ts:3-5) — nel catalogo non esiste alcun modo API di ritirare una skill obsoleta
- skill-taxonomy-edges valida solo il self-loop, nessun check di aciclicita' del grafo IS_A (skill-taxonomy-edges/service.ts:46-51): due edge A->B e B->A o cicli piu' lunghi sono creabili — data-quality gap per traversal gerarchici futuri
- Free-text semantic search e' feature dormiente dietro flag MATCHING_FREETEXT_ENABLED default OFF (semantic-matching/service.ts:152, routes.ts:112-119) — endpoint completo, testato, rate-limited, mai esposto in prod
- seed-approval-decisions POST inserisce solo il record decisione, nessuna transizione dello status del candidate in questo modulo (seed-approval-decisions/repository.ts:80-91): l'apply della decisione vive altrove — verificare che il ciclo approve->promote sia effettivamente chiuso nel modulo seed-acquisition-runs
- Duplicazione interna in semantic-matching/service.ts: userOccupations e userPositions ricalcolano inline canViewOthers (righe 62, 95) invece di usare l'helper isElevated gia' definito a riga 26 — refactor triviale
- Nessuna permission dedicata per la taxonomy: aliases/categories/families/edges riusano skill:create/skill:update ma il vero gate e' l'hardcoded PLATFORM_ADMIN nel service (es. skill-categories/service.ts:22-29) — la permission RBAC skill:update concessa a un tenant admin non basta, comportamento non deducibile dalla matrice role-permission
- skill-aliases updateAliasPartial non tocca updated_at (la tabella espone solo created_at, skill-aliases/repository.ts:171-209): un alias modificato non e' distinguibile da uno mai toccato — gap di audit minore rispetto alle sibling tables che hanno updated_at

### api:c8  {'modules': 9, 'endpoints': 44, 'tables_distinct': 22}
Inventariati i 9 moduli API (succession-pools, successor-candidates, successor-readiness, surveys, teams, tenant-materialization, tenants, training-initiatives, user-career-plans): 44 endpoint totali su 22 tabelle sys.* distinte, tutti registrati in app.ts con prefix /v1/<modulo>. Il cluster career/succession (4 moduli) condivide il set di permission career_succession:* e applica l'org read scope ADR-0027 (orgGate service/aggregate); surveys è tri-entità tenant-only senza orgGate; teams è read-only con il 3° asse scope "my team"; tenant-materialization è un generatore di archetipi PLATFORM_ADMIN service-gated senza permission granulare. Fragment YAML scritto in api_c8.yaml.
- tenant-materialization: POST /v1/tenant-materialization non ha requirePermission (solo CSRF + service-gate ensurePlatformAdmin) e GET /archetypes e' aperto a qualsiasi autenticato — nessuna permission granulare nel seed (apps/api/src/modules/tenant-materialization/routes.ts:19-26, service.ts ensurePlatformAdmin); opportunita': seedare permission dedicate tenant_materialization:*
- DELETE riusa la permission :update invece di una :delete dedicata in succession-pools (routes.ts:48) e user-career-plans (routes.ts:49) — asimmetria col pattern surveys:delete/tenant:delete
- teams e' read-only (2 soli GET, apps/api/src/modules/teams/routes.ts:19-27): i team sono derivati dal seed db/seeds/rtl-rebuild/13_teams_from_org.sql — manca del tutto l'API di lifecycle team (create/update/membership), feature gap per la gestione team runtime
- surveys non ha alcun config.orgGate su nessuna route (apps/api/src/modules/surveys/routes.ts) a differenza del cluster succession (orgGate service/aggregate): i dati engagement/survey-responses potrebbero ricadere in dataclass EVALUATION — verificare coerenza con ADR-0027/I18
- surveys: le responses sono read-only nel modulo admin (routes.ts:73-82, event log); il write path vive nel modulo me (me-surveys.integration.test.ts) — split by design ma da documentare nell'atlante cross-modulo
- successor-readiness e' append-only/immutabile by design (solo list/get/create, routes.ts:3) — nessuna correzione possibile di un assessment errato se non con una nuova riga
- training-initiatives senza DELETE by design (status=CANCELLED, routes.ts:3) e con orgGate catalog sui read — pattern dataclass catalogo distinto da service
- tenant-materialization slice-2b dichiarato residuo (skills/ranked-KPI per gli incumbent generati) in blueprints.ts:9-10 — lavoro aperto tracciabile
- tenants: DELETE = soft-archive (status ARCHIVED, routes.ts:82-96); modulo tocca solo sys.sys_tenancies

### api:c9  {'modules': 8, 'endpoints': 40, 'tables_distinct': 10}
Inventariati 8 moduli API (users + 7 visualization-*): 40 endpoint totali su 10 tabelle sys.* distinte. users e' il modulo piu' ricco (8 endpoint, scope 4-tier ADR-0027 via lib/scope/resolver, orgGate:"service" sulle GET, field-level restriction sul PATCH, soft-delete, gestione role grants). I 7 moduli visualization formano una famiglia coerente tenant-scoped (visibilita' derivata dal graph padre via import cross-modulo del repository di visualization-graphs), con varianti: edges/styles immutabili (no PATCH), node-layouts con PUT upsert, exports append-only senza DELETE. Tutte le mutazioni hanno app.verifyCsrf; nessuna route viz ha orgGate. Oltre ai 7 test file per-modulo esiste visualizations.integration.test.ts che esercita la pipeline completa graph→export.
- Non esiste la permission visualization:delete: tutte le DELETE viz (graphs/nodes/edges/layouts/node-layouts/styles) sono gated su visualization:update_layout — mismatch semantico permesso/azione (apps/api/src/modules/visualization-graphs/routes.ts:50-52; grep db/migrations/*.sql = solo visualization:{create,read,update_layout})
- Versioning dei graph dichiarato ma non esercitabile via API: graph_version + unique (tenant,code,version) esistono, ma create() controlla/pinna sempre version 1 e nessun endpoint crea versioni successive (apps/api/src/modules/visualization-graphs/service.ts:49)
- visualization-exports e' solo un registro metadata append-only: 'the actual rendering happens elsewhere' (service.ts:3), nessun endpoint genera o scarica l'artefatto esportato, nessun DELETE/retention → feature senza motore ne' lifecycle (apps/api/src/modules/visualization-exports/routes.ts:14-30)
- Commento stale: visualization-graphs/routes.ts:3 dichiara '5 endpoints' ma il file ne definisce 7 (GET /summary e GET /:id/render aggiunti dopo)
- Commento stale: users/repository.ts:3-5 dice che il file contiene il MANAGER team lookup via sys.sys_user_position_assignments/sys.sys_positions, ma quel SQL e' migrato in lib/scope/resolver.ts (ADR-0027 F1) — nel repository non c'e' traccia di quelle tabelle
- Asimmetria orgGate: users ha config.orgGate:'service' sulle 3 GET, nessuna route visualization ce l'ha — coerente con la data-class non sensibile ma non documentato nelle route (grep orgGate su visualization-*/routes.ts = 0 hit)
- Coupling cross-modulo sistematico ma pattern-consistente: edges/nodes/layouts/node-layouts/styles/exports importano findGraphById da visualization-graphs/repository.js per la visibility (es. apps/api/src/modules/visualization-edges/service.ts:12)

### web:admin  {'pages': 53, 'endpoints_distinct': 92}
Inventariate tutte le 53 pagine admin sotto apps/web/src/app/(authenticated)/ (esclusa me/): per ognuna route URL, hook, endpoint /v1, componenti @heuresys/ui e note. Il pattern dominante è useQuery/useMutation inline con apiFetch (nessun layer hook per-modulo: solo lib/api/auth.ts e observability.ts espongono hook nominati); i wrapper locali DataTablePanel/EntityTable forniscono la state-machine loading/error/empty a ~27 list page. 92 endpoint /v1 distinti raggiunti dalla zona admin; solo 9/53 pagine hanno mutation (approvals, content x2, mfa-policy, enterprise-typing, insights recompute x3) — il resto è read-only. Gating RBAC client presente su 6 pagine (insights:admin, content:update/publish, mfa_policy:manage, PLATFORM_ADMIN su system-health); il filtro perspective PET è stato ritirato dal layout a favore di sezioni sidebar collassabili. Frammento YAML con sezioni pages/lib/components scritto in scratchpad/atlas_fragments/web_admin.yaml.
- Filtro perspective PET RITIRATO nel layout (commento 'the old PET perspective filter is retired', apps/web/src/app/(authenticated)/layout.tsx:125-127) mentre CLAUDE.md sezione U2/S953 descrive ancora il filtro PET default 'Tutte' — drift documentale da riconciliare
- Value-list enum duplicate come literal locali perche' apps/web puo' importare solo TYPES da @heuresys/shared (commenti espliciti: approvals/page.tsx:23, content/page.tsx:27-29, content/[id]/page.tsx:33) — rischio drift con i CHECK server, stessa classe di feedback_no_hardcoded_test_data
- Admin SPA prevalentemente read-only: solo 9/53 pagine hanno useMutation (grep useMutation count); zero CRUD UI su entita' core users/tenants/positions/skills/kpi-definitions/learning-modules/goals/okrs/organization-units — gap/opportunita' per una fase di admin editing
- Nessuna paginazione UI: le list page hardcodano ?limit=200 (~20 occorrenze, es. skills/page.tsx:44, goals/page.tsx:46) e visualization nodes/edges cap a 500 (visualizations/[graphId]/page.tsx:51,59) — scalabilita' limitata su dataset oltre soglia
- system-health prod ha droppato 4 sezioni del mock per assenza di backend: log tail, slow-query (manca endpoint pg_stat_statements), incident timeline (nessun modulo incident), sparkline KPI (nessun endpoint time-series) — components/SystemHealthLive.tsx:16-22 — quattro API candidate concrete
- SystemHealthDashboard.tsx conserva dati fabbricati (tenant GENESIS_DEMO/ACME_CORP, sparkline hardcoded, righe 246-335) — confinato a /showcase/system-health come artefatto demo documentato, mai importato da route autenticate (verificato via grep importers)
- Console /dev/agent feature-flagged (NEXT_PUBLIC_ENABLE_AGENT_DEV) con live drive blocked-on-Enzo (dev/agent/page.tsx:21-23); parla col gateway non-/v1 via raw fetch+SSE (righe 121,147) fuori da apiFetch/CSRF e fuori dal guardrail i18n
- Gating di route /system-health e' client-side (roles.includes('PLATFORM_ADMIN') + router.replace('/'), system-health/page.tsx:33-38) — i dati restano server-gated ma il pattern di gate route e' solo client, unico caso a ruolo (gli altri gate client sono a permission)
- Selettore org-chart limitato ai primi 10 grafi ORG_CHART (organization/org-chart/page.tsx:40, limit=10 hard) — cap arbitrario sul picker
- SystemHealthLive ha copy inglese fuori dal guardrail i18n di app/** (dichiarato in components/SystemHealthLive.tsx:24-25) — unica pagina prod autenticata non i18n-izzata
- _charts-client.tsx e' un chokepoint obbligato: dynamic ssr:false (CW-B59) + resolver var(--x)->colore per canvas echarts (fix B-05) + subpath @heuresys/ui/charts anti-bundle 1.68MB; i 16 importer verificati rispettano la regola 'mai importare chart da @heuresys/ui direttamente'
- content/[id] e' la pagina piu' complessa dello SPA (5 query + 6 mutation + ContentMediaPanel); upload media usa fetch raw multipart perche' apiFetch e' JSON-only (components/ContentMediaPanel.tsx:6,47) — unico punto non-apiFetch verso /v1

### web:me+public  {'pages': 30, 'endpoints_distinct': 66, 'pages_me': 24, 'pages_public': 6}
Inventariate 30 pagine (24 ESS /me/* + 6 pubbliche: /, /app, /demo, /investors, /login, /privacy) con route, zona, hook lib, endpoint /v1 (risaliti anche nei _components e in lib/api/auth.ts, public-stats.ts, lead-form), componenti @heuresys/ui e note. Tutte le pagine me sono client components useQuery+apiFetch con empty-state reale; il gating RBAC client è assente by design (sidebar server-driven via /v1/me/interfaces con raggruppamento PET). 66 endpoint distinti mappati; la pagina più densa è /me/security (18 endpoint MFA+sessioni), /login ne orchestra 12 nella state machine password→MFA→enrollment-gate.
- STATIC_FACTS hardcoded su /investors (modules 85, endpoints 466, migrations 165, apiTests 1098, e2e 123, pages 98) — drift risk vs SOT_STATE, stesso pattern D-01; evidenza apps/web/src/app/investors/page.tsx:14-17
- Colori chart hardcoded hex (#6366f1/#f59e0b/#94a3b8/#e2e8f0) invece dei token CSS-var brand in me/analytics/page.tsx:21-27 e me/org-chart/page.tsx:44-55 — contrasta la dottrina brand-fidelity token-based
- /me/career/target è write-only: POST /v1/me/career/target-positions non ha alcuna vista lista corrispondente (i career tab leggono solo goals/career-paths/risk); evidenza career/target/page.tsx:42 + career/_components/*.tsx
- learning/catalogue: enroll.isPending globale disabilita tutti i bottoni riga e la conferma successo mostra l'UUID raw del path invece del nome; evidenza me/learning/catalogue/page.tsx:83-86,143-147
- /me/skills e /me/approvals usano tabella HTML raw invece del pattern canonico DataTablePanel (incoerenza residua brand-fidelity); evidenza me/skills/page.tsx:57-88 e me/approvals/page.tsx:55-76; approvals usa anche rowKey da indice (appr-i) non id stabile (riga 67)
- Inbox: push SSE/websocket dichiarato 'post-MVP-3 enhancement' nel commento, ancora polling 30s; evidenza me/inbox/page.tsx:41-46
- Envelope types dei match dichiarati localmente perché @heuresys/shared esporta solo i row types — opportunità: ri-esportare gli envelope; evidenza me/matching/page.tsx:12-15
- Surveys: isRequired è solo asterisco visivo, nessuna validazione client dei campi obbligatori prima del submit; evidenza me/surveys/[surveyId]/page.tsx:88-99,128-131
- /demo usa 10 PNG statici pre-catturati in /public/demo/ — rischio drift visivo rispetto alla UI live; evidenza apps/web/src/app/demo/page.tsx:7-18
- MeProfile interface duplicata localmente in profile/page.tsx (20-30) vs MeProfileFull importato da shared — pattern misto local-vs-shared types ricorrente in ~metà delle pagine me

### web:showcase+lib  {'pages': 18, 'endpoints_distinct': 15, 'lib_files': 8, 'lib_hooks': 10, 'components': 11}
Inventariate le 18 pagine /showcase (tutte demo brand statiche, zero endpoint /v1, gate NEXT_PUBLIC_ENABLE_SHOWCASE + robots noindex, primitive @heuresys/ui importate via _ui-client.tsx con next/dynamic ssr:false per il fix CW-B59), gli 8 file di apps/web/src/lib (apiFetch con silent-refresh single-flight + Web Lock, 7 hook auth/RBAC/preferenze, 2 hook observability, usePlatformStats pubblico, csrfStore a 3 livelli, i18n 10 namespace IT/EN, landingForRoles) e gli 11 componenti in src/components (2 dashboard system-health mock/live, EntityTable/DataTablePanel canonico, ContentMediaPanel, 2 shim QW-E5, LanguageSwitcher, LeadForm, PreferencesApplier, ProfileTabs, SectionTabs). Nessun middleware.ts: l'auth proxy è il rewrite /api/* in next.config.js verso l'API Fastify; la protezione route è client-side + enforcement RBAC lato API. 15 endpoint method+path distinti referenziati da lib+components.
- Nessun middleware.ts in apps/web: protezione route solo client-side (layout + hooks), enforcement interamente delegato all'API — evidenza: Glob apps/web/**/middleware.ts = 0 file; next.config.js:10-13 è solo proxy rewrite
- SystemHealthDashboard.tsx (344 righe, 18 primitive UI + dati 100% hardcoded) resta nel bundle prod ma è consumato solo da /showcase/system-health gated — candidato a esclusione dal build prod o lazy-load — SystemHealthDashboard.tsx:14-16
- Gate showcase permissivo: abilitato con QUALUNQUE NODE_ENV!=production oppure flag NEXT_PUBLIC_ENABLE_SHOWCASE=1 anche in prod (solo robots noindex a protezione) — apps/web/src/app/showcase/layout.tsx:20-25
- showcase/layout.tsx:70 renderizza nel footer il path assoluto Windows 'ux-design-shared/governance/DECISION_REGISTER.md' — viola la regola no-path-assoluti nei file versionati (memoria feedback_no_absolute_paths)
- landing.ts:9-16 hardcoda 6 ADMIN_ROLES e non contempla i ruoli funzionali holderless CEO/TEAM_LEADER/TEAM_MEMBER (mig 000049): un holder puramente funzionale atterra su /me — verificare se intenzionale (pattern anti feedback_no_hardcoded_test_data)
- section-tabs.tsx:28-57 hardcoda i 6 merge-group di route nel componente, duplicando informazione di navigazione che per la sidebar è DB-driven (sys_ui_interfaces) — rename route = doppia manutenzione
- usePlatformStats (lib/api/public-stats.ts:9-16) usa useEffect/useState manuale invece di TanStack Query — unico hook fuori pattern, senza cache/retry/dedup
- apiFetch è JSON-only: ContentMediaPanel.tsx:44-52 duplica a mano header CSRF + credentials per l'upload FormData — opportunità: supporto FormData in apiFetch
- detail-panel.tsx:7 e status-pill.tsx:5 sono shim di puro re-export post-promozione QW-E5 a @heuresys/ui — cleanup possibile (migrare gli import e rimuovere i file)
- LeadForm (lead-form.tsx:22) POSTa /api/v1/leads con raw fetch senza x-csrf-token: coerente solo se l'endpoint pubblico è CSRF-esente — da confermare lato API

### shared:schemas  {'schemas': 87, 'subpath_exports': 87, 'orphans': 1, 'api_only_no_web_consumer': 62, 'export_mismatches': 0}
Inventariati gli 87 file di packages/shared/src/schemas: per ciascuno estratti gli export Zod principali (nomi *Schema, con fallback su const/function per role-codes e _pagination), verificata la parità con i subpath exports di packages/shared/package.json (87/87, zero mismatch in entrambe le direzioni) e mappati i consumer via scan degli import/re-export in apps/api/src e apps/web/src (subpath + barrel, con risoluzione identifier→file). Unico orfano app-level è _pagination, che però è una factory interna importata relativamente da 61 schemi sibling (non codice morto). 62 schemi su 87 non hanno alcun consumer web tipizzato: la SPA admin duplica i tipi con interface locali (es. positions, organization, skills) invece di riusare @heuresys/shared, in contrasto con la doctrine MVP-2a. Nessuna collisione di identifier tra i file.
- Parità subpath perfetta: 87 file ↔ 87 exports './schemas/*' in packages/shared/package.json, zero mismatch nei due sensi (query: confronto set file vs exports)
- Unico orfano app-level: _pagination — ma è factory interna (paginationFields, packages/shared/src/schemas/_pagination.ts:24) con 61 importer intra-shared; il suo subpath export pubblico è probabilmente superfluo
- 62/87 schemi (71%) senza alcun consumer web tipizzato: cluster interi senza wiring UI typed — visualization-* (7), blueprint-* (5), skill-taxonomy (6: skills, skill-aliases, skill-categories, skill-families, skill-proficiency-levels, skill-taxonomy-edges), succession (5), brownfield/seed (7), career/learning (8) — elenco completo nel fragment (consumers_web: [])
- Anti-pattern vs doctrine MVP-2a ('frontend types reused from @heuresys/shared', CLAUDE.md): pagine web definiscono interface locali duplicate — apps/web/src/app/(authenticated)/positions/* definisce 'interface Position'/'PositionsList'/'PositionDetail'; apps/web/src/app/(authenticated)/me/_components/summary-tab.tsx:9 definisce 'interface MeProfile'; skills/ e organization/ non importano nulla da @heuresys/shared (grep vuoto)
- Drift documentale ruoli: ROLE_CODES contiene 12 ruoli (ORG_DIRECTOR aggiunto mig 000145, packages/shared/src/schemas/role-codes.ts:23) ma CLAUDE.md §Security model dice '11 roles'
- users schema consumato dal web solo da app/(authenticated)/approvals/page.tsx — la pagina admin users non riusa i tipi shared (stesso anti-pattern interface locali)
- Zero collisioni di identifier esportati tra gli 87 file (naming module-prefixed disciplinato) — i barrel import sono attribuibili univocamente (script scan, identCollisions: 0)
- Consumer API non-modulo: role-codes ri-esportato da apps/api/src/config/constants.ts:10 (export-from, non import — pattern che uno scan naive perde); notifications usato anche da apps/api/src/lib/notifications/emit.ts

### db:live  {'tables': 276, 'empty_tables': 67, 'views': 12, 'matviews': 0, 'extensions': 6, 'schemas': 6}
Snapshot completo del DB live heuresys_advanced (PG16 via tunnel :5433, 2026-07-05): 6 schemi utente (audit, brownfield, public, staging, sys, temp_sdbi), 276 tabelle utente, 67 confermate vuote con count(*) esatto, 12 viste in sys e zero materialized view (il PIP e' la vista sys_position_intelligence_profiles_v). Il DB e' dominato da audit.import_validation_results (1,55M righe / 547 MB) e dalle tabelle embeddings pgvector (sys_skill_embeddings 372 MB). Contesto chiave: 2 tenant ACTIVE (HEURESYS + RTL_BANK), 162 utenti, 162 posizioni, 26 org unit, 12 ruoli (il 12o e' ORG_DIRECTOR, non presente nella lista di 11 in CLAUDE.md), 698 role_permissions, 156 permissions, 50 ui_interfaces, 167 migrazioni applicate. Estensioni: uuid-ossp, pgcrypto, pg_trgm, pg_stat_statements e vector 0.8.2 (embeddings live su 4 tabelle).
- MFA multi-kind shipped senza dati: sys_auth_mfa_{otp_challenges,recovery_codes,webauthn_credentials,exemptions,exemption_audit}=0; vivi solo 6 mfa_factors TOTP + 2 policies (count(*) live 2026-07-05) — coerente con deferral v1.x, opportunita' di completamento
- Approval engine senza dati: sys_approval_requests=0 e sys_approval_steps=0 (count(*) verificato) — workflow approvativo strutturato ma mai usato
- Reward/variable-pay incompleto: sys_reward_gate_catalog=7 seed ma sys_reward_gates=0, sys_reward_gate_results=0, sys_payout_curves=0, mentre sys_variable_pay_calculations=121 — i calcoli girano senza gate engine
- Lead capture (percorso prospect ADR-0026) senza dati: sys_leads=0 — funnel pubblico shipped, nessun lead registrato
- Visualization a meta': graphs=1/nodes=158/edges=157 popolati ma sys_visualization_{layouts,node_layouts,styles,exports}=0 — persistenza layout ed export mai usate
- Subsistema seed-governance interamente vuoto: sys_seed_{acquisition_runs,approval_decisions,candidate_records,source_evidence,validation_results} tutte =0
- audit.import_validation_results = 1.553.686 righe / 547 MB = ~44% del DB — candidato n.1 per retention/partition/archive
- Crescita token/audit senza pruning apparente: sys_auth_refresh_tokens 44.408 righe/26MB + sys_auth_login_events 91.157/29MB per soli 162 utenti — candidato job di purge
- staging.wave1_* (18 tabelle) tutte a 0 righe ma con size residua (es. wave1_skills 1712kB, wave1_esco_occupation_mappings 2280kB) — consumate e mai troncate: bloat, candidate TRUNCATE/DROP
- Schema temp_sdbi residuo con 4 tabelle pf_* ancora popolate (465/474/390/161 righe) — working set temporaneo da verificare e droppare
- 12 ruoli nel DB vs 11 dichiarati in CLAUDE.md/RBAC cache: il 12o e' ORG_DIRECTOR (SELECT auth_role_code FROM sys.sys_auth_roles) — allineare doc/SOT_STATE
- sys_notification_preferences=0 con sys_inbox_notifications=55 — preferenze notifiche shipped senza dati (UI/flow mai usato?)
- pgvector 0.8.2 live con 4 tabelle embeddings popolate (sys_skill_embeddings 17.631, sys_esco_occupation_embeddings 3.045, sys_job_role_embeddings 136, sys_user_profile_embeddings 156) — capacita' semantic-matching gia' materializzata, leva prodotto forte
- Feature-tabelle sys vuote sparse: sys_organization_hierarchies=0, sys_organization_unit_history=0, sys_user_professional_experiences=0, sys_user_target_positions=0, sys_successor_readiness=0, sys_content_categories=0, sys_content_media=0, sys_payroll_handoff_records=0, sys_position_skill_requirement_history=0, sys_process_kpi_templates=0, sys_blueprint_activations=0, sys_blueprint_overrides=0, sys_auth_sessions=0 — mappa d'oro delle feature schema-ready senza dati

### ops:scripts+ci  {'scripts': 31, 'workflows': 9, 'db_scripts': 26}
Inventario operativo completato: 31 script in scripts/ (30 top-level + test/run-shell-tests.sh, più subdir codemods/ e cowork-exchange/), 9 workflow GitHub Actions (8 su runner self-hosted oci-vm che risiede sull'host PROD con guard D-08 anti fork-PR, solo showcase.yml su ubuntu-latest), 26 file in db/scripts/ (24 + _lib). Il ciclo di vita operativo e' ben strutturato: session-start (session-boot.ps1 + tunnel automation ADR-0021), session-close (close-propagate.sh = align-clones + align-claude-ecosystem + clone-db condizionale), deploy PROD (vm-deploy.sh detached via vm-deploy-remote.sh, fix D-49). Sulla VM girano 2 servizi long-running (api :8013, web :3013) + 8 timer one-shot (backup daily 01:30, auth-housekeeping 02:00, insights 02:15, digest 02:45, approvals-sla hourly :20, scraping/dr-drill/reindex domenicali), piu' il runner GitHub Actions e le 3 unit legacy evo. deploy/README.md copre 10 sezioni da secrets a cross-platform notes.
- Script one-shot debug ormai chiusi ancora in scripts/ top-level: bisect-cw-b59-createctx.ps1 (S937, CW-B59 risolto) e restore-showcase-routes.ps1 (S935) — candidati archiviazione (scripts/bisect-cw-b59-createctx.ps1:6, scripts/restore-showcase-routes.ps1:5)
- scripts/cowork-exchange/ contiene 12 helper del protocollo Cowork<->CLI congelato da S939 (memoria project_cli_sot_takeover) — probabile dead code (ls scripts/cowork-exchange: inbox.mjs, locks.mjs, session-*.mjs, hooks/...)
- deploy/reports/claude-align accumula ~81 drift-report timestampati versionati senza retention/rotazione (35 linuxpc + 29 vm + 17 mac) — opportunita' di pruning o gitignore (ls deploy/reports/claude-align)
- Gap DR dichiarato: backup-db.sh e' local-to-VM only, off-host OPT-IN via BACKUP_OFFHOST_SSH non configurato di default; il proprio header lo marca come 'infra decision left to a later slice' (scripts/backup-db.sh:116-118); archive-dumps.sh copre solo gli snapshot pre-op del PC Windows
- Count hardcoded stale in commento CI: test-integration.yml dichiara '41 integration tests' mentre la dottrina D-01 vieta count hardcoded (vivono in SOT_STATE) (.github/workflows/test-integration.yml:3)
- encrypt-totp-secrets.ts e' ONE-TIME deploy-gated (QW-SEC6): se gia' eseguito in PROD e' esaurito — candidato archiviazione con nota storica (db/scripts/encrypt-totp-secrets.ts:11)
- scripts/codemods/s983-mfa-loginraw.mjs: codemod one-shot S983 esaurito, candidato archiviazione
- 8 workflow su 9 girano sul runner self-hosted che coincide con l'host PROD — rischio concentrazione mitigato solo dal guard if D-08 + label defer-major, ma un push malevolo su main eseguirebbe comunque sul PROD (.github/workflows/build-web.yml:31-37)
- Drift report Mac fermi al 20260626 coerenti col ritiro S1007; mac resta target esplicito on-demand in align-clones.sh:42-44 — coerenza confermata, nessuna azione
- Il timer approvals-sla e' l'unico HOURLY (*:20) tra 8 timer altrimenti daily/weekly — carico DB ricorrente da tenere presente in capacity planning (deploy/systemd/heuresys-advanced-approvals-sla.timer)
- vm-deploy.sh ha il quirk self-modify-buffer documentato (memoria reference_vm_deploy_self_modify_buffer): unit systemd NUOVE nello stesso commit non installate al 1o deploy — automation candidate R15 ancora aperta

### legacy:primary  {'legacy_tables': 588, 'mapped_tables': 94, 'uncovered_big': 65, 'table_mapping_rows': 97, 'column_mappings': 1225, 'legacy_big_gt1000': 78}
Mappato il legacy primario heuresys_platform (VM, 1113 MB, 588 tabelle ordinarie: 582 public + 6 learning; n_live_tup stale, stime da reltuples). Il registry brownfield nell'advanced copre 94 tabelle sorgente distinte (97 mapping, tutti wave-1 APPROVED) con 1225 column_mappings — tutti wave 1, nessuna riga wave-2/3. Delle 78 tabelle legacy con >1000 righe, 65 NON sono coperte da table_mappings: cluster principali knowledge-graph (kg_edges 139k), mirror SAP-infotype pa*/pb* (~30 tabelle), engagement/PULSAR, goals/GOKMER, employee-skill layer, analytics/timeline. Il layer persone/org (employees 270 → sys_users) è stato ingerito via seed RTL-rebuild S950 fuori registry. Data dictionary locale completo in D:/evo.heuresys.com/db-export (catalog.json 3.9MB, 16 domini lexicon).
- Knowledge graph legacy interamente non importato: kg_edges 139451 + kg_nodes 17260 righe senza table_mapping (query: comm legacy>1000 vs brownfield.table_mappings) — asset semantico piu' grande del legacy, candidato oro Wave-2/ESKAP
- Mirror SAP-infotype (~30 tabelle pa*/pb*/pcl2/ext_pa*, da 1010 a 18272 righe, es. ext_pa0024 18272, pcl2 12562, pa0024 9640) tutte non mappate — payroll/HR-core master data assente dall'advanced
- Layer skill a livello-dipendente non coperto: employee_skill_assessments 3140, employee_skills 1445, employee_skill_mappings 1121, career_skills 1106, position_skill_requirements 1632 — wave-1 ha importato solo la tassonomia, non il possesso skill delle persone (gap diretto per /hr skill-gap e people-analytics)
- Dominio engagement/PULSAR non importato: survey_responses 4482, check_ins 2495, engagement_survey_responses 1327, pulse_checks 1145, wellbeing_checkins 1142 — feature engagement dell'advanced senza dati storici
- Performance/GOKMER non importato: goals 1068 + goal_updates 1811
- analytics_events 5000 + employee_timeline 4641 non mappati — storico eventi/timeline dipendente perso
- employee_attendance legacy 5237 vs 3180 seedate S950: ingestione persone/org avvenuta FUORI dal registry brownfield (nessuna riga table_mappings per employees/users/attendance) — il registry sottostima la copertura reale e non e' SoT completa dell'ingestione
- brownfield.table_mappings ha SOLO wave 1 (97 righe APPROVED); wave-2/3 = 0 righe: il residuo deferred v1.x non e' nemmeno pianificato a livello registry
- Stats runtime del DB legacy azzerate (solo 5 tabelle con n_live_tup>0): qualunque tooling che legge pg_stat_user_tables sul legacy da' numeri falsi — usare reltuples o count(*)
- 3 tabelle legacy mappate a 2 target ciascuna (competencies, course_modules, courses) — fan-out da tenere presente nell'atlante
- mv_occupation_similarity 69182 righe (matview legacy) non considerata dal registry (relkind m)
- Data dictionary db-export generato 2026-05-15: 576 tabelle catalogate, 335 tenant-scoped, 950 FK, 376 RLS policies, 16 domini lexicon con conteggi (DGOV 188, H2R 80, SKILGRO 45) — base pronta per pianificare Wave-2 per dominio

### legacy:cantiere  {'commits_since_apr': 483, 'frontend_pages': 231, 'api_endpoints': 1481, 'route_files': 135, 'service_files': 54, 'jest_tests': 3099}
Il cantiere legacy /home/ubuntu/heuresys.com.evo è una produzione matura (231 pagine, 1481 endpoint, ~635 modelli DB, jest 3099 verdi) in pensionamento progressivo secondo il modello PET-driven phased-portfolio (cutover-event abbandonato 2026-05-02). Dei 483 commit da aprile, i temi dominanti sono il programma RTG bridge (Phase 0-5, poi Phase 6+ superseded) e il programma forense 100x di giugno (security D5-xxx scope/authz, RBP scope redesign D5-001, pagination SoT, lint-zero 2171→0, query-builder ADR-012, a11y WCAG-AA 0 axe). Stack: npm workspaces, Express 5 + Next.js 16 + Redis + Sentry/Prometheus, deploy api Docker :8012 + frontend pm2 :3012; il suo DB container heuresys_evo_platform_db:5433 è la data-source ADR-0023 di advanced. Al 2026-06-29 restano 0 CRITICAL ma 4 finding architetturali LARGE gated (D6-003/004/005/009) + un workstream a11y pagine secondarie. Il confronto concettuale evidenzia 12 macro-aree presenti qui e assenti in advanced: recruiting/ATS, time&attendance, payroll ops, AI-native layer (RAG/embeddings/ai-chat/predictive), marketplace plugin, wellbeing/whistleblowing, workforce planning/org-intelligence, integrazioni O*NET/NACE/SAP, SSO OAuth, lifecycle ops (contracts/documents/locations), RBP UI graph data-driven, stack observability.
- Vertical recruiting/ATS completo nel legacy (candidates/interviews/offers/requisitions/job-postings in services/api-gateway/src/routes/) — totalmente assente dagli 83 moduli advanced: candidato naturale come linea di sviluppo
- Time & attendance (attendance.ts, leave.ts, time-off.ts, overtime.ts, time-analytics.ts) assente in advanced benché il DB advanced abbia già 3180 righe attendance seedate dal rebuild RTL: dati senza feature
- Payroll ops (pay-stubs.ts, payroll.ts, salary-bands.ts, merit-cycles.ts, benefits.ts) assente in advanced che ha solo il modulo compensation generico
- AI-native layer maturo nel legacy (rag-chunking.ts, embedding-pipeline.ts, ai-orchestrator.ts, career-coach.ts, predictive-analytics.ts in src/services/) vs advanced che ha solo semantic-matching + predictions: gap strategico per il posizionamento AI-native dichiarato nel package.json legacy ('AI-native HRMS Orchestration Suite')
- Whistleblowing (whistleblowing.ts) è obbligo normativo EU/IT (D.Lgs 24/2023) presente nel legacy e assente in advanced: gap di compliance per un HRMS italiano
- SSO enterprise Azure+Google OAuth (routes/sso.ts, fix open-redirect commit be6df068) assente in advanced (solo login locale + TOTP): blocker tipico per vendite enterprise
- Marketplace/plugin ecosystem (9 route marketplace-* + plugin-runtime.ts + webhook-dispatcher.ts + public-api.ts) assente in advanced: linea di estensibilità piattaforma
- Shared query-builder FilterBuilder/paginate ADR-012 (commit a892b172, adottato su ~74 route) è un pattern portabile ai repository advanced che oggi scrivono SQL paginato a mano
- Programma a11y sistematico (0 axe violations WCAG-AA light+dark su pagine chiave, commit b7a0c877/09c0c112) senza equivalente in advanced: metodo replicabile
- Doctrine divergence: il legacy usa RLS Postgres (commit 23701b00 mig 228 career tables) mentre advanced la vieta (I5) — da tenere presente in ogni ingestione/port di schema
- Finding architetturali LARGE ancora aperti nel cantiere (D6-003/004/005/009, .handoff/STATE.md 2026-06-29): il legacy non è 'finito', il debito architetturale resta gated
- Workstream a11y residuo con causa nel DATO non nel codice: status-badge colors data-driven dal DB status registry con contrasto 1.04 invisibile (.handoff/STATE.md §RIPARTENZA) — anti-pattern da evitare nel design del registry advanced
- HANDOFF.md descrive un TERZO lineage greenfield heuresys-evo (NestJS+Prisma, 566 tabelle, 103 UI components) distinto sia dal legacy sia da advanced: la vision P/E/T Capability Governance (33 aree, 47 rbp_area_perspectives) definita lì è riusabile come cornice prodotto per advanced
- Frontend legacy ha widget engine data-driven (generate-widget-map.mjs) + dashboard RBP per ruolo (rbp_pages/rbp_dashboard_nav_items): advanced ha solo sys_ui_interfaces U1/U2 — possibile evoluzione della shell admin/ESS

### wiki:space  {'pages': 68, 'graph_nodes': 8561, 'graph_edges': 10088, 'vaults': 5, 'adr_pages_wiki': 19, 'adr_files_repo': 25, 'commits_behind': 748}
La wiki esterna (C:/Users/enzospenuso/wiki-space/heuresys-advanced-wiki, llm-wiki v1.3 linked-mode, 68 file md = 63 sources + overview + 2 concepts + 2 entities) è ferma al 2026-05-27 con un solo refresh parziale di 6 pagine il 2026-05-31: l'intero snapshot PRECEDE la v1.0.0 GA (2026-06-02). Il grafo (graphify-out: 8561 nodi, 10088 edge, 802 community) è costruito dal commit 548399ea del 2026-05-27, cioè 748 commit dietro HEAD. Stale hard: le 12 pagine mirror delle SoT (sot-state/backlog/debt/handoff — riscritte ogni sessione da allora, e 2 source path non esistono più a repo root), gli ADR (19 pagine max 0020 vs 25 nel repo max 0027), gli invarianti (I1-I13 vs I1-I20 attuali), tutta la roadmap pre-GA. Riusabili as-is: le pagine ADR 0001-0020 (decisioni immutabili), i record storici brownfield/audit/brand, e la meccanica linked-mode (manifest sha256 per-source rende il refresh incrementale deterministico). Nessun registry centrale vault: .wiki-vaults.yaml non esiste, solo 5 directory vault ciascuna col proprio manifest.
- Wiki interamente pre-GA: last rebuild 2026-05-27 18:04 (index.md:4), solo 6 pagine toccate 2026-05-31 — v1.0.0 GA e' del 2026-06-02, quindi il 100% delle pagine di stato/conteggi e' stale
- Grafo 748 commit dietro HEAD (built_from_commit 548399ea 2026-05-27 in GRAPH_REPORT.md vs HEAD e7aeae45 2026-07-05, verified git rev-list --count) — il refresh e' dichiarato no-API-cost (`graphify update .`), rigenerazione economica
- Gap ADR: wiki copre 19 ADR fino a 0020 vs 25 file in docs/architecture/adr/ fino a 0027 — mancano proprio le dottrine cardine post-GA (0023 data-source, 0024 employee-centric, 0026 single-prod-env, 0027 bi-axial auth)
- Pagina invarianti-architetturali copre solo I1-I13/RD-08/RD-09 (index.md:81) vs I1-I20 nel CLAUDE.md attuale — mancano I14-I20 (employee-centric, bi-axial, ESS floor, peer isolation)
- linked_sources.yaml contiene path morti: D:/heuresys-advanced/HANDOFF.md e BRAND_SESSION_CHARTER.md non esistono piu' a repo root (verified ls 2026-07-05, archiviati in docs/archive) — un refresh cieco fallirebbe su quei source
- linked_sources.yaml usa path assoluti D:\heuresys-advanced\... — viola la dottrina no-absolute-paths per uso cross-machine; opportunita': parametrizzare la source root nel rebuild
- Nessun registry vault centrale: .wiki-vaults.yaml assente in wiki-space (verified ls -A: solo 5 dir) — discovery per-directory; opportunita': creare il registry citato dal task
- Riusabile as-is senza rigenerazione: pagine ADR 0001-0018+0020 (decisioni immutabili), record storici brownfield/wave/audit/brand (chiusi e datati), scaffolding concepts/entities e meccanica manifest sha256 (refresh incrementale deterministico gia' supportato)
- La wiki non ha alcuna pagina sui domini nati dopo il 27/05: teams/my-team scope, ESS shipped, epic RBAC/UIX/PET, docs/product/ (SoT prodotto S997), migrazioni 000051-000169 — per l'atlante conviene trattare la wiki come fonte storica, non corrente
- Vault gemello legacy heuresys-wiki (157 pagine, rebuild 2026-04-27) copre l'evo: utile come fonte brownfield ma da non confondere col vault advanced nell'atlante

### ui:design-shared  {'components': 201, 'value_exports_total': 245, 'stories': 122, 'unused_distinctive_components': 60, 'tiers': 22}
@heuresys/ui e' perfettamente allineato su tutta la catena: working copy D:/ux-design-shared/ui (0.1.9, HEAD c31e4c7 del 2026-06-27, tree pulito salvo un pnpm-lock untracked) == npm latest 0.1.9 == range consumato ^0.1.9 in heuresys-advanced (root+web+showcase) == lockfile 0.1.9. La lib esporta 201 componenti PascalCase (245 value exports totali) organizzati in ~22 tier/categorie, con 122 file Storybook stories. Il consumo reale nelle due app e' concentrato su ~20 componenti (StatusPill, EChartsCard, StatsCard, DashboardShell, KPIStrip, AuditFeed, DataTableWithCrossHair...); oltre 60 componenti distintivi — inclusi 3 costruiti esplicitamente per heuresys (ESCOTreeNavigator, KGGraphCanvas, SAPSyncPanel), gli atomics HR tier17 (SkillHeatmap, SuccessionCard, CareerArc) e l'intero tier forms avanzati — non sono mai importati in apps/web ne' apps/showcase.
- ESCOTreeNavigator/KGGraphCanvas/SAPSyncPanel (Sprint 3.G, costruiti PER heuresys) mai importati in apps/web ne' showcase — evidenza: grep -w 0 occorrenze in apps/web/src e apps/showcase/src; export in D:/ux-design-shared/ui/src/index.ts:329-346
- Atomics HR tier17 inutilizzati (SkillHeatmap, SuccessionCard, CareerArc, KgMiniGraph) nonostante l'API abbia moduli skills/succession — grep -w 0 in apps/web/src; export index.ts:285-318 — opportunita' dashboard people-analytics
- Intero tier6 forms avanzati inutilizzato (IbanInput, TaxIdInput, OtpInput, MoneyInput, FormWizard, SignaturePadField, FileDropzone, PasswordStrengthMeter, PhoneInputField) — grep -w 0; rilevante per pagine compensation/payroll/onboarding (index.ts:177-194)
- Interi tier senza alcun consumo: collab (Kanban/Timeline/CommentThread/CalendarGrid), media, AI (Chatbot/VoiceInput), marketing hero, XR, a11y AccessibilityPanel, i18n LanguagePicker — ~60+ componenti pubblicati mai usati dalle 2 app consumer (grep batch, vedi frammento)
- CLAUDE.md di heuresys-advanced sezione Design System stale: dichiara @heuresys/ui ^0.1.1 e HEAD ad7d5c0 (S932); reale ^0.1.9 (heuresys-advanced/package.json:53), 8 patch release dopo
- Stories coverage parziale: 122 stories vs 201 componenti; i componenti promossi di recente (status-pill, field-grid, esco-tree-navigator, kg-graph-canvas, sap-sync-panel, StatusIcon, dashboard/*) non hanno .stories — evidenza: ls D:/ux-design-shared/ui/src/components
- Naming collision negli export: RbacMatrix (index.ts:293, tier17) vs RBACMatrix (index.ts:439, dashboard pattern) — due componenti diversi con nome quasi identico
- ui/pnpm-lock.yaml untracked nel repo lib mentre il root usa npm package-lock.json (git status D:/ux-design-shared) — package manager non riconciliato
- Tag git v0.4.0-brand-v1 versiona il brand bundle repo-level, disgiunto dal semver npm della lib (0.1.9) — due assi di versione nello stesso repo, potenziale confusione release
