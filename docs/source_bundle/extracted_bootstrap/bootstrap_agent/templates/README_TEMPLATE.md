# Company HRMS/BPM Platform

Clean repository initialized from the Company HRMS/BPM Idempotent Blueprint Bundle.

## First commands

```bash
cp .env.example .env
docker compose up -d
```

Then run DB migrations and validation scripts when generated.
