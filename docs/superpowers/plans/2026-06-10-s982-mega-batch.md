# S982 Mega-batch Implementation Plan — 8 workstream + full close

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> All'avvio esecuzione: copiare questo piano in `docs/superpowers/plans/2026-06-10-s982-mega-batch.md` (convenzione repo).

**Goal:** chiudere in un'unica sessione tutto l'eseguibile del backlog (import Wave-2/B-50, dominio MFA completo §2.5, ESS-media, color-contrast, engines), attivare la mandatory-MFA su RTL slice, e consegnare un punto di ripartenza pulito (push + CI + align + deploy + handoff).

**Architecture:** ogni workstream = atomic commit con gate verde (pattern S981). Sequenza unblocking-first: import dati (chiude il registry al 100%), poi dominio MFA (SMS → TOFU → UI → attivazione), poi CMS/a11y/infra, poi close completo.

**Tech stack:** pattern esistenti — seed baked-VALUES + registry migration; module pattern Fastify/Zod; mailer-factory replicato per SMS; CSS-var tokens app-level.

---

## Contesto

Sessione S982. Stato di partenza: `main @ 9083165`, tree clean, synced, suite 877/0, 105 migration, tunnel :5433 up, DEBT_REGISTER tutto risolto. Enzo ha delegato il batch completo (decision-authority session-scoped, memoria `feedback_batch_delegation_mode`): obiettivo = massimo numero di azioni chiuse + punto solido di ripartenza. **Push/align/deploy/handoff autorizzati dall'approvazione di questo piano.**

### Decisioni PM bloccate (AskUserQuestion, 2026-06-10)

| # | Decisione | Scelta Enzo |
|---|---|---|
| D1 | sys_branches | **B — 6 branch, regola àncora-OU** (10 location orfane RTL = residuo terminale) |
| D2 | succession | **incumbent-anchor + CEO/CFO title-match** (CEO→POS-00000321 exact, CFO→Finance Director POS-00000384 confermato) |
| D3 | talent_pools | **WON'T-DO esplicito** (in rationale registry; career cluster + 86 orfani out-of-scope formale) |
| D4 | attivazione MFA | **RTL slice** `roleCodes=[BLUEPRINT_MANAGER, HRMS_MANAGER, PROCESS_OWNER, READ_ONLY]` (zero impatto E2E/personas) |
| D5 | color-contrast | **delegato a Claude** con checkpoint screenshot before/after prima del commit |
| D6 | Node floor | **engines ≥22 + @types/node ^22**, chiudi PR #30 not-adopted |

### Fatti misurati chiave (verified-by explorer/psql 2026-06-10)

- 0 fattori MFA verificati nel DB; 0 policy in `sys_auth_mfa_policies`. Le 14 righe TOTP di tommaso sono residui non-verificati di test (innocui).
- CHECK `sys_auth_mfa_factors_kind_check` ammette già `SMS_OTP` (mig 000005) → **nessuna migration CHECK per SMS**. `sys_auth_mfa_otp_challenges` è kind-agnostica (canale derivato dal factor) → riusabile as-is.
- `allowedKinds` enrollment è hardcoded a `apps/api/src/modules/auth/service.ts:401`; allowlist `enr` è prefix-based (`/v1/auth/mfa/`) → route SMS raggiungibili senza modifiche middleware.
- Token contrast: `--muted-foreground` definito **app-level** in `apps/web/src/app/globals.css:78` (light `#475569`) e `:115` (dark `#9CA3AF`) → fix senza publish upstream (salvo badge variant lib-side, verificare in esecuzione).
- Endpoint `/v1/succession-pools` + `/v1/successor-candidates` **esistono già** (app.ts:288-290) → l'import li alimenta, nessun modulo nuovo.
- Tenant: RTL_BANK `86ba7a65-217f-48ba-8ce5-5c09b40a66b0` (legacy `0c54b84a-…`), HEURESYS `8bc5bc59-…` (legacy `d5855519-…`).
- Vista reconciliation: `has_rows` = EXISTS live, nessun refresh. CHECK `declared_status` NON ha valore WON'T-DO → si esprime nel testo rationale, `declared_status='IMPORT'` per i 3 target.

### Allocazione numeri (per evitare collisioni tra task)

| Artefatto | Numero |
|---|---|
| Seed import succession | `db/seeds/reconciliation/49_succession_pools_candidates.sql` |
| Seed import branches | `db/seeds/reconciliation/50_branches.sql` |
| Migration registry close | `db/migrations/000106_wave2_succession_branches_close.sql` |
| Migration nav mfa-policy UI | `db/migrations/000107_mfa_policy_ui_interface.sql` |
| Migration TOFU (purpose CONFIRM_ENROLL + evento MFA_FACTOR_ENROLLED) | `db/migrations/000108_mfa_enroll_confirm.sql` |

### Ordine di esecuzione (unblocking-first)

```
A import Wave-2/B-50  →  D SMS_OTP  →  E TOFU v2  →  B UI mfa-policy  →  F ESS-media
→  G color-contrast (checkpoint Enzo)  →  H engines/@types/node
→  I close: full gates → C attivazione PUT → push → CI → align+deploy → handoff
```
Rationale: A chiude il registry (ultimo NEEDS_DECISION → 0) ed è il moltiplicatore; D prima di E così il flusso TOFU copre anche il kind SMS; C (attivazione = mutazione PROD) va DOPO la full suite per evitare che il cleanup dei test la cancelli (fix incluso in B).

---

## Task A — Import Wave-2/B-50 (branches + succession pools/candidates + registry close)

**Files:**
- Create: `db/seeds/reconciliation/_gen_wave2.sql` (generatore VALUES, pattern `_gen_m2b.sql`)
- Create: `db/seeds/reconciliation/49_succession_pools_candidates.sql`
- Create: `db/seeds/reconciliation/50_branches.sql`
- Create: `db/migrations/000106_wave2_succession_branches_close.sql`
- Modify: `apps/api/test/reconciliation-registry.integration.test.ts` (righe ~87-92, ~118-126, ~144-152)
- Modify: `apps/api/test/reconciliation-s970-kpi-bridge.integration.test.ts` (righe ~43-44)
- Modify: `apps/api/test/reconciliation-f3-imports.integration.test.ts` (righe ~40-42)
- Create: `apps/api/test/reconciliation-wave2-succession-branches.integration.test.ts`

Schema target verificati (000009 / 000018): `sys_branches` (OU FK **UNIQUE**, code UNIQUE/tenant, country `char(2)`, **no colonna name** → metadata), `sys_succession_pools` (position NOT NULL, code UNIQUE/tenant, status CHECK ACTIVE/ARCHIVED/PROPOSED), `sys_successor_candidates` (pool+user NOT NULL, UNIQUE(pool,user), readiness CHECK nullable, status CHECK).

- [ ] **A1. Backup**: `pg_dump` → `pg_dump_snapshots/pre-wave2-49-50_<shortsha>_<ts>.dump` (pattern esistente; gitignored).
- [ ] **A2. Estrazione legacy + pre-flight advanced**: via `MSYS_NO_PATHCONV=1 ssh oracle-vm-default "sudo -u postgres psql -d heuresys_platform -tA …"`. Prima `\d critical_roles` e `\d succession_candidates` (nomi colonna non misurati nel dossier). Poi:
  - E1: 6 locations (`code IN ('MI-HQ','MI-OPS','BG-CEN','BS-CEN','MI-CEN','HS-HQ')`) con address/city/province/postal/phone/email/lat/lon/capacity.
  - E2: `succession_plans` RTL+HS (id, position_name, incumbent_employee_id, criticality, status).
  - E3: `critical_roles` RTL (8, current_incumbent_id 8/8).
  - E4: candidati linked — **false friend**: `JOIN succession_plans sp ON sp.id = sc.critical_role_id` (misurato 120/120).
  - Pre-flight advanced (:5433): anchor incumbent→position via `user_external_code='LEGACY_EMP::<id>'` + assignment PRIMARY/ACTIVE (attesi 15/15 distinte: 6 SP RTL + 8 CR RTL + 1 SP HS); risoluzione employee candidati.
  - **Congelare i conteggi esatti** (attesi: branches **6** = 5 RTL+1 HS; pools **17** = 6 SP-incumbent + 2 title-match + 8 CR + 1 HS; candidates **24-26** = 8 piani-con-pool × 3 RTL + 0-2 HS — il numero esatto HS si misura qui) e scriverli negli assert.
- [ ] **A3. Seed 49** (pools §1-§3 + candidates §4 + `DO $post$` assert, stessa txn `BEGIN/COMMIT`). Pattern baked-VALUES + resolution a run-time, PK `gen_random_uuid()`, idempotenza via natural key. Struttura §1 (incumbent-anchor):

```sql
WITH src(lid, vten, name, incumbent_lid, criticality) AS (VALUES …)
INSERT INTO sys.sys_succession_pools
  (succession_pool_tenant_id, succession_pool_position_id, succession_pool_code,
   succession_pool_name, succession_pool_status, succession_pool_metadata)
SELECT src.vten::uuid, a.user_position_assignment_position_id,
  'LEGACY_SPLAN::'||src.lid, src.name, 'ACTIVE',
  jsonb_build_object('legacy_plan_id', src.lid, 'anchor', 'incumbent',
    'legacy_incumbent_employee_id', src.incumbent_lid, 'legacy_criticality', src.criticality,
    'source_table', 'succession_plans')
FROM src
JOIN sys.sys_users u ON u.user_external_code = 'LEGACY_EMP::'||src.incumbent_lid
JOIN sys.sys_user_position_assignments a
  ON a.user_position_assignment_user_id = u.user_id
 AND a.user_position_assignment_kind = 'PRIMARY' AND a.user_position_assignment_status = 'ACTIVE'
ON CONFLICT (succession_pool_tenant_id, succession_pool_code) DO NOTHING;
```
  §2 title-match: JOIN `sys_positions ON position_code IN ('POS-00000321','POS-00000384')`, metadata `anchor:'title_match'` + `pm_signoff:'Enzo 2026-06-10'`. §3 critical_roles: code `'LEGACY_CROLE::'||lid` (restano a 0 candidati — atteso, annotato in header). §4 candidates: JOIN pool via code + user via `LEGACY_EMP::`, readiness CASE (`ready_now→READY_NOW, ready_1_year→READY_1_YEAR, ready_2_years→READY_2_YEARS, ELSE NOT_READY`), metadata `legacy_readiness` raw + strengths/development_needs/rank_order, `ON CONFLICT (pool_id,user_id) DO NOTHING`. Lo user HS DEACTIVATED entra (status CANDIDATE).
- [ ] **A4. Seed 50** (6 branch): OU risolte a run-time via `organization_unit_metadata->>'legacy_org_unit_id'`; àncore: MI-HQ→`RTL` root, MI-OPS→`DIV-OPS`, FIL-* 1:1, HS-HQ→`HS-CORP`. `branch_country_code='IT'` (da ITA), `branch_region_code=province`, name+phone+email+lat/lon+capacity+`anchor_rule` in `branch_metadata`. `ON CONFLICT (branch_tenant_id, branch_code) DO NOTHING`. Assert: count=6, 6 OU distinte, split 5/1.
- [ ] **A5. Apply seed 49 poi 50** su :5433 con `-v ON_ERROR_STOP=1`; **2° run** = 0 mutazioni (idempotenza provata).
- [ ] **A6. Migration 000106** (pattern 000076: no BEGIN/COMMIT, marker-guard, DO $$ assert): UPDATE 3 righe registry → `declared_status='IMPORT'` + rationale prepend `[WAVE2 CLOSE S982]` (branches: regola B + 10 orfane terminali; pools: anchor+title-match + **talent_pools/members WON'T-DO** nel testo + legacy_source aggiornato; candidates: correzione false-friend S972 + 86 orfani + 12 su piani senza pool + career cluster ~2.300 righe = out-of-scope dichiarato). UPDATE rationale `sys_successor_readiness` (ramo cascade decaduto, resta NO_SOURCE — prepend, NON riscrivere: il test S972 fa LIKE sul marker esistente). Assert hard: 3 righe IMPORT+marker, `NEEDS_DECISION view-wide = 0`; assert condizionali sui conteggi (WARNING su rebuild fresco dove i seed non sono ancora girati). `pnpm db:migrate` ×2 idempotente.
- [ ] **A7. Test**: aggiorna i 3 file censiti (NEEDS_DECISION 3→0; "remain EMPTY"/"=0" → conteggi reali) + nuovo `reconciliation-wave2-succession-branches.integration.test.ts`: conteggi esatti, FK/tenant-isolation 100%, readiness nel CHECK, UNIQUE, metadata legacy_* presenti, registry POPULATED ×3, **smoke API** `GET /v1/succession-pools` e `/v1/successor-candidates` 200 con login federica (lezione zod 000096 — qui id v4 nativi, il check chiude il rischio).
- [ ] **A8. Gate + commit**: `pnpm db:validate` 7/7; suite reconciliation + succession verdi; full typecheck.
  `feat(db): Wave-2 B-50 close — branches(6) + succession pools(17)/candidates(N) import + registry 0-NEEDS_DECISION (seeds 49-50, mig 000106)`

**Verifica finale A**: `SELECT resolved_status, count(*) FROM sys.v_reconciliation_status GROUP BY 1` → 0 NEEDS_DECISION, POPULATED 141 (138+3).

---

## Task D — SMS_OTP code-only (ultimo kind §2.5)

**Files:**
- Create: `apps/api/src/modules/auth/sms-sender.ts` (ISmsSender + ConsoleSms + InMemorySms + `makeSmsSender`)
- Modify: `apps/api/src/config/env.ts` (blocco `SMS_PROVIDER?`, `SMS_FROM?` — optional, pattern SMTP :113-121)
- Modify: `packages/shared/src/schemas/mfa.ts` (schemi Enroll/Verify/Resend SMS, specchio EMAIL_OTP :125-133, `phoneHint` mascherato; body enroll `{phoneNumber}` con regex E.164 `^\+[1-9]\d{6,14}$`)
- Modify: `apps/api/src/modules/auth/mfa-service.ts` (issueSmsOtp/enrollSmsOtp/verifySmsOtpSetup/resendSmsOtpEnroll/resendSmsOtpLogin; pre-invio LOGIN in `beginLoginChallenge` :467-486 gemello del blocco email; fallback chain `verifyLoginChallenge` :518-533; `MfaServiceOptions.smsSender?`)
- Modify: `apps/api/src/modules/auth/mfa-routes.ts` (4 route `/sms-otp/{enroll,verify-setup,resend,resend-login}` — stessi rate-limit delle email-otp)
- Modify: `apps/api/src/modules/auth/service.ts:401` (`allowedKinds` include `SMS_OTP` solo se `smsSender.isProductionCapable` — Console=false, InMemory/Twilio=true: il gating PROD è qui)
- Modify: `apps/api/src/app.ts:258-261` (wiring `makeSmsSender` + `BuildAppOptions.smsSender`)
- Modify: `apps/api/test/helpers/build-test-app.ts` (InMemorySms iniettato, ritorna `{app, mailer, sms}`)
- Modify: `apps/web/src/app/login/page.tsx` (`EnrollKind` + branch SMS in startEnroll/onVerifyEnroll, input phone)
- Modify: `apps/web/src/app/(authenticated)/me/security/page.tsx` (card SMS clone del pattern EMAIL_OTP :405-468)
- Modify: `apps/web/src/locales/{it,en}/ess.json` (chiavi `security.sms*`)
- Create: `apps/api/test/mfa-sms.integration.test.ts`

Nessuna migration: CHECK kind già ammette SMS_OTP; `sys_auth_mfa_otp_challenges` factor-scoped riusabile (repo `mfa-repository.ts:190-264` riusato 100%). Numero telefono in `auth_mfa_factor_metadata.phone` (jsonb esistente); hint mascherato `+39•••••1234`.

- [ ] **D1.** Test failing prima: `mfa-sms.integration.test.ts` (enroll→verify-setup→factor verified; wrong-code/lockout-5; login step-up con pre-invio; resend cooldown 30s; CSRF 403; `allowedKinds` esclude SMS quando sender=Console). Run → FAIL (route 404).
- [ ] **D2.** Implementa nell'ordine: sms-sender.ts → env → schemi shared (+subpath export se nuovo file, qui no) → mfa-service (parametrizza il sender: l'unica riga channel-specific di `issueEmailOtp` è la :298) → routes → app wiring → build-test-app.
- [ ] **D3.** Run test → PASS. Regression: suite mfa+auth completa (~80 file sensibili: `vitest run` su `test/mfa*`, `test/auth*`, `test/login*`, `test/me*`).
- [ ] **D4.** UI: login page + /me/security + i18n (IT default + EN, parity check). Il bottone enroll SMS in UI è gated dagli `allowedKinds`/capabilities del server (in dev senza provider: nascosto — coerente col gating).
- [ ] **D5.** `pnpm i18n:check` + typecheck + commit:
  `feat(api,web): MVP-4 §2.5 — SMS_OTP code-only (provider-seam Console/InMemory, 4 route, login step-up, UI; attivazione PROD gated su provider)`

---

## Task E — TOFU v2 hardening (conferma out-of-band primo enrollment)

**Design bloccato (autorità tecnica Claude)**: (1) **notifica email** su OGNI nuovo fattore verificato ("nuovo metodo MFA aggiunto") + evento audit `MFA_FACTOR_ENROLLED`; (2) **conferma bloccante out-of-band** via codice email PRIMA del flip `verified=true`, SOLO quando: è il **primo** fattore dell'utente (`listVerifiedMfaFactorsForUser` count=0) E il kind è self-owned (TOTP/WEBAUTHN/SMS — EMAIL_OTP è già out-of-band by-design) E `MFA_ENROLL_CONFIRM` risolve on. Env `MFA_ENROLL_CONFIRM=auto|on|off` default `auto` = on solo se mailer reale (SMTP configurato) — in PROD oggi (no creds SMTP) resta dormiente: code-complete, attivazione gated su #8 creds (registrare in backlog).

**Files:**
- Create: `db/migrations/000108_mfa_enroll_confirm.sql` — estende CHECK `sys_auth_mfa_otp_purpose_check` (000081:60-63) con `'CONFIRM_ENROLL'` + CHECK login-events con `'MFA_FACTOR_ENROLLED'` (pattern content-aware guard di 000103:84-96)
- Modify: `apps/api/src/modules/auth/mailer.ts` (`IMailer.sendMfaFactorEnrolledNotice(toEmail, kind)` + impl Console/InMemory) + `smtp-mailer.ts`
- Modify: `apps/api/src/config/env.ts` (`MFA_ENROLL_CONFIRM`)
- Modify: `apps/api/src/modules/auth/mfa-repository.ts` (`MfaOtpPurpose` + `'CONFIRM_ENROLL'`)
- Modify: `apps/api/src/modules/auth/mfa-service.ts` (`verifyTotpSetup` :368-387 e `verifySmsOtpSetup`: se confirm-required → NON flip verified, issue OTP email CONFIRM_ENROLL → return `{status:"confirm_required"}`; nuovo `confirmEnroll(userId, code)` → consume → `markMfaFactorVerified` + notice + evento)
- Modify: `apps/api/src/modules/auth/webauthn-service.ts:235` (`verifyRegistration`: insert credential con factor verified=false quando confirm-required, flip in confirmEnroll)
- Modify: `apps/api/src/modules/auth/mfa-routes.ts` (`POST /v1/auth/mfa/enroll-confirm` — CSRF, rate-limit 30/h; raggiungibile da sessione `enr` via prefix allowlist esistente)
- Modify: `packages/shared/src/schemas/mfa.ts` (response verify-setup con union `confirm_required`, body confirm)
- Modify: `apps/web/src/app/login/page.tsx` + `apps/web/src/app/(authenticated)/me/security/page.tsx` (step "inserisci il codice ricevuto via email" — riusa il pattern UI verify email-otp) + i18n it/en
- Create: `apps/api/test/mfa-enroll-confirm.integration.test.ts`

- [ ] **E1.** Verificare PRIMA (grep) che la authentication ceremony passkey e `beginLoginChallenge` filtrino su `verified=true` (un factor pending-confirm NON deve autenticare né comparire in availableKinds). Se non filtrano → fix incluso.
- [ ] **E2.** Test failing: con `MFA_ENROLL_CONFIRM=on` + InMemoryMailer: TOTP verify-setup → `confirm_required` + mail con codice; confirm wrong → 401, replay → 401; confirm ok → verified + `lastNotice` + evento DB; 2° fattore → NO confirm (non è il primo); EMAIL_OTP → mai confirm; con `off` → comportamento attuale invariato (regressione zero).
- [ ] **E3.** Migration 000108 ×2 idempotente → implementazione → test PASS → regression suite mfa+auth+webauthn completa.
- [ ] **E4.** UI + i18n + E2E touch: `login-mfa-enrollment.spec.ts` resta verde (dev: env non settato → auto → mailer è Console in dev? verificare: se i dev-server hanno SMTP unset → auto=off → flusso E2E invariato; il flusso confirm è coperto da integration).
- [ ] **E5.** Commit: `feat(api,web): MVP-4 §2.5 — TOFU v2 (out-of-band enroll confirm env-gated + enrollment notice + MFA_FACTOR_ENROLLED audit, mig 000108)`

---

## Task B — UI admin mfa-policy

**Files:**
- Create: `apps/web/src/app/(authenticated)/admin/mfa-policy/page.tsx`
- Create: `db/migrations/000107_mfa_policy_ui_interface.sql`
- Modify: `apps/web/src/locales/{it,en}/admin.json` (blocco `mfaPolicy`)
- Modify (se manca l'icona): `ICON_MAP` in `apps/web/src/app/(authenticated)/layout.tsx`
- Modify: `apps/api/test/mfa-policy.integration.test.ts` (cleanup → **snapshot&restore** invece di delete — prerequisito Task C)
- Create: `apps/web/tests/e2e/mfa-policy-admin.spec.ts`

API già completa: `GET /v1/mfa-policy/` (`mfa_policy:read`) + `PUT /v1/mfa-policy/:tenantId` (`mfa_policy:manage`, CSRF); schemi in `@heuresys/shared/schemas/mfa-policy`. Permessi solo PLATFORM_ADMIN + TENANT_ADMIN (by-design, mig 000104).

- [ ] **B1.** Pagina (pattern `insights/page.tsx`: `"use client"`, useQuery inline `apiFetch`, `DataTablePanel`/Card, `useCurrentUserPermissions` per gating PUT): tabella tenant (PLATFORM: nomi via `GET /v1/tenants?limit=200`; TENANT_ADMIN: 403 su tenants → mostra solo la propria riga con tenantId) · toggle `enabled` · multiselect `roleCodes` (8 ruoli canonici, lista statica come in `admin/roles`) · `null` = tutti i ruoli (UI: checkbox "tutti") · PUT per riga. Errori → `ErrorState`.
- [ ] **B2.** Migration 000107 nav (pattern 000093): code `admin-mfa-policy`, route `/admin/mfa-policy`, icon esistente in ICON_MAP (es. `ShieldCheck`, verificare), `sidebar_group='admin'`, perspective `ENTERPRISE`, `required_resource='mfa_policy'`, `required_action='read'`, `requires_admin=true`, order 45. ×2 idempotente.
- [ ] **B3.** Fix cleanup test integration: beforeAll snapshot delle policy esistenti dei tenant toccati → afterAll restore esatto (non delete) — così la suite non cancella l'attivazione reale di Task C.
- [ ] **B4.** E2E `mfa-policy-admin.spec.ts` (storageState platformAdmin): naviga via sidebar → toggle sul tenant **Heuresys System** con `roleCodes=['READ_ONLY']` → verify GET → **restore OFF in finally** (mai lasciare il gate ON su HS).
- [ ] **B5.** i18n parity + typecheck + E2E verde + commit:
  `feat(web): MVP-4 §2.5 — admin UI /admin/mfa-policy (primo editor config per-tenant, nav mig 000107) + test snapshot-restore`

---

## Task F — ESS-media serve (chiude cap④ CMS P3)

**Files:**
- Modify: `apps/api/src/modules/content/media-service.ts` (+`listForPublishedDocument(tenantId, docId)` + `resolveForDownloadPublished(tenantId, mediaId)` — guard via `repo.findPublishedByIdForTenant` content/repository.ts:547, NON `findDocumentById`+buildScope)
- Modify: `apps/api/src/modules/me/routes.ts` (o plugin gemello con prefix `/v1/me/content`): `GET /content/:id/media` (list) + `GET /content/media/:mediaId` (stream) — `requirePermission("me:content:read")`, store via opzione (pattern `ContentMediaRoutesOptions`), **niente multipart/CSRF** (solo GET)
- Modify: `apps/web/src/app/(authenticated)/me/handbook/[id]/page.tsx` (prop `components={{img}}` su `MarkdownView`: rewrite prefix `/api/v1/content/media/` → `/api/v1/me/content/media/`; + lista allegati read-only in fondo)
- Modify: `apps/web/src/components/ContentMediaPanel.tsx` (+bottone "Copia markdown" → `![<filename>](/api/v1/content/media/<id>)` — URL admin: il preview admin funziona su qualsiasi status, l'ESS riscrive al proprio endpoint)
- Modify: `apps/web/src/locales/{it,en}/{ess,admin}.json` (chiavi allegati/copy)
- Modify: `apps/api/test/content-media.integration.test.ts` o Create `me-content-media.integration.test.ts`
- Modify/Create E2E: estendi `apps/web/tests/e2e/content-workflow.spec.ts` o nuovo `handbook-media.spec.ts`

Decisioni tecniche: serving ESS **inline** per mime immagine (`content-disposition: inline`) e `attachment` per PDF; `x-content-type-options: nosniff` mantenuto; allowlist mime senza SVG già garantita dall'upload. Schemi Zod riusati (`ListContentMediaResponseSchema`, `ContentMediaIdParamSchema`). Proxy Next già copre `/api/v1/me/*`. Nessuna migration (perm `me:content:read` esistente copre — "media is part of the document", header 000105).

- [ ] **F1.** Test failing (pattern `content.integration.test.ts:246-262`): plainUser scarica media di doc published (200, inline, bytes esatti); media di doc draft → 404; cross-tenant → 404; USER su route admin `/v1/content/media/:id` → 403 invariato; list per doc published → items.
- [ ] **F2.** Implementa service + route → PASS. Regression: suite content+me.
- [ ] **F3.** Web (rewrite img + allegati + copy-markdown) + i18n parity + E2E: admin upload PNG → embed nel body via copy-markdown → publish → employee vede `<img>` renderizzata (assert `naturalWidth>0`) + allegato in lista; cleanup finally.
- [ ] **F4.** Commit: `feat(api,web): cap④ CMS P3 close — ESS media serve (published-only guard, inline images, handbook embed+attachments)`

---

## Task G — color-contrast AA (delegato, con checkpoint Enzo)

**Files:** `apps/web/src/app/globals.css` (token `--muted-foreground` :78/:115 + altri token offendenti misurati); eventuale `ux-design-shared` upstream SOLO se un token vive nella lib (in quel caso: release `@heuresys/ui@0.1.5` + bump — da verificare, l'evidenza dice app-level); `apps/web/tests/e2e/a11y.spec.ts` (bump gate).

- [ ] **G1.** Rigenera audit: `pnpm exec playwright test a11y.spec.ts` (server dev up) → leggi `test-results/a11y-audit/*.json`. Se i JSON non riportano i nodi/colori: estendi temporaneamente lo spec per dump dettagli (fg/bg/ratio per nodo) o ispeziona 3 route campione con chrome-devtools. Raggruppa i 179 nodi per coppia fg/bg → attesi 3-6 pattern (muted-foreground su bg tinted, badge variant).
- [ ] **G2.** Ritara i token al **minimo delta percettivo** per ratio ≥4.5:1 (testo normale) / ≥3:1 (large): mantieni hue/saturation brand, scendi/sali di lightness quanto basta. Calcola i ratio prima di applicare (formula WCAG o tool locale).
- [ ] **G3.** **CHECKPOINT ENZO**: screenshot before/after di 3-4 route campione (dashboard, una analytics, /me, una con badge) light+dark → presentare → attendere ok. Solo dopo l'ok si committa.
- [ ] **G4.** Re-run audit completo → `serious=0` su 35 route × 3 personas. Se stabile, bump del gate in `a11y.spec.ts`: `critical=0 AND serious=0` (rende il guadagno permanente in CI).
- [ ] **G5.** Commit: `fix(web): a11y §2.7 — color-contrast AA token retune (179 nodi → 0 serious, gate CI serious=0)`

---

## Task H — engines ≥22 + @types/node ^22 (chiude PR #30)

**Files:** `package.json` (root: campo `engines` + devDep `@types/node`), `apps/api/package.json`, `apps/web/package.json`, `apps/showcase/package.json`, `packages/shared/package.json` (dove dichiarati — grep `"engines"` e `@types/node` prima).

- [ ] **H1.** Grep stato attuale: `engines` fields + tutte le occorrenze `@types/node` (incl. eventuale pnpm.overrides). Modifica: engines `">=22"`, `@types/node` `"^22"`.
- [ ] **H2.** `pnpm install` (lockfile update) → typecheck 4/4 ws + suite smoke (`vitest run test/healthz*` o 1 file rapido) + `pnpm build`.
- [ ] **H3.** `gh pr comment 30` (rationale: types seguono il runtime floor 22, non 25) + `gh pr close 30`. Commit:
  `chore(deps): engines floor ≥22 + @types/node ^22 (closes dependabot #30 as not-adopted)`

---

## Task C — Attivazione mandatory-MFA su RTL (slice ruoli) — POST full-suite

Prerequisito: B3 (test snapshot-restore) committato. Eseguire DOPO il full-gate di Task I (la suite non deve girare dopo l'attivazione… se rigira, col fix B3 non la cancella comunque).

- [ ] **C1.** Login API admin@heuresys.com (cookie+CSRF) → `PUT /v1/mfa-policy/86ba7a65-217f-48ba-8ce5-5c09b40a66b0` body `{"enabled":true,"roleCodes":["BLUEPRINT_MANAGER","HRMS_MANAGER","PROCESS_OWNER","READ_ONLY"]}`.
- [ ] **C2.** Evidenza: `GET /v1/mfa-policy/` mostra la riga enabled + roleCodes; login federica (TENANT_ADMIN, fuori scope) → 200 normale; integration `mfa-policy` suite già copre il terzo stato per i ruoli in scope. DB: `SELECT * FROM sys.sys_auth_mfa_policies` → 1 riga.
- [ ] **C3.** Nessun commit (è stato DB). Registrare nello STATE/handoff: attivazione fatta, estensione ai ruoli restanti = decisione comunicazione utenti (Enzo).

---

## Task I — Close: gates → push → CI → align+deploy → handoff

- [ ] **I1.** Full gates locali: `pnpm typecheck` · `pnpm lint` · `pnpm i18n:check` · full API suite (`pnpm --filter @heuresys/api test`, atteso ~900+/0) · `pnpm build` (web+showcase) · `bash db/scripts/migrate.sh` ×2 (108 migration, idempotente) · `pnpm db:validate` 7/7 · E2E nuove spec + smoke (login, handbook, mfa-policy).
- [ ] **I2.** Task C (attivazione) + verifica.
- [ ] **I3.** Secret-scan sullo staged/log (`git log -p origin/main..HEAD | grep -iE 'password|secret|api.key|sk-|token|BEGIN PRIVATE'` — escluse fixture note) → **push** (autorizzato dal piano) → `gh run watch` CI verde (7 workflow). CI rossa = fix immediato (R3).
- [ ] **I4.** `bash scripts/align-clones.sh all --deploy` (Mac + VM + linux-pc; vm-deploy: build prod + restart + readyz/login smoke). Smoke PROD: `curl http://80.225.82.207:8013/readyz` + `/v1/mfa-policy` → 401 (route viva) + web /login 200. Gotcha noto: unit systemd nuove = verifica `systemctl is-active` (memoria vm-deploy self-modify buffer — qui non aggiungiamo unit, solo verifica).
- [ ] **I5.** **Handoff** (skill `handoff`): riscrive STATE + SOT_STATE + SOT_BACKLOG + DEBT_REGISTER. Delta chiave da registrare: B-50/Wave-2 **CLOSED** (registry 0 NEEDS_DECISION, 141 POPULATED) · §2.5 MFA **COMPLETO code-side** (SMS_OTP code-only DONE, attivazione SMS PROD ⛔ provider; TOFU v2 DONE, confirm PROD gated su creds SMTP #8) · mfa-policy UI DONE + **attivazione RTL slice FATTA** · cap④ CMS **CHIUSA** (ESS-media DONE) · color-contrast **RISOLTO** + gate serious=0 · engines 22 + #30 chiusa · open-question **hook claude-mem → RISOLTA** (conferma Enzo S982) · residui invariati: mobile a11y sweep, ISTAT/ATECO (⛔ ToS), SMS provider (⛔ PM), SMTP creds (⛔ Enzo), estensione MFA ad altri ruoli (⛔ comunicazione).

---

## Risk register (R20)

| Rischio | Prob | Impatto | Mitigazione |
|---|---|---|---|
| Import: conteggi HS incerti (0-2 candidati) | alta | basso | misura al bake (A2), assert con numero esatto congelato |
| Mig 000106 su rebuild fresco (migrate prima dei seed) | media | medio | assert hard solo su registry; conteggi condizionali con WARNING |
| TOFU: factor pending-confirm autentica (passkey/availableKinds) | media | alto | E1 verifica filtri `verified=true` PRIMA di implementare; test dedicato |
| Login hot-path regressione (D+E toccano mfa-service/service.ts) | media | alto | suite mfa+auth+webauthn completa per task; default `auto=off` in PROD (no SMTP) → comportamento PROD invariato fino a creds |
| Suite cancella l'attivazione MFA reale | alta (senza fix) | medio | B3 snapshot-restore PRIMA di C; C dopo full-gate |
| Contrast: token lib-side non app-side | bassa | medio | evidenza dice app-level; se lib → release 0.1.5 + bump (procedura nota X18/D-15: clean install) |
| E2E flake dev-mode (OOM /me/security noto S980) | media | basso | retry + prod-build per spec pesanti se serve (memoria playwright-stale-servers) |
| engines bump rompe CI runner | bassa | basso | runner VM ha Node 22 (nvm); typecheck+build gate prima del push |

**Budget**: sessione lunga tipo S981 (10+ commit). Pattern tutti repetitivi/noti; coverage test forte (877 base). Ordine pensato per fail-fast: l'import (A) e il dominio MFA (D/E) — i più delicati — girano per primi a contesto fresco.
