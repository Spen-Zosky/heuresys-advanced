# Piano S1053 — batch «#124 + tutti i P1 tranne #76» (R24)

Selezione di Enzo, 2026-08-10: riprendi **#124** e i P1 **#177, #168, #149, #99, #92, #142, #143** (escluso #76).
Indagine preliminare: 6 dossier paralleli read-only (workflow `wf_0f6f4099-597`, 6/6 completati, misure live citate qui sotto). #149 è stato **applicato** all'indagine stessa: ogni consegna lab ri-verificata, esiti nei blocchi per voce.

## Confine di sessione (dichiarato ADESSO, R24 §4)

Il batch intero vale ~15-20 sessioni. **Questa sessione chiude V1–V8** (tabella sotto). Tutto il resto è dichiarato **FUORI SESSIONE** fin d'ora e resta nel register con lo stato aggiornato dall'handoff: #124-D4 (mask EVALUATION sugli ~12 moduli residui — serve prima il censimento campo-giudizio per schema), #124-D6 (frontend masked esteso), #99-F2/F4-completo/F5-qualificatori/F6/F7/F8, #92 passi 4-7, #142 intera famiglia cruscotti (progetto pronto nel dossier), #143 modello progetti (DDL già abbozzato nel dossier). #149 è continuativo: non si chiude, si applica (ed è stato applicato oggi).

## Tabella del ciclo (stato per riga — la chiusura si legge da qui)

| id | cosa | chi | fatto significa | stato |
|---|---|---|---|---|
| V1 | **#177 residui** (a) i revisori registrano il verdetto DA SÉ via `zp_review.py registra … --json -` come ultimo atto (emenda `adversarial.md`); (b) bonifica puntatori morti era-gov in `.zp/interrupted.json` (Z-112); (c) batteria estesa: prova stdin + comportamento sovrascrittura fissato | Claude | `zp-review-tests.sh` ≥15/15 verde (vista rossa almeno una volta); `interrupted.json` senza path che falliscono `ls`; adversarial.md istruisce l'auto-registrazione | **FATTO** — batteria 20/20 (vista rossa 2 prove prima del fix); `interrupted.json`={} via `zp_state.py --pulisci` (Z-112 è chiuso: piano r.448 `[x]`, merge `b6824e7e`); storico/ conserva i verdetti sovrascritti |
| V2 | **#168 completo** (a) emenda `000132`: `approval_step_approver_user_id` nullable + FK `SET NULL` + DO block self-healing; (b) contratto TS (`approvals.ts:56`, `me.ts:301`) + repository stesso commit; (c) vista sentinella `sys.v_history_cascade_to_users`; (d) selftest ST-CASCADE nella custodia; (e) scioglimento tensione C7a(iii) | Claude | ci-rehearsal verde 2 passate; censimento live mostra FK=SET NULL; custodia verde col selftest nuovo che crea utente sintetico, lo cancella, la storia resta; typecheck+3 test approvals verdi | **FATTO** — rehearsal VERDE 2 passate (backfill 751+715→0+0 idempotente); live: FK=SET NULL, 757 passi invariati, 0 approver senza snapshot, sentinella=0; custodia VERDE con `[OK] SELFTEST CASCADE #168` e ST-C7a(iii) emendato; typecheck verde; 21/21 test. In piu: tombstone (email) su approver+decider, `approverEmail` fa COALESCE sullo snapshot |
| V3 | **#124-D1** falla dossier: mask COMPENSATION (paySlips, employment.salary) + EVALUATION (performance) dentro `GET /v1/users/:userId/dossier` | Claude | HTTP live stessa riga, 2 attori reali: platform → chiavi assenti + `masked`; HR → valori presenti; test integration verde | **FATTO** — prova live 2026-08-10T16:40Z su `alberto.colombo@rtl-bank.org`: platform 0 importi nel body grezzo + masked dichiarati (paySlips, employment, contracts, performance), HR tutto presente; test 4/4 (visto rosso 2/4 prima del fix); 27/27 test contigui verdi; typecheck api+web verde. Scoperta assorbita: anche `contracts.grossAnnualSalary` era esposto — mascherato pure lui |
| V4 | **#124-D2** strato 1 sul dossier: partizione anagrafica PRO/PRIV per sezione (PRIV solo self/HR-mandate; manager di linea riceve PRO + marcatore sezione trattenuta); decisione esplicita su contacts/emergency/education che il lab non assegna | Claude | HTTP live: manager di linea reale → addresses/family/banking/documents assenti con marcatore; HRMS_MANAGER → presenti; typecheck web+api verdi | **FATTO** — prova live 2026-08-10T16:48Z: alberto.serra (capo linea) legge cristina.palmieri → 0 sezioni private, `maskedSections` 8/8 dichiarate, 0 valori privati nel body, salario visibile (CONTRACT_PAY=read); federica (HR) → 8/8 presenti. Test 4/4 (rosso 1/4 prima); contratto: `UserDossierProfileSchema` (solo dossier — `/v1/me` intatto); contacts/emergency/education decisi PRIV; typecheck verde |
| V5 | **#124-D3** COMPENSATION residui: mask su variable-pay, variable-pay/:id/evaluation, reward-gates, bonus-pools, position-economic-weight, handoff-records | Claude | HTTP live per endpoint: platform → campo assente+`masked`; HR → valore; test verdi | **FATTO** — prova live 2026-08-10T16:59Z sui 6 endpoint (variable-pay 50 righe, evaluation, reward-gates 50, bonus-pools 6, econ-weight 24, handoff 36): platform campi ASSENTI+dichiarati e 0 `total_gross` nel body (HR: 5); misura-prima: payload handoff portava 664207.28 vero. Test 7/7 (visti rossi 6/7); regressione 36/36; typecheck verde; MaskedCell estesa alle 3 colonne della pagina |
| V6 | **#99-T1** ADR-0036 «Domini gerarchici e funzionali» (sostituisce ADR-0027, recepisce C1-C6 + M1 con le 3 decisioni di Enzo, riscrive I16-I20, aggiunge **I22** HRMS plenipotenziario — I21 è occupato); ADR-0027 → Superseded; `build_adr_index.py` rigenerato; CLAUDE.md emendato | Claude | ADR-0036 Accepted; indice 0001-0036; CLAUDE.md con I16-I20+I22; zero numeri copiati dai doc lab senza ri-misura | da fare |
| V7 | **#99-T2** chiusura F3 senza bonifica (misurato: 0 relazioni implausibili, assorbite da 000244→000263) + sentinella permanente del criterio in `verifica_incrociata.py` con prova di falsificabilità | Claude | Register #99 aggiornato (F3 assorbita, query allegata); sentinella stampa 0 sul vivo e diventa rossa su relazione di prova iniettata (rollback) | da fare |
| V8 | **#92 passo 3/7** API lettura: moduli `review-cycles`, `calibration-sessions`, `performance-reviews` (pattern talent-review, orgGate I18 + mask ADR-0032 su EVALUATION), commit atomico per modulo; register #92 portato a 3/7 | Claude | Integration test verdi; HTTP live login federica.marchetti: 548 review reali, 35 sessioni calibrazione reali, review-cycles empty-state reale; controprova: persona senza deleghe → PERMISSION_DENIED; platform → score mascherato | da fare |

Chiusura binaria dal file: **CICLO CHIUSO = 8/8 FATTO**, altrimenti CICLO NON CHIUSO con la voce mancante nominata.

## Simulazione a 5 domande (R24 §3) — risposte = findings dei dossier

**V1** · Precondizioni: `zp_review.py` esiste (commit `65dff680` di stamattina), batteria 13/13 verde, `.zp/revisori/Z-112-{correttezza,isolamento}.json` presenti (verificato live). · Meccanismo: `registra` accetta `--json -` da stdin (zp_review.py:30-32 già lo ammette); i revisori del Workflow tool hanno Bash (agenti workflow = tool completi). · Propagazione: skill + tool versionati nel repo, viaggiano col commit; `.zp/` è gitignored (stato locale, per design). · Chi: Claude. · Guardia: non distruttiva; unico rischio = sovrascrittura verdetti che cancella `risolto_come` (zp_review.py:100-103) → è esattamente V1c che fissa il comportamento con una prova.

**V2** · Precondizioni: censimento live fatto (262 FK: 190 SET NULL / 62 CASCADE / 9 RESTRICT / 1 NO ACTION); UNICA FK storia/traccia in CASCADE = `sys_approval_step_approver_fk` (000132:99,128-130, nessun altro file la tocca — grep verificato); passi oggi: 757 (691 APPROVED, 25 REJECTED, 24 SKIPPED, 17 PENDING). · Meccanismo: emenda del file che crea (ADR-0035) + DO block idempotente guardato su `pg_constraint.confdeltype='c'`; ci-rehearsal ~26s su linux-pc PRIMA di applicare. · Propagazione: catena migrazioni → deploy armato; clone linux-pc via close-propagate. · Chi: Claude. · Guardia: nessuna DELETE; contratto TS emendato nello stesso commit (approvals.ts:56 e me.ts:301 `z.uuid()`→`.nullable()`, repository.ts:133 cast `as string` via) altrimenti typecheck rosso; post-condizione = 757 passi invariati; C7a(iii) (decided_by NULL = violazione) va sciolta in V2e o la custodia diventa rossa alla prima cancellazione di un decisore; indice `sys_approval_step_request_approver_uq` con NULLS DISTINCT ammette più passi orfani per richiesta (accettato, annotato nella migrazione).

**V3** · Precondizioni: falla misurata (getDossier service.ts:127-167 chiama `meRepo.loadPaySlips` sul target, zero mask; resolver.ts:153 `PLATFORM_ADMIN → true` incondizionato); meccanismo mask esistente e provato su 2 moduli. · Meccanismo: `masksUnderPlatformMandate(actor, classe, targetUserId)` + `maskFields` nel service, schemi `me.ts` con campi `.optional()` + `masked`. · Propagazione: codice versionato; nessun tocco DB. · Chi: Claude. · Guardia: NON toccare `/v1/me/profile/full` (self, I17); test che asserisce la PRESENZA dei campi per l'attore HR nello stesso commit (le prove devono poter fallire).

**V4** · Precondizioni: sezioni del dossier censite (identity, documents, addresses, contacts, emergency, family, education, banking, employment); il lab NON assegna contacts/emergency/education → decisione tecnica: vanno in PRIV (sono dati personali/privati, non professionali — coerente col criterio del lab). Il nome IDENTITY collide con `GdprDataClassEnum.IDENTITY` (gdpr.ts:18): si usano nomi di SEZIONE (pro/priv), non una nuova DataClass, evitando il falso amico. · Meccanismo: gate per sezione in getDossier su `HR_MANDATED_ROLES`/self + `maskedSections`. Le 5 celle funzionali (team_lead ecc.) restano chiuse: serve il resolver di dominio (#99-F7), dichiarato — niente promessa «6 celle». · Propagazione: codice. · Chi: Claude. · Guardia: self e HR invariati, provato con login reali.

**V5** · Precondizioni: 6 endpoint censiti con file:riga (compensation routes.ts:62-155); tutti org-gated, zero mask. bonus-pools/position-economic-weight/handoff-records hanno subject null → il mask morde sul platform actor puro: scelta = mascherare i campi monetari (non sopprimere le righe), coerente con ADR-0032 (la riga resta visibile). · Meccanismo: pattern identico a listRecommendations (service.ts:184-194). · Propagazione: codice. · Chi: Claude. · Guardia: test presenza-campi per attori HR; nessun tocco a /v1/me/*.

**V6** · Precondizioni: prossimo ADR = **0036** (misurato: file fino a 0035; ADR_INDEX stantio a 31/0033 → si rigenera); I* occupati fino a I21 → il nuovo è **I22**; numeri di perimetro ri-misurati (43 unità attive/42 responsabili, 161 persone, 313 posizioni/2 radici, 0 implausibili). · Meccanismo: file ADR + emenda CLAUDE.md + `build_adr_index.py`. · Propagazione: docs versionati. · Chi: Claude. · Guardia: nessun numero copiato dai doc lab (spot-check: l'ADR non contiene «28 unità» né «18 relazioni»).

**V7** · Precondizioni: query dell'assorbimento eseguita (0 righe: posizioni attive con riporti il cui incumbent non è responsabile di unità attiva né MANAGER/CEO). · Meccanismo: check nuovo in `verifica_incrociata.py` (strumento read-only, niente migrazione). · Propagazione: tool versionato. · Chi: Claude. · Guardia: prova di falsificabilità = iniezione in transazione con ROLLBACK (mai COMMIT interno — memoria `seed_internal_commit_defeats_rollback`).

**V8** · Precondizioni: passi 1-2/7 GIÀ FATTI (mig 000256/000257 applicate; live: cycles=0, calib 35/20/40 con lineage; legacy VM verificato 35/20/40 = sottoinsieme RTL di 86/30/60); 548 review (470+78, NON 550/79 — mig 000263); permessi già esistenti (performance-review:read/write, calibration:manage, review-cycle:manage). · Meccanismo: pattern 7 passi di api-module-pattern.md, modello talent-review per orgGate; mask ADR-0032 sui campi giudizio (performance-reviews è EVALUATION). · Propagazione: codice; nessuna migrazione prevista (se servisse → ci-rehearsal). · Chi: Claude. · Guardia: me/repository.ts:388-391,593 legge già review self-scope → integrarsi, non duplicare; atteso dei test derivato dal DB, mai hardcoded (548, non 550); rilievo mapping RBAC largo (calibration:manage a 6 ruoli incl. MANAGER/BLUEPRINT_MANAGER/PROCESS_OWNER vs disegno lab) da riesaminare QUI, con eventuale migrazione correttiva.

## Registro scoperte — fuori da questo ciclo (presentate UNA volta, non bloccano la chiusura)

1. **Policy di cancellazione utente**: 9 FK RESTRICT + 1 NO ACTION bloccano comunque la hard-DELETE di chiunque abbia storia operativa (presenze, ferie, saldi, assegnazioni, audit). Serve prima o poi una policy (soft-delete/deactivation esiste già). Lo vuoi nel prossimo ciclo?
2. **Due liste di ruoli NUOVE** non censite dal piano domini: `positions/service.ts:33` e `teams/service.ts:29` → da assorbire in #99-F4 (già fuori sessione).
3. **Riferimenti stantii a `.zp/verdetti/`** in `zero-pending-plan.md:236,451` e `.zp/PROGRESS.md:18` → bonifica documentale, handoff.
4. **`db_health.py` docstring dice 14 sentinelle, live sono 18** → allineamento all'handoff.
5. **`highestRoleLabel` copre 8 ruoli su 14** (un capo filiale è etichettato «USER») → dentro 142-F1 quando parte.
6. **Multi-unità vietato dall'indice** `ux_organization_unit_manager_unico`: il requisito F4 «unione dei sotto-alberi» descrive un caso che il DB oggi impedisce fuori da HQ → da ridiscutere in #99-F2 (modello dati).
7. **Oracolo dei test dopo F4**: `org-actors.ts` deriva dall'albero unità; se il resolver passa allo stesso albero i test si auto-confermano → serve contro-oracolo (da progettare in #99-T3).

## Esiti #149 su questo batch (applicazione della regola, non chiusura)

- Doc lab domini (2026-08-03): 6/10 affermazioni portanti SMENTITE dalla ri-misura (18 implausibili→0; enzo 0 ruoli→3; paolo 5 unità→1; 28 unità→43+2; 3 liste ruoli su 5 già eliminate; I21 già occupato).
- Register #92: stantio di 2 passi (1-2/7 già eseguiti e applicati).
- Register #177: stantio — il da-fare principale era già committato (`65dff680`); doc-field punta a path morto.
- Register #124: 2 affermazioni SMENTITE (`/distribution` non media classi mascherate; «la busta paga altrui non è esposta da alcuna API» — lo è, dal dossier #81).
- Register #142: «4 console» mai itemizzate; censimento reale = 3 `*-console` + 3 sotto-viste + system-health.
