# Batch P1+P2+P3 + debiti + domande aperte — S1058 (2026-08-14)

**Mandato di Enzo (2026-08-14)**: «consumare tutti i punti elencati in P1, P2 e P3, i debiti e
le tre domande» — con **#76 messo in HOLD**, perché prima di affrontarlo serve una sessione
preliminare di verifiche e aggiustamenti.

**Confine di sessione, dichiarato all'inizio (R24 §4).** L'obiettivo è un **programma**, non una
sessione: le voci residue valgono ~24-32 sessioni secondo le stime del register. Budget misurato
all'apertura del piano: contesto 145.668 token consumati, **604.332 utilizzabili** prima della
soglia 75%; finestra 5h all'8%. In questa sessione si chiudono le voci a costo basso e una voce
media; le voci da 2+ sessioni restano aperte e si dichiarano tali. Nessuna voce si chiude su
«green test»: vale la DoD live (ADR-0026).

---

## Tabella dei deliverable

| id | cosa | chi | cosa significa fatto | stato |
|---|---|---|---|---|
| **A** | #76 «zero pendenze» → HOLD con motivo, decisore e trigger di riattivazione | Claude | blocco `status: HOLD` nel register + `handoff_lint.py` verde | ✅ FATTO |
| **B** | Questo piano-file | Claude | file scritto, una riga per deliverable | ✅ FATTO |
| **C1** | Domanda 1 — `sys_engagement_*` è residuo? | **Enzo decide**, Claude istruisce | istruttoria live consegnata + decisione presa | ⏳ ISTRUITA, attende Enzo |
| **C2** | Domanda 2 — responsabili di vertice non Dirigenti | **Enzo decide**, Claude istruisce | istruttoria live consegnata + decisione presa | ⏳ ISTRUITA, attende Enzo |
| **C3** | Domanda 3 — cancellare `gov/w1-recuperato` e `gov/w2-recuperato` | **Enzo decide**, Claude istruisce | istruttoria live consegnata + decisione presa | ⏳ ISTRUITA, attende Enzo |
| **D** | #149 — consegne del lab trattate come non verificate | Claude | inbox del lab ispezionata oggi; se vuota, verifica registrata | ⬜ DA FARE |
| **E** | #79 — cancello di esposizione | Claude | `check_exposure.py` exit 0 letto **sul processo**, non sui messaggi | ⬜ DA FARE |
| **F** | D-56 — claude-mem workaround | Claude misura, **Enzo decide** se tocca il suo ambiente | stato reale di `settings.json` misurato; riga del registro allineata al vero | ⬜ DA FARE |
| **G** | D-81 — `BLUEPRINT_FIELD_LOCKED` | Claude | riclassificato: non è un difetto da correggere ma una dipendenza dichiarata di P2 (#132, GATED) | ⬜ DA FARE |
| **H** | D-82 — 468 predizioni senza modello | Claude | migrazione di bonifica con le 4 cose del metodo (misura, guardia, post-condizione sulle 156 `GENERIC`, rollback) + `ci-rehearsal.sh` | ⬜ DA FARE |
| **I** | #123 — `organigramma-bis.html`: leggere, ricavare le situazioni, eseguirle | Claude | documento letto per intero, situazioni estratte, correzioni eseguite e dimostrate live | ⬜ DA FARE |
| **J** | #159 — ri-stima del ponte gateway↔pagine col bersaglio cresciuto | Claude | effort dichiarato nel register al posto di «da stimare» | ⬜ DA FARE |
| **K** | #92 passi 4-7 · #142 · #143 · #99 · #54 · #50 | Claude | ~2-8 sessioni ciascuna — **fuori dal confine di questa sessione**, si aprono una per volta | ⬜ PROGRAMMA |

---

## Simulazione a 5 domande — voci di questa sessione (R24 §3)

### D — #149 consegne del lab
- **Precondizioni**: la cartella `<padre del repo>/heuresys-design-lab/inbox/` esiste.
- **Meccanismo**: ispezione diretta della cartella. Non c'è uno strumento: la verifica è guardare.
- **Propagazione**: nessun artefatto nuovo; l'esito va nel register.
- **Chi**: Claude.
- **Guardia**: non distruttiva. Il rischio è dichiarare «vuota» una cartella non guardata → si allega il listato.

### E — #79 cancello di esposizione
- **Precondizioni**: tunnel :5433 su (verificato al boot).
- **Meccanismo**: `python docs/kb/tools/check_exposure.py`, exit 1 su lacuna.
- **Propagazione**: nessuna.
- **Chi**: Claude.
- **Guardia**: l'exit code va letto **sul processo** — in S1057 tre esiti sono stati letti attraverso una pipe e mentivano.

### F — D-56 claude-mem
- **Precondizioni**: `~/.claude/settings.json` leggibile.
- **Meccanismo**: lettura del flag `enabledPlugins`. **Sospetto di drift**: una memoria di sessione dice «claude-mem RIABILITATO 2026-08-06», il registro dice ancora «aperto, serve un sì di Enzo». Una delle due è stantia.
- **Propagazione**: il file è **fuori dal repo**, sulla macchina di Enzo.
- **Chi**: Claude misura; se il ripristino non è ancora avvenuto, decide Enzo.
- **Guardia**: non si tocca `settings.json` a metà sessione — è esattamente l'incidente S1020.

### G — D-81 `BLUEPRINT_FIELD_LOCKED`
- **Precondizioni**: nessuna.
- **Meccanismo**: emendamento della riga nel `DEBT_REGISTER.md`.
- **Propagazione**: repo.
- **Chi**: Claude.
- **Guardia**: non è una chiusura mascherata — la voce resta aperta, cambia la **classe**: dipendenza di P2, non difetto latente. Va detto che #132 è GATED.

### H — D-82 predizioni senza modello
- **Precondizioni**: `sys_model_predictions` 468 righe con `prediction_model_id` NULL; 4 modelli attivi.
- **Meccanismo**: migrazione che deriva il legame dal tipo — `PERFORMANCE`→Performance Predictor, `TURNOVER`→Turnover Risk Model. **`GENERIC` (156) non si indovina**.
- **Propagazione**: la catena si riapplica a ogni deploy → si emenda il file che crea, non si fa una `DELETE` a valle (ADR-0035).
- **Chi**: Claude.
- **Guardia**: (a) misura prima; (b) guardia che ri-verifica la precondizione al momento dell'esecuzione; (c) **post-condizione che protegge le 156 `GENERIC`** — devono restare NULL; (d) rollback dichiarato. Poi `ci-rehearsal.sh`.

### I — #123 organigramma-bis
- **Precondizioni**: il file `D:\heuresys-design-lab\organigramma-bis.html` esiste ed è leggibile.
- **Meccanismo**: lettura integrale, poi esecuzione delle situazioni ricavate. Il mandato dice che **la consegna è il documento**, non un elenco pre-digerito.
- **Propagazione**: dipende da cosa emerge; se tocca `db/**` passa da `ci-rehearsal.sh`.
- **Chi**: Claude.
- **Guardia**: #149 vale anche qui — il documento è una consegna del lab, quindi va trattato come **non verificato**: ogni sua affermazione portante si misura prima di agirci.

---

## Registro delle scoperte (R24 §5 — non entrano in «cosa resta»)

Emerse istruendo le tre domande, **fuori** da questo ciclo. Presentate una volta sola.

1. **I nomi delle due famiglie survey sono incrociati.** Il modulo API `engagement` legge
   `sys_surveys`/`sys_survey_*` (14 sondaggi, 8.288 risposte — i dati vivi); il modulo API
   `surveys` legge `sys_engagement_*` (6 sondaggi, 862 risposte). `org-health` calcola sulla
   famiglia vecchia; `insights` legge **entrambe**. Non è un residuo inerte: è la casa di
   quattro moduli di produzione.
2. **I responsabili di vertice non-Dirigenti sono tre, non due**, e `sara.gallo` sta al
   livello 2, non al livello 1.
3. **La qualifica non segue l'unità retta** in diversi casi (un «IT Director» regge la Direzione
   Antiriciclaggio; due «Compliance officer» reggono filiali; un «Risk analyst» regge il Back
   Office). Famiglia di incoerenze diversa e più ampia della domanda 2.
4. **I tag `archivio/*` non sono su origin.** Conservano i due rami solo su questa macchina.
