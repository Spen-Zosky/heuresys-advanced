# Batch S1059 — «parti da D-83 e poi continua con il resto» (mandato di Enzo, 2026-08-14)

Piano-file R24. Lo stato si legge **da questa tabella**, non dalla memoria.
Chiusura binaria: o *«N/N voci fatte»*, o *«manca la voce K, serve Enzo per Y»*.

## Confine di sessione — dichiarato all'inizio

Il mandato è aperto («il resto» = il menu). **Non è tutto completabile in una sessione**: #99 vale
6-8 sessioni, #143 4-6, #54 5-7. Quindi la regola di questo batch è: **le voci multi-sessione
avanzano di UNA fase per volta, con commit atomico**, e il batch si ferma quando il guardiano lo
impone (contesto ≥75% **oppure** finestra 5h ≥80%) — non quando le voci finiscono.

## Tabella dei deliverable

| id | cosa | chi | «fatto» significa | stato |
|---|---|---|---|---|
| **V1** | **D-83** — il contratto API non segue il vincolo che `000305` ha messo nel DB: CI rossa dal 13/08, deploy fermo | Claude | `Test (api integration)` **verde in CI** (non il verde locale) | ⏳ |
| V2 | **#187** — l'indice di salute organizzativa misura un clima fermo a gennaio 2025 mentre la rilevazione vera è aperta | Claude | l'indice legge la rilevazione corrente; dimostrazione LIVE con login reale | ⏳ |
| V3 | **#99 F3** — resolver unico dai domini, sull'albero delle **unità** (oggi gira su quello delle posizioni) | Claude | 1 fase, commit atomico + prova falsificabile | ⏳ |
| V4 | **#92 F4** — API di scrittura + macchina a stati del ciclo di valutazione | Claude | 1 fase, commit atomico | ⏳ |
| V5 | **#123** — leggere `organigramma-bis.html`, ricavarne le correzioni, eseguirle | Claude | le situazioni reali corrette; i falsi positivi dichiarati | ⏳ |

Legenda stato: ⏳ da fare · 🔄 in corso · ✅ fatto · ⛔ bloccato (con motivo e chi sblocca)

---

## V1 — D-83. Decomposizione fino al comando

### Misura di partenza (fatta, 2026-08-14 — non ereditata dal debito)

Il debito diceva «sei test». **Falso per difetto**: il log del run `31757071073` dice
`Test Files 6 failed | 230 passed (236)`, e i fallimenti sono **13 test + 2 suite in errore**:

| file | fallimenti | come crea il tenant |
|---|---|---|
| `approvals-effects.integration.test.ts` | 5 | `INSERT INTO sys.sys_tenancies` diretto (riga 73) |
| `tenants.integration.test.ts` | 4 | `POST /v1/tenants` |
| `provisioning.integration.test.ts` | 3 | `POST /v1/tenants/provision` |
| `enterprise-typing-profiles.integration.test.ts` | 1 (suite: 3 test saltati) | `INSERT` diretto (riga 62) |
| `tenant-materialization.integration.test.ts` | 1 (suite intera) | `INSERT` diretto (riga 163) |
| `gdpr.integration.test.ts` | 1 | `POST /v1/tenants/provision` |

Errore unico alla radice: `23502 — il valore nullo nella colonna "tenant_industry_code" … viola il
vincolo non nullo`. Sintomo lato API: **6× `expected 500 to be 201`** — cioè un 500 dal database
dove doveva esserci un 400 di validazione.

Stato reale del vincolo (misurato ora, non dal piano):
`sys_tenancies.tenant_industry_code` = **NOT NULL, nessun default**, FK → `sys.sys_industry_codes`
(`ON UPDATE CASCADE ON DELETE RESTRICT`). Catalogo = **12 codici, tutti attivi**
(`CONSTRUCTION`, `EDUCATION`, `FIN_BANKING`, `FIN_INSURANCE`, `HEALTHCARE`, `IT_SOFTWARE`,
`MANUFACTURING`, `MGMT_CONSULTING`, `PUBLIC_ADMIN`, `RETAIL`, `TRANSPORT_LOGISTICS`,
`UTILITIES_ENERGY`).

### Difetto in più, trovato misurando (non era nel debito)

`packages/shared/src/schemas/provisioning.ts:28` dichiara `z.string().max(16)`, ma la colonna è
`varchar(32)` e **due codici del catalogo superano 16 caratteri** — `TRANSPORT_LOGISTICS` (19) e
`UTILITIES_ENERGY` (16 al limite). Il contratto rifiuterebbe un codice valido: va portato a 32.

### I comandi / i file

1. `packages/shared/src/schemas/tenants.ts:52` (create) e `:65` (patch) e `:36` (risposta)
2. `packages/shared/src/schemas/provisioning.ts:28` (`max(16)` → `max(32)`, obbligatorio)
3. `apps/api/src/modules/tenants/repository.ts:152` (`?? null` diventa morto) e `:183-184` (PATCH)
4. i 3 file con `INSERT` diretto + i 3 che passano per l'API (tabella sopra)
5. `cd apps/api && pnpm exec vitest run test/tenants… test/provisioning… test/gdpr… test/approvals-effects… test/enterprise-typing-profiles… test/tenant-materialization…`
6. `pnpm typecheck` (il contratto è condiviso: rompe web e api se qualcosa lo consuma)
7. commit atomico + push + `gh run watch` sul workflow `Test (api integration)`

### Simulazione a 5 domande (risposte = findings, scritte PRIMA di eseguire)

- **Precondizioni** — ✅ tutte misurate sopra: vincolo attivo, catalogo popolato, tunnel su, CI
  rossa riprodotta dal log. Nessuna assunzione ereditata.
- **Meccanismo** — il contratto è **Zod in `packages/shared`**, consumato sia dall'API sia dal web.
  Rendere il campo obbligatorio significa che la validazione respinge **400** prima di arrivare al
  DB: è esattamente ciò che `000305` afferma a livello di dati (**I21**).
- **Propagazione** — ⚠ da verificare **prima di dichiarare fatto**: se `apps/web` ha un form di
  creazione tenant, deve avere il campo settore, altrimenti il frontend si romperebbe in modo
  silenzioso. `grep tenantIndustryCode apps/web` → **0 occorrenze**: da capire se la UI non crea
  tenant affatto, oppure se li crea senza il campo (nel secondo caso il fix è incompleto).
- **Chi** — Claude, per intero. Nessun input di Enzo.
- **Guardia** — non è una scrittura di massa, non tocca `db/**` → niente `ci-rehearsal`. La prova è
  **la CI verde**, non il verde locale (il DB della CI è un clone che non ha i dati da script).
  Falsificabilità: prima del fix i 6 file devono essere **rossi in locale**; se sono verdi, sto
  misurando il DB sbagliato.
