# Batch S1057 — clima+#126, poi sfoltimento governato dal guardiano

**Mandato di Enzo (2026-08-13)**: «costruisci ora la pagina dove la persona rivede le
proprie risposte, aggregata a #126 che è la stessa cosa per le predizioni. Ma voglio anche
che sfoltisci il più alto numero di azioni di P1, P2 e P3 in questa sessione, con le regole
dettate dal flusso che consulta il guardiano.»

## Il confine di questa sessione, dichiarato all'inizio (R24 §4)

**Misurato alle 13:31, non stimato:**

| misura | valore | quanto manca alla soglia |
|---|---|---|
| contesto | 140.784 / 1.000.000 (14,1%) | **609.216 token** alla soglia 75% |
| finestra 5h | 25,0% | **55 punti** alla soglia 80% |

**Ritmo osservato in questa sessione** (13:13 → 13:31, boot + tre decisioni + fix guardiano):
5h da 19,0% a 25,0% = **~0,33 punti/minuto** · contesto da 75.854 a 140.784 = **~3,6k
token/minuto**. I due vincoli convergono su **~2h45 di lavoro effettivo residuo**, ed è una
convergenza attesa: misurano lo stesso lavoro da due lati.

**La conseguenza va detta subito, con i numeri.** Il pacchetto A è dichiarato ~3-4h. Da
solo occuperebbe l'intero residuo e probabilmente lo sforerebbe. Quindi «A + il massimo
numero di altre voci» non entra tutto: A entra, e ciò che resta detta quante altre. La
lista sotto è ordinata per essere tagliata dal fondo, non a metà.

## Le voci

| id | cosa | chi | fatto significa | stato |
|---|---|---|---|---|
| **A** | Clima + #126: la persona vede le predizioni su di sé e rivede le proprie risposte | io | 5 tabelle da SCOPERTE/DA-COSTRUIRE a raggiungibili; rotte `/v1/me/*` + schemi + test + **prova live con login reale** | ✅ `fatta` — commit `61a19efe` (0/8.288 → 8.288 risposte leggibili), `2d8b110b` (#126, tre rotte), `6b91539b` (register + D-82). Cancello: raggiungibili 71→74, «da costruire» a 0 |
| **B** | #136 — la pagina delle nomine del lab mai nominata | io | la pagina è nominata dove serve, o dichiarata ritirata | ✅ `fatta` — `30a9baba`: le nomine erano già entrate nel dato, per la via di #118/#120 |
| **C** | #133 — guardia lab: il ruolo dell'argomento nei comandi di copia | io | la guardia distingue sorgente e destinazione; prova che sa fallire | ✅ `fatta` — `50901dc4`: selftest 112→122, due sabotaggi provati |
| **D** | #148 — rendiconto delle chiusure: la chiusura va riscritta in quattro verbi? | io | letto il rendiconto, decisione presa e registrata | ⛔ `GATED al 2026-08-20` — il diario copre 7 giorni sui 14 che la voce stessa richiede; portata fuori dal menu attivo |
| **E** | #158 — `deriveUserProfiles` riscrive sempre tutti i 156 profili | io | il salto esiste e si misura; nessun corpus senza | ✅ `fatta` — `f8b2eeba`: 0 righe a dato invariato, 1 sporcandone una, 156 con la query vecchia |
| **F** | #174 — i seed `rtl-banking-skills` non sono ri-eseguibili | io | idempotenti, oppure dichiarati one-shot con la guardia che lo impone | ✅ `fatta` — `00894426`: one-shot dichiarati, e uno era **superato**, non solo applicato |
| **G** | #79 + #149 — cancello di esposizione e consegne del lab | io | verifica continuativa: esposizione verde, inbox lab vuota | `già verificato (inbox vuota)` |

**Fuori da questo ciclo, dichiarato una volta sola** (R24 §5): le 22 tabelle ancora scoperte
del cancello di #117 dopo le decisioni di oggi; #123 organigramma-bis (~1 sessione intera);
#99/#92/#142/#143 (multi-sessione per costruzione).

## Simulazione della voce A (R24 §3 — cinque domande, risposte prima di eseguire)

**Precondizioni.** Il modulo del portale personale esiste (`apps/api/src/modules/me/**`,
58 rotte). Le 5 tabelle esistono e sono popolate: `sys_model_predictions` 468 righe/156
persone · `sys_mentor_match_scores` 30 · `sys_engagement_survey_responses` 862/158 ·
`sys_engagement_surveys` 6 · `sys_pulse_checks` 2.834/157. Il tunnel :5433 è su, il DB
risponde. Serve una persona reale per la prova live: `federica.marchetti@rtl-bank.org` o
`paolo.caputo@rtl-bank.org`, con password **derivata** per-email (non `TEST_ADMIN_PASSWORD`
— vedi memoria `reference_persona_login_derived_password`), login in due passi.

**Meccanismo.** Non «aggiungo una rotta»: il modulo `me` compone i servizi di 11 altri
moduli, quindi la tabella diventa raggiungibile per il cancello di #117 **quando il modulo
la nomina o nomina il servizio che la legge**. Le rotte seguono il pattern in 7 passi di
`.claude/rules/api-module-pattern.md`; gli schemi Zod stanno in `packages/shared`. Da
verificare leggendo il codice, non assumendo: se esiste già un servizio che legge le
predizioni per il cruscotto, si compone quello invece di scriverne uno nuovo.

**Propagazione.** Modifiche a `apps/api` e `packages/shared` → CI, poi deploy armato a
chiusura da `close-propagate.sh`. Nessuna migrazione DB prevista (le tabelle esistono):
**se dovesse servirne una**, passa da `ci-rehearsal.sh` sul linux-pc prima del push.

**Chi.** Io, per intero. Le due decisioni di prodotto necessarie sono già state prese da
Enzo (2026-08-04 per le predizioni, 2026-08-13 per il clima) e sono registrate nel cancello.

**Guardia.** Non è una voce distruttiva — aggiunge superficie in lettura. La guardia che
conta è sul **filtro per attore**: una rotta `/v1/me/*` che restituisse le risposte di
qualcun altro sarebbe il difetto peggiore possibile su questo dato. Il test deve provare
che un attore **non** vede le righe di un altro, e deve **sapere fallire** (verificato
rompendo il filtro di proposito). Il vincolo `mask` di I20 non si applica qui:
`PERSONAL` non è fra le classi mascherate.

**Chiusura di A**: `check_completezza_self.py` conta 5 tabelle in meno fra scoperte/da
costruire, e la prova live mostra una persona reale che apre la pagina e vede i **propri**
dati.

## Come si decide di fermarsi

Dopo **ogni** voce chiusa e committata: `python docs/kb/tools/guardiano.py --sorveglia`.
Exit 3 → si chiude, senza rinegoziare. Prima di aprire una voce nuova:
`--budget N` con la stima della voce (che ora misura contro la soglia, non contro la fine
della finestra — corretto in questa sessione, commit `c631c3d8`).
