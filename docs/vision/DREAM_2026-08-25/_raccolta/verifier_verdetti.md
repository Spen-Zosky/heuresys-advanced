# Verdetti del verifier — Onda 4

> Prodotti dall'agente dream-verifier (opus), chiusi 2026-08-26 ~00:00; file scritto
> dall'orchestratore perche' l'agente non dispone di tool di scrittura. Contenuto del
> rapporto riportato integralmente, senza modifiche di merito.

I have no write tool in this subagent (only Read/Grep/Glob/WebSearch/WebFetch), so I cannot create the output file myself — same situation as the `dream-market` raccolti, which the orchestrator saved. Below: the requested summary first, then the full document body to save at the requested path.

**Path previsto**: `D:\heuresys-advanced\docs\vision\DREAM_2026-08-25\_raccolta\verifier_verdetti.md`
**Conteggi**: REGGE **3** · REGGE-CON-CORREZIONI **22** · CADE **4** (29/29 schede giudicate)
**Fatti smentiti (in tutto o nella lettura operativa)**: **4** (F3, F10, F27, F31) + 1 cella di matrice smentita (§12 export)
**Fonti rotte**: **0 fonti false sul web** (6/6 verifiche mirate hanno tenuto) · **3 citazioni [S] mal attribuite** (P-01→S4, P-13→S1, P-20→S9)

---

# Verdetti del verifier — Onda 4

**Chi scrive**: agente verificatore avversariale, 2026-08-26. Sola lettura. Ogni verdetto porta file:riga letto o URL+data.
**Nota di metodo**: nessun accesso psql. Tutti i conteggi di riga viva (63 mentorship, 316 layout, 161 gap, 43 unità, 468 predizioni, 8 richieste GDPR, 0 deleghe, 85 data-map) sono dichiarati **NON-RIVERIFICABILI** e non sono stati usati né a favore né contro.
**Lettura d'insieme**: 25 schede su 29 sopravvivono nella sostanza. Le 4 che cadono non cadono per severità dell'asticella: cadono su prove documentali del progetto stesso (una funzione già accesa in PROD, un cluster di tabelle sbagliato, un canvas che non esiste, una sonda che non può girare). Le 22 "con correzioni" sono in larga maggioranza correzioni di **una citazione** o di **una stima di sforzo**, non del merito.

## Verdetti per proposta

| id | verdetto | ancore verificate | ancore rotte o deboli | correzioni richieste |
|---|---|---|---|---|
| P-01 | REGGE-CON-CORREZIONI | F16 CONFERMATO (`multipart` in esattamente 2 file: `apps/api/src/modules/me/routes.ts`, `.../content/media-routes.ts`); F21 CONFERMATO (604 route / 99 file `*routes.ts`); F24 CONFERMATO (tenant-blueprints 20 route con build-plan/apply/diff) | **[S4] mal citato**: S4 parla di API/SSO/marketplace nel piano Core Pro, **non** di "onboarding dati come leva dichiarata" (`competitor_personio.md` §Integrazioni). Il preambolo "presente in tutti e tre" **non vale per questa riga**: `04_MATRICE.md:192` dà Personio `n.c.` ed Eightfold `n.c.` sull'import da file — solo Zucchetti è `Doc` | Togliere [S4]; dichiarare "condizione d'ingresso" come **ipotesi**, non come [S]; dichiarare la dipendenza da un **file anagrafico reale** (input che solo Enzo/un cliente può fornire → la sonda è WAIT-INPUT); sforzo M→M/L (parser + mappatura + anteprima + giornale undo + lineage `sys_source_lineage_records` che il progetto pretende per ogni riga derivata) |
| P-02 | REGGE-CON-CORREZIONI | S12 **riverificato da me con fetch diretto** (2026-08-26): i 5 tracciati esistono e sono nominati esattamente così | **La direzione è invertita**: la pagina dichiara import **unidirezionale verso** Paghe, "nessun trasferimento in direzione opposta". I tracciati sono ciò che un rilevatore presenze **produce per** Zucchetti, non ciò che Zucchetti esporta. Il "riferimento cedolino" nel TRRIPW è un codice voce **in ingresso** al payroll, non un cedolino in uscita. F34 è dichiarativo (EVIDENZE lo flagga) | Riscrivere il "Cosa": si legge una **copia del file che il rilevatore del cliente manda a Zucchetti** — la controparte è il fornitore presenze, non Zucchetti; cancellare la metà "cedolini" (nessuna fonte la sostiene: S13 dice API dietro SSO, contenuto non verificabile); il titolo "leggere i tracciati **del** gestionale" va corretto in "**verso** il gestionale" |
| P-03 | REGGE-CON-CORREZIONI | F15 CONFERMATO (grep `webhook\|personal_access_token\|api_key` su `apps/api/src` → 3 file, tutti falsi positivi: `config/env.ts`, `semantic-matching/backfill.ts`, `semantic-matching/voyage-client.ts` = chiave in **uscita** verso Voyage); F19 CONFERMATO (`lib/notifications/emit.ts`, 25 usi in 8 moduli) | "presente in tutti e tre [S4][S13][S14]" regge solo al livello "esiste qualcosa": S13 è `Documentato (esistenza), contenuto non verificabile`; S14 è `Riportato`. Il portale developer self-service è **solo Personio** | Aggiungere a "cosa esiste già": **OpenAPI/Swagger già generato** dietro `API_DOCS_ENABLED` (~407 endpoint — `docs/kb/improvement/FINDINGS/WS-J.md:27`), che abbatte la voce "doc"; declassare "condizione d'ingresso in tutti e tre" a "self-service solo nel diretto" |
| P-04 | REGGE-CON-CORREZIONI | F14 CONFERMATO (grep `SAML\|OIDC\|OAuth\|\bSSO\b` su `apps/api/src/modules/auth` → **0 match**); S4 corroborato indipendentemente (Personio supporta Okta/Entra via OIDC; una fonte terza lo colloca nel piano alto) | **Argomento circolare**: "requisito d'ingresso frequente per PMI — l'architettura stessa lo dichiara [F14]". `architettura_raw.md:69` è un giudizio dell'agente raccoglitore, non una misura di mercato: citare il proprio raccolto come prova della domanda non è un ancoraggio | La sonda ("tre prospect") è l'unico strumento che decide la classe e **richiede prospect raggiungibili** (dipendenza su Enzo, non dichiarata): il MUST-T1 resta **provvisorio finché la sonda non torna**. Sforzo M/L→L: la tabella `sys_auth_sessions` è un placeholder da ridisegnare, e vanno decisi provisioning/collegamento utenze (tocca `user_external_code`, I14) e il rapporto con la policy MFA per tenant |
| P-05 | REGGE-CON-CORREZIONI | F12 CONFERMATO: `apps/api/src/modules/auth/routes.ts:311-312` — commento *"no mutations exposed in MVP-2a — full role CRUD lands in MVP-3"*; 6 `app.post` nel modulo, **nessuna** su ruoli; `/role-permissions` è GET con `role_matrix:read` (:316-318) | — | **Due costi taciuti, entrambi verificati**: (a) la cache RBAC è popolata **una volta al boot** (`middleware/rbac.ts:17`, `server.ts:41`) e non esiste alcuna via di ricarica → ogni scrittura sui ruoli richiede un meccanismo di invalidazione nuovo o un restart dell'API; (b) le mappe ruolo→permesso sono seminate da **76 INSERT su 52 file di migrazione** (`grep "INSERT INTO sys.sys_auth_role_permissions" db/migrations`) e **la catena si riapplica a ogni deploy** → la promessa "senza migrazione" collide con ADR-0035. Sforzo M→L |
| P-06 | REGGE-CON-CORREZIONI | F26 CONFERMATO come esistenza moduli; misurato oggi: `assessments` 4 route (list/get/**POST**/PATCH), `assessment-results` 3 (2 GET + **POST**), `assessment-methods` **1** (sola GET), `performance-reviews` **2, entrambe GET**, `review-cycles` 4 con **POST /** e **POST /:cycleId/transition** | Il "cosa esiste già" è **sottostimato** in un punto e **sovrastimato** in un altro: creare e far avanzare un ciclo esiste già lato server; **scrivere una valutazione di performance non esiste** (2 GET) e **creare una scala non esiste** (`assessment-methods` ha solo la lista) | Rimisurare il "cosa esiste già" prima di stimare; la campagna richiede comunque: matrice chi-valuta-chi, avanzamento, chiusura, e gli stati di comunicazione che I20 pretende (`shared_at`/`acknowledged_at`). L resta difendibile, ma è il tetto, non il pavimento |
| P-07 | **CADE** | Il modulo `surveys` esiste con 12 route (template CRUD + survey CRUD + risposte) e zero consumer web — verificato | **La premessa "manca solo la metà crea" è falsa.** Sono **due cluster di tabelle diversi**: `surveys/repository.ts:47-201` interroga `sys.sys_engagement_survey_templates/_surveys/_responses` (modello JSONB), mentre `engagement/repository.ts:27-128` e il portale ESS (`me/repository.ts:913-1108`, `sys_survey_*` + `sys_pulse_checks`) leggono il **cluster normalizzato**. `SOT_BACKLOG.md:2426` lo dice esplicito: *"modulo API /v1/surveys/* 12 endpoint … JSONB model sul cluster `engagement_*`. Resta m2b (cluster normalizzato)"*; `:2307` dichiara la scelta fra i due **decisione semantica aperta, autorità Enzo** | Un sondaggio composto col modulo latente **non arriverebbe ai dipendenti**. Per tornare in gioco serve prima la decisione m2b (WAIT-INPUT su Enzo) oppure un authoring costruito sul cluster normalizzato — che è lavoro diverso da "una UI di composizione sul modulo che c'è" |
| P-08 | REGGE-CON-CORREZIONI | F27 CONFERMATO nella parte che conta: `docs_censimento_pass2_b.md:229-238` elenca 9 mini-spec **MAI eseguite** (02,03,04,06,07,08,09,10,11); F19/F24 CONFERMATI | F27 dice "mini-spec **DDL complete**": la fonte scrive solo *"Mini-specifica … MAI eseguita"* — la completezza del DDL **non è attestata da nessuna riga letta** | Declassare a "mini-specifica, contenuto non verificato" (la sonda già lo farebbe); dichiarare che le scadenze richiedono uno **scheduler**, e citare il precedente esistente `deploy/systemd/heuresys-advanced-approvals-sla.timer` + `modules/approvals/sla.ts` (usa `emitNotification`); l'indice SDBI stima 60-90h per 11 aree (`pass2_b:227`) → ~6-8h ad area, coerente con M |
| P-09 | **REGGE** | F27 CONFERMATO come sopra; Personio 360° `Documentato` (`competitor_personio.md:89`) | La sonda è **contingente** (vive dentro il pilota di P-06) — ma è dichiarata tale e la classe è COULD | — |
| P-10 | REGGE-CON-CORREZIONI | F27 CONFERMATO (`pass2_b:237` — `10_DocumentsSignatures.md`, mai eseguita); Personio firma nativa + DocuSign con limiti per piano `Documentato` (`competitor_personio.md:57-58`) | — | Costo taciuto: gli artefatti firmati finiscono nel media store, dove **solo `LocalDiskStore` è implementato** (`latenti_raw.md` (f), commento in `modules/content/media-store.ts`) — un documento firmato su disco locale è un rischio operativo da dichiarare. La sonda ("preventivo API di 2 provider") richiede un contatto commerciale = input di Enzo |
| P-11 | **REGGE** | F19 CONFERMATO (`lib/notifications/emit.ts:45`, dedupe+preferenze); F11 CONFERMATO nella parte codice (`ConsoleSms`/`ConsoleMailer` sempre) | — | Rafforzativo, non correzione: il "chi guarda il calendario" **ha già un precedente vivo** — `approvals-sla.timer` + `approvals/sla.ts` emettono notifiche su scadenza. La sonda (contare le scadenze reali nei dati RTL) è l'unica cosa che manca ed è genuina |
| P-12 | REGGE-CON-CORREZIONI | La lacuna "resa responsive non misurata in **questo ciclo**" è vera | **Esiste già un cancello mobile**: `apps/web/playwright.config.ts:84-94` e `playwright.prod.config.ts:159` definiscono un progetto **Pixel 7** che gira la spec a11y autenticata su viewport mobile, con censimento dichiarato "0 violazioni" (S983); `tests/e2e/a11y.spec.ts:87` è scritta apposta per reggere lì | Riscrivere come **estensione** del progetto Pixel 7 esistente, non come misura da zero; dire esplicitamente che a11y ≠ resa di layout (il gate verde non dice nulla sull'usabilità); sforzo ben sotto S |
| P-13 | REGGE-CON-CORREZIONI | F22 CONFERMATO da me: `apps/api/src/modules/positions/routes.ts:96` espone `/:id/intelligence-profile`; le uniche altre occorrenze in tutto `apps/` sono due test (`positions.integration.test.ts:328,331,497`) → **zero chiamanti web** | **[S1] mal citato**: S1 è "succession planning Personio non trovato", non dice nulla sul PIP. E "matrice §3 NTE su tutta la riga" è **falso**: la riga è `NTE / n.c. / NTE` (`04_MATRICE.md:55`) — Eightfold è **non censito**, cioè lacuna della raccolta, non assenza del prodotto | Togliere la rivendicazione "nessuno dei tre ha una scheda così" o marcarla `n.c.`; il caso di P-13 sta in piedi da solo su F22 (endpoint pagato, zero chiamanti) e non ha bisogno del competitor |
| P-14 | REGGE-CON-CORREZIONI | F5 CONFERMATO (`gdpr/routes.ts` — 5 route: 2 GET + 3 POST alle righe 32,44,56,68,81); F25 CONFERMATO (whistleblowing pubblico + console, `inventario_raw` B/R); **S3 riverificato sul web** | "Rischio: basso" è **falso per costruzione**: 3 delle 5 route sono POST `export` / `erasure` / `retention/run` — una console che espone la cancellazione di una persona è una superficie **distruttiva** che il Metodo di bonifica §4 obbliga a trattare con guardia, post-condizione e rollback dichiarato. L'obbligo whistleblowing ">49 dipendenti" non porta né [F] né [S] (verificato vero: D.lgs. 24/2023, soglia ≥50 in media nell'ultimo anno) | Separare dalla stima la "pagina di posizionamento pubblica" (è lavoro sul sito pubblico, non sulla console); dichiarare la superficie distruttiva; marcare la soglia di legge come [S] |
| P-15 | REGGE-CON-CORREZIONI | F6 CONFERMATO (`predictions/routes.ts` — 4 GET: /models, /models/:id, /, /:id); **S10 riverificato sul web** | **Il racconto rischia di essere falso**: `SOT_BACKLOG.md:2616` dichiara che la superficie predictions è read-only perché *"i valori sono **precomputed legacy**, non user-facing"*. Una pagina che dice "quali modelli girano e quali predizioni hanno prodotto" mostrerebbe, se il dato è quello importato, **risultati non prodotti dai nostri modelli** — esattamente ciò che un posizionamento "AI dichiarabile" non può permettersi | Prima della pagina, rimisurare la provenienza delle righe (la console provenance esiste: è la prova che serve). Se sono legacy precomputed, la pagina si può fare **solo dicendolo**, e il valore T2 cambia natura |
| P-16 | **REGGE** | F17 CONFERMATO (`reference-sync/routes.ts` 4 route, `sync-cli.ts`, comando `reference-sync:run` in `apps/api/package.json:20`); F32 CONFERMATO (`pass2_c:599` — la spec 2026-06-07 raccomanda **contro** lo scraping e ammette *"ESCO, ISTAT/Eurostat, ATECO/NACE, registri CCNL pubblici"*; `SOURCE_OF_TRUTH_REGISTRY` indica **CNEL per i CCNL**, `pass2_c:197`) | — | Nota, non correzione: la sonda ha già un esito parziale — il CNEL pubblica una sezione **"Contratti in Open Data"** (cnel.it, consultato 2026-08-26), quindi la proposta probabilmente non muore lì. Da dichiarare: `reference_sync` è **PLATFORM_ADMIN-only e globale** (`routes.ts:9`), quindi l'aggancio a job-role/fasce per tenant è una superficie in più |
| P-17 | REGGE-CON-CORREZIONI | F9 CONFERMATO nella parte codice: grep dei due nomi vista su tutto `apps/` → **0 occorrenze**; la pagina `/org-director/health` esiste (`org-health` ha 1 sola route) | — | Dipendenza taciuta: mostrare **43 unità su 45 in violazione** in una demo T2 è un autogol, a meno di bonificare prima i dati organizzativi di RTL — cioè la proposta ne presuppone un'altra che nessuno ha scritto. Minore: la dichiarazione "sentinelle informative per db_health" non serve (le due viste **esistono già** e `db_health.py` interroga tutte le `v_*` dinamicamente) |
| P-18 | **CADE** | Il gateway/console esiste come pagina (`apps/web/src/app/(authenticated)/dev/agent/page.tsx`) | **Tre prove contro**: (1) il commento del file stesso, righe 20-22, dice *"This page renders without a live agent (the live drive is blocked-on-Enzo: dev subscription out_of_credits / PROD credential required)"*; (2) `docs/kb/tools/pagine_waivers.txt:12` misura il 2026-08-16 che `NEXT_PUBLIC_ENABLE_AGENT_DEV` **non esiste in nessun `.env`, PROD compreso** → a flag spento la pagina rende un avviso, non una console; (3) `SOT_BACKLOG.md:2270` dichiara che un serving **customer-facing** (API key/Bedrock/Vertex) è **"nuovo scope"**. F31, come formulato in EVIDENZE, è ambiguo fino a essere fuorviante | **La sonda non può girare**: "10 domande alla console dev" richiede flag acceso in un build + gateway raggiungibile + credenziale, cioè un input che solo Enzo dà. Per tornare in gioco: rimisurare lo stato reale del gateway, dichiarare il contratto di serving come **dipendenza** e la sonda come WAIT-INPUT |
| P-19 | REGGE-CON-CORREZIONI | F28 CONFERMATO (`pass2_c:583` — `SECURITY_AND_PRIVACY_BOUNDARIES.md` righe 59-70, i 6 stati esatti); F24 CONFERMATO (`approvals/routes.ts` 5 route con decide + apply) | **Leva 3 gonfiata**: dei tre consumatori dichiarati, P-18 cade e "ogni futura funzione prescrittiva" non esiste. Resta **uno solo** vivo: la pipeline seed (`seed-approval-decisions`, modulo presente e latente) | Leva 3→1; dichiarare che oggi non c'è alcuna AI che scrive in produzione, quindi la trafila nasce come **cornice** e non come rimedio a un rischio in corso |
| P-20 | REGGE-CON-CORREZIONI | F4 CONFERMATO: `mentorship/routes.ts` = **17** `app.<verb>(`; grep `/v1/mentorship` su tutto `apps/web/src` → **0 occorrenze** | **[S9] mal citato**: S9 in EVIDENZE è "inferenza skill/succession/marketplace"; il mentoring è una citazione Gartner **riportata dal blog del vendor** (`competitor_eightfold.md:26`) — catena a due passaggi, non il pitch del metro | Costo taciuto: le 17 route sono **admin**; la vista ESS "le mie mentorship" richiede nuove rotte `/v1/me/*` perché ADR-0011 vieta di appendere `/me/*` ai moduli esistenti. S copre la pagina admin, non le due cose |
| P-21 | **CADE** | F10 è CONFERMATO **come default di codice**: `MATCHING_FREETEXT_ENABLED` è dichiarato in `apps/api/src/config/env.ts` con default false, e la UI esiste (`semantic-search-panel.tsx`, usata da `/skills` e `/me/matching`) | **Il flag in produzione è ACCESO, e lo dice la SoT del progetto**: `SOT_BACKLOG.md:2623` — *"#6 free-text matching → **LIVE PROD** (config-only): flag `MATCHING_FREETEXT_ENABLED=true` + `VOYAGE_API_KEY` sul `.env` VM → `/v1/matching/search` 200 con kNN Voyage at-request"*; `SOT_BACKLOG.md:1951-1953` — voce **#40 status: DONE**, con dimostrazione LIVE (login reale, ranking dal pgvector); `SOT_STATE.md:2181` idem. E lo stesso ciclo si contraddice: `inventario_raw.md:128` classifica la ricerca semantica free-text **"Completo"** | La proposta descrive uno stato che il progetto ha chiuso. Torna in gioco **solo** se una misura fresca sulla VM mostra il flag spento — misura che il raccolto non ha potuto fare (`.env` negato) e che nessuno ha sostituito con la lettura del backlog, che era leggibile |
| P-22 | **CADE** | F8 CONFERMATO alla lettera: grep `layout` (case-insensitive) su `visualizations/[graphId]/page.tsx` → **0 occorrenze** | **Il fatto è vero e la conclusione è falsa.** Non esiste alcun canvas che possa onorare un layout: quella pagina disegna con **`MermaidDiagram`** (`page.tsx:9`, da `_charts-client.tsx` → `@heuresys/ui`), e Mermaid **calcola** il posizionamento dal testo, non lo accetta. In `apps/web/package.json` non c'è react-flow, d3 o altro renderer interattivo. Per di più il regolamento di progetto vieta di creare primitive UI riusabili e di aggiungere dipendenze UI runtime in questo repo → il lavoro è un renderer nuovo in `ux-design-shared` + release + wiring | "Sforzo S" sbagliato di un ordine di grandezza, e con una **dipendenza cross-repo** non dichiarata. Cade anche la premessa narrativa ("trovarlo come l'avevi sistemato"): non è mai esistita una UI per sistemarlo, quindi le 316 righe non sono lavoro dell'utente |
| P-23 | REGGE-CON-CORREZIONI | Broadcast CONFERMATO: `notifications/routes.ts` = 2 route (POST invio + GET audit), zero consumer web; F11 CONFERMATO (Basic Auth SMTP ritirata il 30/04/2026, `SOT_BACKLOG.md:1946`) | La metà email non è un COULD nuovo: `#39` (digest) è passata a **HOLD il 2026-08-25**, cioè il giorno prima di questo ciclo (`SOT_BACKLOG.md:1944-1950`), e `#8` è HOLD con essa | Spezzare la scheda: il broadcast è una latente da pagina (S) e regge; l'email **non è una proposta**, è una voce già parcheggiata con motivo scritto — riproporla senza citare quella decisione la fa rientrare dalla finestra |
| P-24 | REGGE-CON-CORREZIONI | F7 CONFERMATO (`delegations/routes.ts` — 4 route: list, /:id, POST, POST /:id/revoke); F20 CONFERMATO (`lib/scope/domains.ts:56-66` = **10** domini, `delegation` incluso; `data-classes.ts:36-43` = 7 classi) | **Sonda non eseguibile in questo ciclo**: "nella campagna P-06 pilota…" presuppone che P-06 (sforzo L) sia costruita e che esista un pilota. Una sonda che aspetta una proposta L non è una sonda | Sostituirla con qualcosa di misurabile adesso (es. quante approvazioni restano ferme su un approvatore assente — è una query, non un pilota). Nota: se la tabella è davvero a 0 righe, non c'è nemmeno valore demo finché qualcuno non crea una delega |
| P-25 | REGGE-CON-CORREZIONI | F30 **riverificato oggi sul codice da me** (la componente documentale è sciolta): `config/env.ts:266` `PROM_METRICS_ENABLED` z.enum default `"false"`; `app.ts:251` `enablePrometheus()`; `app.ts:341-351` `/metrics` gated **e loopback-only** | — | La sonda **è già stata eseguita in questo passaggio**: resta solo la decisione (fase 5), che è di Enzo. E la fase 5 è più avanti di quanto la scheda creda: `deploy/systemd/heuresys-prometheus.service` **esiste già nel repo** (collector loopback :9091, retention 15d/2GB, installato da ogni deploy ma abilitato solo dove provisionato). Sforzo S→XS, e il "curl /metrics" va fatto **da loopback** o dà un falso rosso |
| P-26 | REGGE-CON-CORREZIONI | F29 CONFERMATO (`pass2_c:561` cita testualmente *"The platform is not: Core HR Administration; Payroll execution; Time & Attendance execution; Benefits/Welfare administration"* da `AI_CODING_AGENT_BOOTSTRAP_PROMPT.md` §1.6, gemello di `BROWNFIELD_EXCLUSION_RULES.md`); S14 è `Riportato` (zendesk Inrecruiting) | Una eretica WON'T che **presuppone P-03** ("il recruiting si integra attraverso P-03"): il rientro dipende da una SHOULD non ancora costruita — è dichiarato, ma va letto come dipendenza vera | **Sonda che non può fallire**: "quante richieste dei lead menzionano il recruiting" su un volume di lead sconosciuto e verosimilmente minuscolo confermerà sempre "nessuna". Dichiarare la soglia minima di lead sotto la quale la sonda non decide nulla |
| P-27 | REGGE-CON-CORREZIONI | F3 CONFERMATO **come misura**: 31 moduli senza consumer web (`inventario_raw.md:358-388`) | **L'inferenza è sbagliata**: "senza consumer **web**" ≠ "mai raggiunto". `research` è importato da `tenant-blueprints/routes.ts:40`, `seed-candidate-records/routes.ts:14`, `seed-acquisition-runs/routes.ts:13` e da `app.ts:96-97`; `reference-sync` ha una CLI canonica (`apps/api/package.json:20`) ed è registrato in `app.ts:130`. Almeno 2 dei 31 sono infrastruttura viva — e `reference-sync` è **la cosa su cui poggia P-16** | Il criterio binario "o entra in una proposta o si ritira" va corretto con un terzo esito: **consumer non-web** (CLI, altro modulo, invariante). Cancellare l'obiettivo "toglierne metà": non ha base. Il resto (igiene, costo di ritiro in file da emendare per ADR-0035) regge |
| P-28 | REGGE-CON-CORREZIONI | S15 CONFERMATO (nessuno dei tre pubblica numeri; verificato anche che Personio **ha** una pagina `/pricing` ma senza cifre); `pass2_c:606` conferma che i numeri sono autorità esclusiva di Enzo, Q1-Q8 senza risposta | — | Stessa debolezza di P-26: la sonda "quanti lead chiedono il prezzo" non può fallire con un pugno di lead. Dichiarare il volume minimo |
| P-29 | REGGE-CON-CORREZIONI | La lacuna lato utente è vera: **nessuna pagina web** usa un export (grep `format=csv\|/export\|xlsx` su `apps/web/src` → 0 occorrenze) | **Il "Cosa" è già costruito, e da tempo**: `apps/api/src/lib/export/hook.ts:1-15` è un hook globale `onSend` che trasforma **qualunque** endpoint lista in `?format=csv\|xlsx\|pdf`, "zero-touch sulle ~85 list route", applicato **dopo** RBAC/scope; `lib/export/serializers.ts` implementa CSV/XLSX/PDF; `modules/analytics/routes.ts:117-141` espone già l'export dedicato delle 9 viste analytics. Di conseguenza la cella `04_MATRICE.md:157` ("Assente — esiste solo export dei grafici") **è smentita**, e l'inventario non l'ha censito | Riscrivere: non si costruisce l'export, si **espone** quello che c'è (un bottone e una scelta di formato). Sforzo M→S. Cade anche la motivazione della riga scartata "Report builder completo → il primo passo vero è P-29" |

## Fatti F riverificati sul codice

| id | esito | prova |
|---|---|---|
| F1 (228 capacità, 20 aree) | NON-RIVERIFICABILE in toto | Conteggio auto-referenziale sul raccolto; le righe puntuali che ho campionato (aree A, F, G, O, T) combaciano col codice |
| F2 (604 route · 102 pagine) | **CONFERMATO** | `grep "app\.(get\|post\|put\|patch\|delete)\(" apps/api/src/modules --glob **/*routes.ts` → **604 su 99 file**; `apps/web/src/app/**/page.tsx` → **120**, di cui **18** sotto `/showcase/` → 102. Nota: i 102 includono `/app`, che è un puro redirect e che l'inventario esclude |
| F3 (31 moduli latenti + 12 capacità puntuali) | **SMENTITO in parte** | I 31 sono corretti come "nessun consumer **web**", non come "nessun consumer" (→ P-27). Il "12" **non è esaustivo**: `review-cycles` POST e POST `/transition`, `assessment-results` POST, `notifications` POST broadcast sono capacità puntuali latenti non elencate. L'inventario stesso dichiara il rischio (`inventario_raw.md:408`) |
| F4 (mentorship 17 route, zero UI) | **CONFERMATO** | `mentorship/routes.ts` = 17 handler; `grep /v1/mentorship apps/web/src` → 0. Righe vive: NON-RIVERIFICABILI |
| F5 (GDPR 5 endpoint, zero UI) | **CONFERMATO** | `gdpr/routes.ts:32,44,56,68,81`; `grep /v1/gdpr apps/web/src` → 0. Righe: NON-RIVERIFICABILI |
| F6 (predictions 4 GET, zero UI) | **CONFERMATO** | `predictions/routes.ts:19,25,32,38`; 0 occorrenze web. **Da aggiungere**: `SOT_BACKLOG.md:2616` qualifica i valori come *"precomputed legacy"* (→ P-15) |
| F7 (delegations 4 route) | **CONFERMATO** | `delegations/routes.ts:25,34,40,51`; 0 occorrenze web. `sys_user_delegations=0`: NON-RIVERIFICABILE |
| F8 (node-layouts ignorati dal canvas) | **CONFERMATO alla lettera, fuorviante nella conclusione** | 0 occorrenze di `layout` in `visualizations/[graphId]/page.tsx`; ma il renderer è `MermaidDiagram` (`page.tsx:9`) e non esiste alcun canvas posizionabile né dipendenza che lo fornisca (`apps/web/package.json`) |
| F9 (due viste sentinella mai lette) | **CONFERMATO nella parte codice** | grep dei due nomi vista su tutto `apps/` → 0 occorrenze. 161 / 43 righe: NON-RIVERIFICABILI |
| F10 (`MATCHING_FREETEXT_ENABLED` OFF) | **CONFERMATO come default di codice · SMENTITO come stato di produzione** | Default false in `config/env.ts`; ma `SOT_BACKLOG.md:2623` e `:1951-1953` (#40 **DONE**, S1018) e `SOT_STATE.md:2181` registrano flag **=true** sul `.env` della VM con verifica live |
| F11 (email bloccata) | **CONFERMATO** | `makeMailer`/`makeSmsSender` senza ramo reale; `SOT_BACKLOG.md:1946` conferma il ritiro Basic Auth SMTP del 30/04/2026. **Da aggiungere**: `#39` e `#8` sono in **HOLD dal 2026-08-25** |
| F12 (role CRUD assente in UI e API) | **CONFERMATO** | `auth/routes.ts:311-312` (commento MVP-3), `:316-318` sola GET; nessuna scrittura su ruoli fra le 6 POST |
| F13 (SMS sempre ConsoleSms) | **CONFERMATO** | `auth/sms-sender.ts:93-100` — qualunque `SMS_PROVIDER` logga un warning e ricade su `ConsoleSms` |
| F14 (nessun SSO) | **CONFERMATO** | grep `SAML\|OIDC\|OAuth\|\bSSO\b` su `modules/auth` → 0 match |
| F15 (nessuna superficie per terzi) | **CONFERMATO** | grep `webhook\|personal_access_token\|api_key` su `apps/api/src` → 3 file, tutti chiave **in uscita** verso Voyage o nome di variabile |
| F16 (multipart in 2 moduli) | **CONFERMATO** | `me/routes.ts`, `content/media-routes.ts` — nessun altro |
| F17 (reference_sync vivo e riusabile) | **CONFERMATO** | `reference-sync/routes.ts` (4 route, registrato in `app.ts:130`), `sync-cli.ts`, `pnpm reference-sync:run` |
| F18 (pgvector 1024) | **CONFERMATO** | `db/migrations/000060_pgvector_substrate.sql` (4 colonne `vector(1024)`) + `000062` |
| F19 (emitNotification generico) | **CONFERMATO** | `lib/notifications/emit.ts` + 25 usi in 8 moduli, incluso `approvals/sla.ts` |
| F20 (M1 10×7×4) | **CONFERMATO** | `lib/scope/domains.ts:56-66` (10 domini) · `lib/scope/data-classes.ts:36-43` (7 classi). Resta la nota nota: `CLAUDE.md` I16 dice 11 |
| F21 (fattori di velocità) | **CONFERMATO in campione** | 604 route / 99 file; il resto (108 schemi, 238 test, 100 spec) non ri-contato ma coerente con `architettura_raw` |
| F22 (PIP endpoint senza chiamanti) | **CONFERMATO** | `positions/routes.ts:96`; uniche altre occorrenze = test |
| F23 (enterprise-typing POST vs PUT) | **CONFERMATO — difetto reale** | `enterprise-typing-profiles/routes.ts:16,20,24,28` = GET/GET/**PUT**/DELETE; `tenants/[tenantId]/enterprise-typing/page.tsx:64-65` invia `method: "POST"` |
| F24 (blueprint end-to-end + approvals) | **CONFERMATO** | `tenant-blueprints/routes.ts` 20 handler (build-plan/apply/diff), `approvals/routes.ts` 5 (decide + apply) |
| F25 (whistleblowing completo) | **CONFERMATO in campione** | route pubbliche + console (`whistleblowing/routes.ts`, pagina console in inventario R) |
| F26 (moduli admin latenti) | **CONFERMATO** | `surveys` 12, `assessments` 4, `assessment-methods` 1, `assessment-results` 3, `teams` 6, `notifications` 2, `successor-readiness` 3, `user-career-plans` 5 — tutti presenti, nessuno chiamato dal web |
| F27 (9 mini-spec SDBI mai eseguite) | **CONFERMATO nel numero · SMENTITO nel qualificatore** | `pass2_b:229-238`: 9 aree "MAI eseguita". La fonte dice "Mini-specifica": **"DDL complete" non è attestato** |
| F28 (governance AI 6 stati) | **CONFERMATO** | `pass2_c:583` — `SECURITY_AND_PRIVACY_BOUNDARIES.md` righe 59-70, stati identici |
| F29 (perimetro fondativo) | **CONFERMATO** | `pass2_c:561`, citazione letterale da §1.6 del bootstrap prompt |
| F30 (Prometheus fasi 1-4, flag OFF) | **CONFERMATO — non è più documentale** | `config/env.ts:266`, `app.ts:251,341-351`, `modules/observability/prometheus.ts`. In più: `deploy/systemd/heuresys-prometheus.service` esiste già (fase 5 parzialmente in repo) |
| F31 (console agente) | **SMENTITO come formulato** | Le tre prove citate in P-18. "L'accesso in abbonamento risulta chiuso" si legge in due modi opposti e nessuno dei due combacia con lo stato: la questione abbonamento è *risolta* (`SOT_STATE.md:1946`), la console *non è accendibile* (flag assente da ogni `.env`), il serving customer-facing è *nuovo scope* |
| F32 (solo fonti ufficiali, CCNL ammesso) | **CONFERMATO** | `pass2_c:599` + `pass2_c:197` (CNEL per i CCNL nel SOURCE_OF_TRUTH_REGISTRY) |
| F33 (Future Evolution) | **CONFERMATO** | `pass2_c` §Menzioni; usato solo per scartare "process mining" — uso corretto |
| F34 (storia RTL scorrevole) | NON-RIVERIFICABILE | Dichiarativo (CLAUDE.md + memoria); EVIDENZE lo flagga. P-02 vi si appoggia per "le tabelle presenze/cedolini popolate" |

## Fonti S ricontrollate sul web

| id | esito | URL + data + cosa ho visto |
|---|---|---|
| S1 (succession Personio NTE) | **CONFERMATO CON RISERVA** | Ricerca mirata 2026-08-26: nessuna pagina prodotto dedicata alla succession; i risultati primari sono `personio.com/hr-lexicon/succession-planning/` (editoriale). Ma una recensione terza (`employsome.com/review/personio/`, `peoplemanagingpeople.com`) descrive la succession **supportata** via org chart + workforce planning. Il "NTE" regge come "nessuna funzione dedicata documentata"; il lessico "il diretto è SCOPERTO" va ammorbidito |
| S3 (whistleblowing add-on) | **CONFERMATO** | Ricerca 2026-08-26 su `support.personio.de/hc/en-us/articles/12957181436445` e sintesi terze: *"Personio sells one base plan in two versions, Core and Core Pro, plus separately priced apps for … whistleblowing"*; *"You need to add Personio Whistleblowing to your plan before you can begin implementation"*. Regge l'argomento "incluso vs add-on" |
| S4 (API/SSO nel piano alto) | **CONFERMATO CON RISERVA** | 2026-08-26: `support.personio.de/.../4411236757521` (SSO con Microsoft Entra ID via OpenID Connect) e `.../4411251109905` (Okta) esistono e sono pubblici; una fonte terza (`stitchflow.com/scim/personio`) attribuisce SSO/integrazioni avanzate al **piano superiore** ma lo chiama "Enterprise", non "Core Pro". La direzione (SSO = leva di piano) regge; **il nome del piano viene da snippet indicizzati di una pagina 403 e non è verificato** |
| S10 (class action Eightfold) | **CONFERMATO** | 2026-08-26: Ogletree, Jones Walker, Epstein Becker, Outten & Golden, hr-brew, Norton Rose. Deposito **20 gennaio 2026**, California; allegazioni: scraping su oltre **un miliardo** di lavoratori, score 0-5, scarto pre-revisione umana, violazione FCRA; prima causa USA a contestare l'FCRA a un vendor AI di hiring. Nota: EVIDENZE scrive "1,6 mld traiettorie" — la causa parla di "oltre un miliardo di lavoratori"; è una conflazione fra il dataset dichiarato dal vendor e l'allegazione |
| S12 (5 tracciati Zucchetti) | **CONFERMATO — fetch diretto riuscito da me** | `remote.cedbrianteo.it/PagheWEB/help/infinity/ITA/.../intro_interfaccia_presenze.htm`, 2026-08-26: TRRIPW (XML), HGAL_TIMEIMP_TMP, H1TR_CSVVOCI, TRRIPA, FOGPRE — tutti dichiarati **import unidirezionale verso Paghe**, *"senza trasferimento dati in direzione opposta"*. La riserva sul dominio partner resta, ma il contenuto l'ho letto io |
| S13 (API Zucchetti dietro SSO) | Non ri-testato | Il raccolto dichiara `apiportal.zucchetti.it` ENOTFOUND e il redirect a Keycloak: entrambi coerenti con "esistenza documentata, contenuto non verificabile". Non ho speso una verifica qui perché nessun MUST vi poggia |
| S15 (nessun listino pubblico) | **CONFERMATO** | 2026-08-26: `personio.com/pricing/` esiste ma **senza cifre** (fetch diretto 429; la sintesi di ricerca dice *"Personio doesn't publish prices and must request a quote after demo"*); Eightfold: nessuna pagina pricing sul dominio vendor. L'assenza regge |
| S5, S6, S7, S8, S9, S11, S14 | Non ri-testati | Nessuno di essi regge da solo un MUST o una eretica. S9 va comunque **ristretto**: non copre il mentoring (→ P-20) |

## Costi nascosti e sforzi sottostimati

- **P-01 (M→M/L)** — parser + mappatura colonne + anteprima + giornale undo + **lineage obbligatorio** (`sys_source_lineage_records` è il pattern canonico per ogni riga derivata) + permessi + guardia/post-condizione/rollback del Metodo §4. La sonda dipende da un file anagrafico reale che nessuno ha.
- **P-04 (M/L→L)** — `sys_auth_sessions` è un placeholder da ridisegnare; provisioning/collegamento utenze tocca `user_external_code` (I14); resta da decidere cosa succede alla policy MFA per tenant quando l'IdP autentica; logout/refresh per-IdP.
- **P-05 (M→L)** — invalidazione della cache RBAC (oggi inesistente: boot-only) **e** conflitto con la catena di migrazioni che riapplica 76 INSERT di mapping a ogni deploy.
- **P-06 (L, con il tetto più in alto di quanto sembri)** — scrivere una performance review non ha endpoint (2 GET); creare una scala nemmeno (`assessment-methods` = 1 GET); gli stati di comunicazione richiesti da I20 sono lavoro a sé.
- **P-10 (M, più il vincolo di storage)** — solo `LocalDiskStore` implementato: i documenti firmati finirebbero su disco locale.
- **P-14 (S/M → S/M + superficie distruttiva)** — la console GDPR espone erasure e retention-sweep; e la "pagina di posizionamento pubblica" è scope separato.
- **P-20 (S → S + nuovo modulo `/v1/me/*`)** — ADR-0011 vieta di appendere `/me/*` ai moduli esistenti.
- **P-22 (S → cross-repo)** — renderer interattivo assente; nuove primitive UI e dipendenze UI vietate in questo repo.
- **P-29 (M → S)** — al contrario: il costo è **sovrastimato**, il motore c'è già.
- **P-25 (S → XS)** — la riverifica è fatta, il collector è in repo.

**Sonde che non possono fallire o non possono girare**: P-18 (richiede flag+gateway+credenziale = blocked-on-Enzo) · P-24 (richiede che P-06 sia costruita e un pilota in corso) · P-26 e P-28 (poggiano su un volume di lead ignoto e verosimilmente troppo piccolo perché la risposta possa essere diversa da quella attesa) · P-09 (contingente, ma dichiarata e su una COULD) · P-01 e P-10 (richiedono un input esterno non dichiarato: un file cliente, un preventivo commerciale).

## Classi contestate

| id | classe proposta | perché non regge | classe suggerita |
|---|---|---|---|
| P-21 | MUST-T2 · SHOULD-T1 | Non è una proposta: è una voce **DONE** dal S1018 con dimostrazione live, e il flag è `true` sul `.env` della VM dal S975 | Fuori ciclo — al massimo una **riverifica di 10 minuti** sulla VM |
| P-07 | SHOULD-T1 · SHOULD-T2 | Il "chi dice no" presuppone che l'authoring alimenti l'ESS: non lo fa, sono due cluster | GATED su una decisione semantica di Enzo (m2b), non SHOULD |
| P-18 | SHOULD-T2 · COULD-T1 | Il "già esiste" è una console che non si accende in nessun ambiente e il serving per i clienti è dichiarato **nuovo scope** altrove | COULD-T2 al massimo, e solo dopo la rimisura del gateway |
| P-22 | COULD-T2 · COULD-T1 | Coerente come classe, ma lo **sforzo** dichiarato falsa il confronto con le altre COULD: a S entra nella coda degli sblocchi facili, a L/XL non ci entra | COULD con sforzo **L** e dipendenza `ux-design-shared` dichiarata |
| P-04 | MUST-T1 | Il MUST poggia su una frase editoriale del proprio raccolto, non su una misura di domanda | MUST **provvisorio**, sciolto dalla sonda: SHOULD-T1 finché tre prospect non rispondono |
| P-13 | MUST-T2 | La classe regge (è il pezzo pagato e invisibile), ma **non** per il motivo scritto: la riga competitor non dice ciò che la scheda le fa dire | MUST-T2, con la motivazione riscritta su F22 soltanto |
| P-19 | leva 3 | Due dei tre consumatori dichiarati non esistono o cadono | leva 1 (pipeline seed) |
| P-29 | COULD, sforzo M | Il motore c'è; resta un bottone | COULD, sforzo **S**, e la riga scartata "report builder" perde la sua motivazione a catena |

**Regole di classe rispettate**: nessun MUST nasce dal metro di riferimento (P-15/P-18/P-20 citano Eightfold e sono tutte SHOULD/COULD) ✓ · nessuna proposta replica una superficie Zucchetti senza motivazione (P-02 integra, P-26 dichiara il non-fare) ✓.

## Errori d'insieme

1. **Aritmetica: corretta.** 29 schede, ID da P-01 a P-29 tutti presenti una volta sola (P-29 è fuori ordine, fra P-25 e P-26 — cosmetico). Argine MUST: T1 = P-01, P-04, P-06 = 3; T2 = P-13, P-21 = 2; soglia 29/5 → 5. Rispettato. **Se P-21 cade, T2 resta con un solo MUST**: non è una violazione (l'argine è un tetto), ma il traguardo dimostrabilità perde metà della sua spina dorsale dichiarata.
2. **Contraddizione interna al ciclo, non risolta**: `inventario_raw.md:128` classifica la ricerca semantica free-text **Completo**, `04_MATRICE.md:67` e `:226` la classificano **Parziale**, F10 la tratta come spenta e P-21 ci costruisce sopra un MUST. Tre letture diverse dello stesso oggetto dentro gli stessi sei file.
3. **Cella di matrice smentita**: `04_MATRICE.md:157` — "Report builder custom + **export dati** verso strumenti esterni | Assente — esiste solo export dei grafici". L'export CSV/XLSX/PDF esiste su ~85 route più l'export dedicato delle 9 viste analytics. L'inventario non l'ha censito, la matrice ne ha ereditato il buco, P-29 ci ha costruito una proposta.
4. **Il preambolo generalizza oltre le sue fonti**: "la famiglia canale dati copre una lacuna presente, nelle rispettive forme, in tutti e tre i competitor". Per l'import da file la matrice stessa dà `n.c.` a due su tre. La frase regge per "API di qualche tipo", non per la campagna intera.
5. **Nessun doppione vero fra le schede.** Le coppie sospette sono in realtà distinte: P-01/P-02 (formato generico vs tracciato specifico, con dipendenza dichiarata), P-15/P-19 (mostrare vs governare), P-11/P-23 (scadenze vs comunicazioni). L'unica scheda **bicefala** è P-23, che tiene insieme una pagina (S) e una decisione parcheggiata (HOLD): va spezzata.
6. **Un'affermazione senza marcatura che il lettore prenderebbe per verificata**: P-14, "in Italia il whistleblowing è obbligo sopra i 49 dipendenti". È vero (D.lgs. 24/2023, soglia ≥50 in media nell'ultimo anno, sanzioni ANAC 10-50k, verificato 2026-08-26) ma nella scheda non porta né [F] né [S].
7. **Le "scartate" hanno tutte una ragione** ✓, ma una è ormai stale: "Report builder completo → il primo passo vero è P-29 (export)" presuppone che l'export non esista.

## Dove tutto poggia su una sola prova

Questa è l'osservazione che si vede solo verificando una scheda dopo l'altra, e va detta perché tocca l'intero impianto, non una proposta.

1. **L'intera lente 3 (SBLOCCARE) poggia su un solo metodo di misura**: il grep della stringa del path in `apps/web/src`. Zero occorrenze = zero consumer. È un metodo buono e l'ho riprodotto con esito identico su cinque moduli — ma è **uno solo**, non ha un secondo controllo, e produce due errori di segno opposto già materializzati in questo ciclo: dichiara latente ciò che ha un consumer non-web (P-27: `research`, `reference-sync`) e dichiara "una pagina di distanza" ciò che non ha un renderer possibile (P-22). Un secondo controllo — "chi altro importa questo modulo?" e "esiste il pezzo che dovrebbe consumarlo?" — sarebbe costato pochi grep e avrebbe salvato due schede.
2. **Tutta la colonna Personio poggia su un solo canale di acquisizione**: snippet indicizzati, perché il dominio ha risposto 403/429 per l'intera sessione. È dichiarato in testa al raccolto, e le mie tre verifiche indipendenti (S3, S4, S15) l'hanno confermato — ma il **nome del piano** ("Core Pro"), che è l'unica cosa che rende S4 un argomento di valore, resta non letto su pagina, e una fonte terza lo chiama diversamente.
3. **Tutti i numeri di riga viva poggiano su una sola passata psql** del 2026-08-25 che nessuno può ripetere in questa sessione: 63 mentorship, 150 sessioni, 316 layout, 161 gap critici, 43 unità, 468 predizioni, 8 richieste GDPR, 85 righe di data-map, 0 deleghe. Sono l'argomento di vendita di sei schede (P-14, P-15, P-17, P-20, P-22, P-24) e **nessuna di esse ha una seconda fonte**. Per il PUNTO FISSO sono evidenza datata di quel giorno, non stato del presente: prima di trasformarne una in esecuzione, si rimisura.
4. **S12 poggia su una sola pagina, ospitata da un rivenditore.** L'ho riletta io e il contenuto tiene — ma è comunque una pagina sola su un dominio non primario, e su di essa poggia l'intera tesi della coesistenza col gestionale dominante (P-02, e per riflesso la motivazione di P-26).
5. **Tre schede poggiano ciascuna su un solo documento o un solo flag**: P-25 su `PROM_METRICS_ENABLED` (oggi sciolto: l'ho letto nel codice), P-19 su `SECURITY_AND_PRIVACY_BOUNDARIES.md` righe 59-70, P-14 sulla riga di packaging di Personio. La prima è diventata solida in questo passaggio; le altre due restano monofonti.
6. **Il caso peggiore è il più istruttivo**: P-21 poggia su una singola misura *mancata* — `.env` negato dal sistema dei permessi — e nessuno ha cercato la stessa informazione dove era leggibile. Era in `SOT_BACKLOG.md`, due righe, indicizzate sotto il numero della voce. Quando una misura è impossibile, la prima mossa non è dedurla dal default del codice: è cercarla in un'altra fonte del progetto.
