# Heuresys Advanced — v1.0.0 (GA baseline)

First **General Availability** baseline of the Heuresys Advanced HRMS/BPM platform v5 — the
forensically-verified, fully-consolidated starting point for all future development.

## Platform at v1.0.0

- **API** — Fastify 5 + Zod + Argon2id + RS256 JWT on PostgreSQL 16 (native, OCI VM): **~60
  business modules + auth**, 60/60 modules with dedicated integration tests, **576 tests green**.
- **Web** — Next.js 15 admin SPA (MVP-2a) + ESS portal (MVP-2b), DB-driven role-aware sidebar
  with PET perspectives, per-user theme/palette preferences (server source-of-truth).
- **Showcase** — brand identity site deployed to GitHub Pages.
- **Data** — employee-centric (ADR-0024) brownfield ingestion; RTL_BANK reference tenant (161
  real users / 2 tenants / 162 positions / 24 org-derived teams), synthetic case-study (no PII,
  ADR-0023).

## This release — v1.0.0 consolidation (WS-0 → WS-7)

- **RBAC / UI / Perspectives epic COMPLETE** — design system, role categories + CEO, holderless
  role assignment, DB-driven interfaces, perspective-organized sidebar, per-user preferences, and
  **teams + the "my team" 3rd RBAC scope axis** (`sys_teams`/`sys_team_members` +
  TEAM_LEADER/TEAM_MEMBER, derived from the real org — no fixtures).
- **Employee-centric live re-derivation** — `sys_user_*` satellites populated from legacy
  `employees`; a permanent CI doctrine guard locks the keying.
- **Brownfield wave executor made wave-agnostic** — Wave 2/3-ready (data import is
  source-discovery-gated and deferred to v1.x).
- **27-module integration-test backfill**, pg-pool ECONNRESET resilience, nullable-brownfield-FK
  (ADR-0015/0016/0025), observability core, viz-graph regenerated (0 orphaned nodes).
- **55 idempotent migrations**, `db:validate` 7/7 green, twice-run idempotency proven.

## Deferred to the v1.x roadmap

Wave-2/3 data import (executor ready, awaits a loaded source); `sys_activity_classification_mappings`
FK-vs-mapping redesign; SuccessFactors / SAP connector; MFA multi-kind; mobile device-matrix a11y;
observability depth; showcase architectural consolidation (F7).

See `NEXT_GENERATION_ENTRY_POINT.md` for the full consolidated record.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
